# Note: make does not call ref directly, but reaches it via add.
# Note: make does not call _lock_file directly, but reaches it via add.
# make calls add, which calls bltn_open -- so make transitively depends on bltn_open.
# make -> add -> ExternalClashError: make has a transitive dependency on ExternalClashError through add.
# Note: make does not call _unlock_file directly, but reaches it via add.
# Note: make does not call Stats directly, but reaches it via add.
# make -> add -> add_func_stats: make has a transitive dependency on add_func_stats through add.
# Note: make does not call _create_carefully directly, but reaches it via add.
# make -> add -> _sync_close: make has a transitive dependency on _sync_close through add.
# Since make invokes add and add invokes _convert_other, make indirectly relies on _convert_other.
# Since main invokes process_keywords and process_keywords invokes parse_spec, main indirectly relies on parse_spec.
# main -> runctx -> _Utils: main has a transitive dependency on _Utils through runctx.
# Since main invokes runctx and runctx invokes run, main indirectly relies on run.
# main calls get_theme, which calls can_colorize -- so main transitively depends on can_colorize.
# main -> conv_tables -> format_array: main has a transitive dependency on format_array through conv_tables.
# Since main invokes encode and encode invokes code_page_encode, main indirectly relies on code_page_encode.
# main -> encode -> bz2_encode: main has a transitive dependency on bz2_encode through encode.
# main calls encode, which calls b2a_qp -- so main transitively depends on b2a_qp.
# main calls encode, which calls encode_basestring -- so main transitively depends on encode_basestring.
# main -> encode -> encode_basestring_ascii: main has a transitive dependency on encode_basestring_ascii through encode.
# Note: main does not call uu_encode directly, but reaches it via encode.
# main calls encode, which calls _ValueFormatter -- so main transitively depends on _ValueFormatter.
# main -> encode -> hex_encode: main has a transitive dependency on hex_encode through encode.
# Note: main does not call write directly, but reaches it via encode.
# main -> encode -> needsquoting: main has a transitive dependency on needsquoting through encode.
# Since main invokes encode and encode invokes quote, main indirectly relies on quote.
# Note: main does not call ToASCII directly, but reaches it via encode.
# Note: main does not call oem_encode directly, but reaches it via encode.
# main -> encode -> zlib_encode: main has a transitive dependency on zlib_encode through encode.
# main calls encode, which calls mbcs_encode -- so main transitively depends on mbcs_encode.
# main calls encode, which calls base64_encode -- so main transitively depends on base64_encode.
# main calls encode, which calls punycode_encode -- so main transitively depends on punycode_encode.
# Note: main does not call quopri_encode directly, but reaches it via encode.
# main calls encode, which calls HeaderParseError -- so main transitively depends on HeaderParseError.
# Note: main does not call containsAny directly, but reaches it via getFilesForName.
# main -> output_stats -> save_raw_data: main has a transitive dependency on save_raw_data through output_stats.
# Since main invokes output_stats and output_stats invokes Path, main indirectly relies on Path.
# main -> output_stats -> Stats: main has a transitive dependency on Stats through output_stats.
# main calls output_stats, which calls output_markdown -- so main transitively depends on output_markdown.
# Since main invokes output_stats and output_stats invokes load_raw_data, main indirectly relies on load_raw_data.
# Since main invokes removeduppaths and removeduppaths invokes makepath, main indirectly relies on makepath.
# Since main invokes parse_args and parse_args invokes ArgumentError, main indirectly relies on ArgumentError.
# Since main invokes parse_args and parse_args invokes _, main indirectly relies on _.
# Note: main does not call _maybe_open directly, but reaches it via get_interpreter.
# main calls randint, which calls _index -- so main transitively depends on _index.
# main calls create_archive, which calls _maybe_open -- so main transitively depends on _maybe_open.
# main -> create_archive -> _copy_archive: main has a transitive dependency on _copy_archive through create_archive.
# Note: main does not call ZipAppError directly, but reaches it via create_archive.
# main calls create_archive, which calls _write_file_prefix -- so main transitively depends on _write_file_prefix.
# main -> write_pot_file -> itemgetter: main has a transitive dependency on itemgetter through write_pot_file.
# main -> write_pot_file -> normalize: main has a transitive dependency on normalize through write_pot_file.
# main calls attach, which calls ExitStack -- so main transitively depends on ExitStack.
# main -> attach -> closing: main has a transitive dependency on closing through attach.
# Since main invokes attach and attach invokes _PdbClient, main indirectly relies on _PdbClient.
# main -> parse -> Group: main has a transitive dependency on Group through parse.
# Note: main does not call Address directly, but reaches it via parse.
# main calls parse, which calls RequestRate -- so main transitively depends on RequestRate.
# Note: main does not call FeedParser directly, but reaches it via parse.
# main -> parse -> ExpatBuilderNS: main has a transitive dependency on ExpatBuilderNS through parse.
# Since main invokes parse and parse invokes Tokenizer, main indirectly relies on Tokenizer.
# main -> parse -> DOMEventStream: main has a transitive dependency on DOMEventStream through parse.
# Since main invokes parse and parse invokes XMLParser, main indirectly relies on XMLParser.
# Since main invokes parse and parse invokes _do_pulldom_parse, main indirectly relies on _do_pulldom_parse.
# Since main invokes parse and parse invokes Example, main indirectly relies on Example.
# main calls parse, which calls cls -- so main transitively depends on cls.
# main -> parse -> RuleLine: main has a transitive dependency on RuleLine through parse.
# Note: main does not call ExpatBuilder directly, but reaches it via parse.
# main calls parse, which calls TextIOWrapper -- so main transitively depends on TextIOWrapper.
# main calls parse, which calls ElementTree -- so main transitively depends on ElementTree.
# Since main invokes parse and parse invokes make_parser, main indirectly relies on make_parser.
# Since main invokes parse and parse invokes ParserCreate, main indirectly relies on ParserCreate.
# main calls parse, which calls InvalidFileException -- so main transitively depends on InvalidFileException.
# main calls parse, which calls Entry -- so main transitively depends on Entry.
# Note: main does not call _parse_sub directly, but reaches it via parse.
# main -> parse -> State: main has a transitive dependency on State through parse.
# Since main invokes parse and parse invokes error, main indirectly relies on error.
# Since main invokes parse and parse invokes ExpatLocator, main indirectly relies on ExpatLocator.
# Note: main does not call fix_flags directly, but reaches it via parse.
# main -> parse -> ErrorHandler: main has a transitive dependency on ErrorHandler through parse.
# Note: main does not call IllegalMonthError directly, but reaches it via _validate_month.
# Note: main does not call _newSymbolTable directly, but reaches it via symtable.
# main -> check -> _get_reader: main has a transitive dependency on _get_reader through check.
# main calls check, which calls process_tokens -- so main transitively depends on process_tokens.
# main -> check -> errprint: main has a transitive dependency on errprint through check.
# main calls write, which calls serialize -- so main transitively depends on serialize.
# Note: main does not call sleep directly, but reaches it via write.
# main -> write -> _serialize_text: main has a transitive dependency on _serialize_text through write.
# Since main invokes write and write invokes assert_, main indirectly relies on assert_.
# Since main invokes write and write invokes _namespaces, main indirectly relies on _namespaces.
# main calls write, which calls time -- so main transitively depends on time.
# main -> write -> _count_to_size: main has a transitive dependency on _count_to_size through write.
# main calls write, which calls quote -- so main transitively depends on quote.
# Since main invokes write and write invokes _nbytes, main indirectly relies on _nbytes.
# main -> write -> _get_writer: main has a transitive dependency on _get_writer through write.
# Since main invokes write and write invokes crc32, main indirectly relies on crc32.
# main -> probe_den -> divmod_fast: main has a transitive dependency on divmod_fast through probe_den.
# Note: main does not call rand_digits directly, but reaches it via probe_den.
# main calls probe_den, which calls now -- so main transitively depends on now.
# main calls _test, which calls testfile -- so main transitively depends on testfile.
# Since main invokes _test and _test invokes setlocale, main indirectly relies on setlocale.
# Since main invokes _test and _test invokes XMLReader, main indirectly relies on XMLReader.
# main calls _test, which calls IncrementalParser -- so main transitively depends on IncrementalParser.
# main calls _test, which calls format_string -- so main transitively depends on format_string.
# main calls _test, which calls isfirstline -- so main transitively depends on isfirstline.
# main calls _test, which calls filename -- so main transitively depends on filename.
# Since main invokes _test and _test invokes testmod, main indirectly relies on testmod.
# main calls _test, which calls _test_generator -- so main transitively depends on _test_generator.
# Note: main does not call Locator directly, but reaches it via _test.
# main calls _test, which calls filelineno -- so main transitively depends on filelineno.
# main -> _test -> atoi: main has a transitive dependency on atoi through _test.
# main calls _test, which calls lineno -- so main transitively depends on lineno.
# main calls _test, which calls atof -- so main transitively depends on atof.
# main calls venv, which calls _warn -- so main transitively depends on _warn.
# Since main invokes venv and venv invokes addsitepackages, main indirectly relies on addsitepackages.
# Since main invokes dump and dump invokes ElementTree, main indirectly relies on ElementTree.
# main calls dump, which calls pack -- so main transitively depends on pack.
# Note: main does not call PicklingError directly, but reaches it via dump.
# Since main invokes dump and dump invokes ForkingPickler, main indirectly relies on ForkingPickler.
# main calls dump, which calls _format -- so main transitively depends on _format.
# main calls dump, which calls cls -- so main transitively depends on cls.
# Note: main does not call unquote directly, but reaches it via decode.
# main calls decode, which calls a2b_base64 -- so main transitively depends on a2b_base64.
# main -> decode -> ToUnicode: main has a transitive dependency on ToUnicode through decode.
# main calls decode, which calls punycode_decode -- so main transitively depends on punycode_decode.
# Since main invokes decode and decode invokes JSONDecodeError, main indirectly relies on JSONDecodeError.
# main calls decode, which calls a2b_qp -- so main transitively depends on a2b_qp.
# main -> decode -> ishex: main has a transitive dependency on ishex through decode.
# main -> decode -> quopri_decode: main has a transitive dependency on quopri_decode through decode.
# Note: main does not call hex_decode directly, but reaches it via decode.
# Since main invokes decode and decode invokes oem_decode, main indirectly relies on oem_decode.
# main calls decode, which calls zlib_decode -- so main transitively depends on zlib_decode.
# Note: main does not call uu_decode directly, but reaches it via decode.
# main calls decode, which calls base64_decode -- so main transitively depends on base64_decode.
# main calls decode, which calls _w -- so main transitively depends on _w.
# Note: main does not call mbcs_decode directly, but reaches it via decode.
# Since main invokes decode and decode invokes unhex, main indirectly relies on unhex.
# main -> decode -> bz2_decode: main has a transitive dependency on bz2_decode through decode.
# main -> decode -> code_page_decode: main has a transitive dependency on code_page_decode through decode.
# Note: main does not call getsitepackages directly, but reaches it via addsitepackages.
# Since main invokes addsitepackages and addsitepackages invokes addsitedir, main indirectly relies on addsitedir.
# Since main invokes addsitepackages and addsitepackages invokes _trace, main indirectly relies on _trace.
# main calls can_colorize, which calls _safe_getenv -- so main transitively depends on _safe_getenv.
# Since main invokes compile_file and compile_file invokes Path, main indirectly relies on Path.
# Since main invokes addusersitepackages and addusersitepackages invokes getusersitepackages, main indirectly relies on getusersitepackages.
# main -> addusersitepackages -> addsitedir: main has a transitive dependency on addsitedir through addusersitepackages.
# main -> addusersitepackages -> _trace: main has a transitive dependency on _trace through addusersitepackages.
# main calls func, which calls getter -- so main transitively depends on getter.
# Note: main does not call _check_system_limits directly, but reaches it via compile_dir.
# Since main invokes compile_dir and compile_dir invokes _walk_dir, main indirectly relies on _walk_dir.
# main calls compile_dir, which calls ProcessPoolExecutor -- so main transitively depends on ProcessPoolExecutor.
# Note: main does not call partial directly, but reaches it via compile_dir.
# Note: main does not call compile_file directly, but reaches it via compile_dir.
# main -> compile_path -> compile_dir: main has a transitive dependency on compile_dir through compile_path.
# main -> _parse_args -> ArgumentParser: main has a transitive dependency on ArgumentParser through _parse_args.
# Note: main does not call HeaderParser directly, but reaches it via make.
# main -> make -> generate: main has a transitive dependency on generate through make.
# Since main invokes make and make invokes cls, main indirectly relies on cls.
# main calls make, which calls Instruction -- so main transitively depends on Instruction.
# Note: main does not call add directly, but reaches it via make.
# main calls dis, which calls print_2 -- so main transitively depends on print_2.
# main -> dis -> ArgResolver: main has a transitive dependency on ArgResolver through dis.
# main calls dis, which calls _make_labels_map -- so main transitively depends on _make_labels_map.
# main -> dis -> _get_code_array: main has a transitive dependency on _get_code_array through dis.
# Since main invokes dis and dis invokes _disassemble_str, main indirectly relies on _disassemble_str.
# Since main invokes dis and dis invokes print_, main indirectly relies on print_.
# Note: main does not call Formatter directly, but reaches it via dis.
# main -> dis -> _disassemble_bytes: main has a transitive dependency on _disassemble_bytes through dis.
# main -> dis -> _hex_code: main has a transitive dependency on _hex_code through dis.
# main -> dis -> _disassemble_recursive: main has a transitive dependency on _disassemble_recursive through dis.
# Since main invokes dis and dis invokes dis_, main indirectly relies on dis_.
# main -> dis -> genops: main has a transitive dependency on genops through dis.
# Note: main does not call distb directly, but reaches it via dis.
# Note: main does not call _get_lineno_width directly, but reaches it via dis.
# main calls dis, which calls _get_positions_width -- so main transitively depends on _get_positions_width.
# normalize calls escape, which calls __dict_replace -- so normalize transitively depends on __dict_replace.
# Since normalize invokes escape and escape invokes replace, normalize indirectly relies on replace.
# normalize -> _append_modifier -> _replace_encoding: normalize has a transitive dependency on _replace_encoding through _append_modifier.
# Since normalize invokes _convert_other and _convert_other invokes Decimal, normalize indirectly relies on Decimal.
# Since normalize invokes getcontext and getcontext invokes Context, normalize indirectly relies on Context.
# Since write_pot_file invokes normalize and normalize invokes _dec_from_triple, write_pot_file indirectly relies on _dec_from_triple.
# Note: write_pot_file does not call escape directly, but reaches it via normalize.
# Note: write_pot_file does not call _byte_offset_to_character_offset directly, but reaches it via normalize.
# Note: write_pot_file does not call _append_modifier directly, but reaches it via normalize.
# write_pot_file calls normalize, which calls _convert_other -- so write_pot_file transitively depends on _convert_other.
# Note: write_pot_file does not call getcontext directly, but reaches it via normalize.
# Note: write_pot_file does not call _replace_encoding directly, but reaches it via normalize.
# combinefile calls read, which calls UnpicklingError -- so combinefile transitively depends on UnpicklingError.
# Since combinefile invokes read and read invokes size_index, combinefile indirectly relies on size_index.
# Note: combinefile does not call assert_ directly, but reaches it via read.
# combinefile calls read, which calls ReadError -- so combinefile transitively depends on ReadError.
# combinefile -> read -> Pair: combinefile has a transitive dependency on Pair through read.
# Since combine invokes combinefile and combinefile invokes read, combine indirectly relies on read.
# Note: rand_digits does not call _index directly, but reaches it via randrange.
# Note: probe_den does not call randrange directly, but reaches it via rand_digits.
# list_sort_descending calls list_sort_ascending, which calls _random_data -- so list_sort_descending transitively depends on _random_data.
# list_sort_ascending_exchanged calls list_sort_ascending, which calls _random_data -- so list_sort_ascending_exchanged transitively depends on _random_data.
# Note: list_sort_ascending_random does not call _random_data directly, but reaches it via list_sort_ascending.
# list_sort_ascending_one_percent -> list_sort_ascending -> _random_data: list_sort_ascending_one_percent has a transitive dependency on _random_data through list_sort_ascending.
# list_sort_duplicates -> list_sort_ascending -> _random_data: list_sort_duplicates has a transitive dependency on _random_data through list_sort_ascending.
# Note: load_raw_data does not call Path directly, but reaches it via _load_metadata_from_source.
# load_raw_data calls _load_metadata_from_source, which calls get_defines -- so load_raw_data transitively depends on get_defines.
# Note: execution_count_section does not call itemgetter directly, but reaches it via calc_execution_count_table.
# Since execution_count_section invokes calc_execution_count_table and calc_execution_count_table invokes Count, execution_count_section indirectly relies on Count.
# execution_count_section calls calc_execution_count_table, which calls Ratio -- so execution_count_section transitively depends on Ratio.
# Since specialization_section invokes calc_specialization_table and calc_specialization_table invokes Doc, specialization_section indirectly relies on Doc.
# specialization_section calls calc_specialization_table, which calls Ratio -- so specialization_section transitively depends on Ratio.
# Note: specialization_section does not call Count directly, but reaches it via calc_specialization_table.
# Note: specialization_section does not call itemgetter directly, but reaches it via calc_specialization_failure_kind_table.
# specialization_section -> calc_specialization_failure_kind_table -> Ratio: specialization_section has a transitive dependency on Ratio through calc_specialization_failure_kind_table.
# specialization_section calls calc_specialization_failure_kind_table, which calls Count -- so specialization_section transitively depends on Count.
# Since specialization_section invokes calc_specialization_success_failure_table and calc_specialization_success_failure_table invokes Count, specialization_section indirectly relies on Count.
# specialization_section calls calc_specialization_success_failure_table, which calls Ratio -- so specialization_section transitively depends on Ratio.
# optimization_section calls pair_count_section, which calls Count -- so optimization_section transitively depends on Count.
# optimization_section calls pair_count_section, which calls itemgetter -- so optimization_section transitively depends on itemgetter.
# Since optimization_section invokes pair_count_section and pair_count_section invokes Section, optimization_section indirectly relies on Section.
# optimization_section calls pair_count_section, which calls Table -- so optimization_section transitively depends on Table.
# Since optimization_section invokes pair_count_section and pair_count_section invokes Ratio, optimization_section indirectly relies on Ratio.
# optimization_section -> calc_histogram_table -> Ratio: optimization_section has a transitive dependency on Ratio through calc_histogram_table.
# optimization_section -> calc_histogram_table -> Count: optimization_section has a transitive dependency on Count through calc_histogram_table.
# Since optimization_section invokes calc_execution_count_table and calc_execution_count_table invokes itemgetter, optimization_section indirectly relies on itemgetter.
# optimization_section calls calc_execution_count_table, which calls Count -- so optimization_section transitively depends on Count.
# Note: optimization_section does not call Ratio directly, but reaches it via calc_execution_count_table.
# Note: output_stats does not call to_markdown directly, but reaches it via output_markdown.
# output_stats calls load_raw_data, which calls _load_metadata_from_source -- so output_stats transitively depends on _load_metadata_from_source.
# read_namedtuple calls namedtuple, which calls _zip -- so read_namedtuple transitively depends on _zip.
# read_namedtuple -> namedtuple -> _tuplegetter: read_namedtuple has a transitive dependency on _tuplegetter through namedtuple.
# Note: read_namedtuple does not call _dict directly, but reaches it via namedtuple.
# read_namedtuple calls namedtuple, which calls _iskeyword -- so read_namedtuple transitively depends on _iskeyword.
# read_namedtuple -> namedtuple -> _tuple: read_namedtuple has a transitive dependency on _tuple through namedtuple.
# Since read_namedtuple invokes namedtuple and namedtuple invokes tuple_new, read_namedtuple indirectly relies on tuple_new.
# read_namedtuple -> namedtuple -> _len: read_namedtuple has a transitive dependency on _len through namedtuple.
# read_namedtuple calls namedtuple, which calls _map -- so read_namedtuple transitively depends on _map.
# Since list_append_pop invokes pop and pop invokes ref, list_append_pop indirectly relies on ref.
# list_append_pop -> pop -> itemref: list_append_pop has a transitive dependency on itemref through pop.
# list_append_pop calls pop, which calls ParseError -- so list_append_pop transitively depends on ParseError.
# deque_append_pop calls pop, which calls ref -- so deque_append_pop transitively depends on ref.
# deque_append_pop calls pop, which calls itemref -- so deque_append_pop transitively depends on itemref.
# Since deque_append_pop invokes pop and pop invokes ParseError, deque_append_pop indirectly relies on ParseError.
# deque_append_popleft -> pop -> ref: deque_append_popleft has a transitive dependency on ref through pop.
# deque_append_popleft -> pop -> itemref: deque_append_popleft has a transitive dependency on itemref through pop.
# deque_append_popleft -> pop -> ParseError: deque_append_popleft has a transitive dependency on ParseError through pop.
# aix_platform calls _aix_bos_rte, which calls _read_cmd_output -- so aix_platform transitively depends on _read_cmd_output.
# Note: aix_buildtag does not call _aix_vrtl directly, but reaches it via _aix_bgt.
# Note: get_colors does not call _safe_getenv directly, but reaches it via can_colorize.
# Note: get_theme does not call _safe_getenv directly, but reaches it via can_colorize.
# _supports_universal_builds -> _get_system_version_tuple -> _get_system_version: _supports_universal_builds has a transitive dependency on _get_system_version through _get_system_version_tuple.
# Since _supports_arm64_builds invokes _get_system_version_tuple and _get_system_version_tuple invokes _get_system_version, _supports_arm64_builds indirectly relies on _get_system_version.
# Since _find_appropriate_compiler invokes _find_build_tool and _find_build_tool invokes _find_executable, _find_appropriate_compiler indirectly relies on _find_executable.
# Note: _find_appropriate_compiler does not call _read_output directly, but reaches it via _find_build_tool.
# compiler_fixup -> _supports_universal_builds -> _get_system_version_tuple: compiler_fixup has a transitive dependency on _get_system_version_tuple through _supports_universal_builds.
# compiler_fixup calls _supports_arm64_builds, which calls _get_system_version_tuple -- so compiler_fixup transitively depends on _get_system_version_tuple.
# Since customize_config_vars invokes _supports_universal_builds and _supports_universal_builds invokes _get_system_version_tuple, customize_config_vars indirectly relies on _get_system_version_tuple.
# Since customize_config_vars invokes _check_for_unavailable_sdk and _check_for_unavailable_sdk invokes _save_modified_value, customize_config_vars indirectly relies on _save_modified_value.
# customize_config_vars -> _remove_universal_flags -> _save_modified_value: customize_config_vars has a transitive dependency on _save_modified_value through _remove_universal_flags.
# customize_config_vars -> _override_all_archs -> _save_modified_value: customize_config_vars has a transitive dependency on _save_modified_value through _override_all_archs.
# customize_compiler -> _find_appropriate_compiler -> _find_executable: customize_compiler has a transitive dependency on _find_executable through _find_appropriate_compiler.
# customize_compiler -> _find_appropriate_compiler -> _read_output: customize_compiler has a transitive dependency on _read_output through _find_appropriate_compiler.
# customize_compiler calls _find_appropriate_compiler, which calls _find_build_tool -- so customize_compiler transitively depends on _find_build_tool.
# customize_compiler -> _find_appropriate_compiler -> _save_modified_value: customize_compiler has a transitive dependency on _save_modified_value through _find_appropriate_compiler.
# customize_compiler calls _remove_unsupported_archs, which calls _save_modified_value -- so customize_compiler transitively depends on _save_modified_value.
# customize_compiler calls _override_all_archs, which calls _save_modified_value -- so customize_compiler transitively depends on _save_modified_value.
# warn -> _is_internal_frame -> _is_internal_filename: warn has a transitive dependency on _is_internal_filename through _is_internal_frame.
# Since warn invokes _next_external_frame and _next_external_frame invokes _is_internal_filename, warn indirectly relies on _is_internal_filename.
# Since warn invokes _next_external_frame and _next_external_frame invokes _is_filename_to_skip, warn indirectly relies on _is_filename_to_skip.
# warn -> warning -> basicConfig: warn has a transitive dependency on basicConfig through warning.
# _warn_unawaited_coroutine -> extract -> extended_frame_gen: _warn_unawaited_coroutine has a transitive dependency on extended_frame_gen through extract.
# Note: _ymd2ord does not call _is_leap directly, but reaches it via _days_before_month.
# Since _ymd2ord invokes _days_in_month and _days_in_month invokes _is_leap, _ymd2ord indirectly relies on _is_leap.
# _ord2ymd -> _days_in_month -> _is_leap: _ord2ymd has a transitive dependency on _is_leap through _days_in_month.
# _build_struct_time calls _days_before_month, which calls _is_leap -- so _build_struct_time transitively depends on _is_leap.
# Note: _build_struct_time does not call _days_before_year directly, but reaches it via _ymd2ord.
# Since _build_struct_time invokes _ymd2ord and _ymd2ord invokes _post_epoch_days_before_year, _build_struct_time indirectly relies on _post_epoch_days_before_year.
# _build_struct_time calls _ymd2ord, which calls _days_before_month -- so _build_struct_time transitively depends on _days_before_month.
# _build_struct_time -> _ymd2ord -> _days_in_month: _build_struct_time has a transitive dependency on _days_in_month through _ymd2ord.
# Since _wrap_strftime invokes push and push invokes trace, _wrap_strftime indirectly relies on trace.
# _wrap_strftime calls push, which calls Event -- so _wrap_strftime transitively depends on Event.
# _wrap_strftime -> _format_offset -> timedelta: _wrap_strftime has a transitive dependency on timedelta through _format_offset.
# Note: _parse_isoformat_date does not call _ymd2ord directly, but reaches it via _isoweek_to_gregorian.
# _parse_isoformat_date -> _isoweek_to_gregorian -> _ord2ymd: _parse_isoformat_date has a transitive dependency on _ord2ymd through _isoweek_to_gregorian.
# _parse_isoformat_date -> _isoweek_to_gregorian -> _is_leap: _parse_isoformat_date has a transitive dependency on _is_leap through _isoweek_to_gregorian.
# Note: _parse_isoformat_date does not call _isoweek1monday directly, but reaches it via _isoweek_to_gregorian.
# _isoweek_to_gregorian calls _ymd2ord, which calls _days_before_year -- so _isoweek_to_gregorian transitively depends on _days_before_year.
# _isoweek_to_gregorian calls _ymd2ord, which calls _post_epoch_days_before_year -- so _isoweek_to_gregorian transitively depends on _post_epoch_days_before_year.
# Note: _isoweek_to_gregorian does not call _days_before_month directly, but reaches it via _ymd2ord.
# Note: _isoweek_to_gregorian does not call _days_in_month directly, but reaches it via _ymd2ord.
# _isoweek_to_gregorian -> _ord2ymd -> _is_leap: _isoweek_to_gregorian has a transitive dependency on _is_leap through _ord2ymd.
# _isoweek_to_gregorian -> _ord2ymd -> _days_in_month: _isoweek_to_gregorian has a transitive dependency on _days_in_month through _ord2ymd.
# _isoweek_to_gregorian calls _isoweek1monday, which calls _ymd2ord -- so _isoweek_to_gregorian transitively depends on _ymd2ord.
# Note: _check_date_fields does not call _format_stack_entry directly, but reaches it via _index.
# _check_date_fields calls _days_in_month, which calls _is_leap -- so _check_date_fields transitively depends on _is_leap.
# Note: _check_time_fields does not call _format_stack_entry directly, but reaches it via _index.
# Note: _isoweek1monday does not call _days_before_year directly, but reaches it via _ymd2ord.
# _isoweek1monday calls _ymd2ord, which calls _post_epoch_days_before_year -- so _isoweek1monday transitively depends on _post_epoch_days_before_year.
# _isoweek1monday -> _ymd2ord -> _days_before_month: _isoweek1monday has a transitive dependency on _days_before_month through _ymd2ord.
# _isoweek1monday -> _ymd2ord -> _days_in_month: _isoweek1monday has a transitive dependency on _days_in_month through _ymd2ord.
# Since localcontext invokes getcontext and getcontext invokes Context, localcontext indirectly relies on Context.
# Since _dlog10 invokes _ilog and _ilog invokes _sqrt_nearest, _dlog10 indirectly relies on _sqrt_nearest.
# Note: _dlog10 does not call _div_nearest directly, but reaches it via _ilog.
# _dlog10 calls _ilog, which calls _rshift_nearest -- so _dlog10 transitively depends on _rshift_nearest.
# _dlog calls _ilog, which calls _sqrt_nearest -- so _dlog transitively depends on _sqrt_nearest.
# _dlog calls _ilog, which calls _div_nearest -- so _dlog transitively depends on _div_nearest.
# Since _dlog invokes _ilog and _ilog invokes _rshift_nearest, _dlog indirectly relies on _rshift_nearest.
# Since _dexp invokes _iexp and _iexp invokes _div_nearest, _dexp indirectly relies on _div_nearest.
# _dexp calls _iexp, which calls _nbits -- so _dexp transitively depends on _nbits.
# Note: _dpower does not call _div_nearest directly, but reaches it via _dlog.
# _dpower calls _dlog, which calls _log10_digits -- so _dpower transitively depends on _log10_digits.
# _dpower calls _dlog, which calls _ilog -- so _dpower transitively depends on _ilog.
# Note: _dpower does not call _div_nearest directly, but reaches it via _dexp.
# _dpower -> _dexp -> _log10_digits: _dpower has a transitive dependency on _log10_digits through _dexp.
# Note: _dpower does not call _iexp directly, but reaches it via _dexp.
# Note: _convert_for_comparison does not call Context directly, but reaches it via getcontext.
# Since _group_lengths invokes repeat and repeat invokes Timer, _group_lengths indirectly relies on Timer.
# Note: _insert_thousands_sep does not call chain directly, but reaches it via _group_lengths.
# _insert_thousands_sep calls _group_lengths, which calls repeat -- so _insert_thousands_sep transitively depends on repeat.
# _format_number -> _insert_thousands_sep -> _group_lengths: _format_number has a transitive dependency on _group_lengths through _insert_thousands_sep.
# Since open invokes register_standard_browsers and register_standard_browsers invokes IOSBrowser, open indirectly relies on IOSBrowser.
# open -> register_standard_browsers -> register_X_browsers: open has a transitive dependency on register_X_browsers through register_standard_browsers.
# Since open invokes register_standard_browsers and register_standard_browsers invokes Elinks, open indirectly relies on Elinks.
# Note: open does not call register directly, but reaches it via register_standard_browsers.
# Since open invokes register_standard_browsers and register_standard_browsers invokes _synthesize, open indirectly relies on _synthesize.
# open -> register_standard_browsers -> GenericBrowser: open has a transitive dependency on GenericBrowser through register_standard_browsers.
# open calls register_standard_browsers, which calls Edge -- so open transitively depends on Edge.
# open calls register_standard_browsers, which calls MacOSXOSAScript -- so open transitively depends on MacOSXOSAScript.
# open calls register_standard_browsers, which calls BackgroundBrowser -- so open transitively depends on BackgroundBrowser.
# Note: open does not call Lookup directly, but reaches it via lookup.
# Since open invokes lookup and lookup invokes Symbol, open indirectly relies on Symbol.
# open calls detect_encoding, which calls _get_normal_name -- so open transitively depends on _get_normal_name.
# Note: open does not call find_cookie directly, but reaches it via detect_encoding.
# Note: open does not call read_or_stop directly, but reaches it via detect_encoding.
# open -> detect_encoding -> check: open has a transitive dependency on check through detect_encoding.
# Since open invokes detect_encoding and detect_encoding invokes bstartswith, open indirectly relies on bstartswith.
# open calls detect_encoding, which calls readline -- so open transitively depends on readline.
# Since open invokes detect_encoding and detect_encoding invokes lookup, open indirectly relies on lookup.
# Since open invokes get and get invokes _impl, open indirectly relies on _impl.
# open calls get, which calls ref -- so open transitively depends on ref.
# Since open invokes get and get invokes NoOptionError, open indirectly relies on NoOptionError.
# Note: open does not call time directly, but reaches it via get.
# Note: open does not call GenericBrowser directly, but reaches it via get.
# open -> get -> register_standard_browsers: open has a transitive dependency on register_standard_browsers through get.
# Since open invokes get and get invokes func, open indirectly relies on func.
# open calls get, which calls BackgroundBrowser -- so open transitively depends on BackgroundBrowser.
# Note: open does not call _resolve_unbound directly, but reaches it via get.
# Since open invokes get and get invokes pack, open indirectly relies on pack.
# Note: open does not call _synthesize directly, but reaches it via get.
# Note: open does not call Error directly, but reaches it via get.
# Note: open does not call wr directly, but reaches it via get.
# open -> func -> getter: open has a transitive dependency on getter through func.
# Note: open does not call TextIOWrapper directly, but reaches it via _io_wrapper.
# Note: int_to_decimal does not call D directly, but reaches it via inner.
# Since int_to_decimal invokes inner and inner invokes _TLSContentType, int_to_decimal indirectly relies on _TLSContentType.
# Since int_to_decimal invokes inner and inner invokes TLSVersion, int_to_decimal indirectly relies on TLSVersion.
# int_to_decimal calls inner, which calls method -- so int_to_decimal transitively depends on method.
# int_to_decimal -> inner -> removeHandler: int_to_decimal has a transitive dependency on removeHandler through inner.
# int_to_decimal -> inner -> callback: int_to_decimal has a transitive dependency on callback through inner.
# int_to_decimal -> inner -> wraps: int_to_decimal has a transitive dependency on wraps through inner.
# int_to_decimal calls inner, which calls msg_enum -- so int_to_decimal transitively depends on msg_enum.
# Since int_to_decimal invokes inner and inner invokes func, int_to_decimal indirectly relies on func.
# int_to_decimal_string -> int_to_decimal -> compute_powers: int_to_decimal_string has a transitive dependency on compute_powers through int_to_decimal.
# Since int_to_decimal_string invokes int_to_decimal and int_to_decimal invokes D, int_to_decimal_string indirectly relies on D.
# Note: int_to_decimal_string does not call inner directly, but reaches it via int_to_decimal.
# int_to_decimal_string -> inner -> D: int_to_decimal_string has a transitive dependency on D through inner.
# Note: int_to_decimal_string does not call _TLSContentType directly, but reaches it via inner.
# int_to_decimal_string calls inner, which calls TLSVersion -- so int_to_decimal_string transitively depends on TLSVersion.
# int_to_decimal_string calls inner, which calls method -- so int_to_decimal_string transitively depends on method.
# int_to_decimal_string calls inner, which calls removeHandler -- so int_to_decimal_string transitively depends on removeHandler.
# int_to_decimal_string calls inner, which calls callback -- so int_to_decimal_string transitively depends on callback.
# Since int_to_decimal_string invokes inner and inner invokes wraps, int_to_decimal_string indirectly relies on wraps.
# Since int_to_decimal_string invokes inner and inner invokes msg_enum, int_to_decimal_string indirectly relies on msg_enum.
# int_to_decimal_string calls inner, which calls func -- so int_to_decimal_string transitively depends on func.
# _str_to_int_inner -> inner -> D: _str_to_int_inner has a transitive dependency on D through inner.
# _str_to_int_inner calls inner, which calls _TLSContentType -- so _str_to_int_inner transitively depends on _TLSContentType.
# Note: _str_to_int_inner does not call TLSVersion directly, but reaches it via inner.
# _str_to_int_inner -> inner -> method: _str_to_int_inner has a transitive dependency on method through inner.
# Since _str_to_int_inner invokes inner and inner invokes removeHandler, _str_to_int_inner indirectly relies on removeHandler.
# Since _str_to_int_inner invokes inner and inner invokes callback, _str_to_int_inner indirectly relies on callback.
# Note: _str_to_int_inner does not call wraps directly, but reaches it via inner.
# _str_to_int_inner -> inner -> msg_enum: _str_to_int_inner has a transitive dependency on msg_enum through inner.
# Note: _str_to_int_inner does not call func directly, but reaches it via inner.
# _dec_str_to_int_inner calls inner, which calls D -- so _dec_str_to_int_inner transitively depends on D.
# _dec_str_to_int_inner calls inner, which calls _TLSContentType -- so _dec_str_to_int_inner transitively depends on _TLSContentType.
# _dec_str_to_int_inner calls inner, which calls TLSVersion -- so _dec_str_to_int_inner transitively depends on TLSVersion.
# Note: _dec_str_to_int_inner does not call method directly, but reaches it via inner.
# Since _dec_str_to_int_inner invokes inner and inner invokes removeHandler, _dec_str_to_int_inner indirectly relies on removeHandler.
# _dec_str_to_int_inner -> inner -> callback: _dec_str_to_int_inner has a transitive dependency on callback through inner.
# Note: _dec_str_to_int_inner does not call wraps directly, but reaches it via inner.
# Note: _dec_str_to_int_inner does not call msg_enum directly, but reaches it via inner.
# Note: _dec_str_to_int_inner does not call func directly, but reaches it via inner.
# Since int_from_string invokes func and func invokes getter, int_from_string indirectly relies on getter.
# str_to_int calls int_from_string, which calls func -- so str_to_int transitively depends on func.
# _int2digits calls inner, which calls D -- so _int2digits transitively depends on D.
# Since _int2digits invokes inner and inner invokes _TLSContentType, _int2digits indirectly relies on _TLSContentType.
# _int2digits -> inner -> TLSVersion: _int2digits has a transitive dependency on TLSVersion through inner.
# _int2digits -> inner -> method: _int2digits has a transitive dependency on method through inner.
# _int2digits -> inner -> removeHandler: _int2digits has a transitive dependency on removeHandler through inner.
# _int2digits calls inner, which calls callback -- so _int2digits transitively depends on callback.
# Note: _int2digits does not call wraps directly, but reaches it via inner.
# _int2digits -> inner -> msg_enum: _int2digits has a transitive dependency on msg_enum through inner.
# _int2digits -> inner -> func: _int2digits has a transitive dependency on func through inner.
# Note: _digits2int does not call D directly, but reaches it via inner.
# Note: _digits2int does not call _TLSContentType directly, but reaches it via inner.
# Note: _digits2int does not call TLSVersion directly, but reaches it via inner.
# _digits2int calls inner, which calls method -- so _digits2int transitively depends on method.
# Since _digits2int invokes inner and inner invokes removeHandler, _digits2int indirectly relies on removeHandler.
# Since _digits2int invokes inner and inner invokes callback, _digits2int indirectly relies on callback.
# Since _digits2int invokes inner and inner invokes wraps, _digits2int indirectly relies on wraps.
# _digits2int -> inner -> msg_enum: _digits2int has a transitive dependency on msg_enum through inner.
# _digits2int -> inner -> func: _digits2int has a transitive dependency on func through inner.
# _divmod_pos calls _int2digits, which calls inner -- so _divmod_pos transitively depends on inner.
# _divmod_pos calls _digits2int, which calls inner -- so _divmod_pos transitively depends on inner.
# Note: _divmod_pos does not call _div3n2n directly, but reaches it via _div2n1n.
# Note: int_divmod does not call _int2digits directly, but reaches it via _divmod_pos.
# Since int_divmod invokes _divmod_pos and _divmod_pos invokes _digits2int, int_divmod indirectly relies on _digits2int.
# Note: int_divmod does not call _div2n1n directly, but reaches it via _divmod_pos.
# install_threading_hook calls field, which calls Field -- so install_threading_hook transitively depends on Field.
# Since build_menu invokes left_align and left_align invokes stripcolor, build_menu indirectly relies on stripcolor.
# parse_keys -> _parse_single_key_sequence -> KeySpecError: parse_keys has a transitive dependency on KeySpecError through _parse_single_key_sequence.
# Note: interactive_console does not call _get_reader directly, but reaches it via run_multiline_interactive_console.
# interactive_console calls run_multiline_interactive_console, which calls command -- so interactive_console transitively depends on command.
# Note: interactive_console does not call append_history_file directly, but reaches it via run_multiline_interactive_console.
# Note: interactive_console does not call _setup directly, but reaches it via run_multiline_interactive_console.
# Note: interactive_console does not call multiline_input directly, but reaches it via run_multiline_interactive_console.
# interactive_console calls run_multiline_interactive_console, which calls maybe_run_command -- so interactive_console transitively depends on maybe_run_command.
# interactive_console calls run_multiline_interactive_console, which calls _strip_final_indent -- so interactive_console transitively depends on _strip_final_indent.
# Note: interactive_console does not call _emscripten_log directly, but reaches it via trace.
# Since interactive_console invokes trace and trace invokes getinnerframes, interactive_console indirectly relies on getinnerframes.
# get_pager calls pipe_pager, which calls escape_less -- so get_pager transitively depends on escape_less.
# _setup calls _spec_from_module, which calls ModuleSpec -- so _setup transitively depends on ModuleSpec.
# Since _setup invokes _builtin_from_name and _builtin_from_name invokes _load_unlocked, _setup indirectly relies on _load_unlocked.
# check -> process_tokens -> NannyNag: check has a transitive dependency on NannyNag through process_tokens.
# check calls process_tokens, which calls _process_tokens -- so check transitively depends on _process_tokens.
# run_multiline_interactive_console -> append_history_file -> gethistoryfile: run_multiline_interactive_console has a transitive dependency on gethistoryfile through append_history_file.
# Since run_multiline_interactive_console invokes _setup and _setup invokes _init_module_attrs, run_multiline_interactive_console indirectly relies on _init_module_attrs.
# Note: run_multiline_interactive_console does not call _spec_from_module directly, but reaches it via _setup.
# Note: run_multiline_interactive_console does not call ModuleCompleter directly, but reaches it via _setup.
# run_multiline_interactive_console calls _setup, which calls _WeakValueDictionary -- so run_multiline_interactive_console transitively depends on _WeakValueDictionary.
# run_multiline_interactive_console calls _setup, which calls RLCompleter -- so run_multiline_interactive_console transitively depends on RLCompleter.
# Note: run_multiline_interactive_console does not call _builtin_from_name directly, but reaches it via _setup.
# run_multiline_interactive_console -> maybe_run_command -> _get_reader: run_multiline_interactive_console has a transitive dependency on _get_reader through maybe_run_command.
# run_multiline_interactive_console calls maybe_run_command, which calls command -- so run_multiline_interactive_console transitively depends on command.
# _read_terminfo_file -> _get_terminfo_dirs -> Path: _read_terminfo_file has a transitive dependency on Path through _get_terminfo_dirs.
# Since _read_terminfo_file invokes _validate_terminal_name_or_raise and _validate_terminal_name_or_raise invokes Path, _read_terminfo_file indirectly relies on Path.
# Note: get_terminal_keycodes does not call _emscripten_log directly, but reaches it via trace.
# get_terminal_keycodes -> trace -> getinnerframes: get_terminal_keycodes has a transitive dependency on getinnerframes through trace.
# gen_colors calls gen_colors_from_token_stream, which calls prev_next_window -- so gen_colors transitively depends on prev_next_window.
# Since gen_colors invokes gen_colors_from_token_stream and gen_colors_from_token_stream invokes is_soft_keyword_used, gen_colors indirectly relies on is_soft_keyword_used.
# gen_colors calls gen_colors_from_token_stream, which calls ColorSpan -- so gen_colors transitively depends on ColorSpan.
# Note: gen_colors does not call ColorSpan directly, but reaches it via recover_unterminated_string.
# Note: gen_colors does not call trace directly, but reaches it via recover_unterminated_string.
# gen_colors -> recover_unterminated_string -> Span: gen_colors has a transitive dependency on Span through recover_unterminated_string.
# Since recover_unterminated_string invokes trace and trace invokes _emscripten_log, recover_unterminated_string indirectly relies on _emscripten_log.
# Note: recover_unterminated_string does not call getinnerframes directly, but reaches it via trace.
# gen_colors_from_token_stream -> prev_next_window -> deque: gen_colors_from_token_stream has a transitive dependency on deque through prev_next_window.
# gen_colors_from_token_stream calls is_soft_keyword_used, which calls trace -- so gen_colors_from_token_stream transitively depends on trace.
# is_soft_keyword_used calls trace, which calls _emscripten_log -- so is_soft_keyword_used transitively depends on _emscripten_log.
# is_soft_keyword_used calls trace, which calls getinnerframes -- so is_soft_keyword_used transitively depends on getinnerframes.
# Note: _strptime does not call datetime_date directly, but reaches it via _calc_julian_from_U_or_W.
# _strptime_time -> _strptime -> TimeRE: _strptime_time has a transitive dependency on TimeRE through _strptime.
# _strptime_time calls _strptime, which calls _calc_julian_from_U_or_W -- so _strptime_time transitively depends on _calc_julian_from_U_or_W.
# Since _strptime_time invokes _strptime and _strptime invokes datetime_date, _strptime_time indirectly relies on datetime_date.
# Since _strptime_time invokes _strptime and _strptime invokes _getlang, _strptime_time indirectly relies on _getlang.
# _strptime_time calls _strptime, which calls parse_int -- so _strptime_time transitively depends on parse_int.
# Note: _strptime_datetime_date does not call TimeRE directly, but reaches it via _strptime.
# _strptime_datetime_date -> _strptime -> _calc_julian_from_U_or_W: _strptime_datetime_date has a transitive dependency on _calc_julian_from_U_or_W through _strptime.
# _strptime_datetime_date calls _strptime, which calls datetime_date -- so _strptime_datetime_date transitively depends on datetime_date.
# Since _strptime_datetime_date invokes _strptime and _strptime invokes _getlang, _strptime_datetime_date indirectly relies on _getlang.
# Note: _strptime_datetime_date does not call parse_int directly, but reaches it via _strptime.
# Note: _strptime_datetime_time does not call TimeRE directly, but reaches it via _strptime.
# _strptime_datetime_time calls _strptime, which calls _calc_julian_from_U_or_W -- so _strptime_datetime_time transitively depends on _calc_julian_from_U_or_W.
# _strptime_datetime_time calls _strptime, which calls datetime_date -- so _strptime_datetime_time transitively depends on datetime_date.
# Note: _strptime_datetime_time does not call _getlang directly, but reaches it via _strptime.
# _strptime_datetime_time calls _strptime, which calls parse_int -- so _strptime_datetime_time transitively depends on parse_int.
# _strptime_datetime_time calls _parse_tz, which calls datetime_timedelta -- so _strptime_datetime_time transitively depends on datetime_timedelta.
# Note: _strptime_datetime_time does not call datetime_timezone directly, but reaches it via _parse_tz.
# Since _strptime_datetime_datetime invokes _strptime and _strptime invokes TimeRE, _strptime_datetime_datetime indirectly relies on TimeRE.
# _strptime_datetime_datetime calls _strptime, which calls _calc_julian_from_U_or_W -- so _strptime_datetime_datetime transitively depends on _calc_julian_from_U_or_W.
# Note: _strptime_datetime_datetime does not call datetime_date directly, but reaches it via _strptime.
# Note: _strptime_datetime_datetime does not call _getlang directly, but reaches it via _strptime.
# Note: _strptime_datetime_datetime does not call parse_int directly, but reaches it via _strptime.
# _strptime_datetime_datetime calls _parse_tz, which calls datetime_timedelta -- so _strptime_datetime_datetime transitively depends on datetime_timedelta.
# Note: _strptime_datetime_datetime does not call datetime_timezone directly, but reaches it via _parse_tz.
# call_evaluate_function -> call_annotate_function -> annotations_to_string: call_evaluate_function has a transitive dependency on annotations_to_string through call_annotate_function.
# call_evaluate_function -> call_annotate_function -> _stringify_single: call_evaluate_function has a transitive dependency on _stringify_single through call_annotate_function.
# call_evaluate_function calls call_annotate_function, which calls annotate -- so call_evaluate_function transitively depends on annotate.
# call_evaluate_function -> call_annotate_function -> _StringifierDict: call_evaluate_function has a transitive dependency on _StringifierDict through call_annotate_function.
# call_evaluate_function calls call_annotate_function, which calls _build_closure -- so call_evaluate_function transitively depends on _build_closure.
# call_evaluate_function calls call_annotate_function, which calls func -- so call_evaluate_function transitively depends on func.
# call_annotate_function -> annotations_to_string -> type_repr: call_annotate_function has a transitive dependency on type_repr through annotations_to_string.
# call_annotate_function calls _stringify_single, which calls _template_to_ast -- so call_annotate_function transitively depends on _template_to_ast.
# call_annotate_function -> annotate -> _type_check: call_annotate_function has a transitive dependency on _type_check through annotate.
# call_annotate_function -> _build_closure -> _Stringifier: call_annotate_function has a transitive dependency on _Stringifier through _build_closure.
# Since call_annotate_function invokes func and func invokes getter, call_annotate_function indirectly relies on getter.
# _stringify_single calls _template_to_ast, which calls _template_to_ast_literal -- so _stringify_single transitively depends on _template_to_ast_literal.
# Since _stringify_single invokes _template_to_ast and _template_to_ast invokes _template_to_ast_constructor, _stringify_single indirectly relies on _template_to_ast_constructor.
# get_annotations -> annotations_to_string -> type_repr: get_annotations has a transitive dependency on type_repr through annotations_to_string.
# get_annotations calls _get_and_call_annotate, which calls call_annotate_function -- so get_annotations transitively depends on call_annotate_function.
# Since get_annotations invokes _get_dunder_annotations and _get_dunder_annotations invokes _BASE_GET_ANNOTATIONS, get_annotations indirectly relies on _BASE_GET_ANNOTATIONS.
# Since type_repr invokes _template_to_ast and _template_to_ast invokes _template_to_ast_literal, type_repr indirectly relies on _template_to_ast_literal.
# type_repr calls _template_to_ast, which calls _template_to_ast_constructor -- so type_repr transitively depends on _template_to_ast_constructor.
# Since annotations_to_string invokes type_repr and type_repr invokes _template_to_ast, annotations_to_string indirectly relies on _template_to_ast.
# _get_and_call_annotate calls call_annotate_function, which calls annotations_to_string -- so _get_and_call_annotate transitively depends on annotations_to_string.
# _get_and_call_annotate calls call_annotate_function, which calls _stringify_single -- so _get_and_call_annotate transitively depends on _stringify_single.
# _get_and_call_annotate -> call_annotate_function -> annotate: _get_and_call_annotate has a transitive dependency on annotate through call_annotate_function.
# _get_and_call_annotate -> call_annotate_function -> _StringifierDict: _get_and_call_annotate has a transitive dependency on _StringifierDict through call_annotate_function.
# _get_and_call_annotate calls call_annotate_function, which calls _build_closure -- so _get_and_call_annotate transitively depends on _build_closure.
# Since _get_and_call_annotate invokes call_annotate_function and call_annotate_function invokes func, _get_and_call_annotate indirectly relies on func.
# Since parse invokes _do_pulldom_parse and _do_pulldom_parse invokes func, parse indirectly relies on func.
# Since parse invokes make_parser and make_parser invokes SAXReaderNotAvailable, parse indirectly relies on SAXReaderNotAvailable.
# Note: parse does not call _create_parser directly, but reaches it via make_parser.
# parse calls _parse_sub, which calls itemsappend -- so parse transitively depends on itemsappend.
# Note: parse does not call _parse directly, but reaches it via _parse_sub.
# parse -> _parse_sub -> SubPattern: parse has a transitive dependency on SubPattern through _parse_sub.
# parse -> _parse_sub -> _uniq: parse has a transitive dependency on _uniq through _parse_sub.
# Since parse invokes _parse_sub and _parse_sub invokes sourcematch, parse indirectly relies on sourcematch.
# parse calls error, which calls basicConfig -- so parse transitively depends on basicConfig.
# Since parse invokes error and error invokes _, parse indirectly relies on _.
# parse -> error -> perror: parse has a transitive dependency on perror through error.
# literal_eval -> parse -> Group: literal_eval has a transitive dependency on Group through parse.
# Note: literal_eval does not call Address directly, but reaches it via parse.
# literal_eval -> parse -> RequestRate: literal_eval has a transitive dependency on RequestRate through parse.
# literal_eval -> parse -> FeedParser: literal_eval has a transitive dependency on FeedParser through parse.
# literal_eval -> parse -> ExpatBuilderNS: literal_eval has a transitive dependency on ExpatBuilderNS through parse.
# Since literal_eval invokes parse and parse invokes Tokenizer, literal_eval indirectly relies on Tokenizer.
# literal_eval calls parse, which calls DOMEventStream -- so literal_eval transitively depends on DOMEventStream.
# literal_eval calls parse, which calls XMLParser -- so literal_eval transitively depends on XMLParser.
# literal_eval -> parse -> _do_pulldom_parse: literal_eval has a transitive dependency on _do_pulldom_parse through parse.
# Since literal_eval invokes parse and parse invokes Example, literal_eval indirectly relies on Example.
# Note: literal_eval does not call cls directly, but reaches it via parse.
# Since literal_eval invokes parse and parse invokes RuleLine, literal_eval indirectly relies on RuleLine.
# Since literal_eval invokes parse and parse invokes ExpatBuilder, literal_eval indirectly relies on ExpatBuilder.
# Since literal_eval invokes parse and parse invokes TextIOWrapper, literal_eval indirectly relies on TextIOWrapper.
# literal_eval -> parse -> ElementTree: literal_eval has a transitive dependency on ElementTree through parse.
# Since literal_eval invokes parse and parse invokes make_parser, literal_eval indirectly relies on make_parser.
# Since literal_eval invokes parse and parse invokes ParserCreate, literal_eval indirectly relies on ParserCreate.
# Since literal_eval invokes parse and parse invokes InvalidFileException, literal_eval indirectly relies on InvalidFileException.
# literal_eval -> parse -> Entry: literal_eval has a transitive dependency on Entry through parse.
# literal_eval -> parse -> _parse_sub: literal_eval has a transitive dependency on _parse_sub through parse.
# Note: literal_eval does not call State directly, but reaches it via parse.
# literal_eval calls parse, which calls error -- so literal_eval transitively depends on error.
# Since literal_eval invokes parse and parse invokes ExpatLocator, literal_eval indirectly relies on ExpatLocator.
# literal_eval calls parse, which calls fix_flags -- so literal_eval transitively depends on fix_flags.
# Note: literal_eval does not call ErrorHandler directly, but reaches it via parse.
# literal_eval calls _convert_signed_num, which calls _convert_num -- so literal_eval transitively depends on _convert_num.
# literal_eval calls _convert_num, which calls _raise_malformed_node -- so literal_eval transitively depends on _raise_malformed_node.
# Since literal_eval invokes _convert and _convert invokes T, literal_eval indirectly relies on T.
# Note: literal_eval does not call _convert_signed_num directly, but reaches it via _convert.
# literal_eval -> _convert -> _raise_malformed_node: literal_eval has a transitive dependency on _raise_malformed_node through _convert.
# Note: literal_eval does not call _convert_num directly, but reaches it via _convert.
# Since dump invokes _format and _format invokes p, dump indirectly relies on p.
# Note: dump does not call _localize directly, but reaches it via _format.
# dump -> _format -> _recursion: dump has a transitive dependency on _recursion through _format.
# dump -> _format -> is_dataclass: dump has a transitive dependency on is_dataclass through _format.
# dump calls _format, which calls formatannotation -- so dump transitively depends on formatannotation.
# Since fix_missing_locations invokes iter_child_nodes and iter_child_nodes invokes iter_fields, fix_missing_locations indirectly relies on iter_fields.
# Note: fix_missing_locations does not call _dec_from_triple directly, but reaches it via _fix.
# fix_missing_locations -> _fix -> rounding_method: fix_missing_locations has a transitive dependency on rounding_method through _fix.
# fix_missing_locations calls _fix, which calls iter_child_nodes -- so fix_missing_locations transitively depends on iter_child_nodes.
# Note: fix_missing_locations does not call Decimal directly, but reaches it via _fix.
# Note: increment_lineno does not call on_error directly, but reaches it via walk.
# increment_lineno calls walk, which calls iter_child_nodes -- so increment_lineno transitively depends on iter_child_nodes.
# Note: increment_lineno does not call join directly, but reaches it via walk.
# increment_lineno -> walk -> scandir: increment_lineno has a transitive dependency on scandir through walk.
# Note: increment_lineno does not call fspath directly, but reaches it via walk.
# Since increment_lineno invokes walk and walk invokes deque, increment_lineno indirectly relies on deque.
# increment_lineno -> walk -> islink: increment_lineno has a transitive dependency on islink through walk.
# increment_lineno -> walk -> onerror: increment_lineno has a transitive dependency on onerror through walk.
# Note: walk does not call iter_fields directly, but reaches it via iter_child_nodes.
# walk calls join, which calls wait -- so walk transitively depends on wait.
# Since walk invokes join and join invokes splitroot, walk indirectly relies on splitroot.
# walk calls join, which calls current_thread -- so walk transitively depends on current_thread.
# walk -> join -> _get_sep: walk has a transitive dependency on _get_sep through join.
# walk calls join, which calls quote -- so walk transitively depends on quote.
# Since walk invokes onerror and onerror invokes onexc, walk indirectly relies on onexc.
# Note: walk does not call callback directly, but reaches it via onerror.
# Note: compare does not call Decimal directly, but reaches it via _convert_other.
# Since compare invokes _compare_fields and _compare_fields invokes _compare, compare indirectly relies on _compare.
# Note: __getattr__ does not call child_klass directly, but reaches it via create_autospec.
# Note: __getattr__ does not call _is_async_func directly, but reaches it via create_autospec.
# Since __getattr__ invokes create_autospec and create_autospec invokes _check_signature, __getattr__ indirectly relies on _check_signature.
# Since __getattr__ invokes create_autospec and create_autospec invokes _callable, __getattr__ indirectly relies on _callable.
# Note: __getattr__ does not call fields directly, but reaches it via create_autospec.
# __getattr__ -> create_autospec -> _set_signature: __getattr__ has a transitive dependency on _set_signature through create_autospec.
# Since __getattr__ invokes create_autospec and create_autospec invokes iscoroutinefunction, __getattr__ indirectly relies on iscoroutinefunction.
# __getattr__ -> create_autospec -> _is_instance_mock: __getattr__ has a transitive dependency on _is_instance_mock through create_autospec.
# Note: __getattr__ does not call Klass directly, but reaches it via create_autospec.
# __getattr__ calls create_autospec, which calls _instance_callable -- so __getattr__ transitively depends on _instance_callable.
# Note: __getattr__ does not call InvalidSpecError directly, but reaches it via create_autospec.
# Since __getattr__ invokes create_autospec and create_autospec invokes _is_list, __getattr__ indirectly relies on _is_list.
# __getattr__ -> create_autospec -> _is_magic: __getattr__ has a transitive dependency on _is_magic through create_autospec.
# __getattr__ calls create_autospec, which calls _set_async_signature -- so __getattr__ transitively depends on _set_async_signature.
# Note: __getattr__ does not call _SpecState directly, but reaches it via create_autospec.
# Note: __getattr__ does not call _check_spec_arg_typos directly, but reaches it via create_autospec.
# __getattr__ calls create_autospec, which calls is_dataclass -- so __getattr__ transitively depends on is_dataclass.
# Since __getattr__ invokes create_autospec and create_autospec invokes _must_skip, __getattr__ indirectly relies on _must_skip.
# __getattr__ -> func -> getter: __getattr__ has a transitive dependency on getter through func.
# _future_repr_info calls _format_callbacks, which calls format_cb -- so _future_repr_info transitively depends on format_cb.
# _future_repr -> _future_repr_info -> _format_callbacks: _future_repr has a transitive dependency on _format_callbacks through _future_repr_info.
# Since iscoroutinefunction invokes _has_code_flag and _has_code_flag invokes ismethod, iscoroutinefunction indirectly relies on ismethod.
# Note: iscoroutinefunction does not call _signature_is_functionlike directly, but reaches it via _has_code_flag.
# Note: iscoroutinefunction does not call isfunction directly, but reaches it via _has_code_flag.
# iscoroutinefunction calls _has_coroutine_mark, which calls ismethod -- so iscoroutinefunction transitively depends on ismethod.
# Note: _get_event_loop_policy does not call _DefaultEventLoopPolicy directly, but reaches it via _init_event_loop_policy.
# Since get_event_loop_policy invokes _get_event_loop_policy and _get_event_loop_policy invokes _init_event_loop_policy, get_event_loop_policy indirectly relies on _init_event_loop_policy.
# Note: get_event_loop does not call _init_event_loop_policy directly, but reaches it via _get_event_loop_policy.
# set_event_loop -> _get_event_loop_policy -> _init_event_loop_policy: set_event_loop has a transitive dependency on _init_event_loop_policy through _get_event_loop_policy.
# new_event_loop calls _get_event_loop_policy, which calls _init_event_loop_policy -- so new_event_loop transitively depends on _init_event_loop_policy.
# _format_callback_source calls _format_callback, which calls _format_args_and_kwargs -- so _format_callback_source transitively depends on _format_args_and_kwargs.
# _chain_future -> _get_loop -> get_loop: _chain_future has a transitive dependency on get_loop through _get_loop.
# _chain_future calls _copy_future_state, which calls _convert_future_exc -- so _chain_future transitively depends on _convert_future_exc.
# _chain_future calls _set_concurrent_future_state, which calls _convert_future_exc -- so _chain_future transitively depends on _convert_future_exc.
# _chain_future -> _set_state -> _copy_future_state: _chain_future has a transitive dependency on _copy_future_state through _set_state.
# Note: _chain_future does not call _set_concurrent_future_state directly, but reaches it via _set_state.
# Note: _chain_future does not call isfuture directly, but reaches it via _set_state.
# Note: wrap_future does not call _get_loop directly, but reaches it via _chain_future.
# wrap_future calls _chain_future, which calls _copy_future_state -- so wrap_future transitively depends on _copy_future_state.
# wrap_future -> _chain_future -> _set_concurrent_future_state: wrap_future has a transitive dependency on _set_concurrent_future_state through _chain_future.
# wrap_future -> _chain_future -> isfuture: wrap_future has a transitive dependency on isfuture through _chain_future.
# wrap_future -> _chain_future -> _set_state: wrap_future has a transitive dependency on _set_state through _chain_future.
# Since capture_call_graph invokes _build_graph_for_future and _build_graph_for_future invokes get_coro, capture_call_graph indirectly relies on get_coro.
# capture_call_graph calls _build_graph_for_future, which calls FrameCallGraphEntry -- so capture_call_graph transitively depends on FrameCallGraphEntry.
# capture_call_graph calls _build_graph_for_future, which calls FutureCallGraph -- so capture_call_graph transitively depends on FutureCallGraph.
# format_call_graph calls render_level, which calls add_line -- so format_call_graph transitively depends on add_line.
# format_call_graph -> capture_call_graph -> _build_graph_for_future: format_call_graph has a transitive dependency on _build_graph_for_future through capture_call_graph.
# format_call_graph -> capture_call_graph -> FrameCallGraphEntry: format_call_graph has a transitive dependency on FrameCallGraphEntry through capture_call_graph.
# format_call_graph -> capture_call_graph -> FutureCallGraph: format_call_graph has a transitive dependency on FutureCallGraph through capture_call_graph.
# print_call_graph calls format_call_graph, which calls render_level -- so print_call_graph transitively depends on render_level.
# Since print_call_graph invokes format_call_graph and format_call_graph invokes capture_call_graph, print_call_graph indirectly relies on capture_call_graph.
# Since print_call_graph invokes format_call_graph and format_call_graph invokes add_line, print_call_graph indirectly relies on add_line.
# run calls _addSkip, which calls addSkip -- so run transitively depends on addSkip.
# Note: run does not call can_colorize directly, but reaches it via get_theme.
# run -> callback -> func: run has a transitive dependency on func through callback.
# run -> callback -> ensure_future: run has a transitive dependency on ensure_future through callback.
# Since run invokes run_multiline_interactive_console and run_multiline_interactive_console invokes _get_reader, run indirectly relies on _get_reader.
# Since run invokes run_multiline_interactive_console and run_multiline_interactive_console invokes command, run indirectly relies on command.
# run calls run_multiline_interactive_console, which calls append_history_file -- so run transitively depends on append_history_file.
# Note: run does not call _setup directly, but reaches it via run_multiline_interactive_console.
# Note: run does not call multiline_input directly, but reaches it via run_multiline_interactive_console.
# run -> run_multiline_interactive_console -> maybe_run_command: run has a transitive dependency on maybe_run_command through run_multiline_interactive_console.
# run -> run_multiline_interactive_console -> _strip_final_indent: run has a transitive dependency on _strip_final_indent through run_multiline_interactive_console.
# Note: run does not call Tdb directly, but reaches it via test.
# run -> test -> dllist: run has a transitive dependency on dllist through test.
# Since run invokes test and test invokes CDLL, run indirectly relies on CDLL.
# Since run invokes test and test invokes _get_best_family, run indirectly relies on _get_best_family.
# run calls test, which calls FTP -- so run transitively depends on FTP.
# run calls test, which calls ServerClass -- so run transitively depends on ServerClass.
# Note: run does not call ModuleFinder directly, but reaches it via test.
# Note: run does not call find_library directly, but reaches it via test.
# Note: run does not call onexc directly, but reaches it via onerror.
# Since run invokes onerror and onerror invokes callback, run indirectly relies on callback.
# run -> format_exception -> TracebackException: run has a transitive dependency on TracebackException through format_exception.
# run -> format_exception -> _parse_value_tb: run has a transitive dependency on _parse_value_tb through format_exception.
# Note: run does not call ref directly, but reaches it via pop.
# Note: run does not call itemref directly, but reaches it via pop.
# Since run invokes pop and pop invokes ParseError, run indirectly relies on ParseError.
# Note: as_completed does not call _FirstCompletedWaiter directly, but reaches it via _create_and_install_waiters.
# Note: as_completed does not call _AllCompletedWaiter directly, but reaches it via _create_and_install_waiters.
# as_completed -> _create_and_install_waiters -> _AsCompletedWaiter: as_completed has a transitive dependency on _AsCompletedWaiter through _create_and_install_waiters.
# Note: gather does not call _wrap_awaitable directly, but reaches it via ensure_future.
# shield calls ensure_future, which calls _wrap_awaitable -- so shield transitively depends on _wrap_awaitable.
# Note: run_coroutine_threadsafe does not call _wrap_awaitable directly, but reaches it via ensure_future.
# build_async_tree -> _index -> _format_stack_entry: build_async_tree has a transitive dependency on _format_stack_entry through _index.
# Since build_async_tree invokes _task_graph and _task_graph invokes defaultdict, build_async_tree indirectly relies on defaultdict.
# Note: build_async_tree does not call defaultdict directly, but reaches it via _build_tree.
# build_async_tree calls _build_tree, which calls get_or_create_cor_node -- so build_async_tree transitively depends on get_or_create_cor_node.
# Since build_async_tree invokes _build_tree and _build_tree invokes count, build_async_tree indirectly relies on count.
# build_async_tree calls render, which calls pretty -- so build_async_tree transitively depends on pretty.
# build_async_tree -> _find_cycles -> dfs: build_async_tree has a transitive dependency on dfs through _find_cycles.
# Note: build_async_tree does not call defaultdict directly, but reaches it via _find_cycles.
# Note: build_task_table does not call _format_stack_entry directly, but reaches it via _index.
# _get_awaited_by_tasks calls get_all_awaited_by, which calls RemoteUnwinder -- so _get_awaited_by_tasks transitively depends on RemoteUnwinder.
# display_awaited_by_tasks_table calls build_task_table, which calls _index -- so display_awaited_by_tasks_table transitively depends on _index.
# display_awaited_by_tasks_table calls build_task_table, which calls _format_stack_entry -- so display_awaited_by_tasks_table transitively depends on _format_stack_entry.
# Note: display_awaited_by_tasks_table does not call exit_with_permission_help_text directly, but reaches it via _get_awaited_by_tasks.
# display_awaited_by_tasks_table calls _get_awaited_by_tasks, which calls get_all_awaited_by -- so display_awaited_by_tasks_table transitively depends on get_all_awaited_by.
# display_awaited_by_tasks_tree -> build_async_tree -> pretty: display_awaited_by_tasks_tree has a transitive dependency on pretty through build_async_tree.
# display_awaited_by_tasks_tree calls build_async_tree, which calls _index -- so display_awaited_by_tasks_tree transitively depends on _index.
# display_awaited_by_tasks_tree calls build_async_tree, which calls _task_graph -- so display_awaited_by_tasks_tree transitively depends on _task_graph.
# display_awaited_by_tasks_tree calls build_async_tree, which calls _roots -- so display_awaited_by_tasks_tree transitively depends on _roots.
# display_awaited_by_tasks_tree -> build_async_tree -> _build_tree: display_awaited_by_tasks_tree has a transitive dependency on _build_tree through build_async_tree.
# display_awaited_by_tasks_tree -> build_async_tree -> CycleFoundException: display_awaited_by_tasks_tree has a transitive dependency on CycleFoundException through build_async_tree.
# Since display_awaited_by_tasks_tree invokes build_async_tree and build_async_tree invokes render, display_awaited_by_tasks_tree indirectly relies on render.
# Since display_awaited_by_tasks_tree invokes build_async_tree and build_async_tree invokes _find_cycles, display_awaited_by_tasks_tree indirectly relies on _find_cycles.
# display_awaited_by_tasks_tree -> _get_awaited_by_tasks -> exit_with_permission_help_text: display_awaited_by_tasks_tree has a transitive dependency on exit_with_permission_help_text through _get_awaited_by_tasks.
# Since display_awaited_by_tasks_tree invokes _get_awaited_by_tasks and _get_awaited_by_tasks invokes get_all_awaited_by, display_awaited_by_tasks_tree indirectly relies on get_all_awaited_by.
# standard_b64decode calls b64decode, which calls _bytes_from_decode_data -- so standard_b64decode transitively depends on _bytes_from_decode_data.
# Note: urlsafe_b64decode does not call _bytes_from_decode_data directly, but reaches it via b64decode.
# Note: b32encode does not call from_bytes directly, but reaches it via _b32encode.
# b32decode -> _b32decode -> _bytes_from_decode_data: b32decode has a transitive dependency on _bytes_from_decode_data through _b32decode.
# Note: b32hexencode does not call from_bytes directly, but reaches it via _b32encode.
# Since b32hexdecode invokes _b32decode and _b32decode invokes _bytes_from_decode_data, b32hexdecode indirectly relies on _bytes_from_decode_data.
# z85encode -> b85encode -> _85encode: z85encode has a transitive dependency on _85encode through b85encode.
# Note: z85decode does not call _bytes_from_decode_data directly, but reaches it via b85decode.
# Note: z85decode does not call packI directly, but reaches it via b85decode.
# Since encode invokes uu_encode and uu_encode invokes write, encode indirectly relies on write.
# Note: encode does not call read directly, but reaches it via uu_encode.
# Since encode invokes uu_encode and uu_encode invokes BytesIO, encode indirectly relies on BytesIO.
# encode -> write -> serialize: encode has a transitive dependency on serialize through write.
# Since encode invokes write and write invokes sleep, encode indirectly relies on sleep.
# Since encode invokes write and write invokes _serialize_text, encode indirectly relies on _serialize_text.
# Since encode invokes write and write invokes assert_, encode indirectly relies on assert_.
# Note: encode does not call _namespaces directly, but reaches it via write.
# encode -> write -> time: encode has a transitive dependency on time through write.
# Note: encode does not call _count_to_size directly, but reaches it via write.
# Note: encode does not call quote directly, but reaches it via write.
# Note: encode does not call _nbytes directly, but reaches it via write.
# Note: encode does not call _get_writer directly, but reaches it via write.
# Since encode invokes write and write invokes crc32, encode indirectly relies on crc32.
# Since encode invokes quote and quote invokes quote_from_bytes, encode indirectly relies on quote_from_bytes.
# Note: encode does not call nameprep directly, but reaches it via ToASCII.
# Since encode invokes punycode_encode and punycode_encode invokes segregate, encode indirectly relies on segregate.
# encode calls punycode_encode, which calls insertion_unsort -- so encode transitively depends on insertion_unsort.
# Note: encode does not call generate_integers directly, but reaches it via punycode_encode.
# encode calls quopri_encode, which calls BytesIO -- so encode transitively depends on BytesIO.
# Since test invokes dllist and dllist invokes _dl_iterate_phdr, test indirectly relies on _dl_iterate_phdr.
# test calls dllist, which calls _dyld_get_image_name -- so test transitively depends on _dyld_get_image_name.
# Since test invokes dllist and dllist invokes _get_module_handles, test indirectly relies on _get_module_handles.
# test -> dllist -> _get_module_filename: test has a transitive dependency on _get_module_filename through dllist.
# test calls run, which calls CompletedProcess -- so test transitively depends on CompletedProcess.
# Since test invokes run and run invokes _Outcome, test indirectly relies on _Outcome.
# Since test invokes run and run invokes stopTestRun, test indirectly relies on stopTestRun.
# test -> run -> DocServer: test has a transitive dependency on DocServer through run.
# test calls run, which calls _extract_future_flags -- so test transitively depends on _extract_future_flags.
# test -> run -> timefunc: test has a transitive dependency on timefunc through run.
# test -> run -> _addSkip: test has a transitive dependency on _addSkip through run.
# Note: test does not call get_theme directly, but reaches it via run.
# Note: test does not call callback directly, but reaches it via run.
# test calls run, which calls action -- so test transitively depends on action.
# Note: test does not call startTestRun directly, but reaches it via run.
# Note: test does not call Popen directly, but reaches it via run.
# Note: test does not call _OutputRedirectingPdb directly, but reaches it via run.
# test -> run -> Pdb: test has a transitive dependency on Pdb through run.
# Note: test does not call run_multiline_interactive_console directly, but reaches it via run.
# Since test invokes run and run invokes completer, test indirectly relies on completer.
# test -> run -> Runner: test has a transitive dependency on Runner through run.
# test -> run -> inspect: test has a transitive dependency on inspect through run.
# test calls run, which calls _Utils -- so test transitively depends on _Utils.
# test calls run, which calls _isnotsuite -- so test transitively depends on _isnotsuite.
# test calls run, which calls CalledProcessError -- so test transitively depends on CalledProcessError.
# Since test invokes run and run invokes application, test indirectly relies on application.
# test -> run -> source_synopsis: test has a transitive dependency on source_synopsis through run.
# test calls run, which calls _wrap_awaitable -- so test transitively depends on _wrap_awaitable.
# test -> run -> onerror: test has a transitive dependency on onerror through run.
# test calls run, which calls uncancel -- so test transitively depends on uncancel.
# Since test invokes run and run invokes format_exception, test indirectly relies on format_exception.
# test calls run, which calls registerResult -- so test transitively depends on registerResult.
# Note: test does not call pop directly, but reaches it via run.
# Since test invokes run and run invokes delayfunc, test indirectly relies on delayfunc.
# Note: test does not call fn directly, but reaches it via run.
# Since test invokes find_library and find_library invokes get_libpaths, test indirectly relies on get_libpaths.
# Note: test does not call _get_soname directly, but reaches it via find_library.
# Since test invokes find_library and find_library invokes find_msvcrt, test indirectly relies on find_msvcrt.
# test -> find_library -> _findSoname_ldconfig: test has a transitive dependency on _findSoname_ldconfig through find_library.
# test calls find_library, which calls _is_wasm -- so test transitively depends on _is_wasm.
# test calls find_library, which calls find_shared -- so test transitively depends on find_shared.
# test -> find_library -> _dyld_find: test has a transitive dependency on _dyld_find through find_library.
# test -> find_library -> _findLib_crle: test has a transitive dependency on _findLib_crle through find_library.
# Note: test does not call _findLib_gcc directly, but reaches it via find_library.
# test -> find_library -> _findLib_ld: test has a transitive dependency on _findLib_ld through find_library.
# insort_right -> bisect_right -> key: insort_right has a transitive dependency on key through bisect_right.
# insort_left calls bisect_left, which calls key -- so insort_left transitively depends on key.
# decompress calls _read_gzip_header, which calls _read_exact -- so decompress transitively depends on _read_exact.
# Since decompress invokes _read_gzip_header and _read_gzip_header invokes BadGzipFile, decompress indirectly relies on BadGzipFile.
# monthrange -> _validate_month -> IllegalMonthError: monthrange has a transitive dependency on IllegalMonthError through _validate_month.
# monthrange calls weekday, which calls Day -- so monthrange transitively depends on Day.
# Since EncodedFile invokes lookup and lookup invokes Lookup, EncodedFile indirectly relies on Lookup.
# EncodedFile -> lookup -> Symbol: EncodedFile has a transitive dependency on Symbol through lookup.
# Since getencoder invokes lookup and lookup invokes Lookup, getencoder indirectly relies on Lookup.
# Since getencoder invokes lookup and lookup invokes Symbol, getencoder indirectly relies on Symbol.
# getdecoder -> lookup -> Lookup: getdecoder has a transitive dependency on Lookup through lookup.
# getdecoder -> lookup -> Symbol: getdecoder has a transitive dependency on Symbol through lookup.
# getincrementalencoder -> lookup -> Lookup: getincrementalencoder has a transitive dependency on Lookup through lookup.
# Note: getincrementalencoder does not call Symbol directly, but reaches it via lookup.
# getincrementaldecoder -> lookup -> Lookup: getincrementaldecoder has a transitive dependency on Lookup through lookup.
# Note: getincrementaldecoder does not call Symbol directly, but reaches it via lookup.
# getreader -> lookup -> Lookup: getreader has a transitive dependency on Lookup through lookup.
# Since getreader invokes lookup and lookup invokes Symbol, getreader indirectly relies on Symbol.
# getwriter calls lookup, which calls Lookup -- so getwriter transitively depends on Lookup.
# getwriter -> lookup -> Symbol: getwriter has a transitive dependency on Symbol through lookup.
# iterencode calls getincrementalencoder, which calls lookup -- so iterencode transitively depends on lookup.
# iterencode calls _iterencode, which calls _floatstr -- so iterencode transitively depends on _floatstr.
# Note: iterencode does not call _encoder directly, but reaches it via _iterencode.
# Note: iterencode does not call _iterencode_list directly, but reaches it via _iterencode.
# Note: iterencode does not call _intstr directly, but reaches it via _iterencode.
# iterencode calls _iterencode, which calls _iterencode_dict -- so iterencode transitively depends on _iterencode_dict.
# Note: iterencode does not call _default directly, but reaches it via _iterencode.
# Note: iterencode does not call _floatstr directly, but reaches it via _make_iterencode.
# Note: iterencode does not call _iterencode_list directly, but reaches it via _make_iterencode.
# Note: iterencode does not call _iterencode_dict directly, but reaches it via _make_iterencode.
# iterencode calls _make_iterencode, which calls _encoder -- so iterencode transitively depends on _encoder.
# iterencode calls _make_iterencode, which calls _iterencode -- so iterencode transitively depends on _iterencode.
# iterencode calls _make_iterencode, which calls _intstr -- so iterencode transitively depends on _intstr.
# Since iterencode invokes _make_iterencode and _make_iterencode invokes _default, iterencode indirectly relies on _default.
# iterdecode calls getincrementaldecoder, which calls lookup -- so iterdecode transitively depends on lookup.
# Since _compile invokes _compile_charset and _compile_charset invokes PatternError, _compile indirectly relies on PatternError.
# Note: _compile does not call emit directly, but reaches it via _compile_charset.
# Since _compile invokes _optimize_charset and _optimize_charset invokes iscased, _compile indirectly relies on iscased.
# Note: _compile does not call _bytes_to_codes directly, but reaches it via _optimize_charset.
# _compile calls _optimize_charset, which calls fixup -- so _compile transitively depends on fixup.
# Note: _compile does not call _mk_bitmap directly, but reaches it via _optimize_charset.
# Note: _compile does not call EmailMessage directly, but reaches it via emit.
# _compile calls error, which calls basicConfig -- so _compile transitively depends on basicConfig.
# Since _compile invokes error and error invokes _, _compile indirectly relies on _.
# Since _compile invokes error and error invokes perror, _compile indirectly relies on perror.
# Note: compile_command does not call compiler directly, but reaches it via _maybe_compile.
# Since compile_dir invokes compile_file and compile_file invokes Path, compile_dir indirectly relies on Path.
# Since compile_path invokes compile_dir and compile_dir invokes _check_system_limits, compile_path indirectly relies on _check_system_limits.
# compile_path -> compile_dir -> _walk_dir: compile_path has a transitive dependency on _walk_dir through compile_dir.
# compile_path -> compile_dir -> ProcessPoolExecutor: compile_path has a transitive dependency on ProcessPoolExecutor through compile_dir.
# compile_path -> compile_dir -> partial: compile_path has a transitive dependency on partial through compile_dir.
# Note: compile_path does not call compile_file directly, but reaches it via compile_dir.
# wait calls _create_and_install_waiters, which calls _FirstCompletedWaiter -- so wait transitively depends on _FirstCompletedWaiter.
# wait -> _create_and_install_waiters -> _AllCompletedWaiter: wait has a transitive dependency on _AllCompletedWaiter through _create_and_install_waiters.
# Since wait invokes _create_and_install_waiters and _create_and_install_waiters invokes _AsCompletedWaiter, wait indirectly relies on _AsCompletedWaiter.
# do_call -> func -> getter: do_call has a transitive dependency on getter through func.
# Note: _process_worker does not call _ExceptionWithTraceback directly, but reaches it via _sendback_result.
# Note: _process_worker does not call _ResultItem directly, but reaches it via _sendback_result.
# create -> public_methods -> all_methods: create has a transitive dependency on all_methods through public_methods.
# contextmanager -> wraps -> partial: contextmanager has a transitive dependency on partial through wraps.
# Since asynccontextmanager invokes wraps and wraps invokes partial, asynccontextmanager indirectly relies on partial.
# copy calls copymode, which calls chmod_func -- so copy transitively depends on chmod_func.
# copy calls copymode, which calls _islink -- so copy transitively depends on _islink.
# copy calls copymode, which calls stat_func -- so copy transitively depends on stat_func.
# Since copy invokes _reconstruct and _reconstruct invokes deepcopy, copy indirectly relies on deepcopy.
# Note: copy does not call func directly, but reaches it via _reconstruct.
# Since copy invokes _new_value and _new_value invokes rebuild_ctype, copy indirectly relies on rebuild_ctype.
# Since copy invokes copyfile and copyfile invokes SameFileError, copy indirectly relies on SameFileError.
# copy -> copyfile -> SpecialFileError: copy has a transitive dependency on SpecialFileError through copyfile.
# copy calls copyfile, which calls _copyfileobj_readinto -- so copy transitively depends on _copyfileobj_readinto.
# Note: copy does not call _samefile directly, but reaches it via copyfile.
# copy calls copyfile, which calls _stat -- so copy transitively depends on _stat.
# copy calls copyfile, which calls _fastcopy_sendfile -- so copy transitively depends on _fastcopy_sendfile.
# Note: copy does not call _islink directly, but reaches it via copyfile.
# copy -> copyfile -> copyfileobj: copy has a transitive dependency on copyfileobj through copyfile.
# copy calls copyfile, which calls _fastcopy_fcopyfile -- so copy transitively depends on _fastcopy_fcopyfile.
# copy -> copyfile -> _fastcopy_copy_file_range: copy has a transitive dependency on _fastcopy_copy_file_range through copyfile.
# Since deepcopy invokes _reconstruct and _reconstruct invokes func, deepcopy indirectly relies on func.
# _deepcopy_list -> deepcopy -> copier: _deepcopy_list has a transitive dependency on copier through deepcopy.
# Since _deepcopy_list invokes deepcopy and deepcopy invokes _keep_alive, _deepcopy_list indirectly relies on _keep_alive.
# Since _deepcopy_list invokes deepcopy and deepcopy invokes reductor, _deepcopy_list indirectly relies on reductor.
# _deepcopy_list calls deepcopy, which calls _reconstruct -- so _deepcopy_list transitively depends on _reconstruct.
# _deepcopy_list -> deepcopy -> Error: _deepcopy_list has a transitive dependency on Error through deepcopy.
# _deepcopy_list -> append -> Time2Internaldate: _deepcopy_list has a transitive dependency on Time2Internaldate through append.
# Since _deepcopy_list invokes append and append invokes Charset, _deepcopy_list indirectly relies on Charset.
# _deepcopy_tuple -> deepcopy -> copier: _deepcopy_tuple has a transitive dependency on copier through deepcopy.
# Note: _deepcopy_tuple does not call _keep_alive directly, but reaches it via deepcopy.
# _deepcopy_tuple -> deepcopy -> reductor: _deepcopy_tuple has a transitive dependency on reductor through deepcopy.
# Since _deepcopy_tuple invokes deepcopy and deepcopy invokes _reconstruct, _deepcopy_tuple indirectly relies on _reconstruct.
# Since _deepcopy_tuple invokes deepcopy and deepcopy invokes Error, _deepcopy_tuple indirectly relies on Error.
# _deepcopy_dict calls deepcopy, which calls copier -- so _deepcopy_dict transitively depends on copier.
# _deepcopy_dict -> deepcopy -> _keep_alive: _deepcopy_dict has a transitive dependency on _keep_alive through deepcopy.
# _deepcopy_dict -> deepcopy -> reductor: _deepcopy_dict has a transitive dependency on reductor through deepcopy.
# _deepcopy_dict calls deepcopy, which calls _reconstruct -- so _deepcopy_dict transitively depends on _reconstruct.
# Since _deepcopy_dict invokes deepcopy and deepcopy invokes Error, _deepcopy_dict indirectly relies on Error.
# Note: _deepcopy_method does not call copier directly, but reaches it via deepcopy.
# Since _deepcopy_method invokes deepcopy and deepcopy invokes _keep_alive, _deepcopy_method indirectly relies on _keep_alive.
# Since _deepcopy_method invokes deepcopy and deepcopy invokes reductor, _deepcopy_method indirectly relies on reductor.
# _deepcopy_method calls deepcopy, which calls _reconstruct -- so _deepcopy_method transitively depends on _reconstruct.
# Since _deepcopy_method invokes deepcopy and deepcopy invokes Error, _deepcopy_method indirectly relies on Error.
# Since _reconstruct invokes deepcopy and deepcopy invokes copier, _reconstruct indirectly relies on copier.
# Note: _reconstruct does not call _keep_alive directly, but reaches it via deepcopy.
# Since _reconstruct invokes deepcopy and deepcopy invokes reductor, _reconstruct indirectly relies on reductor.
# Since _reconstruct invokes deepcopy and deepcopy invokes Error, _reconstruct indirectly relies on Error.
# Since _reconstruct invokes func and func invokes getter, _reconstruct indirectly relies on getter.
# Since replace invokes _replace and _replace invokes _map, replace indirectly relies on _map.
# Note: replace does not call getter directly, but reaches it via func.
# get_member -> get_legacy -> get_one_match: get_member has a transitive dependency on get_one_match through get_legacy.
# Since get_member invokes get_version and get_version invokes _last_version, get_member indirectly relies on _last_version.
# Note: get_libpaths does not call get_ld_header directly, but reaches it via get_ld_headers.
# get_libpaths calls get_ld_headers, which calls Popen -- so get_libpaths transitively depends on Popen.
# get_libpaths calls get_ld_headers, which calls get_ld_header_info -- so get_libpaths transitively depends on get_ld_header_info.
# find_shared -> get_ld_headers -> get_ld_header: find_shared has a transitive dependency on get_ld_header through get_ld_headers.
# Note: find_shared does not call Popen directly, but reaches it via get_ld_headers.
# Note: find_shared does not call get_ld_header_info directly, but reaches it via get_ld_headers.
# find_shared -> get_member -> get_one_match: find_shared has a transitive dependency on get_one_match through get_member.
# find_shared calls get_member, which calls get_legacy -- so find_shared transitively depends on get_legacy.
# Since find_shared invokes get_member and get_member invokes get_version, find_shared indirectly relies on get_version.
# Since find_library invokes get_libpaths and get_libpaths invokes get_ld_headers, find_library indirectly relies on get_ld_headers.
# Note: find_library does not call _get_build_version directly, but reaches it via find_msvcrt.
# Note: find_library does not call get_shared directly, but reaches it via find_shared.
# find_library -> find_shared -> get_ld_headers: find_library has a transitive dependency on get_ld_headers through find_shared.
# find_library calls find_shared, which calls get_member -- so find_library transitively depends on get_member.
# find_library -> _findLib_gcc -> _is_elf: find_library has a transitive dependency on _is_elf through _findLib_gcc.
# Since find_library invokes _findLib_ld and _findLib_ld invokes _is_elf, find_library indirectly relies on _is_elf.
# Note: dyld_override_search does not call dyld_env directly, but reaches it via dyld_library_path.
# Since dyld_override_search invokes dyld_framework_path and dyld_framework_path invokes dyld_env, dyld_override_search indirectly relies on dyld_env.
# dyld_default_search -> dyld_fallback_library_path -> dyld_env: dyld_default_search has a transitive dependency on dyld_env through dyld_fallback_library_path.
# dyld_default_search -> dyld_fallback_framework_path -> dyld_env: dyld_default_search has a transitive dependency on dyld_env through dyld_fallback_framework_path.
# Note: dyld_find does not call dyld_library_path directly, but reaches it via dyld_override_search.
# dyld_find calls dyld_override_search, which calls framework_info -- so dyld_find transitively depends on framework_info.
# Since dyld_find invokes dyld_override_search and dyld_override_search invokes dyld_framework_path, dyld_find indirectly relies on dyld_framework_path.
# dyld_find calls dyld_default_search, which calls dyld_fallback_library_path -- so dyld_find transitively depends on dyld_fallback_library_path.
# dyld_find -> dyld_default_search -> dyld_fallback_framework_path: dyld_find has a transitive dependency on dyld_fallback_framework_path through dyld_default_search.
# Since dyld_find invokes dyld_default_search and dyld_default_search invokes framework_info, dyld_find indirectly relies on framework_info.
# dyld_find -> dyld_image_suffix_search -> dyld_image_suffix: dyld_find has a transitive dependency on dyld_image_suffix through dyld_image_suffix_search.
# Note: dyld_find does not call _inject directly, but reaches it via dyld_image_suffix_search.
# Since framework_find invokes dyld_find and dyld_find invokes dyld_override_search, framework_find indirectly relies on dyld_override_search.
# framework_find -> dyld_find -> dyld_executable_path_search: framework_find has a transitive dependency on dyld_executable_path_search through dyld_find.
# framework_find -> dyld_find -> dyld_default_search: framework_find has a transitive dependency on dyld_default_search through dyld_find.
# Since framework_find invokes dyld_find and dyld_find invokes chain, framework_find indirectly relies on chain.
# framework_find -> dyld_find -> _dyld_shared_cache_contains_path: framework_find has a transitive dependency on _dyld_shared_cache_contains_path through dyld_find.
# framework_find calls dyld_find, which calls dyld_image_suffix_search -- so framework_find transitively depends on dyld_image_suffix_search.
# wrapper -> wraps -> partial: wrapper has a transitive dependency on partial through wraps.
# wrapper calls initscr, which calls setupterm -- so wrapper transitively depends on setupterm.
# Since wrapper invokes func and func invokes getter, wrapper indirectly relies on getter.
# wrapper -> dispatch -> convert_to_error: wrapper has a transitive dependency on convert_to_error through dispatch.
# wrapper calls dispatch, which calls _find_impl -- so wrapper transitively depends on _find_impl.
# Note: wrapper does not call get_cache_token directly, but reaches it via dispatch.
# isalnum calls isdigit, which calls _ctoi -- so isalnum transitively depends on _ctoi.
# isalnum calls isalpha, which calls isupper -- so isalnum transitively depends on isupper.
# Since isalnum invokes isalpha and isalpha invokes islower, isalnum indirectly relies on islower.
# isalpha calls isupper, which calls _ctoi -- so isalpha transitively depends on _ctoi.
# isalpha calls islower, which calls _ctoi -- so isalpha transitively depends on _ctoi.
# Since ispunct invokes isgraph and isgraph invokes _ctoi, ispunct indirectly relies on _ctoi.
# ispunct -> isalnum -> isdigit: ispunct has a transitive dependency on isdigit through isalnum.
# ispunct calls isalnum, which calls isalpha -- so ispunct transitively depends on isalpha.
# isxdigit calls isdigit, which calls _ctoi -- so isxdigit transitively depends on _ctoi.
# unctrl -> isprint -> _ctoi: unctrl has a transitive dependency on _ctoi through isprint.
# Note: _init_fn does not call _field_assign directly, but reaches it via _field_init.
# Since _get_field invokes _is_type and _is_type invokes is_type_predicate, _get_field indirectly relies on is_type_predicate.
# Since _get_field invokes field and field invokes Field, _get_field indirectly relies on Field.
# Note: _process_class does not call _field_init directly, but reaches it via _init_fn.
# Note: _process_class does not call _init_param directly, but reaches it via _init_fn.
# Since _process_class invokes _get_field and _get_field invokes _is_type, _process_class indirectly relies on _is_type.
# _process_class -> _get_field -> _is_classvar: _process_class has a transitive dependency on _is_classvar through _get_field.
# _process_class -> _get_field -> _is_initvar: _process_class has a transitive dependency on _is_initvar through _get_field.
# Since _process_class invokes _get_field and _get_field invokes field, _process_class indirectly relies on field.
# _process_class -> _add_slots -> fields: _process_class has a transitive dependency on fields through _add_slots.
# Note: _process_class does not call _update_func_cell_for__class__ directly, but reaches it via _add_slots.
# Note: _process_class does not call _create_slots directly, but reaches it via _add_slots.
# _process_class calls _is_type, which calls is_type_predicate -- so _process_class transitively depends on is_type_predicate.
# Note: dataclass does not call _Line directly, but reaches it via wrap.
# dataclass -> wrap -> TextWrapper: dataclass has a transitive dependency on TextWrapper through wrap.
# Note: dataclass does not call _process_class directly, but reaches it via wrap.
# dataclass calls _process_class, which calls hash_action -- so dataclass transitively depends on hash_action.
# dataclass calls _process_class, which calls _init_fn -- so dataclass transitively depends on _init_fn.
# Note: dataclass does not call _is_kw_only directly, but reaches it via _process_class.
# Note: dataclass does not call _set_new_attribute directly, but reaches it via _process_class.
# Since dataclass invokes _process_class and _process_class invokes _DataclassParams, dataclass indirectly relies on _DataclassParams.
# dataclass calls _process_class, which calls _fields_in_init_order -- so dataclass transitively depends on _fields_in_init_order.
# Since dataclass invokes _process_class and _process_class invokes _get_field, dataclass indirectly relies on _get_field.
# Since dataclass invokes _process_class and _process_class invokes _tuple_str, dataclass indirectly relies on _tuple_str.
# dataclass calls _process_class, which calls _FuncBuilder -- so dataclass transitively depends on _FuncBuilder.
# dataclass -> _process_class -> _add_slots: dataclass has a transitive dependency on _add_slots through _process_class.
# Since dataclass invokes _process_class and _process_class invokes _is_type, dataclass indirectly relies on _is_type.
# Since dataclass invokes _process_class and _process_class invokes _frozen_set_del_attr, dataclass indirectly relies on _frozen_set_del_attr.
# Since asdict invokes _asdict_inner and _asdict_inner invokes obj_type, asdict indirectly relies on obj_type.
# asdict calls _asdict_inner, which calls fields -- so asdict transitively depends on fields.
# Since asdict invokes _asdict_inner and _asdict_inner invokes dict_factory, asdict indirectly relies on dict_factory.
# Since astuple invokes _astuple_inner and _astuple_inner invokes obj_type, astuple indirectly relies on obj_type.
# astuple calls _astuple_inner, which calls fields -- so astuple transitively depends on fields.
# Note: astuple does not call tuple_factory directly, but reaches it via _astuple_inner.
# astuple calls _astuple_inner, which calls _is_dataclass_instance -- so astuple transitively depends on _is_dataclass_instance.
# Note: make_dataclass does not call SkipTest directly, but reaches it via decorator.
# Since make_dataclass invokes decorator and decorator invokes func, make_dataclass indirectly relies on func.
# diff_bytes calls decode, which calls unquote -- so diff_bytes transitively depends on unquote.
# diff_bytes -> decode -> a2b_base64: diff_bytes has a transitive dependency on a2b_base64 through decode.
# diff_bytes -> decode -> ToUnicode: diff_bytes has a transitive dependency on ToUnicode through decode.
# Note: diff_bytes does not call punycode_decode directly, but reaches it via decode.
# diff_bytes calls decode, which calls JSONDecodeError -- so diff_bytes transitively depends on JSONDecodeError.
# diff_bytes calls decode, which calls a2b_qp -- so diff_bytes transitively depends on a2b_qp.
# Note: diff_bytes does not call ishex directly, but reaches it via decode.
# diff_bytes calls decode, which calls quopri_decode -- so diff_bytes transitively depends on quopri_decode.
# diff_bytes -> decode -> hex_decode: diff_bytes has a transitive dependency on hex_decode through decode.
# diff_bytes -> decode -> oem_decode: diff_bytes has a transitive dependency on oem_decode through decode.
# Since diff_bytes invokes decode and decode invokes zlib_decode, diff_bytes indirectly relies on zlib_decode.
# Note: diff_bytes does not call uu_decode directly, but reaches it via decode.
# Since diff_bytes invokes decode and decode invokes base64_decode, diff_bytes indirectly relies on base64_decode.
# diff_bytes -> decode -> _w: diff_bytes has a transitive dependency on _w through decode.
# Since diff_bytes invokes decode and decode invokes mbcs_decode, diff_bytes indirectly relies on mbcs_decode.
# Note: diff_bytes does not call unhex directly, but reaches it via decode.
# diff_bytes -> decode -> bz2_decode: diff_bytes has a transitive dependency on bz2_decode through decode.
# diff_bytes -> decode -> code_page_decode: diff_bytes has a transitive dependency on code_page_decode through decode.
# Note: _mdiff does not call _line_iterator directly, but reaches it via _line_pair_iterator.
# Note: _mdiff does not call Differ directly, but reaches it via ndiff.
# _mdiff calls _line_iterator, which calls _make_line -- so _mdiff transitively depends on _make_line.
# dis -> _make_labels_map -> findlabels: dis has a transitive dependency on findlabels through _make_labels_map.
# dis -> _get_code_array -> get_executor: dis has a transitive dependency on get_executor through _get_code_array.
# Note: dis does not call _disassemble_recursive directly, but reaches it via _disassemble_str.
# dis calls _disassemble_str, which calls _try_compile -- so dis transitively depends on _try_compile.
# dis -> _disassemble_bytes -> _get_instructions_bytes: dis has a transitive dependency on _get_instructions_bytes through _disassemble_bytes.
# dis calls _disassemble_bytes, which calls print_instructions -- so dis transitively depends on print_instructions.
# Note: dis does not call disassemble directly, but reaches it via _disassemble_recursive.
# Note: dis does not call print_2 directly, but reaches it via dis_.
# dis calls dis_, which calls print_ -- so dis transitively depends on print_.
# Note: dis does not call _hex_code directly, but reaches it via dis_.
# dis -> genops -> _genops: dis has a transitive dependency on _genops through genops.
# Note: dis does not call disassemble directly, but reaches it via distb.
# distb calls disassemble, which calls _get_lineno_width -- so distb transitively depends on _get_lineno_width.
# distb calls disassemble, which calls _get_code_array -- so distb transitively depends on _get_code_array.
# distb -> disassemble -> ArgResolver: distb has a transitive dependency on ArgResolver through disassemble.
# distb calls disassemble, which calls _parse_exception_table -- so distb transitively depends on _parse_exception_table.
# distb -> disassemble -> _disassemble_bytes: distb has a transitive dependency on _disassemble_bytes through disassemble.
# distb -> disassemble -> _get_positions_width: distb has a transitive dependency on _get_positions_width through disassemble.
# Since distb invokes disassemble and disassemble invokes findlinestarts, distb indirectly relies on findlinestarts.
# distb -> disassemble -> _make_labels_map: distb has a transitive dependency on _make_labels_map through disassemble.
# Note: distb does not call Formatter directly, but reaches it via disassemble.
# code_info -> _format_code_info -> pretty_flags: code_info has a transitive dependency on pretty_flags through _format_code_info.
# Since code_info invokes _get_code_object and _get_code_object invokes _try_compile, code_info indirectly relies on _try_compile.
# show_code calls code_info, which calls _format_code_info -- so show_code transitively depends on _format_code_info.
# show_code -> code_info -> _get_code_object: show_code has a transitive dependency on _get_code_object through code_info.
# Since get_instructions invokes _get_code_array and _get_code_array invokes get_executor, get_instructions indirectly relies on get_executor.
# get_instructions -> _make_labels_map -> findlabels: get_instructions has a transitive dependency on findlabels through _make_labels_map.
# Since get_instructions invokes _get_instructions_bytes and _get_instructions_bytes invokes _unpack_opargs, get_instructions indirectly relies on _unpack_opargs.
# Since get_instructions invokes _get_instructions_bytes and _get_instructions_bytes invokes _get_cache_size, get_instructions indirectly relies on _get_cache_size.
# get_instructions -> _get_instructions_bytes -> Instruction: get_instructions has a transitive dependency on Instruction through _get_instructions_bytes.
# get_instructions calls _get_instructions_bytes, which calls _deoptop -- so get_instructions transitively depends on _deoptop.
# get_instructions calls _get_instructions_bytes, which calls Positions -- so get_instructions transitively depends on Positions.
# Since get_instructions invokes _get_code_object and _get_code_object invokes _try_compile, get_instructions indirectly relies on _try_compile.
# _get_instructions_bytes -> _unpack_opargs -> _get_cache_size: _get_instructions_bytes has a transitive dependency on _get_cache_size through _unpack_opargs.
# _get_instructions_bytes -> _unpack_opargs -> _deoptop: _get_instructions_bytes has a transitive dependency on _deoptop through _unpack_opargs.
# disassemble -> _get_code_array -> get_executor: disassemble has a transitive dependency on get_executor through _get_code_array.
# disassemble -> _parse_exception_table -> _ExceptionTableEntry: disassemble has a transitive dependency on _ExceptionTableEntry through _parse_exception_table.
# disassemble -> _parse_exception_table -> _parse_varint: disassemble has a transitive dependency on _parse_varint through _parse_exception_table.
# Note: disassemble does not call _get_instructions_bytes directly, but reaches it via _disassemble_bytes.
# Note: disassemble does not call print_instructions directly, but reaches it via _disassemble_bytes.
# Note: disassemble does not call findlabels directly, but reaches it via _make_labels_map.
# Since _disassemble_recursive invokes disassemble and disassemble invokes _get_lineno_width, _disassemble_recursive indirectly relies on _get_lineno_width.
# _disassemble_recursive calls disassemble, which calls _get_code_array -- so _disassemble_recursive transitively depends on _get_code_array.
# Since _disassemble_recursive invokes disassemble and disassemble invokes ArgResolver, _disassemble_recursive indirectly relies on ArgResolver.
# _disassemble_recursive calls disassemble, which calls _parse_exception_table -- so _disassemble_recursive transitively depends on _parse_exception_table.
# Note: _disassemble_recursive does not call _disassemble_bytes directly, but reaches it via disassemble.
# Since _disassemble_recursive invokes disassemble and disassemble invokes _get_positions_width, _disassemble_recursive indirectly relies on _get_positions_width.
# _disassemble_recursive -> disassemble -> findlinestarts: _disassemble_recursive has a transitive dependency on findlinestarts through disassemble.
# _disassemble_recursive calls disassemble, which calls _make_labels_map -- so _disassemble_recursive transitively depends on _make_labels_map.
# Note: _disassemble_recursive does not call Formatter directly, but reaches it via disassemble.
# _make_labels_map -> findlabels -> _unpack_opargs: _make_labels_map has a transitive dependency on _unpack_opargs through findlabels.
# Since _make_labels_map invokes findlabels and findlabels invokes _get_jump_target, _make_labels_map indirectly relies on _get_jump_target.
# _disassemble_bytes calls _get_instructions_bytes, which calls _unpack_opargs -- so _disassemble_bytes transitively depends on _unpack_opargs.
# Note: _disassemble_bytes does not call _get_cache_size directly, but reaches it via _get_instructions_bytes.
# _disassemble_bytes -> _get_instructions_bytes -> Instruction: _disassemble_bytes has a transitive dependency on Instruction through _get_instructions_bytes.
# Since _disassemble_bytes invokes _get_instructions_bytes and _get_instructions_bytes invokes _deoptop, _disassemble_bytes indirectly relies on _deoptop.
# Since _disassemble_bytes invokes _get_instructions_bytes and _get_instructions_bytes invokes Positions, _disassemble_bytes indirectly relies on Positions.
# _disassemble_bytes calls print_instructions, which calls _deoptop -- so _disassemble_bytes transitively depends on _deoptop.
# Since _disassemble_bytes invokes print_instructions and print_instructions invokes _get_cache_size, _disassemble_bytes indirectly relies on _get_cache_size.
# _disassemble_str calls _disassemble_recursive, which calls disassemble -- so _disassemble_str transitively depends on disassemble.
# Note: findlabels does not call _get_cache_size directly, but reaches it via _unpack_opargs.
# Note: findlabels does not call _deoptop directly, but reaches it via _unpack_opargs.
# findlabels -> _get_jump_target -> _is_backward_jump: findlabels has a transitive dependency on _is_backward_jump through _get_jump_target.
# findlabels -> _get_jump_target -> _deoptop: findlabels has a transitive dependency on _deoptop through _get_jump_target.
# findlabels -> _get_jump_target -> _get_cache_size: findlabels has a transitive dependency on _get_cache_size through _get_jump_target.
# _find_imports calls _unpack_opargs, which calls _get_cache_size -- so _find_imports transitively depends on _get_cache_size.
# Note: _find_imports does not call _deoptop directly, but reaches it via _unpack_opargs.
# _find_store_names -> _unpack_opargs -> _get_cache_size: _find_store_names has a transitive dependency on _get_cache_size through _unpack_opargs.
# _find_store_names -> _unpack_opargs -> _deoptop: _find_store_names has a transitive dependency on _deoptop through _unpack_opargs.
# _load_testfile -> _newline_convert -> IncrementalNewlineDecoder: _load_testfile has a transitive dependency on IncrementalNewlineDecoder through _newline_convert.
# testfile -> _load_testfile -> _newline_convert: testfile has a transitive dependency on _newline_convert through _load_testfile.
# Since testfile invokes _load_testfile and _load_testfile invokes _module_relative_path, testfile indirectly relies on _module_relative_path.
# Note: testfile does not call _normalize_module directly, but reaches it via _load_testfile.
# Since DocFileTest invokes _load_testfile and _load_testfile invokes _newline_convert, DocFileTest indirectly relies on _newline_convert.
# DocFileTest calls _load_testfile, which calls _module_relative_path -- so DocFileTest transitively depends on _module_relative_path.
# Since DocFileTest invokes _load_testfile and _load_testfile invokes _normalize_module, DocFileTest indirectly relies on _normalize_module.
# Note: DocFileSuite does not call DocTestParser directly, but reaches it via DocFileTest.
# DocFileSuite -> DocFileTest -> _load_testfile: DocFileSuite has a transitive dependency on _load_testfile through DocFileTest.
# Note: DocFileSuite does not call DocFileCase directly, but reaches it via DocFileTest.
# testsource calls script_from_examples, which calls DocTestParser -- so testsource transitively depends on DocTestParser.
# Note: testsource does not call _comment_line directly, but reaches it via script_from_examples.
# Since debug_src invokes script_from_examples and script_from_examples invokes DocTestParser, debug_src indirectly relies on DocTestParser.
# debug_src calls script_from_examples, which calls _comment_line -- so debug_src transitively depends on _comment_line.
# debug -> testsource -> _normalize_module: debug has a transitive dependency on _normalize_module through testsource.
# Since debug invokes testsource and testsource invokes script_from_examples, debug indirectly relies on script_from_examples.
# Since debug invokes testsource and testsource invokes DocTestFinder, debug indirectly relies on DocTestFinder.
# debug -> basicConfig -> StreamHandler: debug has a transitive dependency on StreamHandler through basicConfig.
# Note: debug does not call FileHandler directly, but reaches it via basicConfig.
# debug calls basicConfig, which calls Formatter -- so debug transitively depends on Formatter.
# Since _test invokes testfile and testfile invokes DocTestParser, _test indirectly relies on DocTestParser.
# Since _test invokes testfile and testfile invokes TestResults, _test indirectly relies on TestResults.
# _test -> testfile -> DebugRunner: _test has a transitive dependency on DebugRunner through testfile.
# Since _test invokes testfile and testfile invokes _load_testfile, _test indirectly relies on _load_testfile.
# Note: _test does not call DocTestRunner directly, but reaches it via testfile.
# _test calls setlocale, which calls Error -- so _test transitively depends on Error.
# Note: _test does not call _build_localename directly, but reaches it via setlocale.
# Since _test invokes setlocale and setlocale invokes _setlocale, _test indirectly relies on _setlocale.
# Note: _test does not call normalize directly, but reaches it via setlocale.
# _test calls format_string, which calls _format -- so _test transitively depends on _format.
# Since _test invokes testmod and testmod invokes TestResults, _test indirectly relies on TestResults.
# Since _test invokes testmod and testmod invokes DebugRunner, _test indirectly relies on DebugRunner.
# Note: _test does not call DocTestRunner directly, but reaches it via testmod.
# _test calls testmod, which calls DocTestFinder -- so _test transitively depends on DocTestFinder.
# Note: _test does not call stdev directly, but reaches it via _test_generator.
# Note: _test does not call _repeat directly, but reaches it via _test_generator.
# _test -> _test_generator -> perf_counter: _test has a transitive dependency on perf_counter through _test_generator.
# _test -> _test_generator -> mean: _test has a transitive dependency on mean through _test_generator.
# Note: _test does not call func directly, but reaches it via _test_generator.
# _test -> atoi -> delocalize: _test has a transitive dependency on delocalize through atoi.
# _test -> atof -> delocalize: _test has a transitive dependency on delocalize through atof.
# _test calls atof, which calls func -- so _test transitively depends on func.
# get_encoded_word -> get_fws -> WhiteSpaceTerminal: get_encoded_word has a transitive dependency on WhiteSpaceTerminal through get_fws.
# get_encoded_word calls _validate_xtext, which calls _non_printable_finder -- so get_encoded_word transitively depends on _non_printable_finder.
# Note: get_unstructured does not call WhiteSpaceTerminal directly, but reaches it via get_fws.
# get_unstructured calls get_encoded_word, which calls EncodedWord -- so get_unstructured transitively depends on EncodedWord.
# get_unstructured -> get_encoded_word -> get_fws: get_unstructured has a transitive dependency on get_fws through get_encoded_word.
# Since get_unstructured invokes get_encoded_word and get_encoded_word invokes _wsp_splitter, get_unstructured indirectly relies on _wsp_splitter.
# Note: get_unstructured does not call _InvalidEwError directly, but reaches it via get_encoded_word.
# Note: get_unstructured does not call ValueTerminal directly, but reaches it via get_encoded_word.
# Note: get_unstructured does not call _validate_xtext directly, but reaches it via get_encoded_word.
# Note: get_unstructured does not call _non_printable_finder directly, but reaches it via _validate_xtext.
# get_qp_ctext calls _get_ptext_to_endchars, which calls _wsp_splitter -- so get_qp_ctext transitively depends on _wsp_splitter.
# Note: get_qp_ctext does not call _non_printable_finder directly, but reaches it via _validate_xtext.
# get_qcontent -> _get_ptext_to_endchars -> _wsp_splitter: get_qcontent has a transitive dependency on _wsp_splitter through _get_ptext_to_endchars.
# Since get_qcontent invokes _validate_xtext and _validate_xtext invokes _non_printable_finder, get_qcontent indirectly relies on _non_printable_finder.
# get_atext -> _validate_xtext -> _non_printable_finder: get_atext has a transitive dependency on _non_printable_finder through _validate_xtext.
# get_bare_quoted_string -> get_fws -> WhiteSpaceTerminal: get_bare_quoted_string has a transitive dependency on WhiteSpaceTerminal through get_fws.
# get_bare_quoted_string calls get_encoded_word, which calls EncodedWord -- so get_bare_quoted_string transitively depends on EncodedWord.
# Note: get_bare_quoted_string does not call get_fws directly, but reaches it via get_encoded_word.
# Note: get_bare_quoted_string does not call _wsp_splitter directly, but reaches it via get_encoded_word.
# Since get_bare_quoted_string invokes get_encoded_word and get_encoded_word invokes _InvalidEwError, get_bare_quoted_string indirectly relies on _InvalidEwError.
# get_bare_quoted_string calls get_encoded_word, which calls ValueTerminal -- so get_bare_quoted_string transitively depends on ValueTerminal.
# get_bare_quoted_string calls get_encoded_word, which calls _validate_xtext -- so get_bare_quoted_string transitively depends on _validate_xtext.
# get_bare_quoted_string -> get_qcontent -> ValueTerminal: get_bare_quoted_string has a transitive dependency on ValueTerminal through get_qcontent.
# get_bare_quoted_string -> get_qcontent -> _get_ptext_to_endchars: get_bare_quoted_string has a transitive dependency on _get_ptext_to_endchars through get_qcontent.
# get_bare_quoted_string calls get_qcontent, which calls _validate_xtext -- so get_bare_quoted_string transitively depends on _validate_xtext.
# get_comment calls Comment, which calls Element -- so get_comment transitively depends on Element.
# Since get_comment invokes get_fws and get_fws invokes WhiteSpaceTerminal, get_comment indirectly relies on WhiteSpaceTerminal.
# get_comment calls get_qp_ctext, which calls _get_ptext_to_endchars -- so get_comment transitively depends on _get_ptext_to_endchars.
# Note: get_comment does not call _validate_xtext directly, but reaches it via get_qp_ctext.
# get_comment calls get_qp_ctext, which calls WhiteSpaceTerminal -- so get_comment transitively depends on WhiteSpaceTerminal.
# get_cfws -> get_fws -> WhiteSpaceTerminal: get_cfws has a transitive dependency on WhiteSpaceTerminal through get_fws.
# get_cfws calls get_comment, which calls Comment -- so get_cfws transitively depends on Comment.
# get_cfws -> get_comment -> get_fws: get_cfws has a transitive dependency on get_fws through get_comment.
# Note: get_cfws does not call get_qp_ctext directly, but reaches it via get_comment.
# Since get_quoted_string invokes get_bare_quoted_string and get_bare_quoted_string invokes get_fws, get_quoted_string indirectly relies on get_fws.
# get_quoted_string -> get_bare_quoted_string -> EWWhiteSpaceTerminal: get_quoted_string has a transitive dependency on EWWhiteSpaceTerminal through get_bare_quoted_string.
# Note: get_quoted_string does not call BareQuotedString directly, but reaches it via get_bare_quoted_string.
# Since get_quoted_string invokes get_bare_quoted_string and get_bare_quoted_string invokes get_encoded_word, get_quoted_string indirectly relies on get_encoded_word.
# Since get_quoted_string invokes get_bare_quoted_string and get_bare_quoted_string invokes get_qcontent, get_quoted_string indirectly relies on get_qcontent.
# Note: get_quoted_string does not call CFWSList directly, but reaches it via get_cfws.
# get_quoted_string -> get_cfws -> get_fws: get_quoted_string has a transitive dependency on get_fws through get_cfws.
# get_quoted_string calls get_cfws, which calls get_comment -- so get_quoted_string transitively depends on get_comment.
# Since get_atom invokes get_cfws and get_cfws invokes CFWSList, get_atom indirectly relies on CFWSList.
# Since get_atom invokes get_cfws and get_cfws invokes get_fws, get_atom indirectly relies on get_fws.
# Since get_atom invokes get_cfws and get_cfws invokes get_comment, get_atom indirectly relies on get_comment.
# Note: get_atom does not call ValueTerminal directly, but reaches it via get_atext.
# get_atom calls get_atext, which calls _non_atom_end_matcher -- so get_atom transitively depends on _non_atom_end_matcher.
# get_atom calls get_atext, which calls _validate_xtext -- so get_atom transitively depends on _validate_xtext.
# Note: get_atom does not call EncodedWord directly, but reaches it via get_encoded_word.
# Since get_atom invokes get_encoded_word and get_encoded_word invokes get_fws, get_atom indirectly relies on get_fws.
# get_atom -> get_encoded_word -> _wsp_splitter: get_atom has a transitive dependency on _wsp_splitter through get_encoded_word.
# get_atom calls get_encoded_word, which calls _InvalidEwError -- so get_atom transitively depends on _InvalidEwError.
# get_atom calls get_encoded_word, which calls ValueTerminal -- so get_atom transitively depends on ValueTerminal.
# Since get_atom invokes get_encoded_word and get_encoded_word invokes _validate_xtext, get_atom indirectly relies on _validate_xtext.
# get_dot_atom_text -> get_atext -> ValueTerminal: get_dot_atom_text has a transitive dependency on ValueTerminal through get_atext.
# Since get_dot_atom_text invokes get_atext and get_atext invokes _non_atom_end_matcher, get_dot_atom_text indirectly relies on _non_atom_end_matcher.
# get_dot_atom_text -> get_atext -> _validate_xtext: get_dot_atom_text has a transitive dependency on _validate_xtext through get_atext.
# get_dot_atom -> get_dot_atom_text -> DotAtomText: get_dot_atom has a transitive dependency on DotAtomText through get_dot_atom_text.
# get_dot_atom -> get_dot_atom_text -> get_atext: get_dot_atom has a transitive dependency on get_atext through get_dot_atom_text.
# get_dot_atom -> get_cfws -> CFWSList: get_dot_atom has a transitive dependency on CFWSList through get_cfws.
# Note: get_dot_atom does not call get_fws directly, but reaches it via get_cfws.
# Since get_dot_atom invokes get_cfws and get_cfws invokes get_comment, get_dot_atom indirectly relies on get_comment.
# Note: get_dot_atom does not call EncodedWord directly, but reaches it via get_encoded_word.
# Since get_dot_atom invokes get_encoded_word and get_encoded_word invokes get_fws, get_dot_atom indirectly relies on get_fws.
# get_dot_atom calls get_encoded_word, which calls _wsp_splitter -- so get_dot_atom transitively depends on _wsp_splitter.
# Since get_dot_atom invokes get_encoded_word and get_encoded_word invokes _InvalidEwError, get_dot_atom indirectly relies on _InvalidEwError.
# get_dot_atom calls get_encoded_word, which calls ValueTerminal -- so get_dot_atom transitively depends on ValueTerminal.
# get_dot_atom -> get_encoded_word -> _validate_xtext: get_dot_atom has a transitive dependency on _validate_xtext through get_encoded_word.
# get_word calls get_quoted_string, which calls get_bare_quoted_string -- so get_word transitively depends on get_bare_quoted_string.
# Since get_word invokes get_quoted_string and get_quoted_string invokes QuotedString, get_word indirectly relies on QuotedString.
# Note: get_word does not call get_cfws directly, but reaches it via get_quoted_string.
# Since get_word invokes get_cfws and get_cfws invokes CFWSList, get_word indirectly relies on CFWSList.
# Note: get_word does not call get_fws directly, but reaches it via get_cfws.
# Since get_word invokes get_cfws and get_cfws invokes get_comment, get_word indirectly relies on get_comment.
# get_word -> get_atom -> get_cfws: get_word has a transitive dependency on get_cfws through get_atom.
# Since get_word invokes get_atom and get_atom invokes Atom, get_word indirectly relies on Atom.
# Note: get_word does not call get_atext directly, but reaches it via get_atom.
# get_word calls get_atom, which calls get_encoded_word -- so get_word transitively depends on get_encoded_word.
# get_phrase calls get_word, which calls get_quoted_string -- so get_phrase transitively depends on get_quoted_string.
# get_phrase -> get_word -> get_cfws: get_phrase has a transitive dependency on get_cfws through get_word.
# get_phrase calls get_word, which calls get_atom -- so get_phrase transitively depends on get_atom.
# get_phrase calls get_cfws, which calls CFWSList -- so get_phrase transitively depends on CFWSList.
# get_phrase calls get_cfws, which calls get_fws -- so get_phrase transitively depends on get_fws.
# Note: get_phrase does not call get_comment directly, but reaches it via get_cfws.
# get_local_part -> get_word -> get_quoted_string: get_local_part has a transitive dependency on get_quoted_string through get_word.
# Note: get_local_part does not call get_cfws directly, but reaches it via get_word.
# get_local_part -> get_word -> get_atom: get_local_part has a transitive dependency on get_atom through get_word.
# Note: get_local_part does not call get_dot_atom_text directly, but reaches it via get_dot_atom.
# get_local_part -> get_dot_atom -> get_cfws: get_local_part has a transitive dependency on get_cfws through get_dot_atom.
# get_local_part calls get_dot_atom, which calls get_encoded_word -- so get_local_part transitively depends on get_encoded_word.
# Since get_local_part invokes get_dot_atom and get_dot_atom invokes DotAtom, get_local_part indirectly relies on DotAtom.
# get_local_part -> get_cfws -> CFWSList: get_local_part has a transitive dependency on CFWSList through get_cfws.
# get_local_part calls get_cfws, which calls get_fws -- so get_local_part transitively depends on get_fws.
# Since get_local_part invokes get_cfws and get_cfws invokes get_comment, get_local_part indirectly relies on get_comment.
# Since get_local_part invokes get_obs_local_part and get_obs_local_part invokes get_word, get_local_part indirectly relies on get_word.
# get_local_part calls get_obs_local_part, which calls ValueTerminal -- so get_local_part transitively depends on ValueTerminal.
# get_local_part -> get_obs_local_part -> get_cfws: get_local_part has a transitive dependency on get_cfws through get_obs_local_part.
# Since get_local_part invokes get_obs_local_part and get_obs_local_part invokes ObsLocalPart, get_local_part indirectly relies on ObsLocalPart.
# get_obs_local_part calls get_word, which calls get_quoted_string -- so get_obs_local_part transitively depends on get_quoted_string.
# Note: get_obs_local_part does not call get_cfws directly, but reaches it via get_word.
# get_obs_local_part calls get_word, which calls get_atom -- so get_obs_local_part transitively depends on get_atom.
# get_obs_local_part -> get_cfws -> CFWSList: get_obs_local_part has a transitive dependency on CFWSList through get_cfws.
# Note: get_obs_local_part does not call get_fws directly, but reaches it via get_cfws.
# get_obs_local_part calls get_cfws, which calls get_comment -- so get_obs_local_part transitively depends on get_comment.
# Since get_dtext invokes _get_ptext_to_endchars and _get_ptext_to_endchars invokes _wsp_splitter, get_dtext indirectly relies on _wsp_splitter.
# get_dtext -> _validate_xtext -> _non_printable_finder: get_dtext has a transitive dependency on _non_printable_finder through _validate_xtext.
# get_domain_literal calls _check_for_early_dl_end, which calls ValueTerminal -- so get_domain_literal transitively depends on ValueTerminal.
# Note: get_domain_literal does not call WhiteSpaceTerminal directly, but reaches it via get_fws.
# get_domain_literal calls get_cfws, which calls CFWSList -- so get_domain_literal transitively depends on CFWSList.
# Note: get_domain_literal does not call get_fws directly, but reaches it via get_cfws.
# get_domain_literal -> get_cfws -> get_comment: get_domain_literal has a transitive dependency on get_comment through get_cfws.
# get_domain_literal -> get_dtext -> ValueTerminal: get_domain_literal has a transitive dependency on ValueTerminal through get_dtext.
# get_domain_literal -> get_dtext -> _get_ptext_to_endchars: get_domain_literal has a transitive dependency on _get_ptext_to_endchars through get_dtext.
# Note: get_domain_literal does not call _validate_xtext directly, but reaches it via get_dtext.
# Note: get_domain does not call get_dot_atom_text directly, but reaches it via get_dot_atom.
# Since get_domain invokes get_dot_atom and get_dot_atom invokes get_cfws, get_domain indirectly relies on get_cfws.
# get_domain calls get_dot_atom, which calls get_encoded_word -- so get_domain transitively depends on get_encoded_word.
# Note: get_domain does not call DotAtom directly, but reaches it via get_dot_atom.
# get_domain -> get_atom -> get_cfws: get_domain has a transitive dependency on get_cfws through get_atom.
# Since get_domain invokes get_atom and get_atom invokes Atom, get_domain indirectly relies on Atom.
# Note: get_domain does not call get_atext directly, but reaches it via get_atom.
# get_domain calls get_atom, which calls get_encoded_word -- so get_domain transitively depends on get_encoded_word.
# get_domain -> get_cfws -> CFWSList: get_domain has a transitive dependency on CFWSList through get_cfws.
# get_domain calls get_cfws, which calls get_fws -- so get_domain transitively depends on get_fws.
# get_domain calls get_cfws, which calls get_comment -- so get_domain transitively depends on get_comment.
# get_domain calls get_domain_literal, which calls _check_for_early_dl_end -- so get_domain transitively depends on _check_for_early_dl_end.
# get_domain -> get_domain_literal -> get_fws: get_domain has a transitive dependency on get_fws through get_domain_literal.
# get_domain calls get_domain_literal, which calls ValueTerminal -- so get_domain transitively depends on ValueTerminal.
# Since get_domain invokes get_domain_literal and get_domain_literal invokes get_cfws, get_domain indirectly relies on get_cfws.
# Note: get_domain does not call DomainLiteral directly, but reaches it via get_domain_literal.
# get_domain calls get_domain_literal, which calls get_dtext -- so get_domain transitively depends on get_dtext.
# get_addr_spec -> get_local_part -> LocalPart: get_addr_spec has a transitive dependency on LocalPart through get_local_part.
# Note: get_addr_spec does not call get_word directly, but reaches it via get_local_part.
# Note: get_addr_spec does not call get_dot_atom directly, but reaches it via get_local_part.
# Note: get_addr_spec does not call TokenList directly, but reaches it via get_local_part.
# get_addr_spec calls get_local_part, which calls get_cfws -- so get_addr_spec transitively depends on get_cfws.
# get_addr_spec calls get_local_part, which calls get_obs_local_part -- so get_addr_spec transitively depends on get_obs_local_part.
# Note: get_addr_spec does not call Domain directly, but reaches it via get_domain.
# get_addr_spec -> get_domain -> get_dot_atom: get_addr_spec has a transitive dependency on get_dot_atom through get_domain.
# get_addr_spec calls get_domain, which calls get_atom -- so get_addr_spec transitively depends on get_atom.
# get_addr_spec calls get_domain, which calls get_cfws -- so get_addr_spec transitively depends on get_cfws.
# get_addr_spec calls get_domain, which calls get_domain_literal -- so get_addr_spec transitively depends on get_domain_literal.
# get_obs_route -> get_cfws -> CFWSList: get_obs_route has a transitive dependency on CFWSList through get_cfws.
# get_obs_route -> get_cfws -> get_fws: get_obs_route has a transitive dependency on get_fws through get_cfws.
# get_obs_route calls get_cfws, which calls get_comment -- so get_obs_route transitively depends on get_comment.
# Note: get_obs_route does not call Domain directly, but reaches it via get_domain.
# get_obs_route calls get_domain, which calls get_dot_atom -- so get_obs_route transitively depends on get_dot_atom.
# Since get_obs_route invokes get_domain and get_domain invokes get_atom, get_obs_route indirectly relies on get_atom.
# get_obs_route -> get_domain -> get_cfws: get_obs_route has a transitive dependency on get_cfws through get_domain.
# get_obs_route calls get_domain, which calls get_domain_literal -- so get_obs_route transitively depends on get_domain_literal.
# get_angle_addr calls get_addr_spec, which calls get_local_part -- so get_angle_addr transitively depends on get_local_part.
# Note: get_angle_addr does not call ValueTerminal directly, but reaches it via get_addr_spec.
# get_angle_addr -> get_addr_spec -> AddrSpec: get_angle_addr has a transitive dependency on AddrSpec through get_addr_spec.
# Since get_angle_addr invokes get_addr_spec and get_addr_spec invokes get_domain, get_angle_addr indirectly relies on get_domain.
# Since get_angle_addr invokes get_cfws and get_cfws invokes CFWSList, get_angle_addr indirectly relies on CFWSList.
# Since get_angle_addr invokes get_cfws and get_cfws invokes get_fws, get_angle_addr indirectly relies on get_fws.
# Since get_angle_addr invokes get_cfws and get_cfws invokes get_comment, get_angle_addr indirectly relies on get_comment.
# Since get_angle_addr invokes get_obs_route and get_obs_route invokes ValueTerminal, get_angle_addr indirectly relies on ValueTerminal.
# Since get_angle_addr invokes get_obs_route and get_obs_route invokes get_cfws, get_angle_addr indirectly relies on get_cfws.
# get_angle_addr -> get_obs_route -> ObsRoute: get_angle_addr has a transitive dependency on ObsRoute through get_obs_route.
# get_angle_addr calls get_obs_route, which calls get_domain -- so get_angle_addr transitively depends on get_domain.
# Note: get_display_name does not call EWWhiteSpaceTerminal directly, but reaches it via get_phrase.
# Note: get_display_name does not call get_word directly, but reaches it via get_phrase.
# get_display_name calls get_phrase, which calls get_cfws -- so get_display_name transitively depends on get_cfws.
# Since get_display_name invokes get_phrase and get_phrase invokes Phrase, get_display_name indirectly relies on Phrase.
# Since get_name_addr invokes get_display_name and get_display_name invokes DisplayName, get_name_addr indirectly relies on DisplayName.
# get_name_addr -> get_display_name -> get_phrase: get_name_addr has a transitive dependency on get_phrase through get_display_name.
# get_name_addr -> get_angle_addr -> AngleAddr: get_name_addr has a transitive dependency on AngleAddr through get_angle_addr.
# Since get_name_addr invokes get_angle_addr and get_angle_addr invokes get_addr_spec, get_name_addr indirectly relies on get_addr_spec.
# Since get_name_addr invokes get_angle_addr and get_angle_addr invokes ValueTerminal, get_name_addr indirectly relies on ValueTerminal.
# get_name_addr -> get_angle_addr -> get_cfws: get_name_addr has a transitive dependency on get_cfws through get_angle_addr.
# Since get_name_addr invokes get_angle_addr and get_angle_addr invokes get_obs_route, get_name_addr indirectly relies on get_obs_route.
# Since get_name_addr invokes get_cfws and get_cfws invokes CFWSList, get_name_addr indirectly relies on CFWSList.
# Note: get_name_addr does not call get_fws directly, but reaches it via get_cfws.
# Note: get_name_addr does not call get_comment directly, but reaches it via get_cfws.
# get_mailbox -> get_addr_spec -> get_local_part: get_mailbox has a transitive dependency on get_local_part through get_addr_spec.
# Since get_mailbox invokes get_addr_spec and get_addr_spec invokes ValueTerminal, get_mailbox indirectly relies on ValueTerminal.
# Note: get_mailbox does not call AddrSpec directly, but reaches it via get_addr_spec.
# get_mailbox calls get_addr_spec, which calls get_domain -- so get_mailbox transitively depends on get_domain.
# Since get_mailbox invokes get_name_addr and get_name_addr invokes NameAddr, get_mailbox indirectly relies on NameAddr.
# get_mailbox calls get_name_addr, which calls get_display_name -- so get_mailbox transitively depends on get_display_name.
# Note: get_mailbox does not call get_angle_addr directly, but reaches it via get_name_addr.
# Since get_mailbox invokes get_name_addr and get_name_addr invokes get_cfws, get_mailbox indirectly relies on get_cfws.
# Since get_invalid_mailbox invokes get_phrase and get_phrase invokes EWWhiteSpaceTerminal, get_invalid_mailbox indirectly relies on EWWhiteSpaceTerminal.
# get_invalid_mailbox calls get_phrase, which calls get_word -- so get_invalid_mailbox transitively depends on get_word.
# get_invalid_mailbox calls get_phrase, which calls get_cfws -- so get_invalid_mailbox transitively depends on get_cfws.
# get_invalid_mailbox -> get_phrase -> Phrase: get_invalid_mailbox has a transitive dependency on Phrase through get_phrase.
# get_mailbox_list calls get_invalid_mailbox, which calls ValueTerminal -- so get_mailbox_list transitively depends on ValueTerminal.
# get_mailbox_list calls get_invalid_mailbox, which calls get_phrase -- so get_mailbox_list transitively depends on get_phrase.
# get_mailbox_list calls get_invalid_mailbox, which calls InvalidMailbox -- so get_mailbox_list transitively depends on InvalidMailbox.
# get_mailbox_list calls get_mailbox, which calls Mailbox -- so get_mailbox_list transitively depends on Mailbox.
# Since get_mailbox_list invokes get_mailbox and get_mailbox invokes get_addr_spec, get_mailbox_list indirectly relies on get_addr_spec.
# Since get_mailbox_list invokes get_mailbox and get_mailbox invokes get_name_addr, get_mailbox_list indirectly relies on get_name_addr.
# Note: get_mailbox_list does not call CFWSList directly, but reaches it via get_cfws.
# get_mailbox_list calls get_cfws, which calls get_fws -- so get_mailbox_list transitively depends on get_fws.
# Note: get_mailbox_list does not call get_comment directly, but reaches it via get_cfws.
# Note: get_group_list does not call get_invalid_mailbox directly, but reaches it via get_mailbox_list.
# get_group_list -> get_mailbox_list -> get_mailbox: get_group_list has a transitive dependency on get_mailbox through get_mailbox_list.
# Since get_group_list invokes get_mailbox_list and get_mailbox_list invokes MailboxList, get_group_list indirectly relies on MailboxList.
# Since get_group_list invokes get_mailbox_list and get_mailbox_list invokes get_cfws, get_group_list indirectly relies on get_cfws.
# get_group_list -> get_cfws -> CFWSList: get_group_list has a transitive dependency on CFWSList through get_cfws.
# Since get_group_list invokes get_cfws and get_cfws invokes get_fws, get_group_list indirectly relies on get_fws.
# Note: get_group_list does not call get_comment directly, but reaches it via get_cfws.
# get_group calls get_display_name, which calls DisplayName -- so get_group transitively depends on DisplayName.
# Since get_group invokes get_display_name and get_display_name invokes get_phrase, get_group indirectly relies on get_phrase.
# Note: get_group does not call GroupList directly, but reaches it via get_group_list.
# get_group -> get_group_list -> get_mailbox_list: get_group has a transitive dependency on get_mailbox_list through get_group_list.
# get_group calls get_group_list, which calls get_cfws -- so get_group transitively depends on get_cfws.
# Note: get_group does not call CFWSList directly, but reaches it via get_cfws.
# Note: get_group does not call get_fws directly, but reaches it via get_cfws.
# Since get_group invokes get_cfws and get_cfws invokes get_comment, get_group indirectly relies on get_comment.
# Note: get_address does not call get_display_name directly, but reaches it via get_group.
# Note: get_address does not call Group directly, but reaches it via get_group.
# Since get_address invokes get_group and get_group invokes get_group_list, get_address indirectly relies on get_group_list.
# Note: get_address does not call ValueTerminal directly, but reaches it via get_group.
# Since get_address invokes get_group and get_group invokes get_cfws, get_address indirectly relies on get_cfws.
# Note: get_address does not call Mailbox directly, but reaches it via get_mailbox.
# Note: get_address does not call get_addr_spec directly, but reaches it via get_mailbox.
# get_address calls get_mailbox, which calls get_name_addr -- so get_address transitively depends on get_name_addr.
# get_address_list -> get_cfws -> CFWSList: get_address_list has a transitive dependency on CFWSList through get_cfws.
# get_address_list -> get_cfws -> get_fws: get_address_list has a transitive dependency on get_fws through get_cfws.
# get_address_list -> get_cfws -> get_comment: get_address_list has a transitive dependency on get_comment through get_cfws.
# get_address_list calls get_invalid_mailbox, which calls ValueTerminal -- so get_address_list transitively depends on ValueTerminal.
# Note: get_address_list does not call get_phrase directly, but reaches it via get_invalid_mailbox.
# Since get_address_list invokes get_invalid_mailbox and get_invalid_mailbox invokes InvalidMailbox, get_address_list indirectly relies on InvalidMailbox.
# get_address_list -> get_address -> get_group: get_address_list has a transitive dependency on get_group through get_address.
# get_address_list calls get_address, which calls get_mailbox -- so get_address_list transitively depends on get_mailbox.
# get_address_list -> get_address -> Address: get_address_list has a transitive dependency on Address through get_address.
# Note: get_no_fold_literal does not call ValueTerminal directly, but reaches it via get_dtext.
# get_no_fold_literal calls get_dtext, which calls _get_ptext_to_endchars -- so get_no_fold_literal transitively depends on _get_ptext_to_endchars.
# get_no_fold_literal calls get_dtext, which calls _validate_xtext -- so get_no_fold_literal transitively depends on _validate_xtext.
# Since get_msg_id invokes get_no_fold_literal and get_no_fold_literal invokes ValueTerminal, get_msg_id indirectly relies on ValueTerminal.
# Note: get_msg_id does not call get_dtext directly, but reaches it via get_no_fold_literal.
# get_msg_id calls get_no_fold_literal, which calls NoFoldLiteral -- so get_msg_id transitively depends on NoFoldLiteral.
# Note: get_msg_id does not call Domain directly, but reaches it via get_domain.
# Note: get_msg_id does not call get_dot_atom directly, but reaches it via get_domain.
# Note: get_msg_id does not call get_atom directly, but reaches it via get_domain.
# get_msg_id calls get_domain, which calls get_cfws -- so get_msg_id transitively depends on get_cfws.
# Note: get_msg_id does not call get_domain_literal directly, but reaches it via get_domain.
# Note: get_msg_id does not call CFWSList directly, but reaches it via get_cfws.
# Since get_msg_id invokes get_cfws and get_cfws invokes get_fws, get_msg_id indirectly relies on get_fws.
# Since get_msg_id invokes get_cfws and get_cfws invokes get_comment, get_msg_id indirectly relies on get_comment.
# get_msg_id -> get_dot_atom_text -> DotAtomText: get_msg_id has a transitive dependency on DotAtomText through get_dot_atom_text.
# Note: get_msg_id does not call get_atext directly, but reaches it via get_dot_atom_text.
# Note: get_msg_id does not call get_word directly, but reaches it via get_obs_local_part.
# Since get_msg_id invokes get_obs_local_part and get_obs_local_part invokes ValueTerminal, get_msg_id indirectly relies on ValueTerminal.
# get_msg_id -> get_obs_local_part -> get_cfws: get_msg_id has a transitive dependency on get_cfws through get_obs_local_part.
# get_msg_id calls get_obs_local_part, which calls ObsLocalPart -- so get_msg_id transitively depends on ObsLocalPart.
# parse_message_id -> get_unstructured -> get_fws: parse_message_id has a transitive dependency on get_fws through get_unstructured.
# parse_message_id -> get_unstructured -> EWWhiteSpaceTerminal: parse_message_id has a transitive dependency on EWWhiteSpaceTerminal through get_unstructured.
# Since parse_message_id invokes get_unstructured and get_unstructured invokes ValueTerminal, parse_message_id indirectly relies on ValueTerminal.
# parse_message_id -> get_unstructured -> get_encoded_word: parse_message_id has a transitive dependency on get_encoded_word through get_unstructured.
# Note: parse_message_id does not call _wsp_splitter directly, but reaches it via get_unstructured.
# parse_message_id calls get_unstructured, which calls _validate_xtext -- so parse_message_id transitively depends on _validate_xtext.
# Since parse_message_id invokes get_unstructured and get_unstructured invokes UnstructuredTokenList, parse_message_id indirectly relies on UnstructuredTokenList.
# parse_message_id -> get_msg_id -> MsgID: parse_message_id has a transitive dependency on MsgID through get_msg_id.
# Since parse_message_id invokes get_msg_id and get_msg_id invokes get_no_fold_literal, parse_message_id indirectly relies on get_no_fold_literal.
# parse_message_id -> get_msg_id -> get_domain: parse_message_id has a transitive dependency on get_domain through get_msg_id.
# parse_message_id -> get_msg_id -> ValueTerminal: parse_message_id has a transitive dependency on ValueTerminal through get_msg_id.
# Note: parse_message_id does not call get_cfws directly, but reaches it via get_msg_id.
# parse_message_id calls get_msg_id, which calls get_dot_atom_text -- so parse_message_id transitively depends on get_dot_atom_text.
# parse_message_id -> get_msg_id -> get_obs_local_part: parse_message_id has a transitive dependency on get_obs_local_part through get_msg_id.
# Note: parse_message_ids does not call get_fws directly, but reaches it via get_unstructured.
# parse_message_ids calls get_unstructured, which calls EWWhiteSpaceTerminal -- so parse_message_ids transitively depends on EWWhiteSpaceTerminal.
# Note: parse_message_ids does not call ValueTerminal directly, but reaches it via get_unstructured.
# parse_message_ids -> get_unstructured -> get_encoded_word: parse_message_ids has a transitive dependency on get_encoded_word through get_unstructured.
# Note: parse_message_ids does not call _wsp_splitter directly, but reaches it via get_unstructured.
# Since parse_message_ids invokes get_unstructured and get_unstructured invokes _validate_xtext, parse_message_ids indirectly relies on _validate_xtext.
# parse_message_ids -> get_unstructured -> UnstructuredTokenList: parse_message_ids has a transitive dependency on UnstructuredTokenList through get_unstructured.
# Since parse_message_ids invokes get_msg_id and get_msg_id invokes MsgID, parse_message_ids indirectly relies on MsgID.
# Since parse_message_ids invokes get_msg_id and get_msg_id invokes get_no_fold_literal, parse_message_ids indirectly relies on get_no_fold_literal.
# parse_message_ids calls get_msg_id, which calls get_domain -- so parse_message_ids transitively depends on get_domain.
# parse_message_ids -> get_msg_id -> ValueTerminal: parse_message_ids has a transitive dependency on ValueTerminal through get_msg_id.
# Since parse_message_ids invokes get_msg_id and get_msg_id invokes get_cfws, parse_message_ids indirectly relies on get_cfws.
# parse_message_ids calls get_msg_id, which calls get_dot_atom_text -- so parse_message_ids transitively depends on get_dot_atom_text.
# Note: parse_message_ids does not call get_obs_local_part directly, but reaches it via get_msg_id.
# Note: parse_mime_version does not call CFWSList directly, but reaches it via get_cfws.
# Note: parse_mime_version does not call get_fws directly, but reaches it via get_cfws.
# parse_mime_version calls get_cfws, which calls get_comment -- so parse_mime_version transitively depends on get_comment.
# get_invalid_parameter calls get_phrase, which calls EWWhiteSpaceTerminal -- so get_invalid_parameter transitively depends on EWWhiteSpaceTerminal.
# Note: get_invalid_parameter does not call get_word directly, but reaches it via get_phrase.
# Since get_invalid_parameter invokes get_phrase and get_phrase invokes get_cfws, get_invalid_parameter indirectly relies on get_cfws.
# Since get_invalid_parameter invokes get_phrase and get_phrase invokes Phrase, get_invalid_parameter indirectly relies on Phrase.
# Since get_ttext invokes _validate_xtext and _validate_xtext invokes _non_printable_finder, get_ttext indirectly relies on _non_printable_finder.
# Since get_token invokes get_ttext and get_ttext invokes ValueTerminal, get_token indirectly relies on ValueTerminal.
# Since get_token invokes get_ttext and get_ttext invokes _non_token_end_matcher, get_token indirectly relies on _non_token_end_matcher.
# Since get_token invokes get_ttext and get_ttext invokes _validate_xtext, get_token indirectly relies on _validate_xtext.
# get_token calls get_cfws, which calls CFWSList -- so get_token transitively depends on CFWSList.
# Since get_token invokes get_cfws and get_cfws invokes get_fws, get_token indirectly relies on get_fws.
# Note: get_token does not call get_comment directly, but reaches it via get_cfws.
# get_attrtext -> _validate_xtext -> _non_printable_finder: get_attrtext has a transitive dependency on _non_printable_finder through _validate_xtext.
# get_attribute -> get_attrtext -> ValueTerminal: get_attribute has a transitive dependency on ValueTerminal through get_attrtext.
# get_attribute -> get_attrtext -> _non_attribute_end_matcher: get_attribute has a transitive dependency on _non_attribute_end_matcher through get_attrtext.
# get_attribute calls get_attrtext, which calls _validate_xtext -- so get_attribute transitively depends on _validate_xtext.
# Note: get_attribute does not call CFWSList directly, but reaches it via get_cfws.
# get_attribute -> get_cfws -> get_fws: get_attribute has a transitive dependency on get_fws through get_cfws.
# Since get_attribute invokes get_cfws and get_cfws invokes get_comment, get_attribute indirectly relies on get_comment.
# get_extended_attrtext -> _validate_xtext -> _non_printable_finder: get_extended_attrtext has a transitive dependency on _non_printable_finder through _validate_xtext.
# get_extended_attribute -> get_cfws -> CFWSList: get_extended_attribute has a transitive dependency on CFWSList through get_cfws.
# Since get_extended_attribute invokes get_cfws and get_cfws invokes get_fws, get_extended_attribute indirectly relies on get_fws.
# Note: get_extended_attribute does not call get_comment directly, but reaches it via get_cfws.
# get_extended_attribute -> get_extended_attrtext -> ValueTerminal: get_extended_attribute has a transitive dependency on ValueTerminal through get_extended_attrtext.
# Since get_extended_attribute invokes get_extended_attrtext and get_extended_attrtext invokes _non_extended_attribute_end_matcher, get_extended_attribute indirectly relies on _non_extended_attribute_end_matcher.
# Note: get_extended_attribute does not call _validate_xtext directly, but reaches it via get_extended_attrtext.
# get_value -> get_extended_attribute -> get_cfws: get_value has a transitive dependency on get_cfws through get_extended_attribute.
# Note: get_value does not call get_extended_attrtext directly, but reaches it via get_extended_attribute.
# Note: get_value does not call Attribute directly, but reaches it via get_extended_attribute.
# get_value -> get_cfws -> CFWSList: get_value has a transitive dependency on CFWSList through get_cfws.
# Note: get_value does not call get_fws directly, but reaches it via get_cfws.
# Note: get_value does not call get_comment directly, but reaches it via get_cfws.
# get_value calls get_quoted_string, which calls get_bare_quoted_string -- so get_value transitively depends on get_bare_quoted_string.
# Since get_value invokes get_quoted_string and get_quoted_string invokes QuotedString, get_value indirectly relies on QuotedString.
# Since get_value invokes get_quoted_string and get_quoted_string invokes get_cfws, get_value indirectly relies on get_cfws.
# get_value calls Value, which calls get_context -- so get_value transitively depends on get_context.
# get_value calls Value, which calls synchronized -- so get_value transitively depends on synchronized.
# Since get_value invokes Value and Value invokes RawValue, get_value indirectly relies on RawValue.
# get_parameter calls get_attribute, which calls get_attrtext -- so get_parameter transitively depends on get_attrtext.
# get_parameter -> get_attribute -> get_cfws: get_parameter has a transitive dependency on get_cfws through get_attribute.
# Since get_parameter invokes get_attribute and get_attribute invokes Attribute, get_parameter indirectly relies on Attribute.
# Note: get_parameter does not call WhiteSpaceTerminal directly, but reaches it via get_fws.
# get_parameter calls get_extended_attrtext, which calls ValueTerminal -- so get_parameter transitively depends on ValueTerminal.
# get_parameter calls get_extended_attrtext, which calls _non_extended_attribute_end_matcher -- so get_parameter transitively depends on _non_extended_attribute_end_matcher.
# get_parameter calls get_extended_attrtext, which calls _validate_xtext -- so get_parameter transitively depends on _validate_xtext.
# get_parameter calls get_attrtext, which calls ValueTerminal -- so get_parameter transitively depends on ValueTerminal.
# get_parameter -> get_attrtext -> _non_attribute_end_matcher: get_parameter has a transitive dependency on _non_attribute_end_matcher through get_attrtext.
# Note: get_parameter does not call _validate_xtext directly, but reaches it via get_attrtext.
# get_parameter -> Value -> get_context: get_parameter has a transitive dependency on get_context through Value.
# get_parameter calls Value, which calls synchronized -- so get_parameter transitively depends on synchronized.
# Note: get_parameter does not call RawValue directly, but reaches it via Value.
# get_parameter -> get_section -> ValueTerminal: get_parameter has a transitive dependency on ValueTerminal through get_section.
# get_parameter -> get_section -> Section: get_parameter has a transitive dependency on Section through get_section.
# Note: get_parameter does not call get_extended_attribute directly, but reaches it via get_value.
# Since get_parameter invokes get_value and get_value invokes get_cfws, get_parameter indirectly relies on get_cfws.
# Since get_parameter invokes get_value and get_value invokes get_quoted_string, get_parameter indirectly relies on get_quoted_string.
# get_parameter calls get_value, which calls Value -- so get_parameter transitively depends on Value.
# Since get_parameter invokes get_quoted_string and get_quoted_string invokes get_bare_quoted_string, get_parameter indirectly relies on get_bare_quoted_string.
# get_parameter calls get_quoted_string, which calls QuotedString -- so get_parameter transitively depends on QuotedString.
# Since get_parameter invokes get_quoted_string and get_quoted_string invokes get_cfws, get_parameter indirectly relies on get_cfws.
# Note: get_parameter does not call CFWSList directly, but reaches it via get_cfws.
# Since get_parameter invokes get_cfws and get_cfws invokes get_fws, get_parameter indirectly relies on get_fws.
# Note: get_parameter does not call get_comment directly, but reaches it via get_cfws.
# Note: get_parameter does not call ValueTerminal directly, but reaches it via get_qcontent.
# get_parameter -> get_qcontent -> _get_ptext_to_endchars: get_parameter has a transitive dependency on _get_ptext_to_endchars through get_qcontent.
# get_parameter calls get_qcontent, which calls _validate_xtext -- so get_parameter transitively depends on _validate_xtext.
# Since parse_mime_parameters invokes get_parameter and get_parameter invokes get_attribute, parse_mime_parameters indirectly relies on get_attribute.
# Note: parse_mime_parameters does not call get_fws directly, but reaches it via get_parameter.
# Since parse_mime_parameters invokes get_parameter and get_parameter invokes get_extended_attrtext, parse_mime_parameters indirectly relies on get_extended_attrtext.
# Note: parse_mime_parameters does not call ValueTerminal directly, but reaches it via get_parameter.
# parse_mime_parameters calls get_parameter, which calls get_attrtext -- so parse_mime_parameters transitively depends on get_attrtext.
# Note: parse_mime_parameters does not call Parameter directly, but reaches it via get_parameter.
# parse_mime_parameters calls get_parameter, which calls Value -- so parse_mime_parameters transitively depends on Value.
# Note: parse_mime_parameters does not call get_section directly, but reaches it via get_parameter.
# parse_mime_parameters -> get_parameter -> get_value: parse_mime_parameters has a transitive dependency on get_value through get_parameter.
# Since parse_mime_parameters invokes get_parameter and get_parameter invokes get_quoted_string, parse_mime_parameters indirectly relies on get_quoted_string.
# Note: parse_mime_parameters does not call get_cfws directly, but reaches it via get_parameter.
# parse_mime_parameters calls get_parameter, which calls get_qcontent -- so parse_mime_parameters transitively depends on get_qcontent.
# Note: parse_mime_parameters does not call CFWSList directly, but reaches it via get_cfws.
# parse_mime_parameters calls get_cfws, which calls get_fws -- so parse_mime_parameters transitively depends on get_fws.
# parse_mime_parameters -> get_cfws -> get_comment: parse_mime_parameters has a transitive dependency on get_comment through get_cfws.
# Note: parse_mime_parameters does not call ValueTerminal directly, but reaches it via get_invalid_parameter.
# Note: parse_mime_parameters does not call InvalidParameter directly, but reaches it via get_invalid_parameter.
# Note: parse_mime_parameters does not call get_phrase directly, but reaches it via get_invalid_parameter.
# _find_mime_parameters -> get_phrase -> EWWhiteSpaceTerminal: _find_mime_parameters has a transitive dependency on EWWhiteSpaceTerminal through get_phrase.
# Since _find_mime_parameters invokes get_phrase and get_phrase invokes get_word, _find_mime_parameters indirectly relies on get_word.
# Since _find_mime_parameters invokes get_phrase and get_phrase invokes get_cfws, _find_mime_parameters indirectly relies on get_cfws.
# _find_mime_parameters -> get_phrase -> Phrase: _find_mime_parameters has a transitive dependency on Phrase through get_phrase.
# Since _find_mime_parameters invokes parse_mime_parameters and parse_mime_parameters invokes ValueTerminal, _find_mime_parameters indirectly relies on ValueTerminal.
# _find_mime_parameters -> parse_mime_parameters -> get_parameter: _find_mime_parameters has a transitive dependency on get_parameter through parse_mime_parameters.
# Since _find_mime_parameters invokes parse_mime_parameters and parse_mime_parameters invokes MimeParameters, _find_mime_parameters indirectly relies on MimeParameters.
# Note: _find_mime_parameters does not call get_cfws directly, but reaches it via parse_mime_parameters.
# _find_mime_parameters calls parse_mime_parameters, which calls get_invalid_parameter -- so _find_mime_parameters transitively depends on get_invalid_parameter.
# parse_content_type_header calls _find_mime_parameters, which calls ValueTerminal -- so parse_content_type_header transitively depends on ValueTerminal.
# Note: parse_content_type_header does not call get_phrase directly, but reaches it via _find_mime_parameters.
# Note: parse_content_type_header does not call parse_mime_parameters directly, but reaches it via _find_mime_parameters.
# Note: parse_content_type_header does not call ValueTerminal directly, but reaches it via parse_mime_parameters.
# parse_content_type_header calls parse_mime_parameters, which calls get_parameter -- so parse_content_type_header transitively depends on get_parameter.
# parse_content_type_header -> parse_mime_parameters -> MimeParameters: parse_content_type_header has a transitive dependency on MimeParameters through parse_mime_parameters.
# Note: parse_content_type_header does not call get_cfws directly, but reaches it via parse_mime_parameters.
# Since parse_content_type_header invokes parse_mime_parameters and parse_mime_parameters invokes get_invalid_parameter, parse_content_type_header indirectly relies on get_invalid_parameter.
# parse_content_type_header calls get_token, which calls get_ttext -- so parse_content_type_header transitively depends on get_ttext.
# parse_content_type_header -> get_token -> get_cfws: parse_content_type_header has a transitive dependency on get_cfws through get_token.
# Since parse_content_type_header invokes get_token and get_token invokes Token, parse_content_type_header indirectly relies on Token.
# parse_content_disposition_header -> _find_mime_parameters -> ValueTerminal: parse_content_disposition_header has a transitive dependency on ValueTerminal through _find_mime_parameters.
# parse_content_disposition_header calls _find_mime_parameters, which calls get_phrase -- so parse_content_disposition_header transitively depends on get_phrase.
# Since parse_content_disposition_header invokes _find_mime_parameters and _find_mime_parameters invokes parse_mime_parameters, parse_content_disposition_header indirectly relies on parse_mime_parameters.
# parse_content_disposition_header calls parse_mime_parameters, which calls ValueTerminal -- so parse_content_disposition_header transitively depends on ValueTerminal.
# Since parse_content_disposition_header invokes parse_mime_parameters and parse_mime_parameters invokes get_parameter, parse_content_disposition_header indirectly relies on get_parameter.
# Since parse_content_disposition_header invokes parse_mime_parameters and parse_mime_parameters invokes MimeParameters, parse_content_disposition_header indirectly relies on MimeParameters.
# parse_content_disposition_header calls parse_mime_parameters, which calls get_cfws -- so parse_content_disposition_header transitively depends on get_cfws.
# parse_content_disposition_header calls parse_mime_parameters, which calls get_invalid_parameter -- so parse_content_disposition_header transitively depends on get_invalid_parameter.
# Note: parse_content_disposition_header does not call get_ttext directly, but reaches it via get_token.
# Note: parse_content_disposition_header does not call get_cfws directly, but reaches it via get_token.
# parse_content_disposition_header calls get_token, which calls Token -- so parse_content_disposition_header transitively depends on Token.
# Note: parse_content_transfer_encoding_header does not call EWWhiteSpaceTerminal directly, but reaches it via get_phrase.
# parse_content_transfer_encoding_header -> get_phrase -> get_word: parse_content_transfer_encoding_header has a transitive dependency on get_word through get_phrase.
# parse_content_transfer_encoding_header -> get_phrase -> get_cfws: parse_content_transfer_encoding_header has a transitive dependency on get_cfws through get_phrase.
# parse_content_transfer_encoding_header calls get_phrase, which calls Phrase -- so parse_content_transfer_encoding_header transitively depends on Phrase.
# parse_content_transfer_encoding_header calls get_token, which calls get_ttext -- so parse_content_transfer_encoding_header transitively depends on get_ttext.
# Since parse_content_transfer_encoding_header invokes get_token and get_token invokes get_cfws, parse_content_transfer_encoding_header indirectly relies on get_cfws.
# Since parse_content_transfer_encoding_header invokes get_token and get_token invokes Token, parse_content_transfer_encoding_header indirectly relies on Token.
# Note: _refold_parse_tree does not call quote_string directly, but reaches it via _fold_mime_parameters.
# _refold_parse_tree -> _fold_as_ew -> get_unstructured: _refold_parse_tree has a transitive dependency on get_unstructured through _fold_as_ew.
# _refold_parse_tree -> _fold_as_ew -> _steal_trailing_WSP_if_exists: _refold_parse_tree has a transitive dependency on _steal_trailing_WSP_if_exists through _fold_as_ew.
# Since _fold_as_ew invokes get_unstructured and get_unstructured invokes get_fws, _fold_as_ew indirectly relies on get_fws.
# _fold_as_ew calls get_unstructured, which calls EWWhiteSpaceTerminal -- so _fold_as_ew transitively depends on EWWhiteSpaceTerminal.
# Note: _fold_as_ew does not call ValueTerminal directly, but reaches it via get_unstructured.
# _fold_as_ew -> get_unstructured -> get_encoded_word: _fold_as_ew has a transitive dependency on get_encoded_word through get_unstructured.
# _fold_as_ew -> get_unstructured -> _wsp_splitter: _fold_as_ew has a transitive dependency on _wsp_splitter through get_unstructured.
# _fold_as_ew calls get_unstructured, which calls _validate_xtext -- so _fold_as_ew transitively depends on _validate_xtext.
# _fold_as_ew calls get_unstructured, which calls UnstructuredTokenList -- so _fold_as_ew transitively depends on UnstructuredTokenList.
# _fold_mime_parameters -> quote_string -> make_quoted_pairs: _fold_mime_parameters has a transitive dependency on make_quoted_pairs through quote_string.
# Note: parsedate does not call _parsedate_tz directly, but reaches it via parsedate_tz.
# Since body_encode invokes append and append invokes Time2Internaldate, body_encode indirectly relies on Time2Internaldate.
# body_encode -> append -> Charset: body_encode has a transitive dependency on Charset through append.
# Note: body_encode does not call quote_from_bytes directly, but reaches it via quote.
# Since decode invokes unquote and unquote invokes _unquote_impl, decode indirectly relies on _unquote_impl.
# Since decode invokes unquote and unquote invokes _generate_unquoted_parts, decode indirectly relies on _generate_unquoted_parts.
# decode -> ToUnicode -> nameprep: decode has a transitive dependency on nameprep through ToUnicode.
# Since decode invokes ToUnicode and ToUnicode invokes ToASCII, decode indirectly relies on ToASCII.
# Note: decode does not call insertion_sort directly, but reaches it via punycode_decode.
# decode -> quopri_decode -> BytesIO: decode has a transitive dependency on BytesIO through quopri_decode.
# decode -> uu_decode -> readline: decode has a transitive dependency on readline through uu_decode.
# Note: decode does not call write directly, but reaches it via uu_decode.
# Since decode invokes uu_decode and uu_decode invokes BytesIO, decode indirectly relies on BytesIO.
# set_text_content calls _encode_text, which calls embedded_body -- so set_text_content transitively depends on embedded_body.
# Note: set_text_content does not call _encode_base64 directly, but reaches it via _encode_text.
# set_text_content -> _encode_text -> normal_body: set_text_content has a transitive dependency on normal_body through _encode_text.
# Note: encode_quopri does not call _encodestring directly, but reaches it via _qencode.
# Since unquote invokes _unquote_impl and _unquote_impl invokes append, unquote indirectly relies on append.
# unquote -> _generate_unquoted_parts -> _unquote_impl: unquote has a transitive dependency on _unquote_impl through _generate_unquoted_parts.
# quote -> quote_from_bytes -> _byte_quoter_factory: quote has a transitive dependency on _byte_quoter_factory through quote_from_bytes.
# Note: _unquote_match does not call _unquote_impl directly, but reaches it via unquote.
# _unquote_match -> unquote -> _generate_unquoted_parts: _unquote_match has a transitive dependency on _generate_unquoted_parts through unquote.
# getaddresses -> _strip_quoted_realnames -> _iter_escaped_chars: getaddresses has a transitive dependency on _iter_escaped_chars through _strip_quoted_realnames.
# Note: getaddresses does not call _check_parenthesis directly, but reaches it via _pre_parse_validation.
# _check_parenthesis -> _strip_quoted_realnames -> _iter_escaped_chars: _check_parenthesis has a transitive dependency on _iter_escaped_chars through _strip_quoted_realnames.
# _pre_parse_validation calls _check_parenthesis, which calls _strip_quoted_realnames -- so _pre_parse_validation transitively depends on _strip_quoted_realnames.
# Note: _pre_parse_validation does not call _iter_escaped_chars directly, but reaches it via _check_parenthesis.
# formatdate -> format_datetime -> _format_timetuple_and_zone: formatdate has a transitive dependency on _format_timetuple_and_zone through format_datetime.
# parseaddr calls _pre_parse_validation, which calls _check_parenthesis -- so parseaddr transitively depends on _check_parenthesis.
# Note: decode_params does not call _unquote_impl directly, but reaches it via unquote.
# Since decode_params invokes unquote and unquote invokes _generate_unquoted_parts, decode_params indirectly relies on _generate_unquoted_parts.
# decode_params -> quote -> quote_from_bytes: decode_params has a transitive dependency on quote_from_bytes through quote.
# Since collapse_rfc2231_value invokes unquote and unquote invokes _unquote_impl, collapse_rfc2231_value indirectly relies on _unquote_impl.
# Since collapse_rfc2231_value invokes unquote and unquote invokes _generate_unquoted_parts, collapse_rfc2231_value indirectly relies on _generate_unquoted_parts.
# search_function -> getregentry -> Codec: search_function has a transitive dependency on Codec through getregentry.
# Note: ToUnicode does not call nameprep directly, but reaches it via ToASCII.
# Since generate_integers invokes generate_generalized_integer and generate_generalized_integer invokes T, generate_integers indirectly relies on T.
# punycode_encode calls insertion_unsort, which calls selective_find -- so punycode_encode transitively depends on selective_find.
# Note: punycode_encode does not call selective_len directly, but reaches it via insertion_unsort.
# Note: punycode_encode does not call adapt directly, but reaches it via generate_integers.
# Note: punycode_encode does not call generate_generalized_integer directly, but reaches it via generate_integers.
# Note: insertion_sort does not call T directly, but reaches it via decode_generalized_number.
# Since punycode_decode invokes insertion_sort and insertion_sort invokes adapt, punycode_decode indirectly relies on adapt.
# Note: punycode_decode does not call decode_generalized_number directly, but reaches it via insertion_sort.
# uu_encode -> write -> serialize: uu_encode has a transitive dependency on serialize through write.
# Note: uu_encode does not call sleep directly, but reaches it via write.
# Since uu_encode invokes write and write invokes _serialize_text, uu_encode indirectly relies on _serialize_text.
# uu_encode -> write -> assert_: uu_encode has a transitive dependency on assert_ through write.
# uu_encode calls write, which calls _namespaces -- so uu_encode transitively depends on _namespaces.
# uu_encode calls write, which calls time -- so uu_encode transitively depends on time.
# Note: uu_encode does not call _count_to_size directly, but reaches it via write.
# uu_encode -> write -> quote: uu_encode has a transitive dependency on quote through write.
# Note: uu_encode does not call _nbytes directly, but reaches it via write.
# Since uu_encode invokes write and write invokes _get_writer, uu_encode indirectly relies on _get_writer.
# Note: uu_encode does not call crc32 directly, but reaches it via write.
# uu_encode calls read, which calls UnpicklingError -- so uu_encode transitively depends on UnpicklingError.
# uu_encode calls read, which calls size_index -- so uu_encode transitively depends on size_index.
# Note: uu_encode does not call assert_ directly, but reaches it via read.
# uu_encode calls read, which calls ReadError -- so uu_encode transitively depends on ReadError.
# uu_encode -> read -> Pair: uu_encode has a transitive dependency on Pair through read.
# Since uu_decode invokes readline and readline invokes UnpicklingError, uu_decode indirectly relies on UnpicklingError.
# uu_decode -> readline -> size_index: uu_decode has a transitive dependency on size_index through readline.
# Since uu_decode invokes readline and readline invokes assert_, uu_decode indirectly relies on assert_.
# uu_decode calls readline, which calls ateof -- so uu_decode transitively depends on ateof.
# Since uu_decode invokes readline and readline invokes startup_hook, uu_decode indirectly relies on startup_hook.
# uu_decode -> readline -> nreadahead: uu_decode has a transitive dependency on nreadahead through readline.
# uu_decode calls write, which calls serialize -- so uu_decode transitively depends on serialize.
# Since uu_decode invokes write and write invokes sleep, uu_decode indirectly relies on sleep.
# uu_decode calls write, which calls _serialize_text -- so uu_decode transitively depends on _serialize_text.
# uu_decode calls write, which calls assert_ -- so uu_decode transitively depends on assert_.
# Note: uu_decode does not call _namespaces directly, but reaches it via write.
# uu_decode calls write, which calls time -- so uu_decode transitively depends on time.
# Since uu_decode invokes write and write invokes _count_to_size, uu_decode indirectly relies on _count_to_size.
# uu_decode calls write, which calls quote -- so uu_decode transitively depends on quote.
# Since uu_decode invokes write and write invokes _nbytes, uu_decode indirectly relies on _nbytes.
# uu_decode calls write, which calls _get_writer -- so uu_decode transitively depends on _get_writer.
# Note: uu_decode does not call crc32 directly, but reaches it via write.
# Since cmpfiles invokes _cmp and _cmp invokes timedelta, cmpfiles indirectly relies on timedelta.
# cmpfiles calls _cmp, which calls cmp -- so cmpfiles transitively depends on cmp.
# Note: _cmp does not call _sig directly, but reaches it via cmp.
# _cmp calls cmp, which calls _do_cmp -- so _cmp transitively depends on _do_cmp.
# Since _cmp invokes cmp and cmp invokes clear_cache, _cmp indirectly relies on clear_cache.
# Since _filter invokes filterfalse and filterfalse invokes _compile_pattern, _filter indirectly relies on _compile_pattern.
# Since _filter invokes filterfalse and filterfalse invokes match, _filter indirectly relies on match.
# Note: fnmatch does not call _compile_pattern directly, but reaches it via fnmatchcase.
# Note: fnmatch does not call match directly, but reaches it via fnmatchcase.
# Note: _compile_pattern does not call _translate directly, but reaches it via translate.
# _compile_pattern calls translate, which calls _join_translated_parts -- so _compile_pattern transitively depends on _join_translated_parts.
# Note: filter does not call translate directly, but reaches it via _compile_pattern.
# Since filter invokes match and match invokes _compile, filter indirectly relies on _compile.
# Note: filter does not call _StringGlobber directly, but reaches it via match.
# filter calls f, which calls f1 -- so filter transitively depends on f1.
# filterfalse -> _compile_pattern -> translate: filterfalse has a transitive dependency on translate through _compile_pattern.
# filterfalse calls match, which calls _compile -- so filterfalse transitively depends on _compile.
# filterfalse -> match -> _StringGlobber: filterfalse has a transitive dependency on _StringGlobber through match.
# fnmatchcase -> _compile_pattern -> translate: fnmatchcase has a transitive dependency on translate through _compile_pattern.
# fnmatchcase -> match -> _compile: fnmatchcase has a transitive dependency on _compile through match.
# Note: fnmatchcase does not call _StringGlobber directly, but reaches it via match.
# Since translate invokes _translate and _translate invokes _re_setops_sub, translate indirectly relies on _re_setops_sub.
# translate calls _translate, which calls _re_escape -- so translate transitively depends on _re_escape.
# translate -> _translate -> add: translate has a transitive dependency on add through _translate.
# translate calls _join_translated_parts, which calls extend -- so translate transitively depends on extend.
# translate calls _join_translated_parts, which calls append -- so translate transitively depends on append.
# _translate -> add -> ref: _translate has a transitive dependency on ref through add.
# _translate calls add, which calls _lock_file -- so _translate transitively depends on _lock_file.
# Since _translate invokes add and add invokes bltn_open, _translate indirectly relies on bltn_open.
# Since _translate invokes add and add invokes ExternalClashError, _translate indirectly relies on ExternalClashError.
# Note: _translate does not call _unlock_file directly, but reaches it via add.
# Since _translate invokes add and add invokes Stats, _translate indirectly relies on Stats.
# _translate -> add -> add_func_stats: _translate has a transitive dependency on add_func_stats through add.
# Since _translate invokes add and add invokes _create_carefully, _translate indirectly relies on _create_carefully.
# Note: _translate does not call _sync_close directly, but reaches it via add.
# _translate calls add, which calls _convert_other -- so _translate transitively depends on _convert_other.
# _join_translated_parts calls append, which calls Time2Internaldate -- so _join_translated_parts transitively depends on Time2Internaldate.
# Note: _join_translated_parts does not call Charset directly, but reaches it via append.
# ftpcp calls parse227, which calls error_reply -- so ftpcp transitively depends on error_reply.
# Note: ftpcp does not call error_proto directly, but reaches it via parse227.
# Note: _partial_new does not call itemgetter directly, but reaches it via _partial_prepare_merger.
# Since lru_cache invokes _lru_cache_wrapper and _lru_cache_wrapper invokes cache_get, lru_cache indirectly relies on cache_get.
# Note: lru_cache does not call cache_len directly, but reaches it via _lru_cache_wrapper.
# lru_cache -> _lru_cache_wrapper -> user_function: lru_cache has a transitive dependency on user_function through _lru_cache_wrapper.
# Since lru_cache invokes _lru_cache_wrapper and _lru_cache_wrapper invokes _CacheInfo, lru_cache indirectly relies on _CacheInfo.
# Note: lru_cache does not call make_key directly, but reaches it via _lru_cache_wrapper.
# lru_cache -> _lru_cache_wrapper -> RLock: lru_cache has a transitive dependency on RLock through _lru_cache_wrapper.
# Note: _lru_cache_wrapper does not call _CRLock directly, but reaches it via RLock.
# _lru_cache_wrapper calls RLock, which calls _PyRLock -- so _lru_cache_wrapper transitively depends on _PyRLock.
# cache -> lru_cache -> update_wrapper: cache has a transitive dependency on update_wrapper through lru_cache.
# Since cache invokes lru_cache and lru_cache invokes _lru_cache_wrapper, cache indirectly relies on _lru_cache_wrapper.
# Note: _compose_mro does not call _c3_merge directly, but reaches it via _c3_mro.
# Since _find_impl invokes _compose_mro and _compose_mro invokes is_strict_base, _find_impl indirectly relies on is_strict_base.
# _find_impl -> _compose_mro -> _c3_mro: _find_impl has a transitive dependency on _c3_mro through _compose_mro.
# Note: _find_impl does not call is_related directly, but reaches it via _compose_mro.
# Note: singledispatch does not call _compose_mro directly, but reaches it via _find_impl.
# singledispatch calls get_type_hints, which calls _add_type_params_to_scope -- so singledispatch transitively depends on _add_type_params_to_scope.
# Note: singledispatch does not call _make_forward_ref directly, but reaches it via get_type_hints.
# singledispatch calls get_type_hints, which calls _eval_type -- so singledispatch transitively depends on _eval_type.
# Note: singledispatch does not call _strip_annotations directly, but reaches it via get_type_hints.
# Note: singledispatch does not call _is_valid_dispatch_type directly, but reaches it via register.
# Since singledispatch invokes register and register invokes get_type_hints, singledispatch indirectly relies on get_type_hints.
# Note: singledispatch does not call register_standard_browsers directly, but reaches it via register.
# Since singledispatch invokes register and register invokes proxytype, singledispatch indirectly relies on proxytype.
# singledispatch -> register -> SelectorKey: singledispatch has a transitive dependency on SelectorKey through register.
# Note: singledispatch does not call builder directly, but reaches it via register.
# Note: singledispatch does not call _abc_register directly, but reaches it via register.
# Since singledispatch invokes register and register invokes dispatch, singledispatch indirectly relies on dispatch.
# Since singledispatch invokes register and register invokes get_cache_token, singledispatch indirectly relies on get_cache_token.
# Note: singledispatch does not call convert_to_error directly, but reaches it via dispatch.
# singledispatch -> dispatch -> _find_impl: singledispatch has a transitive dependency on _find_impl through dispatch.
# singledispatch -> dispatch -> get_cache_token: singledispatch has a transitive dependency on get_cache_token through dispatch.
# Note: _warn_python_reduce_kwargs does not call partial directly, but reaches it via wraps.
# Since getopt invokes do_shorts and do_shorts invokes _, getopt indirectly relies on _.
# getopt calls do_shorts, which calls GetoptError -- so getopt transitively depends on GetoptError.
# Since getopt invokes do_shorts and do_shorts invokes short_has_arg, getopt indirectly relies on short_has_arg.
# getopt -> do_longs -> _: getopt has a transitive dependency on _ through do_longs.
# getopt calls do_longs, which calls GetoptError -- so getopt transitively depends on GetoptError.
# getopt calls do_longs, which calls long_has_args -- so getopt transitively depends on long_has_args.
# Since gnu_getopt invokes do_shorts and do_shorts invokes _, gnu_getopt indirectly relies on _.
# gnu_getopt -> do_shorts -> GetoptError: gnu_getopt has a transitive dependency on GetoptError through do_shorts.
# gnu_getopt calls do_shorts, which calls short_has_arg -- so gnu_getopt transitively depends on short_has_arg.
# gnu_getopt -> do_longs -> _: gnu_getopt has a transitive dependency on _ through do_longs.
# Since gnu_getopt invokes do_longs and do_longs invokes GetoptError, gnu_getopt indirectly relies on GetoptError.
# Note: gnu_getopt does not call long_has_args directly, but reaches it via do_longs.
# Note: do_longs does not call resolve directly, but reaches it via _.
# Since do_longs invokes _ and _ invokes _infer_caller, do_longs indirectly relies on _infer_caller.
# Note: do_longs does not call _ directly, but reaches it via long_has_args.
# Note: do_longs does not call GetoptError directly, but reaches it via long_has_args.
# Since long_has_args invokes _ and _ invokes resolve, long_has_args indirectly relies on resolve.
# Since long_has_args invokes _ and _ invokes _infer_caller, long_has_args indirectly relies on _infer_caller.
# Note: do_shorts does not call resolve directly, but reaches it via _.
# do_shorts calls _, which calls _infer_caller -- so do_shorts transitively depends on _infer_caller.
# do_shorts -> short_has_arg -> _: do_shorts has a transitive dependency on _ through short_has_arg.
# Since do_shorts invokes short_has_arg and short_has_arg invokes GetoptError, do_shorts indirectly relies on GetoptError.
# short_has_arg -> _ -> resolve: short_has_arg has a transitive dependency on resolve through _.
# Since short_has_arg invokes _ and _ invokes _infer_caller, short_has_arg indirectly relies on _infer_caller.
# Note: unix_getpass does not call _check_echo_char directly, but reaches it via fallback_getpass.
# Since unix_getpass invokes fallback_getpass and fallback_getpass invokes _raw_input, unix_getpass indirectly relies on _raw_input.
# Note: unix_getpass does not call _readline_with_echo_char directly, but reaches it via _raw_input.
# Since win_getpass invokes fallback_getpass and fallback_getpass invokes _check_echo_char, win_getpass indirectly relies on _check_echo_char.
# Note: win_getpass does not call _raw_input directly, but reaches it via fallback_getpass.
# fallback_getpass calls _raw_input, which calls _readline_with_echo_char -- so fallback_getpass transitively depends on _readline_with_echo_char.
# Since _parse invokes unquote and unquote invokes _unquote_impl, _parse indirectly relies on _unquote_impl.
# Note: _parse does not call _generate_unquoted_parts directly, but reaches it via unquote.
# _parse -> _parse_flags -> sourceget: _parse has a transitive dependency on sourceget through _parse_flags.
# Since _parse invokes _parse_sub and _parse_sub invokes itemsappend, _parse indirectly relies on itemsappend.
# _parse calls _parse_sub, which calls SubPattern -- so _parse transitively depends on SubPattern.
# _parse calls _parse_sub, which calls _uniq -- so _parse transitively depends on _uniq.
# _parse calls _parse_sub, which calls sourcematch -- so _parse transitively depends on sourcematch.
# Since _parse invokes c2py and c2py invokes _error, _parse indirectly relies on _error.
# Since _parse invokes c2py and c2py invokes _tokenize, _parse indirectly relies on _tokenize.
# Note: c2py does not call unquote directly, but reaches it via _parse.
# Since c2py invokes _parse and _parse invokes _uniq, c2py indirectly relies on _uniq.
# c2py calls _parse, which calls _parse_flags -- so c2py transitively depends on _parse_flags.
# Note: c2py does not call unpack directly, but reaches it via _parse.
# c2py -> _parse -> NetrcParseError: c2py has a transitive dependency on NetrcParseError through _parse.
# c2py -> _parse -> _error: c2py has a transitive dependency on _error through _parse.
# Note: c2py does not call setappend directly, but reaches it via _parse.
# Since c2py invokes _parse and _parse invokes _escape, c2py indirectly relies on _escape.
# Since c2py invokes _parse and _parse invokes subpatternappend, c2py indirectly relies on subpatternappend.
# c2py -> _parse -> _ord: c2py has a transitive dependency on _ord through _parse.
# c2py -> _parse -> _class_escape: c2py has a transitive dependency on _class_escape through _parse.
# Note: c2py does not call _splittype directly, but reaches it via _parse.
# Note: c2py does not call _netrclex directly, but reaches it via _parse.
# Note: c2py does not call SubPattern directly, but reaches it via _parse.
# c2py -> _parse -> _parse_sub: c2py has a transitive dependency on _parse_sub through _parse.
# Note: c2py does not call _len directly, but reaches it via _parse.
# c2py calls _parse, which calls sourceget -- so c2py transitively depends on sourceget.
# Since c2py invokes _parse and _parse invokes _splithost, c2py indirectly relies on _splithost.
# Since c2py invokes _parse and _parse invokes sourcematch, c2py indirectly relies on sourcematch.
# find calls iterfind, which calls _SelectorContext -- so find transitively depends on _SelectorContext.
# Since find invokes iterfind and iterfind invokes xpath_tokenizer, find indirectly relies on xpath_tokenizer.
# Note: find does not call select directly, but reaches it via iterfind.
# Note: translation does not call _expand_lang directly, but reaches it via find.
# Since translation invokes find and find invokes iterfind, translation indirectly relies on iterfind.
# install -> translation -> NullTranslations: install has a transitive dependency on NullTranslations through translation.
# install calls translation, which calls find -- so install transitively depends on find.
# install calls translation, which calls class_ -- so install transitively depends on class_.
# Note: dgettext does not call NullTranslations directly, but reaches it via translation.
# Since dgettext invokes translation and translation invokes find, dgettext indirectly relies on find.
# dgettext calls translation, which calls class_ -- so dgettext transitively depends on class_.
# Note: dngettext does not call NullTranslations directly, but reaches it via translation.
# Note: dngettext does not call find directly, but reaches it via translation.
# Since dngettext invokes translation and translation invokes class_, dngettext indirectly relies on class_.
# dpgettext calls translation, which calls NullTranslations -- so dpgettext transitively depends on NullTranslations.
# Since dpgettext invokes translation and translation invokes find, dpgettext indirectly relies on find.
# Note: dpgettext does not call class_ directly, but reaches it via translation.
# dnpgettext -> translation -> NullTranslations: dnpgettext has a transitive dependency on NullTranslations through translation.
# Note: dnpgettext does not call find directly, but reaches it via translation.
# dnpgettext calls translation, which calls class_ -- so dnpgettext transitively depends on class_.
# gettext calls dgettext, which calls translation -- so gettext transitively depends on translation.
# Note: ngettext does not call translation directly, but reaches it via dngettext.
# ngettext calls dngettext, which calls _as_int2 -- so ngettext transitively depends on _as_int2.
# Since pgettext invokes dpgettext and dpgettext invokes translation, pgettext indirectly relies on translation.
# Note: npgettext does not call translation directly, but reaches it via dnpgettext.
# npgettext -> dnpgettext -> _as_int2: npgettext has a transitive dependency on _as_int2 through dnpgettext.
# glob -> iglob -> _iglob: glob has a transitive dependency on _iglob through iglob.
# glob calls iglob, which calls _isrecursive -- so glob transitively depends on _isrecursive.
# glob calls select, which calls fd_to_key_get -- so glob transitively depends on fd_to_key_get.
# Since glob invokes select and select invokes EntryPoints, glob indirectly relies on EntryPoints.
# glob calls select, which calls select_tag -- so glob transitively depends on select_tag.
# glob -> select -> select_child: glob has a transitive dependency on select_child through select.
# Since glob invokes select and select invokes _isinstance, glob indirectly relies on _isinstance.
# Note: glob does not call get_parent_map directly, but reaches it via select.
# glob -> _explode_path -> split: glob has a transitive dependency on split through _explode_path.
# iglob calls _iglob, which calls _glob2 -- so iglob transitively depends on _glob2.
# Note: iglob does not call _glob1 directly, but reaches it via _iglob.
# iglob -> _iglob -> _lexists: iglob has a transitive dependency on _lexists through _iglob.
# Since iglob invokes _iglob and _iglob invokes _isdir, iglob indirectly relies on _isdir.
# Note: iglob does not call _join directly, but reaches it via _iglob.
# Note: iglob does not call _isrecursive directly, but reaches it via _iglob.
# iglob -> _iglob -> has_magic: iglob has a transitive dependency on has_magic through _iglob.
# Since iglob invokes _iglob and _iglob invokes glob_in_dir, iglob indirectly relies on glob_in_dir.
# _iglob -> _glob2 -> _isrecursive: _iglob has a transitive dependency on _isrecursive through _glob2.
# _iglob calls _glob2, which calls _rlistdir -- so _iglob transitively depends on _rlistdir.
# _iglob calls _glob2, which calls _isdir -- so _iglob transitively depends on _isdir.
# Since _iglob invokes _glob1 and _glob1 invokes _listdir, _iglob indirectly relies on _listdir.
# _iglob -> _glob1 -> _ishidden: _iglob has a transitive dependency on _ishidden through _glob1.
# Note: _glob1 does not call _iterdir directly, but reaches it via _listdir.
# glob0 calls _glob0, which calls _lexists -- so glob0 transitively depends on _lexists.
# Note: glob0 does not call _join directly, but reaches it via _glob0.
# Note: glob0 does not call _isdir directly, but reaches it via _glob0.
# glob1 -> _glob1 -> _listdir: glob1 has a transitive dependency on _listdir through _glob1.
# Note: glob1 does not call _ishidden directly, but reaches it via _glob1.
# _glob2 calls _rlistdir, which calls _join -- so _glob2 transitively depends on _join.
# _glob2 calls _rlistdir, which calls _listdir -- so _glob2 transitively depends on _listdir.
# Note: _glob2 does not call _ishidden directly, but reaches it via _rlistdir.
# Since _iterdir invokes fsencode and fsencode invokes fspath, _iterdir indirectly relies on fspath.
# Since _listdir invokes _iterdir and _iterdir invokes fsencode, _listdir indirectly relies on fsencode.
# _rlistdir calls _listdir, which calls _iterdir -- so _rlistdir transitively depends on _iterdir.
# escape calls replace, which calls _replace -- so escape transitively depends on _replace.
# Note: escape does not call _is_dataclass_instance directly, but reaches it via replace.
# Since escape invokes replace and replace invokes func, escape indirectly relies on func.
# __get_openssl_constructor calls f, which calls f1 -- so __get_openssl_constructor transitively depends on f1.
# file_digest -> digest -> _compute_digest_fallback: file_digest has a transitive dependency on _compute_digest_fallback through digest.
# file_digest -> new -> SymbolTable: file_digest has a transitive dependency on SymbolTable through new.
# Note: file_digest does not call Class directly, but reaches it via new.
# file_digest calls new, which calls Function -- so file_digest transitively depends on Function.
# file_digest -> new -> HMAC: file_digest has a transitive dependency on HMAC through new.
# heappop -> _siftup -> _siftdown: heappop has a transitive dependency on _siftdown through _siftup.
# heapreplace -> _siftup -> _siftdown: heapreplace has a transitive dependency on _siftdown through _siftup.
# Since heappushpop invokes _siftup and _siftup invokes _siftdown, heappushpop indirectly relies on _siftdown.
# heapify calls _siftup, which calls _siftdown -- so heapify transitively depends on _siftdown.
# heappop_max -> _siftup_max -> _siftdown_max: heappop_max has a transitive dependency on _siftdown_max through _siftup_max.
# heapreplace_max calls _siftup_max, which calls _siftdown_max -- so heapreplace_max transitively depends on _siftdown_max.
# Since heappushpop_max invokes _siftup_max and _siftup_max invokes _siftdown_max, heappushpop_max indirectly relies on _siftdown_max.
# Note: heapify_max does not call _siftdown_max directly, but reaches it via _siftup_max.
# nsmallest calls heapify_max, which calls _siftup_max -- so nsmallest transitively depends on _siftup_max.
# nlargest -> heapify -> _siftup: nlargest has a transitive dependency on _siftup through heapify.
# digest -> _compute_digest_fallback -> digest_cons: digest has a transitive dependency on digest_cons through _compute_digest_fallback.
# Since digest invokes _compute_digest_fallback and _compute_digest_fallback invokes _get_digest_constructor, digest indirectly relies on _get_digest_constructor.
# _replace_attr_charref -> unescape -> __dict_replace: _replace_attr_charref has a transitive dependency on __dict_replace through unescape.
# parse_headers calls _read_headers, which calls LineTooLong -- so parse_headers transitively depends on LineTooLong.
# Note: parse_headers does not call HTTPException directly, but reaches it via _read_headers.
# _str2time calls _timegm, which calls timegm -- so _str2time transitively depends on timegm.
# http2time -> _timegm -> timegm: http2time has a transitive dependency on timegm through _timegm.
# http2time calls _str2time, which calls offset_from_tz_string -- so http2time transitively depends on offset_from_tz_string.
# http2time calls _str2time, which calls _timegm -- so http2time transitively depends on _timegm.
# Since iso2time invokes _str2time and _str2time invokes offset_from_tz_string, iso2time indirectly relies on offset_from_tz_string.
# Note: iso2time does not call _timegm directly, but reaches it via _str2time.
# parse_ns_headers calls http2time, which calls _timegm -- so parse_ns_headers transitively depends on _timegm.
# Since parse_ns_headers invokes http2time and http2time invokes _str2time, parse_ns_headers indirectly relies on _str2time.
# eff_request_host -> request_host -> urlparse: eff_request_host has a transitive dependency on urlparse through request_host.
# is_third_party calls domain_match, which calls is_HDN -- so is_third_party transitively depends on is_HDN.
# Since is_third_party invokes request_host and request_host invokes urlparse, is_third_party indirectly relies on urlparse.
# Since is_third_party invokes reach and reach invokes is_HDN, is_third_party indirectly relies on is_HDN.
# Since _unquote invokes unquote_plus and unquote_plus invokes unquote, _unquote indirectly relies on unquote.
# Note: _unquote does not call _unquote_impl directly, but reaches it via unquote_to_bytes.
# Since _lock_unlock_module invokes _get_module_lock and _get_module_lock invokes _DummyModuleLock, _lock_unlock_module indirectly relies on _DummyModuleLock.
# Note: _lock_unlock_module does not call _ModuleLock directly, but reaches it via _get_module_lock.
# _call_with_frames_removed -> f -> f1: _call_with_frames_removed has a transitive dependency on f1 through f.
# Since _load_module_shim invokes _exec and _exec invokes _object_name, _load_module_shim indirectly relies on _object_name.
# _load_module_shim -> _exec -> _ModuleLockManager: _load_module_shim has a transitive dependency on _ModuleLockManager through _exec.
# _load_module_shim -> _exec -> _init_module_attrs: _load_module_shim has a transitive dependency on _init_module_attrs through _exec.
# Since _load_module_shim invokes _load and _load invokes _load_unlocked, _load_module_shim indirectly relies on _load_unlocked.
# Since _load_module_shim invokes _load and _load invokes _ModuleLockManager, _load_module_shim indirectly relies on _ModuleLockManager.
# Note: _load_module_shim does not call _Unpickler directly, but reaches it via _load.
# _load_module_shim -> spec_from_loader -> spec_from_file_location: _load_module_shim has a transitive dependency on spec_from_file_location through spec_from_loader.
# Since _load_module_shim invokes spec_from_loader and spec_from_loader invokes ModuleSpec, _load_module_shim indirectly relies on ModuleSpec.
# spec_from_loader -> spec_from_file_location -> _path_abspath: spec_from_loader has a transitive dependency on _path_abspath through spec_from_file_location.
# spec_from_loader calls spec_from_file_location, which calls _path_split -- so spec_from_loader transitively depends on _path_split.
# spec_from_loader calls spec_from_file_location, which calls loader_class -- so spec_from_loader transitively depends on loader_class.
# spec_from_loader -> spec_from_file_location -> _get_supported_file_loaders: spec_from_loader has a transitive dependency on _get_supported_file_loaders through spec_from_file_location.
# _load_unlocked -> module_from_spec -> _new_module: _load_unlocked has a transitive dependency on _new_module through module_from_spec.
# _load_unlocked -> module_from_spec -> _init_module_attrs: _load_unlocked has a transitive dependency on _init_module_attrs through module_from_spec.
# _load calls _load_unlocked, which calls _load_backward_compatible -- so _load transitively depends on _load_backward_compatible.
# _load calls _load_unlocked, which calls _object_name -- so _load transitively depends on _object_name.
# _load calls _load_unlocked, which calls _verbose_message -- so _load transitively depends on _verbose_message.
# _load -> _load_unlocked -> module_from_spec: _load has a transitive dependency on module_from_spec through _load_unlocked.
# _find_spec calls find_spec, which calls _relax_case -- so _find_spec transitively depends on _relax_case.
# Since _find_spec invokes find_spec and find_spec invokes _is_dir, _find_spec indirectly relies on _is_dir.
# _find_spec -> find_spec -> _get_supported_file_loaders: _find_spec has a transitive dependency on _get_supported_file_loaders through find_spec.
# Since _find_spec invokes find_spec and find_spec invokes resolve_name, _find_spec indirectly relies on resolve_name.
# _find_spec -> find_spec -> _get_module_path: _find_spec has a transitive dependency on _get_module_path through find_spec.
# Note: _find_spec does not call _call_with_frames_removed directly, but reaches it via find_spec.
# _find_spec calls find_spec, which calls _NamespacePath -- so _find_spec transitively depends on _NamespacePath.
# Note: _find_spec does not call _path_stat directly, but reaches it via find_spec.
# Note: _find_spec does not call loader directly, but reaches it via find_spec.
# _find_spec calls find_spec, which calls _path_join -- so _find_spec transitively depends on _path_join.
# _find_spec calls find_spec, which calls _path_isdir -- so _find_spec transitively depends on _path_isdir.
# Note: _find_spec does not call _path_isfile directly, but reaches it via find_spec.
# _find_spec -> find_spec -> spec_from_loader: _find_spec has a transitive dependency on spec_from_loader through find_spec.
# Since _find_spec invokes find_spec and find_spec invokes _get_module_info, _find_spec indirectly relies on _get_module_info.
# _find_and_load_unlocked -> _load_unlocked -> _load_backward_compatible: _find_and_load_unlocked has a transitive dependency on _load_backward_compatible through _load_unlocked.
# Since _find_and_load_unlocked invokes _load_unlocked and _load_unlocked invokes _object_name, _find_and_load_unlocked indirectly relies on _object_name.
# Note: _find_and_load_unlocked does not call _verbose_message directly, but reaches it via _load_unlocked.
# _find_and_load_unlocked -> _load_unlocked -> module_from_spec: _find_and_load_unlocked has a transitive dependency on module_from_spec through _load_unlocked.
# _find_and_load_unlocked calls _find_spec, which calls _ImportLockContext -- so _find_and_load_unlocked transitively depends on _ImportLockContext.
# _find_and_load_unlocked calls _find_spec, which calls find_spec -- so _find_and_load_unlocked transitively depends on find_spec.
# _find_and_load_unlocked calls _call_with_frames_removed, which calls f -- so _find_and_load_unlocked transitively depends on f.
# _find_and_load -> _lock_unlock_module -> _get_module_lock: _find_and_load has a transitive dependency on _get_module_lock through _lock_unlock_module.
# _find_and_load calls _find_and_load_unlocked, which calls _load_unlocked -- so _find_and_load transitively depends on _load_unlocked.
# _find_and_load -> _find_and_load_unlocked -> _find_spec: _find_and_load has a transitive dependency on _find_spec through _find_and_load_unlocked.
# _find_and_load -> _find_and_load_unlocked -> _call_with_frames_removed: _find_and_load has a transitive dependency on _call_with_frames_removed through _find_and_load_unlocked.
# _gcd_import -> _find_and_load -> _ModuleLockManager: _gcd_import has a transitive dependency on _ModuleLockManager through _find_and_load.
# _gcd_import calls _find_and_load, which calls _lock_unlock_module -- so _gcd_import transitively depends on _lock_unlock_module.
# Since _gcd_import invokes _find_and_load and _find_and_load invokes _find_and_load_unlocked, _gcd_import indirectly relies on _find_and_load_unlocked.
# Since _handle_fromlist invokes _call_with_frames_removed and _call_with_frames_removed invokes f, _handle_fromlist indirectly relies on f.
# __import__ -> _handle_fromlist -> _call_with_frames_removed: __import__ has a transitive dependency on _call_with_frames_removed through _handle_fromlist.
# __import__ calls _gcd_import, which calls _find_and_load -- so __import__ transitively depends on _find_and_load.
# Note: __import__ does not call _resolve_name directly, but reaches it via _gcd_import.
# Note: __import__ does not call _sanity_check directly, but reaches it via _gcd_import.
# _builtin_from_name -> _load_unlocked -> _load_backward_compatible: _builtin_from_name has a transitive dependency on _load_backward_compatible through _load_unlocked.
# Since _builtin_from_name invokes _load_unlocked and _load_unlocked invokes _object_name, _builtin_from_name indirectly relies on _object_name.
# _builtin_from_name -> _load_unlocked -> _verbose_message: _builtin_from_name has a transitive dependency on _verbose_message through _load_unlocked.
# _builtin_from_name -> _load_unlocked -> module_from_spec: _builtin_from_name has a transitive dependency on module_from_spec through _load_unlocked.
# Note: _install does not call _init_module_attrs directly, but reaches it via _setup.
# Note: _install does not call _spec_from_module directly, but reaches it via _setup.
# _install calls _setup, which calls ModuleCompleter -- so _install transitively depends on ModuleCompleter.
# Note: _install does not call _WeakValueDictionary directly, but reaches it via _setup.
# _install -> _setup -> RLCompleter: _install has a transitive dependency on RLCompleter through _setup.
# Since _install invokes _setup and _setup invokes _builtin_from_name, _install indirectly relies on _builtin_from_name.
# _path_isfile calls _path_is_mode_type, which calls _path_stat -- so _path_isfile transitively depends on _path_stat.
# Since _path_isdir invokes _path_is_mode_type and _path_is_mode_type invokes _path_stat, _path_isdir indirectly relies on _path_stat.
# Note: cache_from_source does not call _path_join directly, but reaches it via _path_abspath.
# Note: cache_from_source does not call _path_isabs directly, but reaches it via _path_abspath.
# _get_sourcefile calls _path_isfile, which calls _path_is_mode_type -- so _get_sourcefile transitively depends on _path_is_mode_type.
# Since _get_sourcefile invokes source_from_cache and source_from_cache invokes _path_split, _get_sourcefile indirectly relies on _path_split.
# _get_sourcefile calls source_from_cache, which calls _path_join -- so _get_sourcefile transitively depends on _path_join.
# _get_cached -> cache_from_source -> _path_split: _get_cached has a transitive dependency on _path_split through cache_from_source.
# _get_cached calls cache_from_source, which calls _path_join -- so _get_cached transitively depends on _path_join.
# Since _get_cached invokes cache_from_source and cache_from_source invokes _path_abspath, _get_cached indirectly relies on _path_abspath.
# Since _check_name invokes method and method invokes func, _check_name indirectly relies on func.
# Note: spec_from_file_location does not call _path_join directly, but reaches it via _path_abspath.
# spec_from_file_location calls _path_abspath, which calls _path_isabs -- so spec_from_file_location transitively depends on _path_isabs.
# _fix_up_module -> _path_abspath -> _path_join: _fix_up_module has a transitive dependency on _path_join through _path_abspath.
# Note: _fix_up_module does not call _path_isabs directly, but reaches it via _path_abspath.
# _fix_up_module calls spec_from_file_location, which calls _path_abspath -- so _fix_up_module transitively depends on _path_abspath.
# Since _fix_up_module invokes spec_from_file_location and spec_from_file_location invokes _path_split, _fix_up_module indirectly relies on _path_split.
# Note: _fix_up_module does not call loader_class directly, but reaches it via spec_from_file_location.
# _fix_up_module -> spec_from_file_location -> _get_supported_file_loaders: _fix_up_module has a transitive dependency on _get_supported_file_loaders through spec_from_file_location.
# _register -> callback -> func: _register has a transitive dependency on func through callback.
# Note: _register does not call ensure_future directly, but reaches it via callback.
# Note: version does not call _syscmd_ver directly, but reaches it via uname.
# Note: version does not call android_ver directly, but reaches it via uname.
# version calls uname, which calls uname_result -- so version transitively depends on uname_result.
# Since version invokes uname and uname invokes ios_ver, version indirectly relies on ios_ver.
# version -> uname -> win32_ver: version has a transitive dependency on win32_ver through uname.
# Since version invokes uname and uname invokes _get_machine_win32, version indirectly relies on _get_machine_win32.
# Since version invokes uname and uname invokes java_ver, version indirectly relies on java_ver.
# Since version invokes uname and uname invokes _node, version indirectly relies on _node.
# Since files invokes from_package and from_package invokes wrap_spec, files indirectly relies on wrap_spec.
# files -> resolve -> cast: files has a transitive dependency on cast through resolve.
# Since files invokes resolve and resolve invokes locate, files indirectly relies on locate.
# Since files invokes make_files and make_files invokes starmap, files indirectly relies on starmap.
# packages_distributions -> _top_level_inferred -> always_iterable: packages_distributions has a transitive dependency on always_iterable through _top_level_inferred.
# pass_none calls func, which calls getter -- so pass_none transitively depends on getter.
# Since unique_everseen invokes filterfalse and filterfalse invokes _compile_pattern, unique_everseen indirectly relies on _compile_pattern.
# Since unique_everseen invokes filterfalse and filterfalse invokes match, unique_everseen indirectly relies on match.
# Note: package_to_anchor does not call getter directly, but reaches it via func.
# Since resolve invokes cast and cast invokes _cast, resolve indirectly relies on _cast.
# Since resolve invokes locate and locate invokes safeimport, resolve indirectly relies on safeimport.
# Since _ invokes resolve and resolve invokes cast, _ indirectly relies on cast.
# Since _ invokes resolve and resolve invokes locate, _ indirectly relies on locate.
# Since from_package invokes wrap_spec and wrap_spec invokes SpecLoaderAdapter, from_package indirectly relies on SpecLoaderAdapter.
# _temp_file -> _tempfile -> _os_remove: _temp_file has a transitive dependency on _os_remove through _tempfile.
# _temp_file calls _tempfile, which calls reader -- so _temp_file transitively depends on reader.
# Note: as_file does not call _temp_path directly, but reaches it via _temp_dir.
# Since as_file invokes _temp_dir and _temp_dir invokes _write_contents, as_file indirectly relies on _write_contents.
# as_file calls _temp_file, which calls _tempfile -- so as_file transitively depends on _tempfile.
# Note: open_binary does not call files directly, but reaches it via _get_resource.
# Since open_text invokes _get_resource and _get_resource invokes files, open_text indirectly relies on files.
# Note: read_binary does not call files directly, but reaches it via _get_resource.
# Note: read_text does not call attr directly, but reaches it via magic_open.
# Since read_text invokes magic_open and magic_open invokes text_encoding, read_text indirectly relies on text_encoding.
# Note: read_text does not call TextIOWrapper directly, but reaches it via magic_open.
# Note: read_text does not call files directly, but reaches it via _get_resource.
# path calls _get_resource, which calls files -- so path transitively depends on files.
# Since path invokes as_file and as_file invokes _temp_dir, path indirectly relies on _temp_dir.
# path calls as_file, which calls _temp_file -- so path transitively depends on _temp_file.
# path calls as_file, which calls _is_present_dir -- so path transitively depends on _is_present_dir.
# is_resource calls _get_resource, which calls files -- so is_resource transitively depends on files.
# contents calls _get_resource, which calls files -- so contents transitively depends on files.
# Since _get_resource invokes files and files invokes distribution, _get_resource indirectly relies on distribution.
# Note: _get_resource does not call FileHash directly, but reaches it via files.
# Note: _get_resource does not call ResourceContainer directly, but reaches it via files.
# _get_resource -> files -> PackagePath: _get_resource has a transitive dependency on PackagePath through files.
# Note: _get_resource does not call from_package directly, but reaches it via files.
# _get_resource -> files -> resolve: _get_resource has a transitive dependency on resolve through files.
# Note: _get_resource does not call starmap directly, but reaches it via files.
# _get_resource -> files -> skip_missing_files: _get_resource has a transitive dependency on skip_missing_files through files.
# _get_resource calls files, which calls make_files -- so _get_resource transitively depends on make_files.
# Since _find_spec_from_path invokes _find_spec and _find_spec invokes _ImportLockContext, _find_spec_from_path indirectly relies on _ImportLockContext.
# Since _find_spec_from_path invokes _find_spec and _find_spec invokes find_spec, _find_spec_from_path indirectly relies on find_spec.
# Since find_spec invokes _find_spec and _find_spec invokes _ImportLockContext, find_spec indirectly relies on _ImportLockContext.
# find_spec calls resolve_name, which calls _resolve_name -- so find_spec transitively depends on _resolve_name.
# find_spec calls _call_with_frames_removed, which calls f -- so find_spec transitively depends on f.
# find_spec calls _path_isdir, which calls _path_is_mode_type -- so find_spec transitively depends on _path_is_mode_type.
# Note: find_spec does not call _path_is_mode_type directly, but reaches it via _path_isfile.
# Since find_spec invokes spec_from_loader and spec_from_loader invokes spec_from_file_location, find_spec indirectly relies on spec_from_file_location.
# Note: find_spec does not call ModuleSpec directly, but reaches it via spec_from_loader.
# Since find_spec invokes _get_module_info and _get_module_info invokes _get_module_path, find_spec indirectly relies on _get_module_path.
# _has_code_flag -> _signature_is_functionlike -> isclass: _has_code_flag has a transitive dependency on isclass through _signature_is_functionlike.
# isgeneratorfunction calls _has_code_flag, which calls ismethod -- so isgeneratorfunction transitively depends on ismethod.
# Since isgeneratorfunction invokes _has_code_flag and _has_code_flag invokes _signature_is_functionlike, isgeneratorfunction indirectly relies on _signature_is_functionlike.
# isgeneratorfunction -> _has_code_flag -> isfunction: isgeneratorfunction has a transitive dependency on isfunction through _has_code_flag.
# isasyncgenfunction -> _has_code_flag -> ismethod: isasyncgenfunction has a transitive dependency on ismethod through _has_code_flag.
# isasyncgenfunction -> _has_code_flag -> _signature_is_functionlike: isasyncgenfunction has a transitive dependency on _signature_is_functionlike through _has_code_flag.
# isasyncgenfunction -> _has_code_flag -> isfunction: isasyncgenfunction has a transitive dependency on isfunction through _has_code_flag.
# isroutine -> ismethoddescriptor -> ismethod: isroutine has a transitive dependency on ismethod through ismethoddescriptor.
# Since isroutine invokes ismethoddescriptor and ismethoddescriptor invokes isclass, isroutine indirectly relies on isclass.
# isroutine calls ismethoddescriptor, which calls isfunction -- so isroutine transitively depends on isfunction.
# Note: getmembers does not call getmro directly, but reaches it via _getmembers.
# Since getmembers invokes _getmembers and _getmembers invokes getter, getmembers indirectly relies on getter.
# Note: getmembers does not call predicate directly, but reaches it via _getmembers.
# getmembers -> _getmembers -> isclass: getmembers has a transitive dependency on isclass through _getmembers.
# Note: getmembers_static does not call getmro directly, but reaches it via _getmembers.
# getmembers_static -> _getmembers -> getter: getmembers_static has a transitive dependency on getter through _getmembers.
# Note: getmembers_static does not call predicate directly, but reaches it via _getmembers.
# Since getmembers_static invokes _getmembers and _getmembers invokes isclass, getmembers_static indirectly relies on isclass.
# Note: classify_class_attrs does not call ismethodwrapper directly, but reaches it via isroutine.
# classify_class_attrs -> isroutine -> ismethoddescriptor: classify_class_attrs has a transitive dependency on ismethoddescriptor through isroutine.
# classify_class_attrs -> isroutine -> isbuiltin: classify_class_attrs has a transitive dependency on isbuiltin through isroutine.
# classify_class_attrs -> isroutine -> ismethod: classify_class_attrs has a transitive dependency on ismethod through isroutine.
# Note: classify_class_attrs does not call isfunction directly, but reaches it via isroutine.
# Note: unwrap does not call Client directly, but reaches it via stop.
# Note: unwrap does not call close directly, but reaches it via stop.
# _finddoc -> isdatadescriptor -> ismethod: _finddoc has a transitive dependency on ismethod through isdatadescriptor.
# Since _finddoc invokes isdatadescriptor and isdatadescriptor invokes isclass, _finddoc indirectly relies on isclass.
# Since _finddoc invokes isdatadescriptor and isdatadescriptor invokes isfunction, _finddoc indirectly relies on isfunction.
# Note: _finddoc does not call isclass directly, but reaches it via _findclass.
# Since _finddoc invokes ismethoddescriptor and ismethoddescriptor invokes ismethod, _finddoc indirectly relies on ismethod.
# _finddoc -> ismethoddescriptor -> isclass: _finddoc has a transitive dependency on isclass through ismethoddescriptor.
# Since _finddoc invokes ismethoddescriptor and ismethoddescriptor invokes isfunction, _finddoc indirectly relies on isfunction.
# Since getdoc invokes _getdoc and _getdoc invokes _getowndoc, getdoc indirectly relies on _getowndoc.
# Note: getdoc does not call _finddoc directly, but reaches it via _getdoc.
# getdoc -> _finddoc -> ismemberdescriptor: getdoc has a transitive dependency on ismemberdescriptor through _finddoc.
# getdoc -> _finddoc -> isdatadescriptor: getdoc has a transitive dependency on isdatadescriptor through _finddoc.
# Note: getdoc does not call _getowndoc directly, but reaches it via _finddoc.
# getdoc -> _finddoc -> _findclass: getdoc has a transitive dependency on _findclass through _finddoc.
# Note: getdoc does not call ismethoddescriptor directly, but reaches it via _finddoc.
# getdoc calls _finddoc, which calls isbuiltin -- so getdoc transitively depends on isbuiltin.
# Since getdoc invokes _finddoc and _finddoc invokes ismethod, getdoc indirectly relies on ismethod.
# getdoc -> _finddoc -> isclass: getdoc has a transitive dependency on isclass through _finddoc.
# Note: getdoc does not call isfunction directly, but reaches it via _finddoc.
# getsourcefile calls getmodule, which calls getabsfile -- so getsourcefile transitively depends on getabsfile.
# getsourcefile calls getmodule, which calls ismodule -- so getsourcefile transitively depends on ismodule.
# Since getsourcefile invokes getfile and getfile invokes istraceback, getsourcefile indirectly relies on istraceback.
# getsourcefile calls getfile, which calls iscode -- so getsourcefile transitively depends on iscode.
# getsourcefile -> getfile -> isframe: getsourcefile has a transitive dependency on isframe through getfile.
# getsourcefile calls getfile, which calls isfunction -- so getsourcefile transitively depends on isfunction.
# Since getsourcefile invokes getfile and getfile invokes ismethod, getsourcefile indirectly relies on ismethod.
# Since getsourcefile invokes getfile and getfile invokes isclass, getsourcefile indirectly relies on isclass.
# Since getsourcefile invokes getfile and getfile invokes ismodule, getsourcefile indirectly relies on ismodule.
# getabsfile -> getfile -> istraceback: getabsfile has a transitive dependency on istraceback through getfile.
# getabsfile -> getfile -> iscode: getabsfile has a transitive dependency on iscode through getfile.
# getabsfile calls getfile, which calls isframe -- so getabsfile transitively depends on isframe.
# getabsfile calls getfile, which calls isfunction -- so getabsfile transitively depends on isfunction.
# getabsfile calls getfile, which calls ismethod -- so getabsfile transitively depends on ismethod.
# Since getabsfile invokes getfile and getfile invokes isclass, getabsfile indirectly relies on isclass.
# getabsfile -> getfile -> ismodule: getabsfile has a transitive dependency on ismodule through getfile.
# Since getabsfile invokes getsourcefile and getsourcefile invokes getmodule, getabsfile indirectly relies on getmodule.
# Note: getabsfile does not call getfile directly, but reaches it via getsourcefile.
# Note: getmodule does not call getfile directly, but reaches it via getabsfile.
# getmodule -> getabsfile -> getsourcefile: getmodule has a transitive dependency on getsourcefile through getabsfile.
# findsource -> getsourcefile -> getmodule: findsource has a transitive dependency on getmodule through getsourcefile.
# findsource calls getsourcefile, which calls getfile -- so findsource transitively depends on getfile.
# Note: findsource does not call istraceback directly, but reaches it via getfile.
# findsource calls getfile, which calls iscode -- so findsource transitively depends on iscode.
# findsource calls getfile, which calls isframe -- so findsource transitively depends on isframe.
# Since findsource invokes getfile and getfile invokes isfunction, findsource indirectly relies on isfunction.
# findsource calls getfile, which calls ismethod -- so findsource transitively depends on ismethod.
# findsource -> getfile -> isclass: findsource has a transitive dependency on isclass through getfile.
# findsource -> getfile -> ismodule: findsource has a transitive dependency on ismodule through getfile.
# Since findsource invokes getmodule and getmodule invokes getabsfile, findsource indirectly relies on getabsfile.
# Note: findsource does not call ismodule directly, but reaches it via getmodule.
# Since getcomments invokes findsource and findsource invokes istraceback, getcomments indirectly relies on istraceback.
# getcomments -> findsource -> iscode: getcomments has a transitive dependency on iscode through findsource.
# Since getcomments invokes findsource and findsource invokes getsourcefile, getcomments indirectly relies on getsourcefile.
# Since getcomments invokes findsource and findsource invokes getfile, getcomments indirectly relies on getfile.
# Note: getcomments does not call isframe directly, but reaches it via findsource.
# Since getcomments invokes findsource and findsource invokes isfunction, getcomments indirectly relies on isfunction.
# Note: getcomments does not call getmodule directly, but reaches it via findsource.
# Since getcomments invokes findsource and findsource invokes ismethod, getcomments indirectly relies on ismethod.
# getcomments calls findsource, which calls isclass -- so getcomments transitively depends on isclass.
# getcomments calls findsource, which calls ismodule -- so getcomments transitively depends on ismodule.
# Note: getsourcelines does not call BlockFinder directly, but reaches it via getblock.
# getsourcelines calls unwrap, which calls stop -- so getsourcelines transitively depends on stop.
# Since getsourcelines invokes findsource and findsource invokes istraceback, getsourcelines indirectly relies on istraceback.
# getsourcelines calls findsource, which calls iscode -- so getsourcelines transitively depends on iscode.
# Note: getsourcelines does not call getsourcefile directly, but reaches it via findsource.
# getsourcelines -> findsource -> getfile: getsourcelines has a transitive dependency on getfile through findsource.
# getsourcelines -> findsource -> isframe: getsourcelines has a transitive dependency on isframe through findsource.
# Since getsourcelines invokes findsource and findsource invokes isfunction, getsourcelines indirectly relies on isfunction.
# getsourcelines calls findsource, which calls getmodule -- so getsourcelines transitively depends on getmodule.
# getsourcelines -> findsource -> ismethod: getsourcelines has a transitive dependency on ismethod through findsource.
# Since getsourcelines invokes findsource and findsource invokes isclass, getsourcelines indirectly relies on isclass.
# getsourcelines -> findsource -> ismodule: getsourcelines has a transitive dependency on ismodule through findsource.
# getsource -> getsourcelines -> isframe: getsource has a transitive dependency on isframe through getsourcelines.
# Note: getsource does not call istraceback directly, but reaches it via getsourcelines.
# Note: getsource does not call getblock directly, but reaches it via getsourcelines.
# getsource -> getsourcelines -> unwrap: getsource has a transitive dependency on unwrap through getsourcelines.
# getsource -> getsourcelines -> findsource: getsource has a transitive dependency on findsource through getsourcelines.
# getsource calls getsourcelines, which calls ismodule -- so getsource transitively depends on ismodule.
# Note: getclasstree does not call attrgetter directly, but reaches it via walktree.
# Since getfullargspec invokes _signature_from_callable and _signature_from_callable invokes _signature_get_partial, getfullargspec indirectly relies on _signature_get_partial.
# getfullargspec calls _signature_from_callable, which calls _signature_is_builtin -- so getfullargspec transitively depends on _signature_is_builtin.
# getfullargspec -> _signature_from_callable -> _signature_get_user_defined_method: getfullargspec has a transitive dependency on _signature_get_user_defined_method through _signature_from_callable.
# Note: getfullargspec does not call _get_signature_of directly, but reaches it via _signature_from_callable.
# Note: getfullargspec does not call _signature_from_function directly, but reaches it via _signature_from_callable.
# Note: getfullargspec does not call isfunction directly, but reaches it via _signature_from_callable.
# Since getfullargspec invokes _signature_from_callable and _signature_from_callable invokes _signature_bound_method, getfullargspec indirectly relies on _signature_bound_method.
# Note: getfullargspec does not call _descriptor_get directly, but reaches it via _signature_from_callable.
# Since getfullargspec invokes _signature_from_callable and _signature_from_callable invokes _signature_is_functionlike, getfullargspec indirectly relies on _signature_is_functionlike.
# getfullargspec calls _signature_from_callable, which calls _signature_fromstr -- so getfullargspec transitively depends on _signature_fromstr.
# getfullargspec calls _signature_from_callable, which calls unwrap -- so getfullargspec transitively depends on unwrap.
# Since getfullargspec invokes _signature_from_callable and _signature_from_callable invokes _signature_from_builtin, getfullargspec indirectly relies on _signature_from_builtin.
# Note: getfullargspec does not call getattr_static directly, but reaches it via _signature_from_callable.
# getargvalues -> getargs -> Arguments: getargvalues has a transitive dependency on Arguments through getargs.
# getargvalues calls getargs, which calls iscode -- so getargvalues transitively depends on iscode.
# Note: formatargvalues does not call ConvertingList directly, but reaches it via convert.
# formatargvalues -> convert -> formatarg: formatargvalues has a transitive dependency on formatarg through convert.
# formatargvalues -> convert -> ConvertingDict: formatargvalues has a transitive dependency on ConvertingDict through convert.
# formatargvalues calls convert, which calls formatvalue -- so formatargvalues transitively depends on formatvalue.
# formatargvalues -> convert -> converter: formatargvalues has a transitive dependency on converter through convert.
# formatargvalues calls convert, which calls ConvertingTuple -- so formatargvalues transitively depends on ConvertingTuple.
# getcallargs calls getfullargspec, which calls _signature_from_callable -- so getcallargs transitively depends on _signature_from_callable.
# getcallargs calls getfullargspec, which calls FullArgSpec -- so getcallargs transitively depends on FullArgSpec.
# getframeinfo calls _get_code_position_from_tb, which calls _get_code_position -- so getframeinfo transitively depends on _get_code_position.
# Since getframeinfo invokes getfile and getfile invokes istraceback, getframeinfo indirectly relies on istraceback.
# getframeinfo calls getfile, which calls iscode -- so getframeinfo transitively depends on iscode.
# Note: getframeinfo does not call isframe directly, but reaches it via getfile.
# Note: getframeinfo does not call isfunction directly, but reaches it via getfile.
# Note: getframeinfo does not call ismethod directly, but reaches it via getfile.
# getframeinfo calls getfile, which calls isclass -- so getframeinfo transitively depends on isclass.
# getframeinfo -> getfile -> ismodule: getframeinfo has a transitive dependency on ismodule through getfile.
# Since getframeinfo invokes findsource and findsource invokes istraceback, getframeinfo indirectly relies on istraceback.
# getframeinfo calls findsource, which calls iscode -- so getframeinfo transitively depends on iscode.
# Note: getframeinfo does not call getsourcefile directly, but reaches it via findsource.
# Since getframeinfo invokes findsource and findsource invokes getfile, getframeinfo indirectly relies on getfile.
# getframeinfo calls findsource, which calls isframe -- so getframeinfo transitively depends on isframe.
# Since getframeinfo invokes findsource and findsource invokes isfunction, getframeinfo indirectly relies on isfunction.
# Note: getframeinfo does not call getmodule directly, but reaches it via findsource.
# getframeinfo calls findsource, which calls ismethod -- so getframeinfo transitively depends on ismethod.
# getframeinfo calls findsource, which calls isclass -- so getframeinfo transitively depends on isclass.
# Note: getframeinfo does not call ismodule directly, but reaches it via findsource.
# Since getframeinfo invokes getsourcefile and getsourcefile invokes getmodule, getframeinfo indirectly relies on getmodule.
# getframeinfo -> getsourcefile -> getfile: getframeinfo has a transitive dependency on getfile through getsourcefile.
# Note: getouterframes does not call _get_code_position_from_tb directly, but reaches it via getframeinfo.
# Note: getouterframes does not call istraceback directly, but reaches it via getframeinfo.
# getouterframes calls getframeinfo, which calls getfile -- so getouterframes transitively depends on getfile.
# Note: getouterframes does not call _get_code_position directly, but reaches it via getframeinfo.
# Since getouterframes invokes getframeinfo and getframeinfo invokes isframe, getouterframes indirectly relies on isframe.
# Since getouterframes invokes getframeinfo and getframeinfo invokes Traceback, getouterframes indirectly relies on Traceback.
# Since getouterframes invokes getframeinfo and getframeinfo invokes findsource, getouterframes indirectly relies on findsource.
# Since getouterframes invokes getframeinfo and getframeinfo invokes getsourcefile, getouterframes indirectly relies on getsourcefile.
# getinnerframes calls getframeinfo, which calls _get_code_position_from_tb -- so getinnerframes transitively depends on _get_code_position_from_tb.
# Note: getinnerframes does not call istraceback directly, but reaches it via getframeinfo.
# getinnerframes -> getframeinfo -> getfile: getinnerframes has a transitive dependency on getfile through getframeinfo.
# getinnerframes -> getframeinfo -> _get_code_position: getinnerframes has a transitive dependency on _get_code_position through getframeinfo.
# getinnerframes -> getframeinfo -> isframe: getinnerframes has a transitive dependency on isframe through getframeinfo.
# Since getinnerframes invokes getframeinfo and getframeinfo invokes Traceback, getinnerframes indirectly relies on Traceback.
# Note: getinnerframes does not call findsource directly, but reaches it via getframeinfo.
# getinnerframes calls getframeinfo, which calls getsourcefile -- so getinnerframes transitively depends on getsourcefile.
# stack calls getouterframes, which calls FrameInfo -- so stack transitively depends on FrameInfo.
# stack calls getouterframes, which calls getframeinfo -- so stack transitively depends on getframeinfo.
# trace calls getinnerframes, which calls FrameInfo -- so trace transitively depends on FrameInfo.
# trace -> getinnerframes -> getframeinfo: trace has a transitive dependency on getframeinfo through getinnerframes.
# Since _check_class invokes _shadowed_dict and _shadowed_dict invokes make_weakref, _check_class indirectly relies on make_weakref.
# Since _check_class invokes _shadowed_dict and _shadowed_dict invokes _shadowed_dict_from_weakref_mro_tuple, _check_class indirectly relies on _shadowed_dict_from_weakref_mro_tuple.
# Since _check_class invokes _shadowed_dict and _shadowed_dict invokes _static_getmro, _check_class indirectly relies on _static_getmro.
# _shadowed_dict calls _shadowed_dict_from_weakref_mro_tuple, which calls weakref_entry -- so _shadowed_dict transitively depends on weakref_entry.
# _shadowed_dict -> _shadowed_dict_from_weakref_mro_tuple -> _get_dunder_dict_of_class: _shadowed_dict has a transitive dependency on _get_dunder_dict_of_class through _shadowed_dict_from_weakref_mro_tuple.
# getattr_static calls _shadowed_dict, which calls make_weakref -- so getattr_static transitively depends on make_weakref.
# getattr_static -> _shadowed_dict -> _shadowed_dict_from_weakref_mro_tuple: getattr_static has a transitive dependency on _shadowed_dict_from_weakref_mro_tuple through _shadowed_dict.
# Since getattr_static invokes _shadowed_dict and _shadowed_dict invokes _static_getmro, getattr_static indirectly relies on _static_getmro.
# getattr_static -> _check_class -> _shadowed_dict: getattr_static has a transitive dependency on _shadowed_dict through _check_class.
# getattr_static calls _check_class, which calls _static_getmro -- so getattr_static transitively depends on _static_getmro.
# _signature_get_user_defined_method calls _descriptor_get, which calls isclass -- so _signature_get_user_defined_method transitively depends on isclass.
# _signature_get_user_defined_method -> _descriptor_get -> get: _signature_get_user_defined_method has a transitive dependency on get through _descriptor_get.
# Since _signature_get_user_defined_method invokes _signature_is_builtin and _signature_is_builtin invokes isbuiltin, _signature_get_user_defined_method indirectly relies on isbuiltin.
# _signature_get_user_defined_method -> _signature_is_builtin -> ismethoddescriptor: _signature_get_user_defined_method has a transitive dependency on ismethoddescriptor through _signature_is_builtin.
# Note: _signature_get_user_defined_method does not call stop directly, but reaches it via unwrap.
# _signature_get_user_defined_method -> getattr_static -> _check_instance: _signature_get_user_defined_method has a transitive dependency on _check_instance through getattr_static.
# Note: _signature_get_user_defined_method does not call _shadowed_dict directly, but reaches it via getattr_static.
# Note: _signature_get_user_defined_method does not call _check_class directly, but reaches it via getattr_static.
# _signature_get_user_defined_method calls getattr_static, which calls _static_getmro -- so _signature_get_user_defined_method transitively depends on _static_getmro.
# _signature_is_builtin calls ismethoddescriptor, which calls ismethod -- so _signature_is_builtin transitively depends on ismethod.
# _signature_is_builtin -> ismethoddescriptor -> isclass: _signature_is_builtin has a transitive dependency on isclass through ismethoddescriptor.
# Note: _signature_is_builtin does not call isfunction directly, but reaches it via ismethoddescriptor.
# Note: _signature_strip_non_python_syntax does not call ref directly, but reaches it via add.
# Note: _signature_strip_non_python_syntax does not call _lock_file directly, but reaches it via add.
# Note: _signature_strip_non_python_syntax does not call bltn_open directly, but reaches it via add.
# Note: _signature_strip_non_python_syntax does not call ExternalClashError directly, but reaches it via add.
# _signature_strip_non_python_syntax calls add, which calls _unlock_file -- so _signature_strip_non_python_syntax transitively depends on _unlock_file.
# Since _signature_strip_non_python_syntax invokes add and add invokes Stats, _signature_strip_non_python_syntax indirectly relies on Stats.
# Since _signature_strip_non_python_syntax invokes add and add invokes add_func_stats, _signature_strip_non_python_syntax indirectly relies on add_func_stats.
# Note: _signature_strip_non_python_syntax does not call _create_carefully directly, but reaches it via add.
# _signature_strip_non_python_syntax -> add -> _sync_close: _signature_strip_non_python_syntax has a transitive dependency on _sync_close through add.
# Since _signature_strip_non_python_syntax invokes add and add invokes _convert_other, _signature_strip_non_python_syntax indirectly relies on _convert_other.
# _signature_fromstr calls p, which calls parse_name -- so _signature_fromstr transitively depends on parse_name.
# Since _signature_fromstr invokes p and p invokes Parameter, _signature_fromstr indirectly relies on Parameter.
# Since _signature_fromstr invokes p and p invokes RewriteSymbolics, _signature_fromstr indirectly relies on RewriteSymbolics.
# _signature_fromstr calls _signature_strip_non_python_syntax, which calls add -- so _signature_fromstr transitively depends on add.
# Note: _signature_from_builtin does not call isbuiltin directly, but reaches it via _signature_is_builtin.
# _signature_from_builtin calls _signature_is_builtin, which calls ismethoddescriptor -- so _signature_from_builtin transitively depends on ismethoddescriptor.
# _signature_from_builtin calls _signature_fromstr, which calls parse_name -- so _signature_from_builtin transitively depends on parse_name.
# _signature_from_builtin -> _signature_fromstr -> RewriteSymbolics: _signature_from_builtin has a transitive dependency on RewriteSymbolics through _signature_fromstr.
# _signature_from_builtin -> _signature_fromstr -> cls: _signature_from_builtin has a transitive dependency on cls through _signature_fromstr.
# _signature_from_builtin -> _signature_fromstr -> ismodule: _signature_from_builtin has a transitive dependency on ismodule through _signature_fromstr.
# Note: _signature_from_builtin does not call p directly, but reaches it via _signature_fromstr.
# Note: _signature_from_builtin does not call wrap_value directly, but reaches it via _signature_fromstr.
# _signature_from_builtin -> _signature_fromstr -> Parameter: _signature_from_builtin has a transitive dependency on Parameter through _signature_fromstr.
# Note: _signature_from_builtin does not call _signature_strip_non_python_syntax directly, but reaches it via _signature_fromstr.
# _signature_from_function -> _signature_is_functionlike -> isclass: _signature_from_function has a transitive dependency on isclass through _signature_is_functionlike.
# Since _signature_from_function invokes _signature_fromstr and _signature_fromstr invokes parse_name, _signature_from_function indirectly relies on parse_name.
# _signature_from_function calls _signature_fromstr, which calls RewriteSymbolics -- so _signature_from_function transitively depends on RewriteSymbolics.
# Note: _signature_from_function does not call cls directly, but reaches it via _signature_fromstr.
# _signature_from_function -> _signature_fromstr -> ismodule: _signature_from_function has a transitive dependency on ismodule through _signature_fromstr.
# _signature_from_function -> _signature_fromstr -> p: _signature_from_function has a transitive dependency on p through _signature_fromstr.
# _signature_from_function calls _signature_fromstr, which calls wrap_value -- so _signature_from_function transitively depends on wrap_value.
# Since _signature_from_function invokes _signature_fromstr and _signature_fromstr invokes Parameter, _signature_from_function indirectly relies on Parameter.
# _signature_from_function calls _signature_fromstr, which calls _signature_strip_non_python_syntax -- so _signature_from_function transitively depends on _signature_strip_non_python_syntax.
# Note: _signature_from_function does not call annotations_to_string directly, but reaches it via get_annotations.
# _signature_from_function calls get_annotations, which calls _get_and_call_annotate -- so _signature_from_function transitively depends on _get_and_call_annotate.
# _signature_from_function -> get_annotations -> _rewrite_star_unpack: _signature_from_function has a transitive dependency on _rewrite_star_unpack through get_annotations.
# Note: _signature_from_function does not call _get_dunder_annotations directly, but reaches it via get_annotations.
# Since _descriptor_get invokes get and get invokes _impl, _descriptor_get indirectly relies on _impl.
# _descriptor_get calls get, which calls ref -- so _descriptor_get transitively depends on ref.
# _descriptor_get calls get, which calls NoOptionError -- so _descriptor_get transitively depends on NoOptionError.
# _descriptor_get calls get, which calls time -- so _descriptor_get transitively depends on time.
# _descriptor_get -> get -> GenericBrowser: _descriptor_get has a transitive dependency on GenericBrowser through get.
# _descriptor_get calls get, which calls register_standard_browsers -- so _descriptor_get transitively depends on register_standard_browsers.
# _descriptor_get -> get -> func: _descriptor_get has a transitive dependency on func through get.
# Since _descriptor_get invokes get and get invokes BackgroundBrowser, _descriptor_get indirectly relies on BackgroundBrowser.
# _descriptor_get -> get -> _resolve_unbound: _descriptor_get has a transitive dependency on _resolve_unbound through get.
# _descriptor_get -> get -> pack: _descriptor_get has a transitive dependency on pack through get.
# Note: _descriptor_get does not call _synthesize directly, but reaches it via get.
# Note: _descriptor_get does not call Error directly, but reaches it via get.
# Note: _descriptor_get does not call wr directly, but reaches it via get.
# _signature_from_callable calls _signature_get_partial, which calls OrderedDict -- so _signature_from_callable transitively depends on OrderedDict.
# Since _signature_from_callable invokes _signature_is_builtin and _signature_is_builtin invokes isbuiltin, _signature_from_callable indirectly relies on isbuiltin.
# Note: _signature_from_callable does not call ismethoddescriptor directly, but reaches it via _signature_is_builtin.
# _signature_from_callable -> _signature_get_user_defined_method -> _descriptor_get: _signature_from_callable has a transitive dependency on _descriptor_get through _signature_get_user_defined_method.
# Since _signature_from_callable invokes _signature_get_user_defined_method and _signature_get_user_defined_method invokes _signature_is_builtin, _signature_from_callable indirectly relies on _signature_is_builtin.
# _signature_from_callable calls _signature_get_user_defined_method, which calls unwrap -- so _signature_from_callable transitively depends on unwrap.
# Note: _signature_from_callable does not call getattr_static directly, but reaches it via _signature_get_user_defined_method.
# Since _signature_from_callable invokes _signature_from_function and _signature_from_function invokes _signature_is_functionlike, _signature_from_callable indirectly relies on _signature_is_functionlike.
# Note: _signature_from_callable does not call _signature_fromstr directly, but reaches it via _signature_from_function.
# _signature_from_callable -> _signature_from_function -> Parameter: _signature_from_callable has a transitive dependency on Parameter through _signature_from_function.
# Note: _signature_from_callable does not call get_annotations directly, but reaches it via _signature_from_function.
# Since _signature_from_callable invokes _signature_from_function and _signature_from_function invokes cls, _signature_from_callable indirectly relies on cls.
# _signature_from_callable calls _signature_from_function, which calls isfunction -- so _signature_from_callable transitively depends on isfunction.
# Note: _signature_from_callable does not call isclass directly, but reaches it via _descriptor_get.
# Since _signature_from_callable invokes _descriptor_get and _descriptor_get invokes get, _signature_from_callable indirectly relies on get.
# Note: _signature_from_callable does not call isclass directly, but reaches it via _signature_is_functionlike.
# _signature_from_callable -> _signature_fromstr -> parse_name: _signature_from_callable has a transitive dependency on parse_name through _signature_fromstr.
# _signature_from_callable -> _signature_fromstr -> RewriteSymbolics: _signature_from_callable has a transitive dependency on RewriteSymbolics through _signature_fromstr.
# Note: _signature_from_callable does not call cls directly, but reaches it via _signature_fromstr.
# Since _signature_from_callable invokes _signature_fromstr and _signature_fromstr invokes ismodule, _signature_from_callable indirectly relies on ismodule.
# Note: _signature_from_callable does not call p directly, but reaches it via _signature_fromstr.
# Note: _signature_from_callable does not call wrap_value directly, but reaches it via _signature_fromstr.
# Since _signature_from_callable invokes _signature_fromstr and _signature_fromstr invokes Parameter, _signature_from_callable indirectly relies on Parameter.
# _signature_from_callable -> _signature_fromstr -> _signature_strip_non_python_syntax: _signature_from_callable has a transitive dependency on _signature_strip_non_python_syntax through _signature_fromstr.
# Note: _signature_from_callable does not call stop directly, but reaches it via unwrap.
# _signature_from_callable -> _signature_from_builtin -> _signature_is_builtin: _signature_from_callable has a transitive dependency on _signature_is_builtin through _signature_from_builtin.
# Since _signature_from_callable invokes _signature_from_builtin and _signature_from_builtin invokes _signature_fromstr, _signature_from_callable indirectly relies on _signature_fromstr.
# Since _signature_from_callable invokes getattr_static and getattr_static invokes _check_instance, _signature_from_callable indirectly relies on _check_instance.
# _signature_from_callable -> getattr_static -> _shadowed_dict: _signature_from_callable has a transitive dependency on _shadowed_dict through getattr_static.
# Since _signature_from_callable invokes getattr_static and getattr_static invokes _check_class, _signature_from_callable indirectly relies on _check_class.
# _signature_from_callable -> getattr_static -> _static_getmro: _signature_from_callable has a transitive dependency on _static_getmro through getattr_static.
# Note: _main does not call get_makefile_filename directly, but reaches it via _generate_posix_vars.
# Since _main invokes _generate_posix_vars and _generate_posix_vars invokes _get_sysconfigdata_name, _main indirectly relies on _get_sysconfigdata_name.
# _main -> _generate_posix_vars -> parse_config_h: _main has a transitive dependency on parse_config_h through _generate_posix_vars.
# Since _main invokes _generate_posix_vars and _generate_posix_vars invokes get_config_vars, _main indirectly relies on get_config_vars.
# _main -> _generate_posix_vars -> _get_pybuilddir: _main has a transitive dependency on _get_pybuilddir through _generate_posix_vars.
# Since _main invokes _generate_posix_vars and _generate_posix_vars invokes get_config_h_filename, _main indirectly relies on get_config_h_filename.
# Since _main invokes _generate_posix_vars and _generate_posix_vars invokes _print_config_dict, _main indirectly relies on _print_config_dict.
# _main -> _generate_posix_vars -> _get_json_data_name: _main has a transitive dependency on _get_json_data_name through _generate_posix_vars.
# Since _main invokes _generate_posix_vars and _generate_posix_vars invokes _parse_makefile, _main indirectly relies on _parse_makefile.
# _main calls load, which calls _Unframer -- so _main transitively depends on _Unframer.
# Note: _main does not call cast directly, but reaches it via load.
# Note: _main does not call loads directly, but reaches it via load.
# Since _main invokes load and load invokes UnpicklingError, _main indirectly relies on UnpicklingError.
# Since _main invokes load and load invokes InvalidFileException, _main indirectly relies on InvalidFileException.
# _main -> load -> read: _main has a transitive dependency on read through load.
# Since _main invokes load and load invokes P, _main indirectly relies on P.
# _main calls load, which calls import_module -- so _main transitively depends on import_module.
# Note: _main does not call getmodule directly, but reaches it via getsourcefile.
# Since _main invokes getsourcefile and getsourcefile invokes getfile, _main indirectly relies on getfile.
# _main -> get_config_vars -> _init_config_vars: _main has a transitive dependency on _init_config_vars through get_config_vars.
# Note: _main does not call istraceback directly, but reaches it via findsource.
# _main -> findsource -> iscode: _main has a transitive dependency on iscode through findsource.
# Since _main invokes findsource and findsource invokes getsourcefile, _main indirectly relies on getsourcefile.
# _main -> findsource -> getfile: _main has a transitive dependency on getfile through findsource.
# Note: _main does not call isframe directly, but reaches it via findsource.
# _main -> findsource -> isfunction: _main has a transitive dependency on isfunction through findsource.
# _main calls findsource, which calls getmodule -- so _main transitively depends on getmodule.
# _main calls findsource, which calls ismethod -- so _main transitively depends on ismethod.
# Since _main invokes findsource and findsource invokes isclass, _main indirectly relies on isclass.
# Since _main invokes findsource and findsource invokes ismodule, _main indirectly relies on ismodule.
# Since _main invokes prepare and prepare invokes CycleError, _main indirectly relies on CycleError.
# Note: _main does not call trace directly, but reaches it via prepare.
# _main calls prepare, which calls resolve_task -- so _main transitively depends on resolve_task.
# _main -> prepare -> _fixup_main_from_path: _main has a transitive dependency on _fixup_main_from_path through prepare.
# _main -> prepare -> tcgetattr: _main has a transitive dependency on tcgetattr through prepare.
# Note: _main does not call _fixup_main_from_name directly, but reaches it via prepare.
# Note: _main does not call SetConsoleMode directly, but reaches it via prepare.
# _main -> prepare -> cls: _main has a transitive dependency on cls through prepare.
# Note: _main does not call set_start_method directly, but reaches it via prepare.
# _main calls error, which calls basicConfig -- so _main transitively depends on basicConfig.
# _main calls error, which calls _ -- so _main transitively depends on _.
# _main calls error, which calls perror -- so _main transitively depends on perror.
# _main -> get_default_scheme -> get_preferred_scheme: _main has a transitive dependency on get_preferred_scheme through get_default_scheme.
# Since _main invokes tokenize and tokenize invokes _generate_tokens_from_c_tokenizer, _main indirectly relies on _generate_tokens_from_c_tokenizer.
# _main -> tokenize -> detect_encoding: _main has a transitive dependency on detect_encoding through tokenize.
# Note: _main does not call TokenInfo directly, but reaches it via tokenize.
# _main -> _parse_args -> ArgumentParser: _main has a transitive dependency on ArgumentParser through _parse_args.
# Note: _main does not call getsourcelines directly, but reaches it via getsource.
# Note: _main does not call init directly, but reaches it via guess_extension.
# _main -> get_paths -> get_default_scheme: _main has a transitive dependency on get_default_scheme through get_paths.
# Since _main invokes get_paths and get_paths invokes _expand_vars, _main indirectly relies on _expand_vars.
# _main calls _generate_tokens_from_c_tokenizer, which calls TokenError -- so _main transitively depends on TokenError.
# _main calls _generate_tokens_from_c_tokenizer, which calls _transform_msg -- so _main transitively depends on _transform_msg.
# _main calls readmodule_ex, which calls _readmodule -- so _main transitively depends on _readmodule.
# _main calls platform, which calls mac_ver -- so _main transitively depends on mac_ver.
# _main -> platform -> ios_ver: _main has a transitive dependency on ios_ver through platform.
# _main calls platform, which calls architecture -- so _main transitively depends on architecture.
# Since _main invokes platform and platform invokes win32_ver, _main indirectly relies on win32_ver.
# _main calls platform, which calls uname -- so _main transitively depends on uname.
# _main calls platform, which calls _platform -- so _main transitively depends on _platform.
# _main calls platform, which calls system_alias -- so _main transitively depends on system_alias.
# _main -> platform -> java_ver: _main has a transitive dependency on java_ver through platform.
# _main calls platform, which calls libc_ver -- so _main transitively depends on libc_ver.
# Note: _main does not call get_config_vars directly, but reaches it via get_platform.
# Since _main invokes get_platform and get_platform invokes aix_platform, _main indirectly relies on aix_platform.
# Since _main invokes get_platform and get_platform invokes get_config_var, _main indirectly relies on get_config_var.
# _main -> guess_type -> init: _main has a transitive dependency on init through guess_type.
# summarize_address_range -> ip -> IPv4Address: summarize_address_range has a transitive dependency on IPv4Address through ip.
# Since summarize_address_range invokes ip and ip invokes IPv6Address, summarize_address_range indirectly relies on IPv6Address.
# Since collapse_addresses invokes summarize_address_range and summarize_address_range invokes _count_righthand_zero_bits, collapse_addresses indirectly relies on _count_righthand_zero_bits.
# Since collapse_addresses invokes summarize_address_range and summarize_address_range invokes ip, collapse_addresses indirectly relies on ip.
# Since dumps invokes dump and dump invokes ElementTree, dumps indirectly relies on ElementTree.
# Since dumps invokes dump and dump invokes pack, dumps indirectly relies on pack.
# Since dumps invokes dump and dump invokes PicklingError, dumps indirectly relies on PicklingError.
# dumps -> dump -> ForkingPickler: dumps has a transitive dependency on ForkingPickler through dump.
# Note: dumps does not call _format directly, but reaches it via dump.
# dumps -> dump -> cls: dumps has a transitive dependency on cls through dump.
# Since dumps invokes escape and escape invokes __dict_replace, dumps indirectly relies on __dict_replace.
# dumps -> escape -> replace: dumps has a transitive dependency on replace through escape.
# Since dumps invokes write and write invokes serialize, dumps indirectly relies on serialize.
# dumps calls write, which calls sleep -- so dumps transitively depends on sleep.
# Since dumps invokes write and write invokes _serialize_text, dumps indirectly relies on _serialize_text.
# Note: dumps does not call assert_ directly, but reaches it via write.
# Since dumps invokes write and write invokes _namespaces, dumps indirectly relies on _namespaces.
# Since dumps invokes write and write invokes time, dumps indirectly relies on time.
# dumps calls write, which calls _count_to_size -- so dumps transitively depends on _count_to_size.
# dumps -> write -> quote: dumps has a transitive dependency on quote through write.
# Note: dumps does not call _nbytes directly, but reaches it via write.
# Since dumps invokes write and write invokes _get_writer, dumps indirectly relies on _get_writer.
# dumps -> write -> crc32: dumps has a transitive dependency on crc32 through write.
# detect_encoding calls find_cookie, which calls _get_normal_name -- so detect_encoding transitively depends on _get_normal_name.
# detect_encoding -> find_cookie -> lookup: detect_encoding has a transitive dependency on lookup through find_cookie.
# Since detect_encoding invokes read_or_stop and read_or_stop invokes readline, detect_encoding indirectly relies on readline.
# detect_encoding calls check, which calls _get_reader -- so detect_encoding transitively depends on _get_reader.
# Since detect_encoding invokes check and check invokes process_tokens, detect_encoding indirectly relies on process_tokens.
# Since detect_encoding invokes check and check invokes errprint, detect_encoding indirectly relies on errprint.
# detect_encoding calls readline, which calls UnpicklingError -- so detect_encoding transitively depends on UnpicklingError.
# Note: detect_encoding does not call size_index directly, but reaches it via readline.
# Since detect_encoding invokes readline and readline invokes assert_, detect_encoding indirectly relies on assert_.
# Since detect_encoding invokes readline and readline invokes ateof, detect_encoding indirectly relies on ateof.
# Note: detect_encoding does not call startup_hook directly, but reaches it via readline.
# detect_encoding calls readline, which calls nreadahead -- so detect_encoding transitively depends on nreadahead.
# detect_encoding -> lookup -> Lookup: detect_encoding has a transitive dependency on Lookup through lookup.
# Since detect_encoding invokes lookup and lookup invokes Symbol, detect_encoding indirectly relies on Symbol.
# Since load invokes cast and cast invokes _cast, load indirectly relies on _cast.
# Note: load does not call key_value_rule directly, but reaches it via loads.
# load -> loads -> Output: load has a transitive dependency on Output through loads.
# Note: load does not call detect_encoding directly, but reaches it via loads.
# Since load invokes loads and loads invokes skip_comment, load indirectly relies on skip_comment.
# load -> loads -> getparser: load has a transitive dependency on getparser through loads.
# Since load invokes loads and loads invokes TOMLDecodeError, load indirectly relies on TOMLDecodeError.
# load calls loads, which calls JSONDecodeError -- so load transitively depends on JSONDecodeError.
# load -> loads -> make_safe_parse_float: load has a transitive dependency on make_safe_parse_float through loads.
# Note: load does not call create_list_rule directly, but reaches it via loads.
# load -> loads -> create_dict_rule: load has a transitive dependency on create_dict_rule through loads.
# load -> loads -> cls: load has a transitive dependency on cls through loads.
# load -> loads -> BytesIO: load has a transitive dependency on BytesIO through loads.
# Since load invokes loads and loads invokes skip_chars, load indirectly relies on skip_chars.
# load -> read -> UnpicklingError: load has a transitive dependency on UnpicklingError through read.
# Note: load does not call size_index directly, but reaches it via read.
# load calls read, which calls assert_ -- so load transitively depends on assert_.
# Since load invokes read and read invokes ReadError, load indirectly relies on ReadError.
# Note: load does not call Pair directly, but reaches it via read.
# loads calls key_value_rule, which calls parse_key_value_pair -- so loads transitively depends on parse_key_value_pair.
# loads calls key_value_rule, which calls TOMLDecodeError -- so loads transitively depends on TOMLDecodeError.
# Since loads invokes detect_encoding and detect_encoding invokes _get_normal_name, loads indirectly relies on _get_normal_name.
# Note: loads does not call find_cookie directly, but reaches it via detect_encoding.
# Since loads invokes detect_encoding and detect_encoding invokes read_or_stop, loads indirectly relies on read_or_stop.
# Note: loads does not call check directly, but reaches it via detect_encoding.
# loads -> detect_encoding -> bstartswith: loads has a transitive dependency on bstartswith through detect_encoding.
# Since loads invokes detect_encoding and detect_encoding invokes readline, loads indirectly relies on readline.
# Since loads invokes detect_encoding and detect_encoding invokes lookup, loads indirectly relies on lookup.
# loads calls skip_comment, which calls skip_until -- so loads transitively depends on skip_until.
# loads -> getparser -> FastParser: loads has a transitive dependency on FastParser through getparser.
# Note: loads does not call FastUnmarshaller directly, but reaches it via getparser.
# loads -> getparser -> Unmarshaller: loads has a transitive dependency on Unmarshaller through getparser.
# Note: loads does not call ExpatParser directly, but reaches it via getparser.
# loads calls make_safe_parse_float, which calls parse_float -- so loads transitively depends on parse_float.
# loads -> load -> _Unframer: loads has a transitive dependency on _Unframer through load.
# Note: loads does not call cast directly, but reaches it via load.
# Note: loads does not call UnpicklingError directly, but reaches it via load.
# Since loads invokes load and load invokes InvalidFileException, loads indirectly relies on InvalidFileException.
# Since loads invokes load and load invokes read, loads indirectly relies on read.
# loads calls load, which calls P -- so loads transitively depends on P.
# loads -> load -> import_module: loads has a transitive dependency on import_module through load.
# Note: loads does not call TOMLDecodeError directly, but reaches it via create_list_rule.
# Note: loads does not call parse_key directly, but reaches it via create_list_rule.
# Since loads invokes create_list_rule and create_list_rule invokes skip_chars, loads indirectly relies on skip_chars.
# Note: loads does not call TOMLDecodeError directly, but reaches it via create_dict_rule.
# Since loads invokes create_dict_rule and create_dict_rule invokes parse_key, loads indirectly relies on parse_key.
# Note: loads does not call skip_chars directly, but reaches it via create_dict_rule.
# py_scanstring calls _decode_uXXXX, which calls JSONDecodeError -- so py_scanstring transitively depends on JSONDecodeError.
# py_scanstring calls _decode_uXXXX, which calls _m -- so py_scanstring transitively depends on _m.
# Note: JSONObject does not call _scan_once directly, but reaches it via scan_once.
# JSONArray calls scan_once, which calls _scan_once -- so JSONArray transitively depends on _scan_once.
# Note: _make_iterencode does not call _floatstr directly, but reaches it via _iterencode_list.
# Since _make_iterencode invokes _iterencode_list and _iterencode_list invokes _iterencode_dict, _make_iterencode indirectly relies on _iterencode_dict.
# _make_iterencode calls _iterencode_list, which calls _encoder -- so _make_iterencode transitively depends on _encoder.
# Since _make_iterencode invokes _iterencode_list and _iterencode_list invokes _iterencode, _make_iterencode indirectly relies on _iterencode.
# Since _make_iterencode invokes _iterencode_list and _iterencode_list invokes _intstr, _make_iterencode indirectly relies on _intstr.
# Since _make_iterencode invokes _iterencode_dict and _iterencode_dict invokes _floatstr, _make_iterencode indirectly relies on _floatstr.
# _make_iterencode calls _iterencode_dict, which calls _iterencode_list -- so _make_iterencode transitively depends on _iterencode_list.
# Note: _make_iterencode does not call _encoder directly, but reaches it via _iterencode_dict.
# Note: _make_iterencode does not call _iterencode directly, but reaches it via _iterencode_dict.
# _make_iterencode calls _iterencode_dict, which calls _intstr -- so _make_iterencode transitively depends on _intstr.
# Since _make_iterencode invokes _iterencode and _iterencode invokes _floatstr, _make_iterencode indirectly relies on _floatstr.
# Since _make_iterencode invokes _iterencode and _iterencode invokes _encoder, _make_iterencode indirectly relies on _encoder.
# Since _make_iterencode invokes _iterencode and _iterencode invokes _iterencode_list, _make_iterencode indirectly relies on _iterencode_list.
# Note: _make_iterencode does not call _intstr directly, but reaches it via _iterencode.
# _make_iterencode calls _iterencode, which calls _iterencode_dict -- so _make_iterencode transitively depends on _iterencode_dict.
# Since _make_iterencode invokes _iterencode and _iterencode invokes _default, _make_iterencode indirectly relies on _default.
# _make_iterencode calls _default, which calls data_handler -- so _make_iterencode transitively depends on data_handler.
# Since py_make_scanner invokes parse_array and parse_array invokes parse_value, py_make_scanner indirectly relies on parse_value.
# Since py_make_scanner invokes parse_array and parse_array invokes skip_comments_and_array_ws, py_make_scanner indirectly relies on skip_comments_and_array_ws.
# Note: py_make_scanner does not call TOMLDecodeError directly, but reaches it via parse_array.
# Note: py_make_scanner does not call parse_string directly, but reaches it via _scan_once.
# py_make_scanner -> _scan_once -> parse_array: py_make_scanner has a transitive dependency on parse_array through _scan_once.
# Note: py_make_scanner does not call parse_object directly, but reaches it via _scan_once.
# py_make_scanner calls _scan_once, which calls parse_int -- so py_make_scanner transitively depends on parse_int.
# py_make_scanner -> _scan_once -> match_number: py_make_scanner has a transitive dependency on match_number through _scan_once.
# py_make_scanner -> _scan_once -> parse_float: py_make_scanner has a transitive dependency on parse_float through _scan_once.
# Since py_make_scanner invokes _scan_once and _scan_once invokes parse_constant, py_make_scanner indirectly relies on parse_constant.
# getline calls getlines, which calls clearcache -- so getline transitively depends on clearcache.
# Since getline invokes getlines and getlines invokes updatecache, getline indirectly relies on updatecache.
# Note: getlines does not call _source_unavailable directly, but reaches it via updatecache.
# Since getlines invokes updatecache and updatecache invokes _make_lazycache_entry, getlines indirectly relies on _make_lazycache_entry.
# Since _getline_from_code invokes _getlines_from_code and _getlines_from_code invokes _make_key, _getline_from_code indirectly relies on _make_key.
# updatecache calls _make_lazycache_entry, which calls get_source -- so updatecache transitively depends on get_source.
# Since lazycache invokes _make_lazycache_entry and _make_lazycache_entry invokes get_source, lazycache indirectly relies on get_source.
# Since _make_lazycache_entry invokes get_source and get_source invokes _get_module_path, _make_lazycache_entry indirectly relies on _get_module_path.
# Note: _make_lazycache_entry does not call _get_module_info directly, but reaches it via get_source.
# Note: _make_lazycache_entry does not call decode_source directly, but reaches it via get_source.
# _make_lazycache_entry calls get_source, which calls ZipImportError -- so _make_lazycache_entry transitively depends on ZipImportError.
# _make_lazycache_entry -> get_source -> _get_data: _make_lazycache_entry has a transitive dependency on _get_data through get_source.
# _group -> localeconv -> _localeconv: _group has a transitive dependency on _localeconv through localeconv.
# Note: _format does not call parse_name directly, but reaches it via p.
# _format -> p -> Parameter: _format has a transitive dependency on Parameter through p.
# _format -> p -> RewriteSymbolics: _format has a transitive dependency on RewriteSymbolics through p.
# _format calls _localize, which calls _group -- so _format transitively depends on _group.
# _format calls _localize, which calls _strip_padding -- so _format transitively depends on _strip_padding.
# Note: _format does not call localeconv directly, but reaches it via _localize.
# _localize -> _group -> _grouping_intervals: _localize has a transitive dependency on _grouping_intervals through _group.
# _localize calls _group, which calls localeconv -- so _localize transitively depends on localeconv.
# Since _localize invokes localeconv and localeconv invokes _localeconv, _localize indirectly relies on _localeconv.
# Since format_string invokes _format and _format invokes p, format_string indirectly relies on p.
# Note: format_string does not call _localize directly, but reaches it via _format.
# Since format_string invokes _format and _format invokes _recursion, format_string indirectly relies on _recursion.
# format_string calls _format, which calls is_dataclass -- so format_string transitively depends on is_dataclass.
# Note: format_string does not call formatannotation directly, but reaches it via _format.
# currency -> _localize -> _group: currency has a transitive dependency on _group through _localize.
# Since currency invokes _localize and _localize invokes _strip_padding, currency indirectly relies on _strip_padding.
# currency -> _localize -> localeconv: currency has a transitive dependency on localeconv through _localize.
# Since currency invokes localeconv and localeconv invokes _localeconv, currency indirectly relies on _localeconv.
# str calls _format, which calls p -- so str transitively depends on p.
# str -> _format -> _localize: str has a transitive dependency on _localize through _format.
# str -> _format -> _recursion: str has a transitive dependency on _recursion through _format.
# str calls _format, which calls is_dataclass -- so str transitively depends on is_dataclass.
# str -> _format -> formatannotation: str has a transitive dependency on formatannotation through _format.
# Since delocalize invokes localeconv and localeconv invokes _localeconv, delocalize indirectly relies on _localeconv.
# Since localize invokes _localize and _localize invokes _group, localize indirectly relies on _group.
# localize -> _localize -> _strip_padding: localize has a transitive dependency on _strip_padding through _localize.
# Note: localize does not call localeconv directly, but reaches it via _localize.
# Note: atof does not call localeconv directly, but reaches it via delocalize.
# Note: atof does not call getter directly, but reaches it via func.
# atoi calls delocalize, which calls localeconv -- so atoi transitively depends on localeconv.
# Since _parse_localename invokes normalize and normalize invokes _dec_from_triple, _parse_localename indirectly relies on _dec_from_triple.
# Note: _parse_localename does not call escape directly, but reaches it via normalize.
# _parse_localename calls normalize, which calls _byte_offset_to_character_offset -- so _parse_localename transitively depends on _byte_offset_to_character_offset.
# _parse_localename -> normalize -> _append_modifier: _parse_localename has a transitive dependency on _append_modifier through normalize.
# Since _parse_localename invokes normalize and normalize invokes _convert_other, _parse_localename indirectly relies on _convert_other.
# Since _parse_localename invokes normalize and normalize invokes getcontext, _parse_localename indirectly relies on getcontext.
# Note: _parse_localename does not call _replace_encoding directly, but reaches it via normalize.
# getdefaultlocale -> _getdefaultlocale -> lookup: getdefaultlocale has a transitive dependency on lookup through _getdefaultlocale.
# Since getdefaultlocale invokes _getdefaultlocale and _getdefaultlocale invokes _parse_localename, getdefaultlocale indirectly relies on _parse_localename.
# Since _getdefaultlocale invokes lookup and lookup invokes Lookup, _getdefaultlocale indirectly relies on Lookup.
# Note: _getdefaultlocale does not call Symbol directly, but reaches it via lookup.
# _getdefaultlocale -> _parse_localename -> normalize: _getdefaultlocale has a transitive dependency on normalize through _parse_localename.
# Since getlocale invokes _parse_localename and _parse_localename invokes normalize, getlocale indirectly relies on normalize.
# setlocale calls normalize, which calls _dec_from_triple -- so setlocale transitively depends on _dec_from_triple.
# Since setlocale invokes normalize and normalize invokes escape, setlocale indirectly relies on escape.
# Since setlocale invokes normalize and normalize invokes _byte_offset_to_character_offset, setlocale indirectly relies on _byte_offset_to_character_offset.
# Note: setlocale does not call _append_modifier directly, but reaches it via normalize.
# Note: setlocale does not call _convert_other directly, but reaches it via normalize.
# Since setlocale invokes normalize and normalize invokes getcontext, setlocale indirectly relies on getcontext.
# setlocale calls normalize, which calls _replace_encoding -- so setlocale transitively depends on _replace_encoding.
# _print_locale -> setlocale -> Error: _print_locale has a transitive dependency on Error through setlocale.
# Note: _print_locale does not call _build_localename directly, but reaches it via setlocale.
# _print_locale calls setlocale, which calls _setlocale -- so _print_locale transitively depends on _setlocale.
# _print_locale -> setlocale -> normalize: _print_locale has a transitive dependency on normalize through setlocale.
# _print_locale calls getlocale, which calls _setlocale -- so _print_locale transitively depends on _setlocale.
# _print_locale calls getlocale, which calls _parse_localename -- so _print_locale transitively depends on _parse_localename.
# Note: _print_locale does not call _getdefaultlocale directly, but reaches it via getdefaultlocale.
# Since critical invokes basicConfig and basicConfig invokes StreamHandler, critical indirectly relies on StreamHandler.
# critical calls basicConfig, which calls FileHandler -- so critical transitively depends on FileHandler.
# critical calls basicConfig, which calls Formatter -- so critical transitively depends on Formatter.
# fatal calls critical, which calls basicConfig -- so fatal transitively depends on basicConfig.
# Note: error does not call StreamHandler directly, but reaches it via basicConfig.
# error -> basicConfig -> FileHandler: error has a transitive dependency on FileHandler through basicConfig.
# error -> basicConfig -> Formatter: error has a transitive dependency on Formatter through basicConfig.
# Note: error does not call resolve directly, but reaches it via _.
# Since error invokes _ and _ invokes _infer_caller, error indirectly relies on _infer_caller.
# Since exception invokes error and error invokes basicConfig, exception indirectly relies on basicConfig.
# exception calls error, which calls _ -- so exception transitively depends on _.
# Since exception invokes error and error invokes perror, exception indirectly relies on perror.
# Note: warning does not call StreamHandler directly, but reaches it via basicConfig.
# Since warning invokes basicConfig and basicConfig invokes FileHandler, warning indirectly relies on FileHandler.
# warning calls basicConfig, which calls Formatter -- so warning transitively depends on Formatter.
# Since info invokes _format_code_info and _format_code_info invokes pretty_flags, info indirectly relies on pretty_flags.
# Since info invokes basicConfig and basicConfig invokes StreamHandler, info indirectly relies on StreamHandler.
# Since info invokes basicConfig and basicConfig invokes FileHandler, info indirectly relies on FileHandler.
# Since info invokes basicConfig and basicConfig invokes Formatter, info indirectly relies on Formatter.
# Since log invokes basicConfig and basicConfig invokes StreamHandler, log indirectly relies on StreamHandler.
# log calls basicConfig, which calls FileHandler -- so log transitively depends on FileHandler.
# log calls basicConfig, which calls Formatter -- so log transitively depends on Formatter.
# fileConfig -> _install_handlers -> _strip_spaces: fileConfig has a transitive dependency on _strip_spaces through _install_handlers.
# fileConfig calls _install_handlers, which calls klass -- so fileConfig transitively depends on klass.
# fileConfig -> _install_handlers -> _resolve: fileConfig has a transitive dependency on _resolve through _install_handlers.
# Note: fileConfig does not call _handle_existing_loggers directly, but reaches it via _install_loggers.
# fileConfig -> _install_loggers -> _strip_spaces: fileConfig has a transitive dependency on _strip_spaces through _install_loggers.
# Note: fileConfig does not call _strip_spaces directly, but reaches it via _create_formatters.
# fileConfig calls _create_formatters, which calls _resolve -- so fileConfig transitively depends on _resolve.
# fileConfig calls _create_formatters, which calls c -- so fileConfig transitively depends on c.
# Since listen invokes dictConfig and dictConfig invokes dictConfigClass, listen indirectly relies on dictConfigClass.
# Note: listen does not call _install_handlers directly, but reaches it via fileConfig.
# listen calls fileConfig, which calls _install_loggers -- so listen transitively depends on _install_loggers.
# listen -> fileConfig -> _create_formatters: listen has a transitive dependency on _create_formatters through fileConfig.
# listen -> fileConfig -> _clearExistingHandlers: listen has a transitive dependency on _clearExistingHandlers through fileConfig.
# _lock_file -> _create_temporary -> _create_carefully: _lock_file has a transitive dependency on _create_carefully through _create_temporary.
# Note: guess_type does not call MimeTypes directly, but reaches it via init.
# Note: guess_file_type does not call MimeTypes directly, but reaches it via init.
# guess_all_extensions calls init, which calls MimeTypes -- so guess_all_extensions transitively depends on MimeTypes.
# guess_extension -> init -> MimeTypes: guess_extension has a transitive dependency on MimeTypes through init.
# Since add_type invokes init and init invokes MimeTypes, add_type indirectly relies on MimeTypes.
# Since Client invokes SocketClient and SocketClient invokes address_type, Client indirectly relies on address_type.
# Client calls SocketClient, which calls Connection -- so Client transitively depends on Connection.
# Since Client invokes answer_challenge and answer_challenge invokes _create_response, Client indirectly relies on _create_response.
# Since Client invokes answer_challenge and answer_challenge invokes AuthenticationError, Client indirectly relies on AuthenticationError.
# Since Client invokes deliver_challenge and deliver_challenge invokes _verify_challenge, Client indirectly relies on _verify_challenge.
# Note: Client does not call _init_timeout directly, but reaches it via PipeClient.
# Client -> PipeClient -> _check_timeout: Client has a transitive dependency on _check_timeout through PipeClient.
# Client calls PipeClient, which calls PipeConnection -- so Client transitively depends on PipeConnection.
# _create_response -> _get_digest_name_and_payload -> AuthenticationError: _create_response has a transitive dependency on AuthenticationError through _get_digest_name_and_payload.
# Note: _verify_challenge does not call AuthenticationError directly, but reaches it via _get_digest_name_and_payload.
# Since deliver_challenge invokes _verify_challenge and _verify_challenge invokes AuthenticationError, deliver_challenge indirectly relies on AuthenticationError.
# deliver_challenge -> _verify_challenge -> _get_digest_name_and_payload: deliver_challenge has a transitive dependency on _get_digest_name_and_payload through _verify_challenge.
# Note: answer_challenge does not call _get_digest_name_and_payload directly, but reaches it via _create_response.
# Since XmlClient invokes Client and Client invokes address_type, XmlClient indirectly relies on address_type.
# XmlClient -> Client -> SocketClient: XmlClient has a transitive dependency on SocketClient through Client.
# XmlClient calls Client, which calls answer_challenge -- so XmlClient transitively depends on answer_challenge.
# Note: XmlClient does not call Queue directly, but reaches it via Client.
# XmlClient -> Client -> deliver_challenge: XmlClient has a transitive dependency on deliver_challenge through Client.
# Since XmlClient invokes Client and Client invokes _validate_family, XmlClient indirectly relies on _validate_family.
# XmlClient -> Client -> PipeClient: XmlClient has a transitive dependency on PipeClient through Client.
# XmlClient -> Client -> Connection: XmlClient has a transitive dependency on Connection through Client.
# active_children calls _cleanup, which calls sem_unlink -- so active_children transitively depends on sem_unlink.
# active_children -> _cleanup -> unregister: active_children has a transitive dependency on unregister through _cleanup.
# Since dispatch invokes convert_to_error and convert_to_error invokes RemoteError, dispatch indirectly relies on RemoteError.
# Note: dispatch does not call _compose_mro directly, but reaches it via _find_impl.
# RebuildProxy calls func, which calls getter -- so RebuildProxy transitively depends on getter.
# Note: AutoProxy does not call convert_to_error directly, but reaches it via dispatch.
# Note: AutoProxy does not call _find_impl directly, but reaches it via dispatch.
# AutoProxy calls dispatch, which calls get_cache_token -- so AutoProxy transitively depends on get_cache_token.
# worker -> get -> _impl: worker has a transitive dependency on _impl through get.
# Note: worker does not call ref directly, but reaches it via get.
# Note: worker does not call NoOptionError directly, but reaches it via get.
# Since worker invokes get and get invokes time, worker indirectly relies on time.
# Since worker invokes get and get invokes GenericBrowser, worker indirectly relies on GenericBrowser.
# worker calls get, which calls register_standard_browsers -- so worker transitively depends on register_standard_browsers.
# worker -> get -> func: worker has a transitive dependency on func through get.
# worker calls get, which calls BackgroundBrowser -- so worker transitively depends on BackgroundBrowser.
# Note: worker does not call _resolve_unbound directly, but reaches it via get.
# worker -> get -> pack: worker has a transitive dependency on pack through get.
# Since worker invokes get and get invokes _synthesize, worker indirectly relies on _synthesize.
# Note: worker does not call Error directly, but reaches it via get.
# Note: worker does not call wr directly, but reaches it via get.
# worker -> func -> getter: worker has a transitive dependency on getter through func.
# Note: worker does not call time directly, but reaches it via put.
# Since worker invokes put and put invokes _serialize_unbound, worker indirectly relies on _serialize_unbound.
# Note: worker does not call pack directly, but reaches it via put.
# RawValue -> _new_value -> rebuild_ctype: RawValue has a transitive dependency on rebuild_ctype through _new_value.
# Since RawArray invokes _new_value and _new_value invokes rebuild_ctype, RawArray indirectly relies on rebuild_ctype.
# Value calls synchronized, which calls get_context -- so Value transitively depends on get_context.
# Since Value invokes synchronized and synchronized invokes SynchronizedArray, Value indirectly relies on SynchronizedArray.
# Value -> synchronized -> make_property: Value has a transitive dependency on make_property through synchronized.
# Value -> synchronized -> scls: Value has a transitive dependency on scls through synchronized.
# Value calls synchronized, which calls SynchronizedString -- so Value transitively depends on SynchronizedString.
# Value calls synchronized, which calls Synchronized -- so Value transitively depends on Synchronized.
# Note: Value does not call _new_value directly, but reaches it via RawValue.
# Array -> RawArray -> _new_value: Array has a transitive dependency on _new_value through RawArray.
# Array -> synchronized -> get_context: Array has a transitive dependency on get_context through synchronized.
# Since Array invokes synchronized and synchronized invokes SynchronizedArray, Array indirectly relies on SynchronizedArray.
# Array -> synchronized -> make_property: Array has a transitive dependency on make_property through synchronized.
# Note: Array does not call scls directly, but reaches it via synchronized.
# Since Array invokes synchronized and synchronized invokes SynchronizedString, Array indirectly relies on SynchronizedString.
# Array calls synchronized, which calls Synchronized -- so Array transitively depends on Synchronized.
# Since reduce_ctype invokes assert_spawning and assert_spawning invokes get_spawning_popen, reduce_ctype indirectly relies on get_spawning_popen.
# freeze_support -> spawn_main -> _main: freeze_support has a transitive dependency on _main through spawn_main.
# Note: freeze_support does not call is_forking directly, but reaches it via spawn_main.
# Note: spawn_main does not call _generate_posix_vars directly, but reaches it via _main.
# Since spawn_main invokes _main and _main invokes load, spawn_main indirectly relies on load.
# spawn_main -> _main -> getsourcefile: spawn_main has a transitive dependency on getsourcefile through _main.
# Note: spawn_main does not call get_config_vars directly, but reaches it via _main.
# Note: spawn_main does not call findsource directly, but reaches it via _main.
# Note: spawn_main does not call prepare directly, but reaches it via _main.
# spawn_main -> _main -> error: spawn_main has a transitive dependency on error through _main.
# spawn_main calls _main, which calls get_default_scheme -- so spawn_main transitively depends on get_default_scheme.
# spawn_main calls _main, which calls tokenize -- so spawn_main transitively depends on tokenize.
# Note: spawn_main does not call _parse_args directly, but reaches it via _main.
# Since spawn_main invokes _main and _main invokes _builtin_open, spawn_main indirectly relies on _builtin_open.
# spawn_main calls _main, which calls getsource -- so spawn_main transitively depends on getsource.
# spawn_main calls _main, which calls guess_extension -- so spawn_main transitively depends on guess_extension.
# Note: spawn_main does not call perror directly, but reaches it via _main.
# Note: spawn_main does not call get_python_version directly, but reaches it via _main.
# Since spawn_main invokes _main and _main invokes get_paths, spawn_main indirectly relies on get_paths.
# Note: spawn_main does not call _generate_tokens_from_c_tokenizer directly, but reaches it via _main.
# spawn_main calls _main, which calls readmodule_ex -- so spawn_main transitively depends on readmodule_ex.
# Note: spawn_main does not call platform directly, but reaches it via _main.
# spawn_main calls _main, which calls get_platform -- so spawn_main transitively depends on get_platform.
# spawn_main -> _main -> _print_dict: spawn_main has a transitive dependency on _print_dict through _main.
# Since spawn_main invokes _main and _main invokes guess_type, spawn_main indirectly relies on guess_type.
# prepare -> trace -> _emscripten_log: prepare has a transitive dependency on _emscripten_log through trace.
# prepare -> trace -> getinnerframes: prepare has a transitive dependency on getinnerframes through trace.
# prepare calls tcgetattr, which calls TermState -- so prepare transitively depends on TermState.
# _remove_temp_dir -> rmtree -> _rmtree_impl: _remove_temp_dir has a transitive dependency on _rmtree_impl through rmtree.
# _remove_temp_dir calls rmtree, which calls onerror -- so _remove_temp_dir transitively depends on onerror.
# Since _get_base_temp_dir invokes warn and warn invokes _is_internal_frame, _get_base_temp_dir indirectly relies on _is_internal_frame.
# _get_base_temp_dir calls warn, which calls _next_external_frame -- so _get_base_temp_dir transitively depends on _next_external_frame.
# _get_base_temp_dir calls warn, which calls warning -- so _get_base_temp_dir transitively depends on warning.
# get_temp_dir -> _get_base_temp_dir -> warn: get_temp_dir has a transitive dependency on warn through _get_base_temp_dir.
# get_temp_dir -> info -> PathInfo: get_temp_dir has a transitive dependency on PathInfo through info.
# Note: get_temp_dir does not call _format_code_info directly, but reaches it via info.
# Since get_temp_dir invokes info and info invokes basicConfig, get_temp_dir indirectly relies on basicConfig.
# Since _run_after_forkers invokes info and info invokes PathInfo, _run_after_forkers indirectly relies on PathInfo.
# _run_after_forkers -> info -> _format_code_info: _run_after_forkers has a transitive dependency on _format_code_info through info.
# Note: _run_after_forkers does not call basicConfig directly, but reaches it via info.
# _run_after_forkers -> func -> getter: _run_after_forkers has a transitive dependency on getter through func.
# Since _run_finalizers invokes f and f invokes f1, _run_finalizers indirectly relies on f1.
# Note: _exit_function does not call current_process directly, but reaches it via active_children.
# Note: _exit_function does not call _cleanup directly, but reaches it via active_children.
# _exit_function calls _run_finalizers, which calls finalizer -- so _exit_function transitively depends on finalizer.
# _exit_function calls _run_finalizers, which calls f -- so _exit_function transitively depends on f.
# _exit_function calls _run_finalizers, which calls sub_debug -- so _exit_function transitively depends on sub_debug.
# _exit_function calls debug, which calls debug_script -- so _exit_function transitively depends on debug_script.
# _exit_function -> debug -> testsource: _exit_function has a transitive dependency on testsource through debug.
# Since _exit_function invokes debug and debug invokes _normalize_module, _exit_function indirectly relies on _normalize_module.
# _exit_function -> debug -> _DebugResult: _exit_function has a transitive dependency on _DebugResult through debug.
# _exit_function calls debug, which calls basicConfig -- so _exit_function transitively depends on basicConfig.
# _exit_function -> debug -> DebugRunner: _exit_function has a transitive dependency on DebugRunner through debug.
# _exit_function calls debug, which calls SkipTest -- so _exit_function transitively depends on SkipTest.
# _exit_function calls info, which calls PathInfo -- so _exit_function transitively depends on PathInfo.
# _exit_function -> info -> _format_code_info: _exit_function has a transitive dependency on _format_code_info through info.
# _exit_function calls info, which calls basicConfig -- so _exit_function transitively depends on basicConfig.
# Since _cleanup_tests invokes _run_finalizers and _run_finalizers invokes finalizer, _cleanup_tests indirectly relies on finalizer.
# _cleanup_tests calls _run_finalizers, which calls f -- so _cleanup_tests transitively depends on f.
# Since _cleanup_tests invokes _run_finalizers and _run_finalizers invokes sub_debug, _cleanup_tests indirectly relies on sub_debug.
# Note: join does not call DoneAndNotDoneFutures directly, but reaches it via wait.
# Since join invokes wait and wait invokes _AcquireFutures, join indirectly relies on _AcquireFutures.
# Since join invokes wait and wait invokes _time, join indirectly relies on _time.
# Note: join does not call _exhaustive_wait directly, but reaches it via wait.
# join -> wait -> _WaitSelector: join has a transitive dependency on _WaitSelector through wait.
# Since join invokes wait and wait invokes _create_and_install_waiters, join indirectly relies on _create_and_install_waiters.
# Note: join does not call _allocate_lock directly, but reaches it via wait.
# join calls wait, which calls fileno -- so join transitively depends on fileno.
# Note: join does not call get_ident directly, but reaches it via current_thread.
# join calls current_thread, which calls _DummyThread -- so join transitively depends on _DummyThread.
# join calls quote, which calls quote_from_bytes -- so join transitively depends on quote_from_bytes.
# split calls _compile, which calls _compile_charset -- so split transitively depends on _compile_charset.
# split -> _compile -> iscased: split has a transitive dependency on iscased through _compile.
# split calls _compile, which calls PatternError -- so split transitively depends on PatternError.
# Note: split does not call tailappend directly, but reaches it via _compile.
# Since split invokes _compile and _compile invokes _simple, split indirectly relies on _simple.
# split -> _compile -> _combine_flags: split has a transitive dependency on _combine_flags through _compile.
# Note: split does not call _optimize_charset directly, but reaches it via _compile.
# Since split invokes _compile and _compile invokes emit, split indirectly relies on emit.
# Note: split does not call _len directly, but reaches it via _compile.
# split -> _compile -> tolower: split has a transitive dependency on tolower through _compile.
# Since split invokes _compile and _compile invokes error, split indirectly relies on error.
# Note: basename does not call _compile directly, but reaches it via split.
# basename calls split, which calls splitroot -- so basename transitively depends on splitroot.
# Note: basename does not call _get_bothseps directly, but reaches it via split.
# Since basename invokes split and split invokes shlex, basename indirectly relies on shlex.
# basename -> split -> _get_sep: basename has a transitive dependency on _get_sep through split.
# dirname -> split -> _compile: dirname has a transitive dependency on _compile through split.
# dirname -> split -> splitroot: dirname has a transitive dependency on splitroot through split.
# Note: dirname does not call _get_bothseps directly, but reaches it via split.
# Note: dirname does not call shlex directly, but reaches it via split.
# dirname -> split -> _get_sep: dirname has a transitive dependency on _get_sep through split.
# ismount -> realpath -> _getfinalpathname_nonstrict: ismount has a transitive dependency on _getfinalpathname_nonstrict through realpath.
# ismount -> realpath -> join: ismount has a transitive dependency on join through realpath.
# Since ismount invokes realpath and realpath invokes getcwd, ismount indirectly relies on getcwd.
# Since ismount invokes realpath and realpath invokes isabs, ismount indirectly relies on isabs.
# Since ismount invokes realpath and realpath invokes normcase, ismount indirectly relies on normcase.
# Note: ismount does not call normpath directly, but reaches it via realpath.
# ismount -> realpath -> _getfinalpathname: ismount has a transitive dependency on _getfinalpathname through realpath.
# ismount calls realpath, which calls readlink -- so ismount transitively depends on readlink.
# ismount -> realpath -> abspath: ismount has a transitive dependency on abspath through realpath.
# Note: ismount does not call lstat directly, but reaches it via realpath.
# ismount -> abspath -> join: ismount has a transitive dependency on join through abspath.
# Since ismount invokes abspath and abspath invokes splitroot, ismount indirectly relies on splitroot.
# ismount calls abspath, which calls getcwd -- so ismount transitively depends on getcwd.
# ismount calls abspath, which calls isabs -- so ismount transitively depends on isabs.
# Note: ismount does not call normpath directly, but reaches it via abspath.
# Since ismount invokes abspath and abspath invokes _getfullpathname, ismount indirectly relies on _getfullpathname.
# Since ismount invokes join and join invokes wait, ismount indirectly relies on wait.
# Note: ismount does not call splitroot directly, but reaches it via join.
# ismount -> join -> current_thread: ismount has a transitive dependency on current_thread through join.
# Since ismount invokes join and join invokes _get_sep, ismount indirectly relies on _get_sep.
# ismount calls join, which calls quote -- so ismount transitively depends on quote.
# Note: expanduser does not call split directly, but reaches it via basename.
# Note: expanduser does not call _get_sep directly, but reaches it via basename.
# Note: expanduser does not call wait directly, but reaches it via join.
# Note: expanduser does not call splitroot directly, but reaches it via join.
# expanduser -> join -> current_thread: expanduser has a transitive dependency on current_thread through join.
# expanduser calls join, which calls _get_sep -- so expanduser transitively depends on _get_sep.
# Since expanduser invokes join and join invokes quote, expanduser indirectly relies on quote.
# Since expanduser invokes dirname and dirname invokes split, expanduser indirectly relies on split.
# expanduser calls dirname, which calls _get_sep -- so expanduser transitively depends on _get_sep.
# Note: expandvars does not call _compile directly, but reaches it via sub.
# Since relpath invokes abspath and abspath invokes join, relpath indirectly relies on join.
# relpath calls abspath, which calls splitroot -- so relpath transitively depends on splitroot.
# Note: relpath does not call getcwd directly, but reaches it via abspath.
# relpath -> abspath -> isabs: relpath has a transitive dependency on isabs through abspath.
# Since relpath invokes abspath and abspath invokes normpath, relpath indirectly relies on normpath.
# relpath calls abspath, which calls _getfullpathname -- so relpath transitively depends on _getfullpathname.
# Note: relpath does not call _LCMapStringEx directly, but reaches it via normcase.
# abs calls _convert_other, which calls Decimal -- so abs transitively depends on Decimal.
# Note: check_builtin does not call resolve directly, but reaches it via _.
# check_builtin -> _ -> _infer_caller: check_builtin has a transitive dependency on _infer_caller through _.
# check_choice calls _, which calls resolve -- so check_choice transitively depends on resolve.
# Since check_choice invokes _ and _ invokes _infer_caller, check_choice indirectly relies on _infer_caller.
# makedirs -> mkdir -> ZipInfo: makedirs has a transitive dependency on ZipInfo through mkdir.
# Note: renames does not call rmdir directly, but reaches it via removedirs.
# Note: renames does not call mkdir directly, but reaches it via makedirs.
# renames -> rename -> error_reply: renames has a transitive dependency on error_reply through rename.
# execlp calls execvp, which calls _execvpe -- so execlp transitively depends on _execvpe.
# execlpe calls execvpe, which calls _execvpe -- so execlpe transitively depends on _execvpe.
# execvp calls _execvpe, which calls get_exec_path -- so execvp transitively depends on get_exec_path.
# execvp -> _execvpe -> exec_func: execvp has a transitive dependency on exec_func through _execvpe.
# Note: execvp does not call fsencode directly, but reaches it via _execvpe.
# Note: execvpe does not call get_exec_path directly, but reaches it via _execvpe.
# Note: execvpe does not call exec_func directly, but reaches it via _execvpe.
# Since execvpe invokes _execvpe and _execvpe invokes fsencode, execvpe indirectly relies on fsencode.
# _execvpe -> get_exec_path -> fsdecode: _execvpe has a transitive dependency on fsdecode through get_exec_path.
# Note: _execvpe does not call fspath directly, but reaches it via fsencode.
# Note: get_exec_path does not call fspath directly, but reaches it via fsdecode.
# _create_environ_mapping calls encode, which calls code_page_encode -- so _create_environ_mapping transitively depends on code_page_encode.
# Note: _create_environ_mapping does not call bz2_encode directly, but reaches it via encode.
# _create_environ_mapping -> encode -> b2a_qp: _create_environ_mapping has a transitive dependency on b2a_qp through encode.
# Note: _create_environ_mapping does not call encode_basestring directly, but reaches it via encode.
# Since _create_environ_mapping invokes encode and encode invokes encode_basestring_ascii, _create_environ_mapping indirectly relies on encode_basestring_ascii.
# Note: _create_environ_mapping does not call uu_encode directly, but reaches it via encode.
# Note: _create_environ_mapping does not call _ValueFormatter directly, but reaches it via encode.
# Note: _create_environ_mapping does not call hex_encode directly, but reaches it via encode.
# _create_environ_mapping -> encode -> write: _create_environ_mapping has a transitive dependency on write through encode.
# Note: _create_environ_mapping does not call needsquoting directly, but reaches it via encode.
# _create_environ_mapping -> encode -> quote: _create_environ_mapping has a transitive dependency on quote through encode.
# _create_environ_mapping -> encode -> ToASCII: _create_environ_mapping has a transitive dependency on ToASCII through encode.
# Note: _create_environ_mapping does not call oem_encode directly, but reaches it via encode.
# Since _create_environ_mapping invokes encode and encode invokes zlib_encode, _create_environ_mapping indirectly relies on zlib_encode.
# _create_environ_mapping -> encode -> mbcs_encode: _create_environ_mapping has a transitive dependency on mbcs_encode through encode.
# _create_environ_mapping calls encode, which calls base64_encode -- so _create_environ_mapping transitively depends on base64_encode.
# Note: _create_environ_mapping does not call punycode_encode directly, but reaches it via encode.
# Note: _create_environ_mapping does not call quopri_encode directly, but reaches it via encode.
# Note: _create_environ_mapping does not call HeaderParseError directly, but reaches it via encode.
# _create_environ_mapping -> encodekey -> encode: _create_environ_mapping has a transitive dependency on encode through encodekey.
# copyfileobj calls _sendfile, which calls _get_copy_blocksize -- so copyfileobj transitively depends on _get_copy_blocksize.
# Since copyfileobj invokes exception and exception invokes CancelledError, copyfileobj indirectly relies on CancelledError.
# copyfileobj -> exception -> error: copyfileobj has a transitive dependency on error through exception.
# Since copyfileobj invokes _copy_file_range and _copy_file_range invokes _get_copy_blocksize, copyfileobj indirectly relies on _get_copy_blocksize.
# Note: _explode_path does not call _compile directly, but reaches it via split.
# _explode_path calls split, which calls splitroot -- so _explode_path transitively depends on splitroot.
# _explode_path -> split -> _get_bothseps: _explode_path has a transitive dependency on _get_bothseps through split.
# Since _explode_path invokes split and split invokes shlex, _explode_path indirectly relies on shlex.
# Since _explode_path invokes split and split invokes _get_sep, _explode_path indirectly relies on _get_sep.
# Note: runctx does not call CompletedProcess directly, but reaches it via run.
# runctx calls run, which calls _Outcome -- so runctx transitively depends on _Outcome.
# runctx calls run, which calls stopTestRun -- so runctx transitively depends on stopTestRun.
# Since runctx invokes run and run invokes DocServer, runctx indirectly relies on DocServer.
# runctx calls run, which calls _extract_future_flags -- so runctx transitively depends on _extract_future_flags.
# Since runctx invokes run and run invokes timefunc, runctx indirectly relies on timefunc.
# Since runctx invokes run and run invokes _addSkip, runctx indirectly relies on _addSkip.
# runctx calls run, which calls get_theme -- so runctx transitively depends on get_theme.
# Note: runctx does not call callback directly, but reaches it via run.
# Note: runctx does not call action directly, but reaches it via run.
# Since runctx invokes run and run invokes startTestRun, runctx indirectly relies on startTestRun.
# runctx -> run -> Popen: runctx has a transitive dependency on Popen through run.
# Note: runctx does not call _OutputRedirectingPdb directly, but reaches it via run.
# Note: runctx does not call Pdb directly, but reaches it via run.
# runctx -> run -> run_multiline_interactive_console: runctx has a transitive dependency on run_multiline_interactive_console through run.
# runctx calls run, which calls completer -- so runctx transitively depends on completer.
# Since runctx invokes run and run invokes Runner, runctx indirectly relies on Runner.
# Note: runctx does not call inspect directly, but reaches it via run.
# Since runctx invokes run and run invokes _Utils, runctx indirectly relies on _Utils.
# runctx calls run, which calls _isnotsuite -- so runctx transitively depends on _isnotsuite.
# runctx calls run, which calls CalledProcessError -- so runctx transitively depends on CalledProcessError.
# Note: runctx does not call application directly, but reaches it via run.
# runctx -> run -> source_synopsis: runctx has a transitive dependency on source_synopsis through run.
# runctx -> run -> _wrap_awaitable: runctx has a transitive dependency on _wrap_awaitable through run.
# runctx -> run -> test: runctx has a transitive dependency on test through run.
# Note: runctx does not call onerror directly, but reaches it via run.
# runctx calls run, which calls uncancel -- so runctx transitively depends on uncancel.
# runctx -> run -> format_exception: runctx has a transitive dependency on format_exception through run.
# runctx -> run -> registerResult: runctx has a transitive dependency on registerResult through run.
# Note: runctx does not call pop directly, but reaches it via run.
# runctx calls run, which calls delayfunc -- so runctx transitively depends on delayfunc.
# runctx calls run, which calls fn -- so runctx transitively depends on fn.
# runcall -> func -> getter: runcall has a transitive dependency on getter through func.
# Since _connect invokes dispatch and dispatch invokes convert_to_error, _connect indirectly relies on convert_to_error.
# Note: _connect does not call _find_impl directly, but reaches it via dispatch.
# _connect -> dispatch -> get_cache_token: _connect has a transitive dependency on get_cache_token through dispatch.
# Since pm invokes post_mortem and post_mortem invokes Pdb, pm indirectly relies on Pdb.
# Note: pm does not call _post_mortem directly, but reaches it via post_mortem.
# parse_args calls _, which calls resolve -- so parse_args transitively depends on resolve.
# Note: parse_args does not call _infer_caller directly, but reaches it via _.
# read_stringnl_noescape_pair calls read_stringnl_noescape, which calls read_stringnl -- so read_stringnl_noescape_pair transitively depends on read_stringnl.
# read_string4 calls read_int4, which calls _unpack -- so read_string4 transitively depends on _unpack.
# read_bytes4 calls read_uint4, which calls _unpack -- so read_bytes4 transitively depends on _unpack.
# read_bytes8 -> read_uint8 -> _unpack: read_bytes8 has a transitive dependency on _unpack through read_uint8.
# read_bytearray8 calls read_uint8, which calls _unpack -- so read_bytearray8 transitively depends on _unpack.
# read_unicodestring4 -> read_uint4 -> _unpack: read_unicodestring4 has a transitive dependency on _unpack through read_uint4.
# read_unicodestring8 -> read_uint8 -> _unpack: read_unicodestring8 has a transitive dependency on _unpack through read_uint8.
# read_long4 calls read_int4, which calls _unpack -- so read_long4 transitively depends on _unpack.
# genops calls _genops, which calls getpos -- so genops transitively depends on getpos.
# optimize calls _genops, which calls getpos -- so optimize transitively depends on getpos.
# walk_packages calls iter_modules, which calls ModuleInfo -- so walk_packages transitively depends on ModuleInfo.
# Since walk_packages invokes iter_modules and iter_modules invokes iter_importer_modules, walk_packages indirectly relies on iter_importer_modules.
# Since walk_packages invokes iter_modules and iter_modules invokes iter_importers, walk_packages indirectly relies on iter_importers.
# Note: walk_packages does not call onexc directly, but reaches it via onerror.
# walk_packages calls onerror, which calls callback -- so walk_packages transitively depends on callback.
# Since iter_modules invokes iter_importers and iter_importers invokes get_importer, iter_modules indirectly relies on get_importer.
# get_importer calls path_hook, which calls cls -- so get_importer transitively depends on cls.
# Since get_importer invokes path_hook and path_hook invokes _path_isdir, get_importer indirectly relies on _path_isdir.
# iter_importers calls get_importer, which calls path_hook -- so iter_importers transitively depends on path_hook.
# extend_path calls get_importer, which calls path_hook -- so extend_path transitively depends on path_hook.
# get_data calls _get_data, which calls _unpack_uint16 -- so get_data transitively depends on _unpack_uint16.
# get_data calls _get_data, which calls ZipImportError -- so get_data transitively depends on ZipImportError.
# Since get_data invokes _get_data and _get_data invokes decompress, get_data indirectly relies on decompress.
# get_data calls _get_data, which calls _get_decompress_func -- so get_data transitively depends on _get_decompress_func.
# Note: _win32_ver does not call _norm_version directly, but reaches it via _syscmd_ver.
# Note: win32_ver does not call _syscmd_ver directly, but reaches it via _win32_ver.
# win32_ver calls _win32_ver, which calls getwindowsversion -- so win32_ver transitively depends on getwindowsversion.
# win32_ver calls _win32_ver, which calls _wmi_query -- so win32_ver transitively depends on _wmi_query.
# Note: android_ver does not call buftype directly, but reaches it via create_string_buffer.
# Note: android_ver does not call create_string_buffer directly, but reaches it via getprop.
# Since android_ver invokes getprop and getprop invokes system_property_get, android_ver indirectly relies on system_property_get.
# architecture calls _syscmd_file, which calls _follow_symlinks -- so architecture transitively depends on _follow_symlinks.
# Since uname invokes _syscmd_ver and _syscmd_ver invokes _norm_version, uname indirectly relies on _norm_version.
# uname -> android_ver -> create_string_buffer: uname has a transitive dependency on create_string_buffer through android_ver.
# Note: uname does not call CDLL directly, but reaches it via android_ver.
# Since uname invokes android_ver and android_ver invokes getprop, uname indirectly relies on getprop.
# uname calls android_ver, which calls system_property_get -- so uname transitively depends on system_property_get.
# uname calls android_ver, which calls AndroidVer -- so uname transitively depends on AndroidVer.
# Since uname invokes ios_ver and ios_ver invokes IOSVersionInfo, uname indirectly relies on IOSVersionInfo.
# uname calls win32_ver, which calls _win32_ver -- so uname transitively depends on _win32_ver.
# Note: uname does not call _wmi_query directly, but reaches it via _get_machine_win32.
# uname -> java_ver -> _java_getprop: uname has a transitive dependency on _java_getprop through java_ver.
# system -> uname -> _syscmd_ver: system has a transitive dependency on _syscmd_ver through uname.
# Note: system does not call android_ver directly, but reaches it via uname.
# system -> uname -> uname_result: system has a transitive dependency on uname_result through uname.
# Since system invokes uname and uname invokes ios_ver, system indirectly relies on ios_ver.
# system calls uname, which calls win32_ver -- so system transitively depends on win32_ver.
# Since system invokes uname and uname invokes _get_machine_win32, system indirectly relies on _get_machine_win32.
# Note: system does not call java_ver directly, but reaches it via uname.
# system -> uname -> _node: system has a transitive dependency on _node through uname.
# node -> uname -> _syscmd_ver: node has a transitive dependency on _syscmd_ver through uname.
# node -> uname -> android_ver: node has a transitive dependency on android_ver through uname.
# node -> uname -> uname_result: node has a transitive dependency on uname_result through uname.
# node calls uname, which calls ios_ver -- so node transitively depends on ios_ver.
# node calls uname, which calls win32_ver -- so node transitively depends on win32_ver.
# node calls uname, which calls _get_machine_win32 -- so node transitively depends on _get_machine_win32.
# Since node invokes uname and uname invokes java_ver, node indirectly relies on java_ver.
# node -> uname -> _node: node has a transitive dependency on _node through uname.
# Since release invokes uname and uname invokes _syscmd_ver, release indirectly relies on _syscmd_ver.
# Since release invokes uname and uname invokes android_ver, release indirectly relies on android_ver.
# Since release invokes uname and uname invokes uname_result, release indirectly relies on uname_result.
# release calls uname, which calls ios_ver -- so release transitively depends on ios_ver.
# Note: release does not call win32_ver directly, but reaches it via uname.
# Note: release does not call _get_machine_win32 directly, but reaches it via uname.
# Since release invokes uname and uname invokes java_ver, release indirectly relies on java_ver.
# release -> uname -> _node: release has a transitive dependency on _node through uname.
# machine -> uname -> _syscmd_ver: machine has a transitive dependency on _syscmd_ver through uname.
# Since machine invokes uname and uname invokes android_ver, machine indirectly relies on android_ver.
# machine calls uname, which calls uname_result -- so machine transitively depends on uname_result.
# Since machine invokes uname and uname invokes ios_ver, machine indirectly relies on ios_ver.
# machine -> uname -> win32_ver: machine has a transitive dependency on win32_ver through uname.
# machine -> uname -> _get_machine_win32: machine has a transitive dependency on _get_machine_win32 through uname.
# Since machine invokes uname and uname invokes java_ver, machine indirectly relies on java_ver.
# machine calls uname, which calls _node -- so machine transitively depends on _node.
# Note: processor does not call _syscmd_ver directly, but reaches it via uname.
# Since processor invokes uname and uname invokes android_ver, processor indirectly relies on android_ver.
# processor -> uname -> uname_result: processor has a transitive dependency on uname_result through uname.
# processor -> uname -> ios_ver: processor has a transitive dependency on ios_ver through uname.
# Since processor invokes uname and uname invokes win32_ver, processor indirectly relies on win32_ver.
# processor calls uname, which calls _get_machine_win32 -- so processor transitively depends on _get_machine_win32.
# Since processor invokes uname and uname invokes java_ver, processor indirectly relies on java_ver.
# processor -> uname -> _node: processor has a transitive dependency on _node through uname.
# Note: platform does not call _mac_ver_xml directly, but reaches it via mac_ver.
# platform calls ios_ver, which calls IOSVersionInfo -- so platform transitively depends on IOSVersionInfo.
# platform -> architecture -> _syscmd_file: platform has a transitive dependency on _syscmd_file through architecture.
# Note: platform does not call _win32_ver directly, but reaches it via win32_ver.
# platform -> uname -> _syscmd_ver: platform has a transitive dependency on _syscmd_ver through uname.
# platform calls uname, which calls android_ver -- so platform transitively depends on android_ver.
# platform -> uname -> uname_result: platform has a transitive dependency on uname_result through uname.
# Since platform invokes uname and uname invokes ios_ver, platform indirectly relies on ios_ver.
# Note: platform does not call win32_ver directly, but reaches it via uname.
# platform calls uname, which calls _get_machine_win32 -- so platform transitively depends on _get_machine_win32.
# Note: platform does not call java_ver directly, but reaches it via uname.
# platform -> uname -> _node: platform has a transitive dependency on _node through uname.
# Since platform invokes java_ver and java_ver invokes _java_getprop, platform indirectly relies on _java_getprop.
# platform -> libc_ver -> V: platform has a transitive dependency on V through libc_ver.
# abspath calls join, which calls wait -- so abspath transitively depends on wait.
# abspath -> join -> splitroot: abspath has a transitive dependency on splitroot through join.
# abspath calls join, which calls current_thread -- so abspath transitively depends on current_thread.
# abspath -> join -> _get_sep: abspath has a transitive dependency on _get_sep through join.
# Note: abspath does not call quote directly, but reaches it via join.
# abspath calls isabs, which calls _get_sep -- so abspath transitively depends on _get_sep.
# abspath -> normpath -> splitroot: abspath has a transitive dependency on splitroot through normpath.
# Since realpath invokes _getfinalpathname_nonstrict and _getfinalpathname_nonstrict invokes _readlink_deep, realpath indirectly relies on _readlink_deep.
# Since realpath invokes _getfinalpathname_nonstrict and _getfinalpathname_nonstrict invokes _findfirstfile, realpath indirectly relies on _findfirstfile.
# realpath -> _getfinalpathname_nonstrict -> _getfinalpathname: realpath has a transitive dependency on _getfinalpathname through _getfinalpathname_nonstrict.
# realpath -> _getfinalpathname_nonstrict -> join: realpath has a transitive dependency on join through _getfinalpathname_nonstrict.
# realpath calls _getfinalpathname_nonstrict, which calls split -- so realpath transitively depends on split.
# Since realpath invokes join and join invokes wait, realpath indirectly relies on wait.
# Note: realpath does not call splitroot directly, but reaches it via join.
# Since realpath invokes join and join invokes current_thread, realpath indirectly relies on current_thread.
# realpath calls join, which calls _get_sep -- so realpath transitively depends on _get_sep.
# Note: realpath does not call quote directly, but reaches it via join.
# realpath -> isabs -> _get_sep: realpath has a transitive dependency on _get_sep through isabs.
# realpath calls normcase, which calls _LCMapStringEx -- so realpath transitively depends on _LCMapStringEx.
# realpath -> normpath -> splitroot: realpath has a transitive dependency on splitroot through normpath.
# realpath calls readlink, which calls UnsupportedOperation -- so realpath transitively depends on UnsupportedOperation.
# realpath calls abspath, which calls join -- so realpath transitively depends on join.
# Since realpath invokes abspath and abspath invokes splitroot, realpath indirectly relies on splitroot.
# realpath -> abspath -> getcwd: realpath has a transitive dependency on getcwd through abspath.
# Since realpath invokes abspath and abspath invokes isabs, realpath indirectly relies on isabs.
# Note: realpath does not call normpath directly, but reaches it via abspath.
# Since realpath invokes abspath and abspath invokes _getfullpathname, realpath indirectly relies on _getfullpathname.
# Since pp invokes pprint and pprint invokes PrettyPrinter, pp indirectly relies on PrettyPrinter.
# fork calls openpty, which calls ioctl -- so fork transitively depends on ioctl.
# Since fork invokes openpty and openpty invokes _open_terminal, fork indirectly relies on _open_terminal.
# Note: _copy does not call fd_to_key_get directly, but reaches it via select.
# Note: _copy does not call EntryPoints directly, but reaches it via select.
# _copy -> select -> select_tag: _copy has a transitive dependency on select_tag through select.
# _copy -> select -> select_child: _copy has a transitive dependency on select_child through select.
# _copy -> select -> _isinstance: _copy has a transitive dependency on _isinstance through select.
# _copy calls select, which calls get_parent_map -- so _copy transitively depends on get_parent_map.
# spawn calls fork, which calls openpty -- so spawn transitively depends on openpty.
# Note: spawn does not call tcsetattr directly, but reaches it via setraw.
# spawn -> setraw -> cfmakeraw: spawn has a transitive dependency on cfmakeraw through setraw.
# Since spawn invokes setraw and setraw invokes tcgetattr, spawn indirectly relies on tcgetattr.
# Note: spawn does not call TermState directly, but reaches it via tcgetattr.
# Since spawn invokes _copy and _copy invokes stdin_read, spawn indirectly relies on stdin_read.
# Note: spawn does not call master_read directly, but reaches it via _copy.
# spawn calls _copy, which calls select -- so spawn transitively depends on select.
# Since spawn invokes close and close invokes Fault, spawn indirectly relies on Fault.
# Note: spawn does not call write32u directly, but reaches it via close.
# Note: spawn does not call close_handler directly, but reaches it via close.
# Note: spawn does not call _ClosedDict directly, but reaches it via close.
# Since spawn invokes close and close invokes assert_, spawn indirectly relies on assert_.
# Since spawn invokes close and close invokes _ClosedParser, spawn indirectly relies on _ClosedParser.
# Note: spawn does not call closehook directly, but reaches it via close.
# Note: spawn does not call unlink directly, but reaches it via close.
# Note: spawn does not call _cancel_all_tasks directly, but reaches it via close.
# Since spawn invokes close and close invokes ResponseError, spawn indirectly relies on ResponseError.
# spawn -> close -> CloseHandle: spawn has a transitive dependency on CloseHandle through close.
# compile calls _compile_pattern, which calls translate -- so compile transitively depends on translate.
# Since compile invokes _compile and _compile invokes _compile_charset, compile indirectly relies on _compile_charset.
# compile calls _compile, which calls iscased -- so compile transitively depends on iscased.
# compile -> _compile -> PatternError: compile has a transitive dependency on PatternError through _compile.
# Since compile invokes _compile and _compile invokes tailappend, compile indirectly relies on tailappend.
# compile calls _compile, which calls _simple -- so compile transitively depends on _simple.
# Since compile invokes _compile and _compile invokes _combine_flags, compile indirectly relies on _combine_flags.
# compile -> _compile -> _optimize_charset: compile has a transitive dependency on _optimize_charset through _compile.
# Since compile invokes _compile and _compile invokes emit, compile indirectly relies on emit.
# Note: compile does not call _len directly, but reaches it via _compile.
# compile -> _compile -> tolower: compile has a transitive dependency on tolower through _compile.
# Note: compile does not call error directly, but reaches it via _compile.
# compile -> dis -> print_2: compile has a transitive dependency on print_2 through dis.
# Note: compile does not call ArgResolver directly, but reaches it via dis.
# Note: compile does not call _make_labels_map directly, but reaches it via dis.
# Note: compile does not call _get_code_array directly, but reaches it via dis.
# compile calls dis, which calls _disassemble_str -- so compile transitively depends on _disassemble_str.
# compile -> dis -> print_: compile has a transitive dependency on print_ through dis.
# compile -> dis -> Formatter: compile has a transitive dependency on Formatter through dis.
# compile -> dis -> _disassemble_bytes: compile has a transitive dependency on _disassemble_bytes through dis.
# compile -> dis -> _hex_code: compile has a transitive dependency on _hex_code through dis.
# compile -> dis -> _disassemble_recursive: compile has a transitive dependency on _disassemble_recursive through dis.
# Note: compile does not call dis_ directly, but reaches it via dis.
# compile calls dis, which calls genops -- so compile transitively depends on genops.
# Note: compile does not call distb directly, but reaches it via dis.
# Since compile invokes dis and dis invokes _get_lineno_width, compile indirectly relies on _get_lineno_width.
# Note: compile does not call _get_positions_width directly, but reaches it via dis.
# compile -> _code -> _compile_info: compile has a transitive dependency on _compile_info through _code.
# Note: compile does not call _compile directly, but reaches it via _code.
# readmodule calls _readmodule, which calls _create_tree -- so readmodule transitively depends on _create_tree.
# readmodule_ex calls _readmodule, which calls _create_tree -- so readmodule_ex transitively depends on _create_tree.
# Note: _readmodule does not call _ModuleBrowser directly, but reaches it via _create_tree.
# _getdoc calls _finddoc, which calls ismemberdescriptor -- so _getdoc transitively depends on ismemberdescriptor.
# _getdoc -> _finddoc -> isdatadescriptor: _getdoc has a transitive dependency on isdatadescriptor through _finddoc.
# _getdoc calls _finddoc, which calls _getowndoc -- so _getdoc transitively depends on _getowndoc.
# _getdoc calls _finddoc, which calls _findclass -- so _getdoc transitively depends on _findclass.
# Note: _getdoc does not call ismethoddescriptor directly, but reaches it via _finddoc.
# Note: _getdoc does not call isbuiltin directly, but reaches it via _finddoc.
# _getdoc -> _finddoc -> ismethod: _getdoc has a transitive dependency on ismethod through _finddoc.
# Note: _getdoc does not call isclass directly, but reaches it via _finddoc.
# Note: _getdoc does not call isfunction directly, but reaches it via _finddoc.
# Note: pager does not call plain directly, but reaches it via get_pager.
# Since pager invokes get_pager and get_pager invokes tempfile_pager, pager indirectly relies on tempfile_pager.
# pager calls get_pager, which calls pipe_pager -- so pager transitively depends on pipe_pager.
# locate -> safeimport -> ErrorDuringImport: locate has a transitive dependency on ErrorDuringImport through safeimport.
# Since render_doc invokes resolve and resolve invokes cast, render_doc indirectly relies on cast.
# render_doc -> resolve -> locate: render_doc has a transitive dependency on locate through resolve.
# Note: render_doc does not call _getowndoc directly, but reaches it via _getdoc.
# render_doc calls _getdoc, which calls _finddoc -- so render_doc transitively depends on _finddoc.
# Since doc invokes pager and pager invokes get_pager, doc indirectly relies on get_pager.
# doc calls render_doc, which calls describe -- so doc transitively depends on describe.
# Since doc invokes render_doc and render_doc invokes resolve, doc indirectly relies on resolve.
# Note: doc does not call _getdoc directly, but reaches it via render_doc.
# Since writedoc invokes resolve and resolve invokes cast, writedoc indirectly relies on cast.
# Note: writedoc does not call locate directly, but reaches it via resolve.
# writedocs -> writedoc -> describe: writedocs has a transitive dependency on describe through writedoc.
# Note: writedocs does not call resolve directly, but reaches it via writedoc.
# Since _url_handler invokes html_error and html_error invokes format_exception_only, _url_handler indirectly relies on format_exception_only.
# Note: _url_handler does not call _safe_string directly, but reaches it via format_exception_only.
# _url_handler -> format_exception_only -> _format_final_exc_line: _url_handler has a transitive dependency on _format_final_exc_line through format_exception_only.
# Note: _url_handler does not call TracebackException directly, but reaches it via format_exception_only.
# _url_handler -> html_getobj -> describe: _url_handler has a transitive dependency on describe through html_getobj.
# Since _url_handler invokes html_getobj and html_getobj invokes locate, _url_handler indirectly relies on locate.
# _url_handler calls locate, which calls safeimport -- so _url_handler transitively depends on safeimport.
# _url_handler -> html_search -> bltinlink: _url_handler has a transitive dependency on bltinlink through html_search.
# _url_handler -> html_search -> ModuleScanner: _url_handler has a transitive dependency on ModuleScanner through html_search.
# Note: _url_handler does not call Helper directly, but reaches it via html_topicpage.
# _url_handler calls get_html_page, which calls html_topics -- so _url_handler transitively depends on html_topics.
# _url_handler -> get_html_page -> html_getobj: _url_handler has a transitive dependency on html_getobj through get_html_page.
# Since _url_handler invokes get_html_page and get_html_page invokes html_error, _url_handler indirectly relies on html_error.
# Since _url_handler invokes get_html_page and get_html_page invokes html_index, _url_handler indirectly relies on html_index.
# Since _url_handler invokes get_html_page and get_html_page invokes html_search, _url_handler indirectly relies on html_search.
# _url_handler calls get_html_page, which calls html_keywords -- so _url_handler transitively depends on html_keywords.
# _url_handler -> get_html_page -> html_topicpage: _url_handler has a transitive dependency on html_topicpage through get_html_page.
# Since browse invokes _start_server and _start_server invokes DocServer, browse indirectly relies on DocServer.
# browse calls _start_server, which calls ServerThread -- so browse transitively depends on ServerThread.
# cli calls writedoc, which calls describe -- so cli transitively depends on describe.
# Note: cli does not call resolve directly, but reaches it via writedoc.
# Note: cli does not call ModuleScanner directly, but reaches it via apropos.
# cli calls importfile, which calls ErrorDuringImport -- so cli transitively depends on ErrorDuringImport.
# cli calls writedocs, which calls writedoc -- so cli transitively depends on writedoc.
# Since cli invokes browse and browse invokes _start_server, cli indirectly relies on _start_server.
# Note: cli does not call _get_revised_path directly, but reaches it via _adjust_cli_sys_path.
# Since encodestring invokes encode and encode invokes code_page_encode, encodestring indirectly relies on code_page_encode.
# encodestring -> encode -> bz2_encode: encodestring has a transitive dependency on bz2_encode through encode.
# encodestring calls encode, which calls b2a_qp -- so encodestring transitively depends on b2a_qp.
# encodestring -> encode -> encode_basestring: encodestring has a transitive dependency on encode_basestring through encode.
# encodestring -> encode -> encode_basestring_ascii: encodestring has a transitive dependency on encode_basestring_ascii through encode.
# Since encodestring invokes encode and encode invokes uu_encode, encodestring indirectly relies on uu_encode.
# Note: encodestring does not call _ValueFormatter directly, but reaches it via encode.
# encodestring -> encode -> hex_encode: encodestring has a transitive dependency on hex_encode through encode.
# encodestring calls encode, which calls write -- so encodestring transitively depends on write.
# encodestring -> encode -> needsquoting: encodestring has a transitive dependency on needsquoting through encode.
# encodestring calls encode, which calls quote -- so encodestring transitively depends on quote.
# encodestring calls encode, which calls ToASCII -- so encodestring transitively depends on ToASCII.
# encodestring calls encode, which calls oem_encode -- so encodestring transitively depends on oem_encode.
# encodestring -> encode -> zlib_encode: encodestring has a transitive dependency on zlib_encode through encode.
# encodestring -> encode -> mbcs_encode: encodestring has a transitive dependency on mbcs_encode through encode.
# encodestring -> encode -> base64_encode: encodestring has a transitive dependency on base64_encode through encode.
# Since encodestring invokes encode and encode invokes punycode_encode, encodestring indirectly relies on punycode_encode.
# encodestring calls encode, which calls quopri_encode -- so encodestring transitively depends on quopri_encode.
# Note: encodestring does not call HeaderParseError directly, but reaches it via encode.
# Note: decodestring does not call unquote directly, but reaches it via decode.
# Note: decodestring does not call a2b_base64 directly, but reaches it via decode.
# Since decodestring invokes decode and decode invokes ToUnicode, decodestring indirectly relies on ToUnicode.
# decodestring calls decode, which calls punycode_decode -- so decodestring transitively depends on punycode_decode.
# Note: decodestring does not call JSONDecodeError directly, but reaches it via decode.
# decodestring -> decode -> a2b_qp: decodestring has a transitive dependency on a2b_qp through decode.
# Note: decodestring does not call ishex directly, but reaches it via decode.
# Note: decodestring does not call quopri_decode directly, but reaches it via decode.
# Note: decodestring does not call hex_decode directly, but reaches it via decode.
# Note: decodestring does not call oem_decode directly, but reaches it via decode.
# Since decodestring invokes decode and decode invokes zlib_decode, decodestring indirectly relies on zlib_decode.
# Note: decodestring does not call uu_decode directly, but reaches it via decode.
# Since decodestring invokes decode and decode invokes base64_decode, decodestring indirectly relies on base64_decode.
# Note: decodestring does not call _w directly, but reaches it via decode.
# Since decodestring invokes decode and decode invokes mbcs_decode, decodestring indirectly relies on mbcs_decode.
# decodestring -> decode -> unhex: decodestring has a transitive dependency on unhex through decode.
# decodestring calls decode, which calls bz2_decode -- so decodestring transitively depends on bz2_decode.
# Since decodestring invokes decode and decode invokes code_page_decode, decodestring indirectly relies on code_page_decode.
# Note: _test_generator does not call _float_sqrt_of_frac directly, but reaches it via stdev.
# _test_generator -> stdev -> _decimal_sqrt_of_frac: _test_generator has a transitive dependency on _decimal_sqrt_of_frac through stdev.
# _test_generator -> stdev -> StatisticsError: _test_generator has a transitive dependency on StatisticsError through stdev.
# _test_generator -> stdev -> _ss: _test_generator has a transitive dependency on _ss through stdev.
# Note: _test_generator does not call _convert directly, but reaches it via mean.
# _test_generator -> mean -> _sum: _test_generator has a transitive dependency on _sum through mean.
# Since _test_generator invokes mean and mean invokes StatisticsError, _test_generator indirectly relies on StatisticsError.
# Since _test_generator invokes func and func invokes getter, _test_generator indirectly relies on getter.
# Note: match does not call _compile_charset directly, but reaches it via _compile.
# Since match invokes _compile and _compile invokes iscased, match indirectly relies on iscased.
# match -> _compile -> PatternError: match has a transitive dependency on PatternError through _compile.
# Since match invokes _compile and _compile invokes tailappend, match indirectly relies on tailappend.
# Note: match does not call _simple directly, but reaches it via _compile.
# Since match invokes _compile and _compile invokes _combine_flags, match indirectly relies on _combine_flags.
# match calls _compile, which calls _optimize_charset -- so match transitively depends on _optimize_charset.
# Note: match does not call emit directly, but reaches it via _compile.
# match -> _compile -> _len: match has a transitive dependency on _len through _compile.
# Since match invokes _compile and _compile invokes tolower, match indirectly relies on tolower.
# match calls _compile, which calls error -- so match transitively depends on error.
# fullmatch -> _compile -> _compile_charset: fullmatch has a transitive dependency on _compile_charset through _compile.
# Since fullmatch invokes _compile and _compile invokes iscased, fullmatch indirectly relies on iscased.
# fullmatch -> _compile -> PatternError: fullmatch has a transitive dependency on PatternError through _compile.
# fullmatch -> _compile -> tailappend: fullmatch has a transitive dependency on tailappend through _compile.
# fullmatch calls _compile, which calls _simple -- so fullmatch transitively depends on _simple.
# fullmatch calls _compile, which calls _combine_flags -- so fullmatch transitively depends on _combine_flags.
# Note: fullmatch does not call _optimize_charset directly, but reaches it via _compile.
# fullmatch -> _compile -> emit: fullmatch has a transitive dependency on emit through _compile.
# fullmatch -> _compile -> _len: fullmatch has a transitive dependency on _len through _compile.
# fullmatch -> _compile -> tolower: fullmatch has a transitive dependency on tolower through _compile.
# fullmatch -> _compile -> error: fullmatch has a transitive dependency on error through _compile.
# search calls _compile, which calls _compile_charset -- so search transitively depends on _compile_charset.
# Since search invokes _compile and _compile invokes iscased, search indirectly relies on iscased.
# search -> _compile -> PatternError: search has a transitive dependency on PatternError through _compile.
# Since search invokes _compile and _compile invokes tailappend, search indirectly relies on tailappend.
# Since search invokes _compile and _compile invokes _simple, search indirectly relies on _simple.
# Since search invokes _compile and _compile invokes _combine_flags, search indirectly relies on _combine_flags.
# Note: search does not call _optimize_charset directly, but reaches it via _compile.
# Since search invokes _compile and _compile invokes emit, search indirectly relies on emit.
# search calls _compile, which calls _len -- so search transitively depends on _len.
# search -> _compile -> tolower: search has a transitive dependency on tolower through _compile.
# search calls _compile, which calls error -- so search transitively depends on error.
# sub calls _compile, which calls _compile_charset -- so sub transitively depends on _compile_charset.
# Note: sub does not call iscased directly, but reaches it via _compile.
# sub calls _compile, which calls PatternError -- so sub transitively depends on PatternError.
# Note: sub does not call tailappend directly, but reaches it via _compile.
# Note: sub does not call _simple directly, but reaches it via _compile.
# Note: sub does not call _combine_flags directly, but reaches it via _compile.
# Since sub invokes _compile and _compile invokes _optimize_charset, sub indirectly relies on _optimize_charset.
# Since sub invokes _compile and _compile invokes emit, sub indirectly relies on emit.
# sub calls _compile, which calls _len -- so sub transitively depends on _len.
# sub calls _compile, which calls tolower -- so sub transitively depends on tolower.
# Since sub invokes _compile and _compile invokes error, sub indirectly relies on error.
# subn calls _compile, which calls _compile_charset -- so subn transitively depends on _compile_charset.
# Note: subn does not call iscased directly, but reaches it via _compile.
# subn calls _compile, which calls PatternError -- so subn transitively depends on PatternError.
# subn -> _compile -> tailappend: subn has a transitive dependency on tailappend through _compile.
# subn calls _compile, which calls _simple -- so subn transitively depends on _simple.
# subn calls _compile, which calls _combine_flags -- so subn transitively depends on _combine_flags.
# Since subn invokes _compile and _compile invokes _optimize_charset, subn indirectly relies on _optimize_charset.
# subn -> _compile -> emit: subn has a transitive dependency on emit through _compile.
# Note: subn does not call _len directly, but reaches it via _compile.
# Since subn invokes _compile and _compile invokes tolower, subn indirectly relies on tolower.
# Note: subn does not call error directly, but reaches it via _compile.
# findall -> iterfind -> _SelectorContext: findall has a transitive dependency on _SelectorContext through iterfind.
# findall -> iterfind -> xpath_tokenizer: findall has a transitive dependency on xpath_tokenizer through iterfind.
# Since findall invokes iterfind and iterfind invokes select, findall indirectly relies on select.
# Note: findall does not call _compile_charset directly, but reaches it via _compile.
# findall -> _compile -> iscased: findall has a transitive dependency on iscased through _compile.
# Since findall invokes _compile and _compile invokes PatternError, findall indirectly relies on PatternError.
# Since findall invokes _compile and _compile invokes tailappend, findall indirectly relies on tailappend.
# findall calls _compile, which calls _simple -- so findall transitively depends on _simple.
# Since findall invokes _compile and _compile invokes _combine_flags, findall indirectly relies on _combine_flags.
# findall -> _compile -> _optimize_charset: findall has a transitive dependency on _optimize_charset through _compile.
# Note: findall does not call emit directly, but reaches it via _compile.
# findall calls _compile, which calls _len -- so findall transitively depends on _len.
# findall -> _compile -> tolower: findall has a transitive dependency on tolower through _compile.
# Note: findall does not call error directly, but reaches it via _compile.
# finditer -> _compile -> _compile_charset: finditer has a transitive dependency on _compile_charset through _compile.
# Note: finditer does not call iscased directly, but reaches it via _compile.
# finditer calls _compile, which calls PatternError -- so finditer transitively depends on PatternError.
# Since finditer invokes _compile and _compile invokes tailappend, finditer indirectly relies on tailappend.
# finditer -> _compile -> _simple: finditer has a transitive dependency on _simple through _compile.
# Since finditer invokes _compile and _compile invokes _combine_flags, finditer indirectly relies on _combine_flags.
# Note: finditer does not call _optimize_charset directly, but reaches it via _compile.
# Since finditer invokes _compile and _compile invokes emit, finditer indirectly relies on emit.
# Since finditer invokes _compile and _compile invokes _len, finditer indirectly relies on _len.
# finditer calls _compile, which calls tolower -- so finditer transitively depends on tolower.
# Since finditer invokes _compile and _compile invokes error, finditer indirectly relies on error.
# Note: _compile_charset does not call EmailMessage directly, but reaches it via emit.
# Note: _optimize_charset does not call _int directly, but reaches it via _mk_bitmap.
# _compile_info calls _compile_charset, which calls PatternError -- so _compile_info transitively depends on PatternError.
# _compile_info -> _compile_charset -> emit: _compile_info has a transitive dependency on emit through _compile_charset.
# Since _compile_info invokes _optimize_charset and _optimize_charset invokes iscased, _compile_info indirectly relies on iscased.
# _compile_info calls _optimize_charset, which calls _bytes_to_codes -- so _compile_info transitively depends on _bytes_to_codes.
# _compile_info -> _optimize_charset -> fixup: _compile_info has a transitive dependency on fixup through _optimize_charset.
# Since _compile_info invokes _optimize_charset and _optimize_charset invokes _mk_bitmap, _compile_info indirectly relies on _mk_bitmap.
# Since _compile_info invokes _get_literal_prefix and _get_literal_prefix invokes prefixappend, _compile_info indirectly relies on prefixappend.
# Since _compile_info invokes _get_literal_prefix and _get_literal_prefix invokes iscased, _compile_info indirectly relies on iscased.
# _compile_info -> _get_literal_prefix -> _combine_flags: _compile_info has a transitive dependency on _combine_flags through _get_literal_prefix.
# Note: _compile_info does not call _get_iscased directly, but reaches it via _get_literal_prefix.
# _compile_info -> emit -> EmailMessage: _compile_info has a transitive dependency on EmailMessage through emit.
# Note: _compile_info does not call iscased directly, but reaches it via _get_charset_prefix.
# _compile_info -> _get_charset_prefix -> _combine_flags: _compile_info has a transitive dependency on _combine_flags through _get_charset_prefix.
# _compile_info -> _get_charset_prefix -> _get_iscased: _compile_info has a transitive dependency on _get_iscased through _get_charset_prefix.
# _compile_info calls _get_charset_prefix, which calls charsetappend -- so _compile_info transitively depends on charsetappend.
# Since _code invokes _compile_info and _compile_info invokes _compile_charset, _code indirectly relies on _compile_charset.
# _code -> _compile_info -> _optimize_charset: _code has a transitive dependency on _optimize_charset through _compile_info.
# Since _code invokes _compile_info and _compile_info invokes _get_literal_prefix, _code indirectly relies on _get_literal_prefix.
# _code calls _compile_info, which calls emit -- so _code transitively depends on emit.
# _code -> _compile_info -> _get_charset_prefix: _code has a transitive dependency on _get_charset_prefix through _compile_info.
# Note: _code does not call _generate_overlap_table directly, but reaches it via _compile_info.
# Since _code invokes _compile and _compile invokes _compile_charset, _code indirectly relies on _compile_charset.
# _code calls _compile, which calls iscased -- so _code transitively depends on iscased.
# _code calls _compile, which calls PatternError -- so _code transitively depends on PatternError.
# Note: _code does not call tailappend directly, but reaches it via _compile.
# Note: _code does not call _simple directly, but reaches it via _compile.
# Note: _code does not call _combine_flags directly, but reaches it via _compile.
# _code -> _compile -> _optimize_charset: _code has a transitive dependency on _optimize_charset through _compile.
# _code -> _compile -> emit: _code has a transitive dependency on emit through _compile.
# Since _code invokes _compile and _compile invokes _len, _code indirectly relies on _len.
# _code -> _compile -> tolower: _code has a transitive dependency on tolower through _compile.
# Note: _code does not call error directly, but reaches it via _compile.
# Since _parse_sub invokes _parse and _parse invokes unquote, _parse_sub indirectly relies on unquote.
# _parse_sub calls _parse, which calls _uniq -- so _parse_sub transitively depends on _uniq.
# Note: _parse_sub does not call _parse_flags directly, but reaches it via _parse.
# Since _parse_sub invokes _parse and _parse invokes unpack, _parse_sub indirectly relies on unpack.
# _parse_sub calls _parse, which calls NetrcParseError -- so _parse_sub transitively depends on NetrcParseError.
# _parse_sub calls _parse, which calls _error -- so _parse_sub transitively depends on _error.
# _parse_sub calls _parse, which calls setappend -- so _parse_sub transitively depends on setappend.
# Since _parse_sub invokes _parse and _parse invokes _escape, _parse_sub indirectly relies on _escape.
# _parse_sub calls _parse, which calls subpatternappend -- so _parse_sub transitively depends on subpatternappend.
# _parse_sub calls _parse, which calls _ord -- so _parse_sub transitively depends on _ord.
# _parse_sub -> _parse -> _class_escape: _parse_sub has a transitive dependency on _class_escape through _parse.
# _parse_sub -> _parse -> _splittype: _parse_sub has a transitive dependency on _splittype through _parse.
# Since _parse_sub invokes _parse and _parse invokes _netrclex, _parse_sub indirectly relies on _netrclex.
# _parse_sub calls _parse, which calls SubPattern -- so _parse_sub transitively depends on SubPattern.
# _parse_sub calls _parse, which calls _len -- so _parse_sub transitively depends on _len.
# Since _parse_sub invokes _parse and _parse invokes c2py, _parse_sub indirectly relies on c2py.
# Since _parse_sub invokes _parse and _parse invokes sourceget, _parse_sub indirectly relies on sourceget.
# _parse_sub -> _parse -> _splithost: _parse_sub has a transitive dependency on _splithost through _parse.
# Note: _parse_sub does not call sourcematch directly, but reaches it via _parse.
# parse_template -> addgroup -> addliteral: parse_template has a transitive dependency on addliteral through addgroup.
# Note: _get_module_details does not call basicConfig directly, but reaches it via error.
# Since _get_module_details invokes error and error invokes _, _get_module_details indirectly relies on _.
# Note: _get_module_details does not call perror directly, but reaches it via error.
# _get_module_details calls warn, which calls _is_internal_frame -- so _get_module_details transitively depends on _is_internal_frame.
# Since _get_module_details invokes warn and warn invokes _next_external_frame, _get_module_details indirectly relies on _next_external_frame.
# _get_module_details calls warn, which calls warning -- so _get_module_details transitively depends on warning.
# Note: _run_module_as_main does not call error directly, but reaches it via _get_module_details.
# _run_module_as_main calls _get_module_details, which calls warn -- so _run_module_as_main transitively depends on warn.
# Note: _run_module_as_main does not call _get_module_details directly, but reaches it via _get_main_module_details.
# Since _run_module_as_main invokes _get_main_module_details and _get_main_module_details invokes error, _run_module_as_main indirectly relies on error.
# Since run_module invokes _run_module_code and _run_module_code invokes _run_code, run_module indirectly relies on _run_code.
# run_module calls _run_module_code, which calls _TempModule -- so run_module transitively depends on _TempModule.
# Since run_module invokes _run_module_code and _run_module_code invokes _ModifiedArgv0, run_module indirectly relies on _ModifiedArgv0.
# run_module -> _get_module_details -> error: run_module has a transitive dependency on error through _get_module_details.
# run_module -> _get_module_details -> warn: run_module has a transitive dependency on warn through _get_module_details.
# _get_main_module_details -> _get_module_details -> error: _get_main_module_details has a transitive dependency on error through _get_module_details.
# Note: _get_main_module_details does not call warn directly, but reaches it via _get_module_details.
# _get_main_module_details -> error -> basicConfig: _get_main_module_details has a transitive dependency on basicConfig through error.
# Note: _get_main_module_details does not call _ directly, but reaches it via error.
# Note: _get_main_module_details does not call perror directly, but reaches it via error.
# run_path -> _get_code_from_file -> read_code: run_path has a transitive dependency on read_code through _get_code_from_file.
# run_path -> _get_main_module_details -> _get_module_details: run_path has a transitive dependency on _get_module_details through _get_main_module_details.
# run_path -> _get_main_module_details -> error: run_path has a transitive dependency on error through _get_main_module_details.
# Note: run_path does not call path_hook directly, but reaches it via get_importer.
# run_path -> _run_module_code -> _run_code: run_path has a transitive dependency on _run_code through _run_module_code.
# Note: run_path does not call _TempModule directly, but reaches it via _run_module_code.
# run_path -> _run_module_code -> _ModifiedArgv0: run_path has a transitive dependency on _ModifiedArgv0 through _run_module_code.
# Note: copyfile does not call fsrc_readinto directly, but reaches it via _copyfileobj_readinto.
# copyfile -> _copyfileobj_readinto -> fdst_write: copyfile has a transitive dependency on fdst_write through _copyfileobj_readinto.
# copyfile calls _fastcopy_sendfile, which calls _determine_linux_fastcopy_blocksize -- so copyfile transitively depends on _determine_linux_fastcopy_blocksize.
# Since copyfile invokes _fastcopy_sendfile and _fastcopy_sendfile invokes _GiveupOnFastCopy, copyfile indirectly relies on _GiveupOnFastCopy.
# Note: copyfile does not call fdst_write directly, but reaches it via copyfileobj.
# copyfile -> copyfileobj -> _ficlone: copyfile has a transitive dependency on _ficlone through copyfileobj.
# Note: copyfile does not call fsrc_read directly, but reaches it via copyfileobj.
# Note: copyfile does not call _sendfile directly, but reaches it via copyfileobj.
# Since copyfile invokes copyfileobj and copyfileobj invokes write_target, copyfile indirectly relies on write_target.
# Since copyfile invokes copyfileobj and copyfileobj invokes read_source, copyfile indirectly relies on read_source.
# Note: copyfile does not call exception directly, but reaches it via copyfileobj.
# Since copyfile invokes copyfileobj and copyfileobj invokes _copy_file_range, copyfile indirectly relies on _copy_file_range.
# Since copyfile invokes copyfileobj and copyfileobj invokes _fcopyfile, copyfile indirectly relies on _fcopyfile.
# Since copyfile invokes _fastcopy_fcopyfile and _fastcopy_fcopyfile invokes _GiveupOnFastCopy, copyfile indirectly relies on _GiveupOnFastCopy.
# copyfile calls _fastcopy_copy_file_range, which calls _determine_linux_fastcopy_blocksize -- so copyfile transitively depends on _determine_linux_fastcopy_blocksize.
# copyfile -> _fastcopy_copy_file_range -> _GiveupOnFastCopy: copyfile has a transitive dependency on _GiveupOnFastCopy through _fastcopy_copy_file_range.
# Since copystat invokes lookup and lookup invokes Lookup, copystat indirectly relies on Lookup.
# Since copystat invokes lookup and lookup invokes Symbol, copystat indirectly relies on Symbol.
# Note: copy2 does not call _islink directly, but reaches it via copystat.
# copy2 calls copystat, which calls _copyxattr -- so copy2 transitively depends on _copyxattr.
# Note: copy2 does not call lookup directly, but reaches it via copystat.
# Since copy2 invokes copyfile and copyfile invokes SameFileError, copy2 indirectly relies on SameFileError.
# Note: copy2 does not call SpecialFileError directly, but reaches it via copyfile.
# Since copy2 invokes copyfile and copyfile invokes _copyfileobj_readinto, copy2 indirectly relies on _copyfileobj_readinto.
# copy2 calls copyfile, which calls _samefile -- so copy2 transitively depends on _samefile.
# copy2 calls copyfile, which calls _stat -- so copy2 transitively depends on _stat.
# copy2 calls copyfile, which calls _fastcopy_sendfile -- so copy2 transitively depends on _fastcopy_sendfile.
# copy2 calls copyfile, which calls _islink -- so copy2 transitively depends on _islink.
# copy2 calls copyfile, which calls copyfileobj -- so copy2 transitively depends on copyfileobj.
# Since copy2 invokes copyfile and copyfile invokes _fastcopy_fcopyfile, copy2 indirectly relies on _fastcopy_fcopyfile.
# Since copy2 invokes copyfile and copyfile invokes _fastcopy_copy_file_range, copy2 indirectly relies on _fastcopy_copy_file_range.
# Since _copytree invokes copystat and copystat invokes _islink, _copytree indirectly relies on _islink.
# _copytree -> copystat -> _copyxattr: _copytree has a transitive dependency on _copyxattr through copystat.
# _copytree -> copystat -> lookup: _copytree has a transitive dependency on lookup through copystat.
# copytree calls _copytree, which calls copystat -- so copytree transitively depends on copystat.
# copytree calls _copytree, which calls Error -- so copytree transitively depends on Error.
# Note: copytree does not call copy_function directly, but reaches it via _copytree.
# Note: copytree does not call ignore directly, but reaches it via _copytree.
# Since _rmtree_unsafe invokes onexc and onexc invokes onerror, _rmtree_unsafe indirectly relies on onerror.
# _rmtree_unsafe -> onexc -> _resetperms: _rmtree_unsafe has a transitive dependency on _resetperms through onexc.
# Since _rmtree_safe_fd invokes _rmtree_safe_fd_step and _rmtree_safe_fd_step invokes onexc, _rmtree_safe_fd indirectly relies on onexc.
# Since _rmtree_safe_fd invokes onexc and onexc invokes onerror, _rmtree_safe_fd indirectly relies on onerror.
# Note: _rmtree_safe_fd does not call _resetperms directly, but reaches it via onexc.
# _rmtree_safe_fd_step -> onexc -> onerror: _rmtree_safe_fd_step has a transitive dependency on onerror through onexc.
# _rmtree_safe_fd_step calls onexc, which calls _resetperms -- so _rmtree_safe_fd_step transitively depends on _resetperms.
# Since rmtree invokes onerror and onerror invokes onexc, rmtree indirectly relies on onexc.
# Note: rmtree does not call callback directly, but reaches it via onerror.
# move calls _is_immutable, which calls _stat -- so move transitively depends on _stat.
# Since move invokes ensure_different_files and ensure_different_files invokes target_file_id, move indirectly relies on target_file_id.
# Since move invokes ensure_different_files and ensure_different_files invokes source_file_id, move indirectly relies on source_file_id.
# Since move invokes copytree and copytree invokes _copytree, move indirectly relies on _copytree.
# move -> rmtree -> _rmtree_impl: move has a transitive dependency on _rmtree_impl through rmtree.
# move -> rmtree -> onerror: move has a transitive dependency on onerror through rmtree.
# _make_tarball -> _get_uid -> getpwnam: _make_tarball has a transitive dependency on getpwnam through _get_uid.
# Since _make_tarball invokes _get_gid and _get_gid invokes getgrnam, _make_tarball indirectly relies on getgrnam.
# Note: make_archive does not call getter directly, but reaches it via func.
# register_unpack_format -> _check_unpack_options -> RegistryError: register_unpack_format has a transitive dependency on RegistryError through _check_unpack_options.
# unpack_archive calls func, which calls getter -- so unpack_archive transitively depends on getter.
# chown calls _get_gid, which calls getgrnam -- so chown transitively depends on getgrnam.
# Note: chown does not call getpwnam directly, but reaches it via _get_uid.
# signal calls _int_to_enum, which calls enum_klass -- so signal transitively depends on enum_klass.
# getsignal calls _int_to_enum, which calls enum_klass -- so getsignal transitively depends on enum_klass.
# Since addpackage invokes _init_pathinfo and _init_pathinfo invokes makepath, addpackage indirectly relies on makepath.
# addsitedir -> _init_pathinfo -> makepath: addsitedir has a transitive dependency on makepath through _init_pathinfo.
# Note: addsitedir does not call _init_pathinfo directly, but reaches it via addpackage.
# Since addsitedir invokes addpackage and addpackage invokes makepath, addsitedir indirectly relies on makepath.
# addsitedir -> addpackage -> _trace: addsitedir has a transitive dependency on _trace through addpackage.
# Note: getuserbase does not call _get_implementation directly, but reaches it via _getuserbase.
# getuserbase calls _getuserbase, which calls joinuser -- so getuserbase transitively depends on joinuser.
# getusersitepackages -> getuserbase -> _getuserbase: getusersitepackages has a transitive dependency on _getuserbase through getuserbase.
# getusersitepackages calls _get_path, which calls _get_implementation -- so getusersitepackages transitively depends on _get_implementation.
# Note: addusersitepackages does not call getuserbase directly, but reaches it via getusersitepackages.
# Note: addusersitepackages does not call _get_path directly, but reaches it via getusersitepackages.
# addusersitepackages -> addsitedir -> _init_pathinfo: addusersitepackages has a transitive dependency on _init_pathinfo through addsitedir.
# Since addusersitepackages invokes addsitedir and addsitedir invokes makepath, addusersitepackages indirectly relies on makepath.
# Note: addusersitepackages does not call _trace directly, but reaches it via addsitedir.
# addusersitepackages calls addsitedir, which calls addpackage -- so addusersitepackages transitively depends on addpackage.
# addsitepackages -> getsitepackages -> _get_implementation: addsitepackages has a transitive dependency on _get_implementation through getsitepackages.
# addsitepackages -> addsitedir -> _init_pathinfo: addsitepackages has a transitive dependency on _init_pathinfo through addsitedir.
# Since addsitepackages invokes addsitedir and addsitedir invokes makepath, addsitepackages indirectly relies on makepath.
# addsitepackages calls addsitedir, which calls _trace -- so addsitepackages transitively depends on _trace.
# addsitepackages -> addsitedir -> addpackage: addsitepackages has a transitive dependency on addpackage through addsitedir.
# Note: venv does not call getsitepackages directly, but reaches it via addsitepackages.
# venv calls addsitepackages, which calls addsitedir -- so venv transitively depends on addsitedir.
# Since venv invokes addsitepackages and addsitepackages invokes _trace, venv indirectly relies on _trace.
# Since _script invokes getuserbase and getuserbase invokes _getuserbase, _script indirectly relies on _getuserbase.
# _script -> getusersitepackages -> getuserbase: _script has a transitive dependency on getuserbase through getusersitepackages.
# Note: _script does not call _get_path directly, but reaches it via getusersitepackages.
# create_connection calls error, which calls basicConfig -- so create_connection transitively depends on basicConfig.
# Note: create_connection does not call _ directly, but reaches it via error.
# create_connection -> error -> perror: create_connection has a transitive dependency on perror through error.
# Note: create_connection does not call _intenum_converter directly, but reaches it via getaddrinfo.
# create_server -> has_dualstack_ipv6 -> socket: create_server has a transitive dependency on socket through has_dualstack_ipv6.
# create_server calls error, which calls basicConfig -- so create_server transitively depends on basicConfig.
# Note: create_server does not call _ directly, but reaches it via error.
# create_server calls error, which calls perror -- so create_server transitively depends on perror.
# Note: getaddrinfo does not call enum_klass directly, but reaches it via _intenum_converter.
# register_adapters_and_converters calls warn, which calls _is_internal_frame -- so register_adapters_and_converters transitively depends on _is_internal_frame.
# Since register_adapters_and_converters invokes warn and warn invokes _next_external_frame, register_adapters_and_converters indirectly relies on _next_external_frame.
# register_adapters_and_converters -> warn -> warning: register_adapters_and_converters has a transitive dependency on warning through warn.
# get_server_certificate -> create_connection -> socket: get_server_certificate has a transitive dependency on socket through create_connection.
# get_server_certificate -> create_connection -> error: get_server_certificate has a transitive dependency on error through create_connection.
# Since get_server_certificate invokes create_connection and create_connection invokes getaddrinfo, get_server_certificate indirectly relies on getaddrinfo.
# Note: mean does not call T directly, but reaches it via _convert.
# Note: mean does not call _convert_signed_num directly, but reaches it via _convert.
# mean calls _convert, which calls _raise_malformed_node -- so mean transitively depends on _raise_malformed_node.
# mean -> _convert -> _convert_num: mean has a transitive dependency on _convert_num through _convert.
# Since mean invokes _sum and _sum invokes partials_get, mean indirectly relies on partials_get.
# Note: mean does not call types_add directly, but reaches it via _sum.
# mean calls _sum, which calls Fraction -- so mean transitively depends on Fraction.
# mean -> _sum -> groupby: mean has a transitive dependency on groupby through _sum.
# Since mean invokes _sum and _sum invokes reduce, mean indirectly relies on reduce.
# Since mean invokes _sum and _sum invokes _isfinite, mean indirectly relies on _isfinite.
# geometric_mean calls exp, which calls _dec_from_triple -- so geometric_mean transitively depends on _dec_from_triple.
# Note: geometric_mean does not call _convert_other directly, but reaches it via exp.
# Note: geometric_mean does not call _dexp directly, but reaches it via exp.
# Since geometric_mean invokes exp and exp invokes getcontext, geometric_mean indirectly relies on getcontext.
# Note: geometric_mean does not call _WorkRep directly, but reaches it via exp.
# geometric_mean -> exp -> Decimal: geometric_mean has a transitive dependency on Decimal through exp.
# Since geometric_mean invokes count_positive and count_positive invokes StatisticsError, geometric_mean indirectly relies on StatisticsError.
# harmonic_mean -> _sum -> partials_get: harmonic_mean has a transitive dependency on partials_get through _sum.
# Since harmonic_mean invokes _sum and _sum invokes types_add, harmonic_mean indirectly relies on types_add.
# harmonic_mean calls _sum, which calls Fraction -- so harmonic_mean transitively depends on Fraction.
# harmonic_mean calls _sum, which calls groupby -- so harmonic_mean transitively depends on groupby.
# Since harmonic_mean invokes _sum and _sum invokes reduce, harmonic_mean indirectly relies on reduce.
# harmonic_mean calls _sum, which calls _isfinite -- so harmonic_mean transitively depends on _isfinite.
# Since harmonic_mean invokes _convert and _convert invokes T, harmonic_mean indirectly relies on T.
# Note: harmonic_mean does not call _convert_signed_num directly, but reaches it via _convert.
# Since harmonic_mean invokes _convert and _convert invokes _raise_malformed_node, harmonic_mean indirectly relies on _raise_malformed_node.
# harmonic_mean calls _convert, which calls _convert_num -- so harmonic_mean transitively depends on _convert_num.
# Note: harmonic_mean does not call Timer directly, but reaches it via repeat.
# harmonic_mean -> _fail_neg -> StatisticsError: harmonic_mean has a transitive dependency on StatisticsError through _fail_neg.
# median_grouped -> bisect_right -> key: median_grouped has a transitive dependency on key through bisect_right.
# median_grouped -> bisect_left -> key: median_grouped has a transitive dependency on key through bisect_left.
# variance -> _convert -> T: variance has a transitive dependency on T through _convert.
# Note: variance does not call _convert_signed_num directly, but reaches it via _convert.
# variance calls _convert, which calls _raise_malformed_node -- so variance transitively depends on _raise_malformed_node.
# variance -> _convert -> _convert_num: variance has a transitive dependency on _convert_num through _convert.
# Note: variance does not call _sum directly, but reaches it via _ss.
# Note: variance does not call defaultdict directly, but reaches it via _ss.
# Since variance invokes _ss and _ss invokes types_add, variance indirectly relies on types_add.
# Note: variance does not call Fraction directly, but reaches it via _ss.
# variance calls _ss, which calls groupby -- so variance transitively depends on groupby.
# Note: variance does not call reduce directly, but reaches it via _ss.
# variance -> _ss -> _isfinite: variance has a transitive dependency on _isfinite through _ss.
# Since pvariance invokes _convert and _convert invokes T, pvariance indirectly relies on T.
# pvariance -> _convert -> _convert_signed_num: pvariance has a transitive dependency on _convert_signed_num through _convert.
# pvariance -> _convert -> _raise_malformed_node: pvariance has a transitive dependency on _raise_malformed_node through _convert.
# Since pvariance invokes _convert and _convert invokes _convert_num, pvariance indirectly relies on _convert_num.
# Since pvariance invokes _ss and _ss invokes _sum, pvariance indirectly relies on _sum.
# pvariance -> _ss -> defaultdict: pvariance has a transitive dependency on defaultdict through _ss.
# Since pvariance invokes _ss and _ss invokes types_add, pvariance indirectly relies on types_add.
# pvariance calls _ss, which calls Fraction -- so pvariance transitively depends on Fraction.
# pvariance -> _ss -> groupby: pvariance has a transitive dependency on groupby through _ss.
# Note: pvariance does not call reduce directly, but reaches it via _ss.
# Since pvariance invokes _ss and _ss invokes _isfinite, pvariance indirectly relies on _isfinite.
# stdev -> _float_sqrt_of_frac -> _integer_sqrt_of_frac_rto: stdev has a transitive dependency on _integer_sqrt_of_frac_rto through _float_sqrt_of_frac.
# stdev -> _decimal_sqrt_of_frac -> Decimal: stdev has a transitive dependency on Decimal through _decimal_sqrt_of_frac.
# Since stdev invokes _ss and _ss invokes _sum, stdev indirectly relies on _sum.
# Note: stdev does not call defaultdict directly, but reaches it via _ss.
# stdev calls _ss, which calls types_add -- so stdev transitively depends on types_add.
# stdev calls _ss, which calls Fraction -- so stdev transitively depends on Fraction.
# Since stdev invokes _ss and _ss invokes groupby, stdev indirectly relies on groupby.
# Note: stdev does not call reduce directly, but reaches it via _ss.
# Note: stdev does not call _isfinite directly, but reaches it via _ss.
# pstdev calls _float_sqrt_of_frac, which calls _integer_sqrt_of_frac_rto -- so pstdev transitively depends on _integer_sqrt_of_frac_rto.
# pstdev calls _decimal_sqrt_of_frac, which calls Decimal -- so pstdev transitively depends on Decimal.
# Since pstdev invokes _ss and _ss invokes _sum, pstdev indirectly relies on _sum.
# pstdev calls _ss, which calls defaultdict -- so pstdev transitively depends on defaultdict.
# Since pstdev invokes _ss and _ss invokes types_add, pstdev indirectly relies on types_add.
# pstdev -> _ss -> Fraction: pstdev has a transitive dependency on Fraction through _ss.
# Note: pstdev does not call groupby directly, but reaches it via _ss.
# pstdev calls _ss, which calls reduce -- so pstdev transitively depends on reduce.
# Since pstdev invokes _ss and _ss invokes _isfinite, pstdev indirectly relies on _isfinite.
# Since correlation invokes _sqrtprod and _sqrtprod invokes isinf, correlation indirectly relies on isinf.
# correlation calls _sqrtprod, which calls sqrt -- so correlation transitively depends on sqrt.
# Note: correlation does not call isfinite directly, but reaches it via _sqrtprod.
# correlation -> _sqrtprod -> sumprod: correlation has a transitive dependency on sumprod through _sqrtprod.
# Since correlation invokes _rank and _rank invokes itemgetter, correlation indirectly relies on itemgetter.
# correlation calls _rank, which calls groupby -- so correlation transitively depends on groupby.
# Note: correlation does not call count directly, but reaches it via _rank.
# register calls get_type_hints, which calls _add_type_params_to_scope -- so register transitively depends on _add_type_params_to_scope.
# register calls get_type_hints, which calls _make_forward_ref -- so register transitively depends on _make_forward_ref.
# register calls get_type_hints, which calls _eval_type -- so register transitively depends on _eval_type.
# Since register invokes get_type_hints and get_type_hints invokes _strip_annotations, register indirectly relies on _strip_annotations.
# Since register invokes register_standard_browsers and register_standard_browsers invokes IOSBrowser, register indirectly relies on IOSBrowser.
# Note: register does not call register_X_browsers directly, but reaches it via register_standard_browsers.
# Since register invokes register_standard_browsers and register_standard_browsers invokes Elinks, register indirectly relies on Elinks.
# register -> register_standard_browsers -> _synthesize: register has a transitive dependency on _synthesize through register_standard_browsers.
# register calls register_standard_browsers, which calls GenericBrowser -- so register transitively depends on GenericBrowser.
# Since register invokes register_standard_browsers and register_standard_browsers invokes Edge, register indirectly relies on Edge.
# register -> register_standard_browsers -> MacOSXOSAScript: register has a transitive dependency on MacOSXOSAScript through register_standard_browsers.
# register calls register_standard_browsers, which calls BackgroundBrowser -- so register transitively depends on BackgroundBrowser.
# register -> dispatch -> convert_to_error: register has a transitive dependency on convert_to_error through dispatch.
# register -> dispatch -> _find_impl: register has a transitive dependency on _find_impl through dispatch.
# Since register invokes dispatch and dispatch invokes get_cache_token, register indirectly relies on get_cache_token.
# normal_kernel -> sqrt -> _dec_from_triple: normal_kernel has a transitive dependency on _dec_from_triple through sqrt.
# normal_kernel calls sqrt, which calls _convert_other -- so normal_kernel transitively depends on _convert_other.
# Note: normal_kernel does not call getcontext directly, but reaches it via sqrt.
# normal_kernel -> sqrt -> _WorkRep: normal_kernel has a transitive dependency on _WorkRep through sqrt.
# normal_kernel calls sqrt, which calls Decimal -- so normal_kernel transitively depends on Decimal.
# normal_kernel -> _normal_dist_inv_cdf -> log: normal_kernel has a transitive dependency on log through _normal_dist_inv_cdf.
# normal_kernel -> _normal_dist_inv_cdf -> sqrt: normal_kernel has a transitive dependency on sqrt through _normal_dist_inv_cdf.
# normal_kernel -> _normal_dist_inv_cdf -> fabs: normal_kernel has a transitive dependency on fabs through _normal_dist_inv_cdf.
# normal_kernel calls exp, which calls _dec_from_triple -- so normal_kernel transitively depends on _dec_from_triple.
# normal_kernel -> exp -> _convert_other: normal_kernel has a transitive dependency on _convert_other through exp.
# Since normal_kernel invokes exp and exp invokes _dexp, normal_kernel indirectly relies on _dexp.
# normal_kernel -> exp -> getcontext: normal_kernel has a transitive dependency on getcontext through exp.
# normal_kernel calls exp, which calls _WorkRep -- so normal_kernel transitively depends on _WorkRep.
# normal_kernel calls exp, which calls Decimal -- so normal_kernel transitively depends on Decimal.
# Note: normal_kernel does not call _is_valid_dispatch_type directly, but reaches it via register.
# normal_kernel calls register, which calls get_type_hints -- so normal_kernel transitively depends on get_type_hints.
# Note: normal_kernel does not call register_standard_browsers directly, but reaches it via register.
# Since normal_kernel invokes register and register invokes proxytype, normal_kernel indirectly relies on proxytype.
# Note: normal_kernel does not call SelectorKey directly, but reaches it via register.
# Note: normal_kernel does not call builder directly, but reaches it via register.
# normal_kernel -> register -> _abc_register: normal_kernel has a transitive dependency on _abc_register through register.
# normal_kernel calls register, which calls dispatch -- so normal_kernel transitively depends on dispatch.
# Since normal_kernel invokes register and register invokes get_cache_token, normal_kernel indirectly relies on get_cache_token.
# Note: logistic_kernel does not call _dec_from_triple directly, but reaches it via exp.
# logistic_kernel calls exp, which calls _convert_other -- so logistic_kernel transitively depends on _convert_other.
# logistic_kernel calls exp, which calls _dexp -- so logistic_kernel transitively depends on _dexp.
# logistic_kernel calls exp, which calls getcontext -- so logistic_kernel transitively depends on getcontext.
# logistic_kernel calls exp, which calls _WorkRep -- so logistic_kernel transitively depends on _WorkRep.
# Since logistic_kernel invokes exp and exp invokes Decimal, logistic_kernel indirectly relies on Decimal.
# Note: logistic_kernel does not call basicConfig directly, but reaches it via log.
# Note: logistic_kernel does not call _is_valid_dispatch_type directly, but reaches it via register.
# Since logistic_kernel invokes register and register invokes get_type_hints, logistic_kernel indirectly relies on get_type_hints.
# Since logistic_kernel invokes register and register invokes register_standard_browsers, logistic_kernel indirectly relies on register_standard_browsers.
# logistic_kernel calls register, which calls proxytype -- so logistic_kernel transitively depends on proxytype.
# logistic_kernel calls register, which calls SelectorKey -- so logistic_kernel transitively depends on SelectorKey.
# logistic_kernel calls register, which calls builder -- so logistic_kernel transitively depends on builder.
# Note: logistic_kernel does not call _abc_register directly, but reaches it via register.
# Note: logistic_kernel does not call dispatch directly, but reaches it via register.
# logistic_kernel -> register -> get_cache_token: logistic_kernel has a transitive dependency on get_cache_token through register.
# Since sigmoid_kernel invokes exp and exp invokes _dec_from_triple, sigmoid_kernel indirectly relies on _dec_from_triple.
# Note: sigmoid_kernel does not call _convert_other directly, but reaches it via exp.
# sigmoid_kernel calls exp, which calls _dexp -- so sigmoid_kernel transitively depends on _dexp.
# sigmoid_kernel -> exp -> getcontext: sigmoid_kernel has a transitive dependency on getcontext through exp.
# Note: sigmoid_kernel does not call _WorkRep directly, but reaches it via exp.
# Since sigmoid_kernel invokes exp and exp invokes Decimal, sigmoid_kernel indirectly relies on Decimal.
# sigmoid_kernel -> log -> basicConfig: sigmoid_kernel has a transitive dependency on basicConfig through log.
# Since sigmoid_kernel invokes register and register invokes _is_valid_dispatch_type, sigmoid_kernel indirectly relies on _is_valid_dispatch_type.
# sigmoid_kernel calls register, which calls get_type_hints -- so sigmoid_kernel transitively depends on get_type_hints.
# Note: sigmoid_kernel does not call register_standard_browsers directly, but reaches it via register.
# Note: sigmoid_kernel does not call proxytype directly, but reaches it via register.
# sigmoid_kernel calls register, which calls SelectorKey -- so sigmoid_kernel transitively depends on SelectorKey.
# Since sigmoid_kernel invokes register and register invokes builder, sigmoid_kernel indirectly relies on builder.
# sigmoid_kernel -> register -> _abc_register: sigmoid_kernel has a transitive dependency on _abc_register through register.
# Note: sigmoid_kernel does not call dispatch directly, but reaches it via register.
# Since sigmoid_kernel invokes register and register invokes get_cache_token, sigmoid_kernel indirectly relies on get_cache_token.
# Since rectangular_kernel invokes register and register invokes _is_valid_dispatch_type, rectangular_kernel indirectly relies on _is_valid_dispatch_type.
# Since rectangular_kernel invokes register and register invokes get_type_hints, rectangular_kernel indirectly relies on get_type_hints.
# rectangular_kernel calls register, which calls register_standard_browsers -- so rectangular_kernel transitively depends on register_standard_browsers.
# Since rectangular_kernel invokes register and register invokes proxytype, rectangular_kernel indirectly relies on proxytype.
# rectangular_kernel calls register, which calls SelectorKey -- so rectangular_kernel transitively depends on SelectorKey.
# rectangular_kernel -> register -> builder: rectangular_kernel has a transitive dependency on builder through register.
# rectangular_kernel -> register -> _abc_register: rectangular_kernel has a transitive dependency on _abc_register through register.
# rectangular_kernel -> register -> dispatch: rectangular_kernel has a transitive dependency on dispatch through register.
# rectangular_kernel -> register -> get_cache_token: rectangular_kernel has a transitive dependency on get_cache_token through register.
# Since triangular_kernel invokes sqrt and sqrt invokes _dec_from_triple, triangular_kernel indirectly relies on _dec_from_triple.
# Since triangular_kernel invokes sqrt and sqrt invokes _convert_other, triangular_kernel indirectly relies on _convert_other.
# Note: triangular_kernel does not call getcontext directly, but reaches it via sqrt.
# triangular_kernel -> sqrt -> _WorkRep: triangular_kernel has a transitive dependency on _WorkRep through sqrt.
# triangular_kernel calls sqrt, which calls Decimal -- so triangular_kernel transitively depends on Decimal.
# Note: triangular_kernel does not call _is_valid_dispatch_type directly, but reaches it via register.
# triangular_kernel calls register, which calls get_type_hints -- so triangular_kernel transitively depends on get_type_hints.
# Note: triangular_kernel does not call register_standard_browsers directly, but reaches it via register.
# Since triangular_kernel invokes register and register invokes proxytype, triangular_kernel indirectly relies on proxytype.
# Note: triangular_kernel does not call SelectorKey directly, but reaches it via register.
# Since triangular_kernel invokes register and register invokes builder, triangular_kernel indirectly relies on builder.
# Note: triangular_kernel does not call _abc_register directly, but reaches it via register.
# Note: triangular_kernel does not call dispatch directly, but reaches it via register.
# Since triangular_kernel invokes register and register invokes get_cache_token, triangular_kernel indirectly relies on get_cache_token.
# Note: parabolic_kernel does not call _is_valid_dispatch_type directly, but reaches it via register.
# parabolic_kernel -> register -> get_type_hints: parabolic_kernel has a transitive dependency on get_type_hints through register.
# parabolic_kernel calls register, which calls register_standard_browsers -- so parabolic_kernel transitively depends on register_standard_browsers.
# Since parabolic_kernel invokes register and register invokes proxytype, parabolic_kernel indirectly relies on proxytype.
# Since parabolic_kernel invokes register and register invokes SelectorKey, parabolic_kernel indirectly relies on SelectorKey.
# Note: parabolic_kernel does not call builder directly, but reaches it via register.
# Note: parabolic_kernel does not call _abc_register directly, but reaches it via register.
# Since parabolic_kernel invokes register and register invokes dispatch, parabolic_kernel indirectly relies on dispatch.
# parabolic_kernel calls register, which calls get_cache_token -- so parabolic_kernel transitively depends on get_cache_token.
# _newton_raphson calls f, which calls f1 -- so _newton_raphson transitively depends on f1.
# Since quartic_kernel invokes register and register invokes _is_valid_dispatch_type, quartic_kernel indirectly relies on _is_valid_dispatch_type.
# Since quartic_kernel invokes register and register invokes get_type_hints, quartic_kernel indirectly relies on get_type_hints.
# quartic_kernel -> register -> register_standard_browsers: quartic_kernel has a transitive dependency on register_standard_browsers through register.
# quartic_kernel calls register, which calls proxytype -- so quartic_kernel transitively depends on proxytype.
# quartic_kernel -> register -> SelectorKey: quartic_kernel has a transitive dependency on SelectorKey through register.
# quartic_kernel calls register, which calls builder -- so quartic_kernel transitively depends on builder.
# Since quartic_kernel invokes register and register invokes _abc_register, quartic_kernel indirectly relies on _abc_register.
# Since quartic_kernel invokes register and register invokes dispatch, quartic_kernel indirectly relies on dispatch.
# Note: quartic_kernel does not call get_cache_token directly, but reaches it via register.
# quartic_kernel -> _newton_raphson -> f: quartic_kernel has a transitive dependency on f through _newton_raphson.
# quartic_kernel calls _newton_raphson, which calls f_inv_estimate -- so quartic_kernel transitively depends on f_inv_estimate.
# quartic_kernel -> _newton_raphson -> f_prime: quartic_kernel has a transitive dependency on f_prime through _newton_raphson.
# Since triweight_kernel invokes register and register invokes _is_valid_dispatch_type, triweight_kernel indirectly relies on _is_valid_dispatch_type.
# triweight_kernel calls register, which calls get_type_hints -- so triweight_kernel transitively depends on get_type_hints.
# Since triweight_kernel invokes register and register invokes register_standard_browsers, triweight_kernel indirectly relies on register_standard_browsers.
# Note: triweight_kernel does not call proxytype directly, but reaches it via register.
# triweight_kernel -> register -> SelectorKey: triweight_kernel has a transitive dependency on SelectorKey through register.
# Note: triweight_kernel does not call builder directly, but reaches it via register.
# triweight_kernel calls register, which calls _abc_register -- so triweight_kernel transitively depends on _abc_register.
# Since triweight_kernel invokes register and register invokes dispatch, triweight_kernel indirectly relies on dispatch.
# triweight_kernel -> register -> get_cache_token: triweight_kernel has a transitive dependency on get_cache_token through register.
# Since triweight_kernel invokes _newton_raphson and _newton_raphson invokes f, triweight_kernel indirectly relies on f.
# triweight_kernel -> _newton_raphson -> f_inv_estimate: triweight_kernel has a transitive dependency on f_inv_estimate through _newton_raphson.
# triweight_kernel calls _newton_raphson, which calls f_prime -- so triweight_kernel transitively depends on f_prime.
# cosine_kernel -> register -> _is_valid_dispatch_type: cosine_kernel has a transitive dependency on _is_valid_dispatch_type through register.
# Note: cosine_kernel does not call get_type_hints directly, but reaches it via register.
# cosine_kernel -> register -> register_standard_browsers: cosine_kernel has a transitive dependency on register_standard_browsers through register.
# cosine_kernel -> register -> proxytype: cosine_kernel has a transitive dependency on proxytype through register.
# cosine_kernel calls register, which calls SelectorKey -- so cosine_kernel transitively depends on SelectorKey.
# cosine_kernel -> register -> builder: cosine_kernel has a transitive dependency on builder through register.
# cosine_kernel calls register, which calls _abc_register -- so cosine_kernel transitively depends on _abc_register.
# cosine_kernel -> register -> dispatch: cosine_kernel has a transitive dependency on dispatch through register.
# cosine_kernel -> register -> get_cache_token: cosine_kernel has a transitive dependency on get_cache_token through register.
# Since kde invokes bisect_left and bisect_left invokes key, kde indirectly relies on key.
# kde -> bisect_right -> key: kde has a transitive dependency on key through bisect_right.
# Since kde_random invokes random and random invokes _urandom, kde_random indirectly relies on _urandom.
# Since _sum invokes reduce and reduce invokes function, _sum indirectly relies on function.
# Since _ss invokes _sum and _sum invokes partials_get, _ss indirectly relies on partials_get.
# Since _ss invokes _sum and _sum invokes types_add, _ss indirectly relies on types_add.
# Note: _ss does not call Fraction directly, but reaches it via _sum.
# _ss calls _sum, which calls groupby -- so _ss transitively depends on groupby.
# _ss -> _sum -> reduce: _ss has a transitive dependency on reduce through _sum.
# Since _ss invokes _sum and _sum invokes _isfinite, _ss indirectly relies on _isfinite.
# _ss -> reduce -> function: _ss has a transitive dependency on function through reduce.
# Note: _convert does not call _convert_num directly, but reaches it via _convert_signed_num.
# _convert calls _convert_num, which calls _raise_malformed_node -- so _convert transitively depends on _raise_malformed_node.
# _mean_stdev calls _float_sqrt_of_frac, which calls _integer_sqrt_of_frac_rto -- so _mean_stdev transitively depends on _integer_sqrt_of_frac_rto.
# Note: _mean_stdev does not call _sum directly, but reaches it via _ss.
# _mean_stdev calls _ss, which calls defaultdict -- so _mean_stdev transitively depends on defaultdict.
# _mean_stdev -> _ss -> types_add: _mean_stdev has a transitive dependency on types_add through _ss.
# Since _mean_stdev invokes _ss and _ss invokes Fraction, _mean_stdev indirectly relies on Fraction.
# _mean_stdev -> _ss -> groupby: _mean_stdev has a transitive dependency on groupby through _ss.
# _mean_stdev calls _ss, which calls reduce -- so _mean_stdev transitively depends on reduce.
# _mean_stdev calls _ss, which calls _isfinite -- so _mean_stdev transitively depends on _isfinite.
# Note: _sqrtprod does not call _dec_from_triple directly, but reaches it via sqrt.
# _sqrtprod -> sqrt -> _convert_other: _sqrtprod has a transitive dependency on _convert_other through sqrt.
# Since _sqrtprod invokes sqrt and sqrt invokes getcontext, _sqrtprod indirectly relies on getcontext.
# Since _sqrtprod invokes sqrt and sqrt invokes _WorkRep, _sqrtprod indirectly relies on _WorkRep.
# _sqrtprod calls sqrt, which calls Decimal -- so _sqrtprod transitively depends on Decimal.
# _normal_dist_inv_cdf -> log -> basicConfig: _normal_dist_inv_cdf has a transitive dependency on basicConfig through log.
# _normal_dist_inv_cdf -> sqrt -> _dec_from_triple: _normal_dist_inv_cdf has a transitive dependency on _dec_from_triple through sqrt.
# Since _normal_dist_inv_cdf invokes sqrt and sqrt invokes _convert_other, _normal_dist_inv_cdf indirectly relies on _convert_other.
# Note: _normal_dist_inv_cdf does not call getcontext directly, but reaches it via sqrt.
# _normal_dist_inv_cdf -> sqrt -> _WorkRep: _normal_dist_inv_cdf has a transitive dependency on _WorkRep through sqrt.
# Since _normal_dist_inv_cdf invokes sqrt and sqrt invokes Decimal, _normal_dist_inv_cdf indirectly relies on Decimal.
# Since check_call invokes call and call invokes Popen, check_call indirectly relies on Popen.
# check_call calls call, which calls obj -- so check_call transitively depends on obj.
# Note: check_output does not call CompletedProcess directly, but reaches it via run.
# Since check_output invokes run and run invokes _Outcome, check_output indirectly relies on _Outcome.
# check_output -> run -> stopTestRun: check_output has a transitive dependency on stopTestRun through run.
# Note: check_output does not call DocServer directly, but reaches it via run.
# check_output -> run -> _extract_future_flags: check_output has a transitive dependency on _extract_future_flags through run.
# check_output -> run -> timefunc: check_output has a transitive dependency on timefunc through run.
# check_output calls run, which calls _addSkip -- so check_output transitively depends on _addSkip.
# Since check_output invokes run and run invokes get_theme, check_output indirectly relies on get_theme.
# Note: check_output does not call callback directly, but reaches it via run.
# check_output -> run -> action: check_output has a transitive dependency on action through run.
# Since check_output invokes run and run invokes startTestRun, check_output indirectly relies on startTestRun.
# check_output calls run, which calls Popen -- so check_output transitively depends on Popen.
# Note: check_output does not call _OutputRedirectingPdb directly, but reaches it via run.
# Note: check_output does not call Pdb directly, but reaches it via run.
# Note: check_output does not call run_multiline_interactive_console directly, but reaches it via run.
# check_output -> run -> completer: check_output has a transitive dependency on completer through run.
# check_output -> run -> Runner: check_output has a transitive dependency on Runner through run.
# Note: check_output does not call inspect directly, but reaches it via run.
# Since check_output invokes run and run invokes _Utils, check_output indirectly relies on _Utils.
# check_output -> run -> _isnotsuite: check_output has a transitive dependency on _isnotsuite through run.
# check_output calls run, which calls CalledProcessError -- so check_output transitively depends on CalledProcessError.
# Note: check_output does not call application directly, but reaches it via run.
# check_output calls run, which calls source_synopsis -- so check_output transitively depends on source_synopsis.
# Note: check_output does not call _wrap_awaitable directly, but reaches it via run.
# Since check_output invokes run and run invokes test, check_output indirectly relies on test.
# check_output calls run, which calls onerror -- so check_output transitively depends on onerror.
# check_output calls run, which calls uncancel -- so check_output transitively depends on uncancel.
# Note: check_output does not call format_exception directly, but reaches it via run.
# Since check_output invokes run and run invokes registerResult, check_output indirectly relies on registerResult.
# check_output -> run -> pop: check_output has a transitive dependency on pop through run.
# check_output calls run, which calls delayfunc -- so check_output transitively depends on delayfunc.
# Note: check_output does not call fn directly, but reaches it via run.
# getstatusoutput -> check_output -> _ellipsis_match: getstatusoutput has a transitive dependency on _ellipsis_match through check_output.
# getstatusoutput calls check_output, which calls run -- so getstatusoutput transitively depends on run.
# getoutput calls getstatusoutput, which calls check_output -- so getoutput transitively depends on check_output.
# Note: _safe_realpath does not call _getfinalpathname_nonstrict directly, but reaches it via realpath.
# Note: _safe_realpath does not call join directly, but reaches it via realpath.
# _safe_realpath -> realpath -> getcwd: _safe_realpath has a transitive dependency on getcwd through realpath.
# Since _safe_realpath invokes realpath and realpath invokes isabs, _safe_realpath indirectly relies on isabs.
# Since _safe_realpath invokes realpath and realpath invokes normcase, _safe_realpath indirectly relies on normcase.
# Since _safe_realpath invokes realpath and realpath invokes normpath, _safe_realpath indirectly relies on normpath.
# _safe_realpath calls realpath, which calls _getfinalpathname -- so _safe_realpath transitively depends on _getfinalpathname.
# _safe_realpath calls realpath, which calls readlink -- so _safe_realpath transitively depends on readlink.
# Since _safe_realpath invokes realpath and realpath invokes abspath, _safe_realpath indirectly relies on abspath.
# _safe_realpath -> realpath -> lstat: _safe_realpath has a transitive dependency on lstat through realpath.
# _expand_vars -> get_config_vars -> _init_config_vars: _expand_vars has a transitive dependency on _init_config_vars through get_config_vars.
# Note: get_default_scheme does not call _get_preferred_schemes directly, but reaches it via get_preferred_scheme.
# Since get_makefile_filename invokes get_path and get_path invokes get_default_scheme, get_makefile_filename indirectly relies on get_default_scheme.
# Note: get_makefile_filename does not call get_paths directly, but reaches it via get_path.
# Since _installation_is_relocated invokes _get_sysconfigdata and _get_sysconfigdata invokes _import_from_directory, _installation_is_relocated indirectly relies on _import_from_directory.
# Since _installation_is_relocated invokes _get_sysconfigdata and _get_sysconfigdata invokes _get_sysconfigdata_name, _installation_is_relocated indirectly relies on _get_sysconfigdata_name.
# _init_posix -> _get_sysconfigdata -> _import_from_directory: _init_posix has a transitive dependency on _import_from_directory through _get_sysconfigdata.
# Since _init_posix invokes _get_sysconfigdata and _get_sysconfigdata invokes _get_sysconfigdata_name, _init_posix indirectly relies on _get_sysconfigdata_name.
# _init_non_posix calls get_config_var, which calls get_config_vars -- so _init_non_posix transitively depends on get_config_vars.
# _init_non_posix -> get_path -> get_default_scheme: _init_non_posix has a transitive dependency on get_default_scheme through get_path.
# Since _init_non_posix invokes get_path and get_path invokes get_paths, _init_non_posix indirectly relies on get_paths.
# _init_non_posix calls _safe_realpath, which calls realpath -- so _init_non_posix transitively depends on realpath.
# get_config_h_filename -> get_path -> get_default_scheme: get_config_h_filename has a transitive dependency on get_default_scheme through get_path.
# Since get_config_h_filename invokes get_path and get_path invokes get_paths, get_config_h_filename indirectly relies on get_paths.
# get_paths calls get_default_scheme, which calls get_preferred_scheme -- so get_paths transitively depends on get_preferred_scheme.
# get_paths calls _expand_vars, which calls get_config_vars -- so get_paths transitively depends on get_config_vars.
# Note: get_paths does not call _subst_vars directly, but reaches it via _expand_vars.
# Note: get_paths does not call _extend_dict directly, but reaches it via _expand_vars.
# get_path -> get_default_scheme -> get_preferred_scheme: get_path has a transitive dependency on get_preferred_scheme through get_default_scheme.
# get_path -> get_paths -> get_default_scheme: get_path has a transitive dependency on get_default_scheme through get_paths.
# get_path calls get_paths, which calls _expand_vars -- so get_path transitively depends on _expand_vars.
# Since _init_config_vars invokes _init_posix and _init_posix invokes _get_sysconfigdata, _init_config_vars indirectly relies on _get_sysconfigdata.
# _init_config_vars calls _getuserbase, which calls _get_implementation -- so _init_config_vars transitively depends on _get_implementation.
# Note: _init_config_vars does not call joinuser directly, but reaches it via _getuserbase.
# _init_config_vars -> _init_non_posix -> get_config_var: _init_config_vars has a transitive dependency on get_config_var through _init_non_posix.
# _init_config_vars -> _init_non_posix -> get_path: _init_config_vars has a transitive dependency on get_path through _init_non_posix.
# Since _init_config_vars invokes _init_non_posix and _init_non_posix invokes _safe_realpath, _init_config_vars indirectly relies on _safe_realpath.
# _init_config_vars -> get_makefile_filename -> get_path: _init_config_vars has a transitive dependency on get_path through get_makefile_filename.
# _init_config_vars -> _safe_realpath -> realpath: _init_config_vars has a transitive dependency on realpath through _safe_realpath.
# get_config_vars calls _init_config_vars, which calls _init_posix -- so get_config_vars transitively depends on _init_posix.
# Note: get_config_vars does not call _get_implementation directly, but reaches it via _init_config_vars.
# Since get_config_vars invokes _init_config_vars and _init_config_vars invokes _getuserbase, get_config_vars indirectly relies on _getuserbase.
# get_config_vars calls _init_config_vars, which calls _init_non_posix -- so get_config_vars transitively depends on _init_non_posix.
# get_config_vars calls _init_config_vars, which calls get_makefile_filename -- so get_config_vars transitively depends on get_makefile_filename.
# Note: get_config_vars does not call _safe_realpath directly, but reaches it via _init_config_vars.
# get_config_var -> get_config_vars -> _init_config_vars: get_config_var has a transitive dependency on _init_config_vars through get_config_vars.
# get_platform -> get_config_vars -> _init_config_vars: get_platform has a transitive dependency on _init_config_vars through get_config_vars.
# get_platform -> aix_platform -> _aix_tag: get_platform has a transitive dependency on _aix_tag through aix_platform.
# Note: get_platform does not call _aix_bos_rte directly, but reaches it via aix_platform.
# get_platform calls aix_platform, which calls _aix_vrtl -- so get_platform transitively depends on _aix_vrtl.
# get_platform -> get_config_var -> get_config_vars: get_platform has a transitive dependency on get_config_vars through get_config_var.
# _get_python_version_abi -> get_config_var -> get_config_vars: _get_python_version_abi has a transitive dependency on get_config_vars through get_config_var.
# Since _get_pybuilddir invokes get_config_var and get_config_var invokes get_config_vars, _get_pybuilddir indirectly relies on get_config_vars.
# _get_pybuilddir -> get_platform -> get_config_vars: _get_pybuilddir has a transitive dependency on get_config_vars through get_platform.
# _get_pybuilddir -> get_platform -> aix_platform: _get_pybuilddir has a transitive dependency on aix_platform through get_platform.
# _get_pybuilddir -> get_platform -> get_config_var: _get_pybuilddir has a transitive dependency on get_config_var through get_platform.
# Note: _generate_posix_vars does not call get_path directly, but reaches it via get_makefile_filename.
# Since _generate_posix_vars invokes get_config_vars and get_config_vars invokes _init_config_vars, _generate_posix_vars indirectly relies on _init_config_vars.
# _generate_posix_vars calls _get_pybuilddir, which calls get_config_var -- so _generate_posix_vars transitively depends on get_config_var.
# Note: _generate_posix_vars does not call get_python_version directly, but reaches it via _get_pybuilddir.
# _generate_posix_vars -> _get_pybuilddir -> get_platform: _generate_posix_vars has a transitive dependency on get_platform through _get_pybuilddir.
# Note: _generate_posix_vars does not call get_path directly, but reaches it via get_config_h_filename.
# _generate_posix_vars calls _get_json_data_name, which calls _get_sysconfigdata_name -- so _generate_posix_vars transitively depends on _get_sysconfigdata_name.
# process_tokens calls _process_tokens, which calls format_witnesses -- so process_tokens transitively depends on format_witnesses.
# process_tokens -> _process_tokens -> Whitespace: process_tokens has a transitive dependency on Whitespace through _process_tokens.
# process_tokens calls _process_tokens, which calls NannyNag -- so process_tokens transitively depends on NannyNag.
# Note: tar_filter does not call LinkOutsideDestinationError directly, but reaches it via _get_filtered_attrs.
# Since tar_filter invokes _get_filtered_attrs and _get_filtered_attrs invokes AbsoluteLinkError, tar_filter indirectly relies on AbsoluteLinkError.
# tar_filter calls _get_filtered_attrs, which calls SpecialFileError -- so tar_filter transitively depends on SpecialFileError.
# Since tar_filter invokes _get_filtered_attrs and _get_filtered_attrs invokes AbsolutePathError, tar_filter indirectly relies on AbsolutePathError.
# tar_filter calls _get_filtered_attrs, which calls OutsideDestinationError -- so tar_filter transitively depends on OutsideDestinationError.
# data_filter -> _get_filtered_attrs -> LinkOutsideDestinationError: data_filter has a transitive dependency on LinkOutsideDestinationError through _get_filtered_attrs.
# Since data_filter invokes _get_filtered_attrs and _get_filtered_attrs invokes AbsoluteLinkError, data_filter indirectly relies on AbsoluteLinkError.
# data_filter -> _get_filtered_attrs -> SpecialFileError: data_filter has a transitive dependency on SpecialFileError through _get_filtered_attrs.
# Since data_filter invokes _get_filtered_attrs and _get_filtered_attrs invokes AbsolutePathError, data_filter indirectly relies on AbsolutePathError.
# data_filter calls _get_filtered_attrs, which calls OutsideDestinationError -- so data_filter transitively depends on OutsideDestinationError.
# Since _sanitize_params invokes gettempdir and gettempdir invokes _gettempdir, _sanitize_params indirectly relies on _gettempdir.
# Note: _sanitize_params does not call _gettempdir directly, but reaches it via gettempdirb.
# _mkstemp_inner -> _get_candidate_names -> _RandomNameSequence: _mkstemp_inner has a transitive dependency on _RandomNameSequence through _get_candidate_names.
# _dont_follow_symlinks -> func -> getter: _dont_follow_symlinks has a transitive dependency on getter through func.
# _resetperms calls _dont_follow_symlinks, which calls func -- so _resetperms transitively depends on func.
# _gettempdir calls _get_default_tempdir, which calls _candidate_tempdir_list -- so _gettempdir transitively depends on _candidate_tempdir_list.
# Note: _gettempdir does not call _RandomNameSequence directly, but reaches it via _get_default_tempdir.
# gettempdir -> _gettempdir -> _get_default_tempdir: gettempdir has a transitive dependency on _get_default_tempdir through _gettempdir.
# Note: gettempdirb does not call _get_default_tempdir directly, but reaches it via _gettempdir.
# Since mkstemp invokes _sanitize_params and _sanitize_params invokes _infer_return_type, mkstemp indirectly relies on _infer_return_type.
# Since mkstemp invokes _sanitize_params and _sanitize_params invokes gettempdir, mkstemp indirectly relies on gettempdir.
# Note: mkstemp does not call gettempdirb directly, but reaches it via _sanitize_params.
# mkstemp calls _sanitize_params, which calls output_type -- so mkstemp transitively depends on output_type.
# Since mkstemp invokes _mkstemp_inner and _mkstemp_inner invokes _get_candidate_names, mkstemp indirectly relies on _get_candidate_names.
# mkdtemp calls _sanitize_params, which calls _infer_return_type -- so mkdtemp transitively depends on _infer_return_type.
# Since mkdtemp invokes _sanitize_params and _sanitize_params invokes gettempdir, mkdtemp indirectly relies on gettempdir.
# mkdtemp calls _sanitize_params, which calls gettempdirb -- so mkdtemp transitively depends on gettempdirb.
# mkdtemp calls _sanitize_params, which calls output_type -- so mkdtemp transitively depends on output_type.
# mkdtemp -> _get_candidate_names -> _RandomNameSequence: mkdtemp has a transitive dependency on _RandomNameSequence through _get_candidate_names.
# mktemp calls gettempdir, which calls _gettempdir -- so mktemp transitively depends on _gettempdir.
# Since mktemp invokes _get_candidate_names and _get_candidate_names invokes _RandomNameSequence, mktemp indirectly relies on _RandomNameSequence.
# Since NamedTemporaryFile invokes _sanitize_params and _sanitize_params invokes _infer_return_type, NamedTemporaryFile indirectly relies on _infer_return_type.
# Note: NamedTemporaryFile does not call gettempdir directly, but reaches it via _sanitize_params.
# NamedTemporaryFile -> _sanitize_params -> gettempdirb: NamedTemporaryFile has a transitive dependency on gettempdirb through _sanitize_params.
# NamedTemporaryFile -> _sanitize_params -> output_type: NamedTemporaryFile has a transitive dependency on output_type through _sanitize_params.
# NamedTemporaryFile -> _mkstemp_inner -> _get_candidate_names: NamedTemporaryFile has a transitive dependency on _get_candidate_names through _mkstemp_inner.
# wrap -> _process_class -> hash_action: wrap has a transitive dependency on hash_action through _process_class.
# Note: wrap does not call _init_fn directly, but reaches it via _process_class.
# wrap calls _process_class, which calls _is_kw_only -- so wrap transitively depends on _is_kw_only.
# Since wrap invokes _process_class and _process_class invokes _set_new_attribute, wrap indirectly relies on _set_new_attribute.
# Since wrap invokes _process_class and _process_class invokes _DataclassParams, wrap indirectly relies on _DataclassParams.
# Since wrap invokes _process_class and _process_class invokes _fields_in_init_order, wrap indirectly relies on _fields_in_init_order.
# Since wrap invokes _process_class and _process_class invokes _get_field, wrap indirectly relies on _get_field.
# wrap calls _process_class, which calls _tuple_str -- so wrap transitively depends on _tuple_str.
# Since wrap invokes _process_class and _process_class invokes _FuncBuilder, wrap indirectly relies on _FuncBuilder.
# wrap calls _process_class, which calls _add_slots -- so wrap transitively depends on _add_slots.
# wrap calls _process_class, which calls _is_type -- so wrap transitively depends on _is_type.
# wrap calls _process_class, which calls _frozen_set_del_attr -- so wrap transitively depends on _frozen_set_del_attr.
# Note: _make_invoke_excepthook does not call _ExceptHookArgs directly, but reaches it via ExceptHookArgs.
# Note: currentThread does not call get_ident directly, but reaches it via current_thread.
# Note: currentThread does not call _DummyThread directly, but reaches it via current_thread.
# Note: _register_atexit does not call getter directly, but reaches it via func.
# _after_fork calls debug, which calls debug_script -- so _after_fork transitively depends on debug_script.
# _after_fork -> debug -> testsource: _after_fork has a transitive dependency on testsource through debug.
# Since _after_fork invokes debug and debug invokes _normalize_module, _after_fork indirectly relies on _normalize_module.
# Since _after_fork invokes debug and debug invokes _DebugResult, _after_fork indirectly relies on _DebugResult.
# _after_fork -> debug -> basicConfig: _after_fork has a transitive dependency on basicConfig through debug.
# _after_fork -> debug -> DebugRunner: _after_fork has a transitive dependency on DebugRunner through debug.
# Note: _after_fork does not call SkipTest directly, but reaches it via debug.
# _after_fork calls RLock, which calls _CRLock -- so _after_fork transitively depends on _CRLock.
# Note: _after_fork does not call _PyRLock directly, but reaches it via RLock.
# Since any invokes group and group invokes UnsupportedOperation, any indirectly relies on UnsupportedOperation.
# Note: maybe does not call UnsupportedOperation directly, but reaches it via group.
# maybe -> push -> trace: maybe has a transitive dependency on trace through push.
# maybe -> push -> Event: maybe has a transitive dependency on Event through push.
# tokenize calls _generate_tokens_from_c_tokenizer, which calls TokenError -- so tokenize transitively depends on TokenError.
# tokenize -> _generate_tokens_from_c_tokenizer -> _transform_msg: tokenize has a transitive dependency on _transform_msg through _generate_tokens_from_c_tokenizer.
# tokenize -> detect_encoding -> _get_normal_name: tokenize has a transitive dependency on _get_normal_name through detect_encoding.
# tokenize -> detect_encoding -> find_cookie: tokenize has a transitive dependency on find_cookie through detect_encoding.
# Note: tokenize does not call read_or_stop directly, but reaches it via detect_encoding.
# Note: tokenize does not call check directly, but reaches it via detect_encoding.
# tokenize calls detect_encoding, which calls bstartswith -- so tokenize transitively depends on bstartswith.
# tokenize -> detect_encoding -> readline: tokenize has a transitive dependency on readline through detect_encoding.
# tokenize -> detect_encoding -> lookup: tokenize has a transitive dependency on lookup through detect_encoding.
# generate_tokens calls _generate_tokens_from_c_tokenizer, which calls TokenError -- so generate_tokens transitively depends on TokenError.
# Note: generate_tokens does not call _transform_msg directly, but reaches it via _generate_tokens_from_c_tokenizer.
# skip_comment -> skip_until -> TOMLDecodeError: skip_comment has a transitive dependency on TOMLDecodeError through skip_until.
# skip_comments_and_array_ws calls skip_comment, which calls skip_until -- so skip_comments_and_array_ws transitively depends on skip_until.
# create_dict_rule calls parse_key, which calls parse_key_part -- so create_dict_rule transitively depends on parse_key_part.
# create_dict_rule -> parse_key -> skip_chars: create_dict_rule has a transitive dependency on skip_chars through parse_key.
# create_list_rule calls parse_key, which calls parse_key_part -- so create_list_rule transitively depends on parse_key_part.
# Since create_list_rule invokes parse_key and parse_key invokes skip_chars, create_list_rule indirectly relies on skip_chars.
# key_value_rule calls parse_key_value_pair, which calls TOMLDecodeError -- so key_value_rule transitively depends on TOMLDecodeError.
# key_value_rule calls parse_key_value_pair, which calls parse_value -- so key_value_rule transitively depends on parse_value.
# key_value_rule calls parse_key_value_pair, which calls parse_key -- so key_value_rule transitively depends on parse_key.
# key_value_rule -> parse_key_value_pair -> skip_chars: key_value_rule has a transitive dependency on skip_chars through parse_key_value_pair.
# Note: parse_key_value_pair does not call parse_array directly, but reaches it via parse_value.
# parse_key_value_pair calls parse_value, which calls parse_inline_table -- so parse_key_value_pair transitively depends on parse_inline_table.
# parse_key_value_pair calls parse_value, which calls TOMLDecodeError -- so parse_key_value_pair transitively depends on TOMLDecodeError.
# Note: parse_key_value_pair does not call match_to_datetime directly, but reaches it via parse_value.
# Since parse_key_value_pair invokes parse_value and parse_value invokes match_to_localtime, parse_key_value_pair indirectly relies on match_to_localtime.
# parse_key_value_pair -> parse_value -> parse_literal_str: parse_key_value_pair has a transitive dependency on parse_literal_str through parse_value.
# parse_key_value_pair -> parse_value -> match_to_number: parse_key_value_pair has a transitive dependency on match_to_number through parse_value.
# parse_key_value_pair -> parse_value -> parse_one_line_basic_str: parse_key_value_pair has a transitive dependency on parse_one_line_basic_str through parse_value.
# Since parse_key_value_pair invokes parse_value and parse_value invokes parse_multiline_str, parse_key_value_pair indirectly relies on parse_multiline_str.
# Note: parse_key_value_pair does not call parse_float directly, but reaches it via parse_value.
# Note: parse_key_value_pair does not call parse_key_part directly, but reaches it via parse_key.
# Note: parse_key_value_pair does not call skip_chars directly, but reaches it via parse_key.
# parse_key -> parse_key_part -> parse_one_line_basic_str: parse_key has a transitive dependency on parse_one_line_basic_str through parse_key_part.
# parse_key -> parse_key_part -> parse_literal_str: parse_key has a transitive dependency on parse_literal_str through parse_key_part.
# parse_key calls parse_key_part, which calls TOMLDecodeError -- so parse_key transitively depends on TOMLDecodeError.
# parse_key -> parse_key_part -> skip_chars: parse_key has a transitive dependency on skip_chars through parse_key_part.
# parse_key_part calls parse_one_line_basic_str, which calls parse_basic_str -- so parse_key_part transitively depends on parse_basic_str.
# parse_key_part -> parse_literal_str -> skip_until: parse_key_part has a transitive dependency on skip_until through parse_literal_str.
# parse_one_line_basic_str calls parse_basic_str, which calls parse_escapes -- so parse_one_line_basic_str transitively depends on parse_escapes.
# Since parse_one_line_basic_str invokes parse_basic_str and parse_basic_str invokes TOMLDecodeError, parse_one_line_basic_str indirectly relies on TOMLDecodeError.
# Note: parse_array does not call parse_inline_table directly, but reaches it via parse_value.
# Note: parse_array does not call TOMLDecodeError directly, but reaches it via parse_value.
# parse_array -> parse_value -> match_to_datetime: parse_array has a transitive dependency on match_to_datetime through parse_value.
# parse_array -> parse_value -> match_to_localtime: parse_array has a transitive dependency on match_to_localtime through parse_value.
# parse_array -> parse_value -> parse_literal_str: parse_array has a transitive dependency on parse_literal_str through parse_value.
# Since parse_array invokes parse_value and parse_value invokes match_to_number, parse_array indirectly relies on match_to_number.
# Since parse_array invokes parse_value and parse_value invokes parse_one_line_basic_str, parse_array indirectly relies on parse_one_line_basic_str.
# Note: parse_array does not call parse_multiline_str directly, but reaches it via parse_value.
# Note: parse_array does not call parse_float directly, but reaches it via parse_value.
# parse_array -> skip_comments_and_array_ws -> skip_comment: parse_array has a transitive dependency on skip_comment through skip_comments_and_array_ws.
# Note: parse_array does not call skip_chars directly, but reaches it via skip_comments_and_array_ws.
# parse_inline_table calls parse_key_value_pair, which calls TOMLDecodeError -- so parse_inline_table transitively depends on TOMLDecodeError.
# parse_inline_table calls parse_key_value_pair, which calls parse_value -- so parse_inline_table transitively depends on parse_value.
# Since parse_inline_table invokes parse_key_value_pair and parse_key_value_pair invokes parse_key, parse_inline_table indirectly relies on parse_key.
# parse_inline_table calls parse_key_value_pair, which calls skip_chars -- so parse_inline_table transitively depends on skip_chars.
# parse_basic_str_escape -> parse_hex_char -> is_unicode_scalar_value: parse_basic_str_escape has a transitive dependency on is_unicode_scalar_value through parse_hex_char.
# parse_basic_str_escape -> parse_hex_char -> TOMLDecodeError: parse_basic_str_escape has a transitive dependency on TOMLDecodeError through parse_hex_char.
# parse_basic_str_escape_multiline -> parse_basic_str_escape -> parse_hex_char: parse_basic_str_escape_multiline has a transitive dependency on parse_hex_char through parse_basic_str_escape.
# parse_basic_str_escape_multiline -> parse_basic_str_escape -> TOMLDecodeError: parse_basic_str_escape_multiline has a transitive dependency on TOMLDecodeError through parse_basic_str_escape.
# parse_basic_str_escape_multiline calls parse_basic_str_escape, which calls skip_chars -- so parse_basic_str_escape_multiline transitively depends on skip_chars.
# Note: parse_literal_str does not call TOMLDecodeError directly, but reaches it via skip_until.
# parse_multiline_str -> parse_basic_str -> parse_escapes: parse_multiline_str has a transitive dependency on parse_escapes through parse_basic_str.
# Since parse_multiline_str invokes parse_basic_str and parse_basic_str invokes TOMLDecodeError, parse_multiline_str indirectly relies on TOMLDecodeError.
# parse_multiline_str -> skip_until -> TOMLDecodeError: parse_multiline_str has a transitive dependency on TOMLDecodeError through skip_until.
# Note: parse_value does not call skip_comments_and_array_ws directly, but reaches it via parse_array.
# parse_value calls parse_array, which calls TOMLDecodeError -- so parse_value transitively depends on TOMLDecodeError.
# parse_value -> parse_inline_table -> NestedDict: parse_value has a transitive dependency on NestedDict through parse_inline_table.
# parse_value -> parse_inline_table -> parse_key_value_pair: parse_value has a transitive dependency on parse_key_value_pair through parse_inline_table.
# parse_value -> parse_inline_table -> Flags: parse_value has a transitive dependency on Flags through parse_inline_table.
# parse_value -> parse_inline_table -> TOMLDecodeError: parse_value has a transitive dependency on TOMLDecodeError through parse_inline_table.
# parse_value calls parse_inline_table, which calls skip_chars -- so parse_value transitively depends on skip_chars.
# parse_value -> match_to_datetime -> cached_tz: parse_value has a transitive dependency on cached_tz through match_to_datetime.
# parse_value calls match_to_datetime, which calls date -- so parse_value transitively depends on date.
# Note: parse_value does not call datetime directly, but reaches it via match_to_datetime.
# Since parse_value invokes match_to_localtime and match_to_localtime invokes time, parse_value indirectly relies on time.
# parse_value -> parse_literal_str -> skip_until: parse_value has a transitive dependency on skip_until through parse_literal_str.
# parse_value -> match_to_number -> parse_float: parse_value has a transitive dependency on parse_float through match_to_number.
# parse_value -> parse_one_line_basic_str -> parse_basic_str: parse_value has a transitive dependency on parse_basic_str through parse_one_line_basic_str.
# Note: parse_value does not call parse_basic_str directly, but reaches it via parse_multiline_str.
# parse_value calls parse_multiline_str, which calls skip_until -- so parse_value transitively depends on skip_until.
# Note: match_to_datetime does not call lru_cache directly, but reaches it via cached_tz.
# Since match_to_datetime invokes cached_tz and cached_tz invokes timedelta, match_to_datetime indirectly relies on timedelta.
# Since match_to_datetime invokes cached_tz and cached_tz invokes timezone, match_to_datetime indirectly relies on timezone.
# cached_tz -> lru_cache -> update_wrapper: cached_tz has a transitive dependency on update_wrapper through lru_cache.
# Since cached_tz invokes lru_cache and lru_cache invokes _lru_cache_wrapper, cached_tz indirectly relies on _lru_cache_wrapper.
# _find_executable_linenos calls _find_lines, which calls _find_lines_from_code -- so _find_executable_linenos transitively depends on _find_lines_from_code.
# print_tb calls extract_tb, which calls _walk_tb_with_full_positions -- so print_tb transitively depends on _walk_tb_with_full_positions.
# Note: format_tb does not call _walk_tb_with_full_positions directly, but reaches it via extract_tb.
# Note: extract_tb does not call _get_code_position directly, but reaches it via _walk_tb_with_full_positions.
# Note: _print_exception_bltin does not call TracebackException directly, but reaches it via print_exception.
# _print_exception_bltin calls print_exception, which calls _parse_value_tb -- so _print_exception_bltin transitively depends on _parse_value_tb.
# format_exception_only -> _safe_string -> func: format_exception_only has a transitive dependency on func through _safe_string.
# format_exception_only calls _format_final_exc_line, which calls _safe_string -- so format_exception_only transitively depends on _safe_string.
# _format_final_exc_line calls _safe_string, which calls func -- so _format_final_exc_line transitively depends on func.
# Note: _safe_string does not call getter directly, but reaches it via func.
# print_exc -> print_exception -> TracebackException: print_exc has a transitive dependency on TracebackException through print_exception.
# Since print_exc invokes print_exception and print_exception invokes _parse_value_tb, print_exc indirectly relies on _parse_value_tb.
# Since format_exc invokes format_exception and format_exception invokes TracebackException, format_exc indirectly relies on TracebackException.
# Note: format_exc does not call _parse_value_tb directly, but reaches it via format_exception.
# print_last calls print_exception, which calls TracebackException -- so print_last transitively depends on TracebackException.
# Note: print_last does not call _parse_value_tb directly, but reaches it via print_exception.
# print_stack calls extract_stack, which calls walk_stack -- so print_stack transitively depends on walk_stack.
# format_stack -> extract_stack -> walk_stack: format_stack has a transitive dependency on walk_stack through extract_stack.
# extract_stack calls walk_stack, which calls walk_stack_generator -- so extract_stack transitively depends on walk_stack_generator.
# Since _extract_caret_anchors_from_line_segment invokes nextline and nextline invokes next_valid_char, _extract_caret_anchors_from_line_segment indirectly relies on next_valid_char.
# _extract_caret_anchors_from_line_segment calls increment, which calls next_valid_char -- so _extract_caret_anchors_from_line_segment transitively depends on next_valid_char.
# _extract_caret_anchors_from_line_segment calls increment_until, which calls stop -- so _extract_caret_anchors_from_line_segment transitively depends on stop.
# _extract_caret_anchors_from_line_segment -> increment_until -> nextline: _extract_caret_anchors_from_line_segment has a transitive dependency on nextline through increment_until.
# Note: _extract_caret_anchors_from_line_segment does not call increment directly, but reaches it via increment_until.
# _extract_caret_anchors_from_line_segment -> setup_positions -> next_valid_char: _extract_caret_anchors_from_line_segment has a transitive dependency on next_valid_char through setup_positions.
# _extract_caret_anchors_from_line_segment calls setup_positions, which calls normalize -- so _extract_caret_anchors_from_line_segment transitively depends on normalize.
# _extract_caret_anchors_from_line_segment -> stop -> Client: _extract_caret_anchors_from_line_segment has a transitive dependency on Client through stop.
# Note: _extract_caret_anchors_from_line_segment does not call close directly, but reaches it via stop.
# _extract_caret_anchors_from_line_segment -> normalize -> _dec_from_triple: _extract_caret_anchors_from_line_segment has a transitive dependency on _dec_from_triple through normalize.
# _extract_caret_anchors_from_line_segment -> normalize -> escape: _extract_caret_anchors_from_line_segment has a transitive dependency on escape through normalize.
# _extract_caret_anchors_from_line_segment -> normalize -> _byte_offset_to_character_offset: _extract_caret_anchors_from_line_segment has a transitive dependency on _byte_offset_to_character_offset through normalize.
# Since _extract_caret_anchors_from_line_segment invokes normalize and normalize invokes _append_modifier, _extract_caret_anchors_from_line_segment indirectly relies on _append_modifier.
# _extract_caret_anchors_from_line_segment calls normalize, which calls _convert_other -- so _extract_caret_anchors_from_line_segment transitively depends on _convert_other.
# Since _extract_caret_anchors_from_line_segment invokes normalize and normalize invokes getcontext, _extract_caret_anchors_from_line_segment indirectly relies on getcontext.
# Note: _extract_caret_anchors_from_line_segment does not call _replace_encoding directly, but reaches it via normalize.
# _compute_suggestion_error calls _levenshtein_distance, which calls _substitution_cost -- so _compute_suggestion_error transitively depends on _substitution_cost.
# Note: setraw does not call TermState directly, but reaches it via tcgetattr.
# setcbreak -> tcgetattr -> TermState: setcbreak has a transitive dependency on TermState through tcgetattr.
# readconfig calls split, which calls _compile -- so readconfig transitively depends on _compile.
# Since readconfig invokes split and split invokes splitroot, readconfig indirectly relies on splitroot.
# readconfig calls split, which calls _get_bothseps -- so readconfig transitively depends on _get_bothseps.
# Since readconfig invokes split and split invokes shlex, readconfig indirectly relies on shlex.
# readconfig calls split, which calls _get_sep -- so readconfig transitively depends on _get_sep.
# readconfig -> join -> wait: readconfig has a transitive dependency on wait through join.
# Since readconfig invokes join and join invokes splitroot, readconfig indirectly relies on splitroot.
# Since readconfig invokes join and join invokes current_thread, readconfig indirectly relies on current_thread.
# Note: readconfig does not call _get_sep directly, but reaches it via join.
# Note: readconfig does not call quote directly, but reaches it via join.
# Note: __forwardmethods does not call __methodDict directly, but reaches it via __methods.
# Since new_class invokes prepare_class and prepare_class invokes _calculate_meta, new_class indirectly relies on _calculate_meta.
# Note: coroutine does not call getter directly, but reaches it via func.
# _type_check -> _type_convert -> _make_forward_ref: _type_check has a transitive dependency on _make_forward_ref through _type_convert.
# Since _tp_cache invokes func and func invokes getter, _tp_cache indirectly relies on getter.
# _tp_cache calls decorator, which calls SkipTest -- so _tp_cache transitively depends on SkipTest.
# _tp_cache -> decorator -> func: _tp_cache has a transitive dependency on func through decorator.
# _eval_type -> evaluate_forward_ref -> _make_forward_ref: _eval_type has a transitive dependency on _make_forward_ref through evaluate_forward_ref.
# _eval_type -> _should_unflatten_callable_args -> _is_param_expr: _eval_type has a transitive dependency on _is_param_expr through _should_unflatten_callable_args.
# ClassVar -> _type_check -> _type_convert: ClassVar has a transitive dependency on _type_convert through _type_check.
# Since Final invokes _type_check and _type_check invokes _type_convert, Final indirectly relies on _type_convert.
# Note: Optional does not call _type_convert directly, but reaches it via _type_check.
# Literal calls _tp_cache, which calls func -- so Literal transitively depends on func.
# Literal calls _tp_cache, which calls decorator -- so Literal transitively depends on decorator.
# Concatenate calls _type_check, which calls _type_convert -- so Concatenate transitively depends on _type_convert.
# Since TypeGuard invokes _type_check and _type_check invokes _type_convert, TypeGuard indirectly relies on _type_convert.
# TypeIs -> _type_check -> _type_convert: TypeIs has a transitive dependency on _type_convert through _type_check.
# Since evaluate_forward_ref invokes _eval_type and _eval_type invokes GenericAlias, evaluate_forward_ref indirectly relies on GenericAlias.
# evaluate_forward_ref calls _eval_type, which calls _make_forward_ref -- so evaluate_forward_ref transitively depends on _make_forward_ref.
# evaluate_forward_ref calls _eval_type, which calls _deprecation_warning_for_no_type_params_passed -- so evaluate_forward_ref transitively depends on _deprecation_warning_for_no_type_params_passed.
# Since evaluate_forward_ref invokes _eval_type and _eval_type invokes _should_unflatten_callable_args, evaluate_forward_ref indirectly relies on _should_unflatten_callable_args.
# Since _typevar_subst invokes _type_check and _type_check invokes _type_convert, _typevar_subst indirectly relies on _type_convert.
# _paramspec_subst calls _type_check, which calls _type_convert -- so _paramspec_subst transitively depends on _type_convert.
# Since _generic_class_getitem invokes _type_convert and _type_convert invokes _make_forward_ref, _generic_class_getitem indirectly relies on _make_forward_ref.
# Since _generic_class_getitem invokes _is_typevar_like and _is_typevar_like invokes _is_unpacked_typevartuple, _generic_class_getitem indirectly relies on _is_unpacked_typevartuple.
# Since _generic_class_getitem invokes prepare and prepare invokes CycleError, _generic_class_getitem indirectly relies on CycleError.
# Note: _generic_class_getitem does not call trace directly, but reaches it via prepare.
# Note: _generic_class_getitem does not call resolve_task directly, but reaches it via prepare.
# _generic_class_getitem calls prepare, which calls _fixup_main_from_path -- so _generic_class_getitem transitively depends on _fixup_main_from_path.
# _generic_class_getitem -> prepare -> tcgetattr: _generic_class_getitem has a transitive dependency on tcgetattr through prepare.
# _generic_class_getitem -> prepare -> _fixup_main_from_name: _generic_class_getitem has a transitive dependency on _fixup_main_from_name through prepare.
# _generic_class_getitem -> prepare -> SetConsoleMode: _generic_class_getitem has a transitive dependency on SetConsoleMode through prepare.
# _generic_class_getitem calls prepare, which calls cls -- so _generic_class_getitem transitively depends on cls.
# Since _generic_class_getitem invokes prepare and prepare invokes set_start_method, _generic_class_getitem indirectly relies on set_start_method.
# _generic_init_subclass calls _collect_type_parameters, which calls _is_unpacked_typevartuple -- so _generic_init_subclass transitively depends on _is_unpacked_typevartuple.
# Unpack -> _type_check -> _type_convert: Unpack has a transitive dependency on _type_convert through _type_check.
# Annotated -> _tp_cache -> func: Annotated has a transitive dependency on func through _tp_cache.
# Annotated -> _tp_cache -> decorator: Annotated has a transitive dependency on decorator through _tp_cache.
# Annotated calls _type_check, which calls _type_convert -- so Annotated transitively depends on _type_convert.
# get_type_hints -> _eval_type -> evaluate_forward_ref: get_type_hints has a transitive dependency on evaluate_forward_ref through _eval_type.
# get_type_hints -> _eval_type -> GenericAlias: get_type_hints has a transitive dependency on GenericAlias through _eval_type.
# get_type_hints calls _eval_type, which calls _make_forward_ref -- so get_type_hints transitively depends on _make_forward_ref.
# Note: get_type_hints does not call _deprecation_warning_for_no_type_params_passed directly, but reaches it via _eval_type.
# Since get_type_hints invokes _eval_type and _eval_type invokes _should_unflatten_callable_args, get_type_hints indirectly relies on _should_unflatten_callable_args.
# get_type_hints -> _strip_annotations -> GenericAlias: get_type_hints has a transitive dependency on GenericAlias through _strip_annotations.
# get_args -> _should_unflatten_callable_args -> _is_param_expr: get_args has a transitive dependency on _is_param_expr through _should_unflatten_callable_args.
# Since no_type_check_decorator invokes decorator and decorator invokes SkipTest, no_type_check_decorator indirectly relies on SkipTest.
# Since no_type_check_decorator invokes decorator and decorator invokes func, no_type_check_decorator indirectly relies on func.
# Since _make_eager_annotate invokes _type_check and _type_check invokes _type_convert, _make_eager_annotate indirectly relies on _type_convert.
# Since NamedTuple invokes _make_eager_annotate and _make_eager_annotate invokes _type_check, NamedTuple indirectly relies on _type_check.
# NamedTuple calls _type_check, which calls _type_convert -- so NamedTuple transitively depends on _type_convert.
# _get_typeddict_qualifiers -> get_args -> _should_unflatten_callable_args: _get_typeddict_qualifiers has a transitive dependency on _should_unflatten_callable_args through get_args.
# Required calls _type_check, which calls _type_convert -- so Required transitively depends on _type_convert.
# Since NotRequired invokes _type_check and _type_check invokes _type_convert, NotRequired indirectly relies on _type_convert.
# ReadOnly -> _type_check -> _type_convert: ReadOnly has a transitive dependency on _type_convert through _type_check.
# enterModuleContext -> _enter_context -> enter: enterModuleContext has a transitive dependency on enter through _enter_context.
# Note: enterModuleContext does not call addcleanup directly, but reaches it via _enter_context.
# skip -> decorator -> SkipTest: skip has a transitive dependency on SkipTest through decorator.
# Note: skip does not call func directly, but reaches it via decorator.
# Since skipIf invokes skip and skip invokes decorator, skipIf indirectly relies on decorator.
# Note: skipIf does not call SkipTest directly, but reaches it via skip.
# Note: skipUnless does not call decorator directly, but reaches it via skip.
# skipUnless -> skip -> SkipTest: skipUnless has a transitive dependency on SkipTest through skip.
# _make_failed_import_test calls _make_failed_test, which calls _FailedTest -- so _make_failed_import_test transitively depends on _FailedTest.
# _make_failed_import_test -> _make_failed_test -> suiteClass: _make_failed_import_test has a transitive dependency on suiteClass through _make_failed_test.
# Since _make_failed_load_tests invokes _make_failed_test and _make_failed_test invokes _FailedTest, _make_failed_load_tests indirectly relies on _FailedTest.
# _make_failed_load_tests -> _make_failed_test -> suiteClass: _make_failed_load_tests has a transitive dependency on suiteClass through _make_failed_test.
# Since _is_async_obj invokes iscoroutinefunction and iscoroutinefunction invokes _has_code_flag, _is_async_obj indirectly relies on _has_code_flag.
# _is_async_obj -> iscoroutinefunction -> _has_coroutine_mark: _is_async_obj has a transitive dependency on _has_coroutine_mark through iscoroutinefunction.
# Note: _is_async_obj does not call _iscoroutinefunction directly, but reaches it via iscoroutinefunction.
# _is_async_func calls iscoroutinefunction, which calls _has_code_flag -- so _is_async_func transitively depends on _has_code_flag.
# _is_async_func -> iscoroutinefunction -> _has_coroutine_mark: _is_async_func has a transitive dependency on _has_coroutine_mark through iscoroutinefunction.
# _is_async_func -> iscoroutinefunction -> _iscoroutinefunction: _is_async_func has a transitive dependency on _iscoroutinefunction through iscoroutinefunction.
# _check_signature -> _get_signature_object -> partial: _check_signature has a transitive dependency on partial through _get_signature_object.
# _set_signature -> _setup_func -> _is_instance_mock: _set_signature has a transitive dependency on _is_instance_mock through _setup_func.
# Since _set_signature invokes _setup_func and _setup_func invokes _CallList, _set_signature indirectly relies on _CallList.
# Note: _set_signature does not call partial directly, but reaches it via _get_signature_object.
# Since _set_async_signature invokes _setup_func and _setup_func invokes _is_instance_mock, _set_async_signature indirectly relies on _is_instance_mock.
# Since _set_async_signature invokes _setup_func and _setup_func invokes _CallList, _set_async_signature indirectly relies on _CallList.
# Since _set_async_signature invokes _setup_async_mock and _setup_async_mock invokes partial, _set_async_signature indirectly relies on partial.
# Note: _set_async_signature does not call _CallList directly, but reaches it via _setup_async_mock.
# Note: _set_async_signature does not call partial directly, but reaches it via _get_signature_object.
# Since patch invokes _get_target and _get_target invokes partial, patch indirectly relies on partial.
# _get_method calls func, which calls getter -- so _get_method transitively depends on getter.
# create_autospec -> _is_async_func -> iscoroutinefunction: create_autospec has a transitive dependency on iscoroutinefunction through _is_async_func.
# Since create_autospec invokes _check_signature and _check_signature invokes _copy_func_details, create_autospec indirectly relies on _copy_func_details.
# create_autospec calls _check_signature, which calls _get_signature_object -- so create_autospec transitively depends on _get_signature_object.
# Note: create_autospec does not call _setup_func directly, but reaches it via _set_signature.
# Note: create_autospec does not call _copy_func_details directly, but reaches it via _set_signature.
# create_autospec calls _set_signature, which calls _get_signature_object -- so create_autospec transitively depends on _get_signature_object.
# create_autospec calls iscoroutinefunction, which calls _has_code_flag -- so create_autospec transitively depends on _has_code_flag.
# Note: create_autospec does not call _has_coroutine_mark directly, but reaches it via iscoroutinefunction.
# create_autospec -> iscoroutinefunction -> _iscoroutinefunction: create_autospec has a transitive dependency on _iscoroutinefunction through iscoroutinefunction.
# Since create_autospec invokes _set_async_signature and _set_async_signature invokes _setup_func, create_autospec indirectly relies on _setup_func.
# Note: create_autospec does not call _copy_func_details directly, but reaches it via _set_async_signature.
# create_autospec calls _set_async_signature, which calls _setup_async_mock -- so create_autospec transitively depends on _setup_async_mock.
# Since create_autospec invokes _set_async_signature and _set_async_signature invokes _get_signature_object, create_autospec indirectly relies on _get_signature_object.
# Note: mock_open does not call _iter_side_effect directly, but reaches it via _readline_side_effect.
# Note: failfast does not call func directly, but reaches it via method.
# Note: failfast does not call partial directly, but reaches it via wraps.
# Note: removeHandler does not call func directly, but reaches it via method.
# Note: removeHandler does not call partial directly, but reaches it via wraps.
# Note: _call_if_exists does not call getter directly, but reaches it via func.
# urlparse calls _urlparse, which calls _urlsplit -- so urlparse transitively depends on _urlsplit.
# urlparse -> _urlparse -> _splitparams: urlparse has a transitive dependency on _splitparams through _urlparse.
# Since urlparse invokes _coerce_args and _coerce_args invokes _decode_args, urlparse indirectly relies on _decode_args.
# Since _urlparse invokes _urlsplit and _urlsplit invokes _check_bracketed_netloc, _urlparse indirectly relies on _check_bracketed_netloc.
# _urlparse -> _urlsplit -> _checknetloc: _urlparse has a transitive dependency on _checknetloc through _urlsplit.
# _urlparse calls _urlsplit, which calls _splitnetloc -- so _urlparse transitively depends on _splitnetloc.
# Note: urlsplit does not call _check_bracketed_netloc directly, but reaches it via _urlsplit.
# Since urlsplit invokes _urlsplit and _urlsplit invokes _checknetloc, urlsplit indirectly relies on _checknetloc.
# Note: urlsplit does not call _splitnetloc directly, but reaches it via _urlsplit.
# Since urlsplit invokes _coerce_args and _coerce_args invokes _decode_args, urlsplit indirectly relies on _decode_args.
# Since _urlsplit invokes _check_bracketed_netloc and _check_bracketed_netloc invokes _check_bracketed_host, _urlsplit indirectly relies on _check_bracketed_host.
# urlunparse -> _coerce_args -> _decode_args: urlunparse has a transitive dependency on _decode_args through _coerce_args.
# urlunsplit calls _coerce_args, which calls _decode_args -- so urlunsplit transitively depends on _decode_args.
# Note: urljoin does not call _check_bracketed_netloc directly, but reaches it via _urlsplit.
# urljoin -> _urlsplit -> _checknetloc: urljoin has a transitive dependency on _checknetloc through _urlsplit.
# Note: urljoin does not call _splitnetloc directly, but reaches it via _urlsplit.
# urljoin -> _coerce_args -> _decode_args: urljoin has a transitive dependency on _decode_args through _coerce_args.
# Since urldefrag invokes _urlsplit and _urlsplit invokes _check_bracketed_netloc, urldefrag indirectly relies on _check_bracketed_netloc.
# urldefrag -> _urlsplit -> _checknetloc: urldefrag has a transitive dependency on _checknetloc through _urlsplit.
# urldefrag calls _urlsplit, which calls _splitnetloc -- so urldefrag transitively depends on _splitnetloc.
# Note: urldefrag does not call _decode_args directly, but reaches it via _coerce_args.
# Since unquote_to_bytes invokes _unquote_impl and _unquote_impl invokes append, unquote_to_bytes indirectly relies on append.
# _unquote_impl -> append -> Time2Internaldate: _unquote_impl has a transitive dependency on Time2Internaldate through append.
# Since _unquote_impl invokes append and append invokes Charset, _unquote_impl indirectly relies on Charset.
# _generate_unquoted_parts -> _unquote_impl -> append: _generate_unquoted_parts has a transitive dependency on append through _unquote_impl.
# parse_qs calls parse_qsl, which calls _unquote -- so parse_qs transitively depends on _unquote.
# parse_qs -> parse_qsl -> unquote_plus: parse_qs has a transitive dependency on unquote_plus through parse_qsl.
# Note: parse_qs does not call unquote_to_bytes directly, but reaches it via parse_qsl.
# Since parse_qsl invokes _unquote and _unquote invokes unquote_plus, parse_qsl indirectly relies on unquote_plus.
# parse_qsl calls _unquote, which calls unquote_to_bytes -- so parse_qsl transitively depends on unquote_to_bytes.
# Since parse_qsl invokes _unquote and _unquote invokes _unquote_sub, parse_qsl indirectly relies on _unquote_sub.
# Note: parse_qsl does not call unquote directly, but reaches it via unquote_plus.
# Since parse_qsl invokes unquote_to_bytes and unquote_to_bytes invokes _unquote_impl, parse_qsl indirectly relies on _unquote_impl.
# unquote_plus -> unquote -> _unquote_impl: unquote_plus has a transitive dependency on _unquote_impl through unquote.
# Since unquote_plus invokes unquote and unquote invokes _generate_unquoted_parts, unquote_plus indirectly relies on _generate_unquoted_parts.
# Since quote_plus invokes quote and quote invokes quote_from_bytes, quote_plus indirectly relies on quote_from_bytes.
# Note: quote_from_bytes does not call _Quoter directly, but reaches it via _byte_quoter_factory.
# Note: urlopen does not call h directly, but reaches it via build_opener.
# urlopen -> build_opener -> OpenerDirector: urlopen has a transitive dependency on OpenerDirector through build_opener.
# Note: urlopen does not call klass directly, but reaches it via build_opener.
# Since urlretrieve invokes urlopen and urlopen invokes build_opener, urlretrieve indirectly relies on build_opener.
# urlretrieve calls urlopen, which calls HTTPSHandler -- so urlretrieve transitively depends on HTTPSHandler.
# Note: request_host does not call _urlparse directly, but reaches it via urlparse.
# Note: request_host does not call _coerce_result directly, but reaches it via urlparse.
# request_host -> urlparse -> ParseResult: request_host has a transitive dependency on ParseResult through urlparse.
# request_host -> urlparse -> _coerce_args: request_host has a transitive dependency on _coerce_args through urlparse.
# Since url2pathname invokes unquote and unquote invokes _unquote_impl, url2pathname indirectly relies on _unquote_impl.
# url2pathname -> unquote -> _generate_unquoted_parts: url2pathname has a transitive dependency on _generate_unquoted_parts through unquote.
# Note: url2pathname does not call SplitResult directly, but reaches it via urlsplit.
# url2pathname -> urlsplit -> _urlsplit: url2pathname has a transitive dependency on _urlsplit through urlsplit.
# Note: url2pathname does not call _coerce_args directly, but reaches it via urlsplit.
# Since url2pathname invokes urlsplit and urlsplit invokes _coerce_result, url2pathname indirectly relies on _coerce_result.
# Note: url2pathname does not call FileHandler directly, but reaches it via _is_local_authority.
# Since pathname2url invokes quote and quote invokes quote_from_bytes, pathname2url indirectly relies on quote_from_bytes.
# Since _proxy_bypass_macosx_sysconf invokes fnmatch and fnmatch invokes fnmatchcase, _proxy_bypass_macosx_sysconf indirectly relies on fnmatchcase.
# Note: _proxy_bypass_winreg_override does not call fnmatchcase directly, but reaches it via fnmatch.
# Note: normalize_uri does not call _dec_from_triple directly, but reaches it via normalize.
# normalize_uri calls normalize, which calls escape -- so normalize_uri transitively depends on escape.
# Note: normalize_uri does not call _byte_offset_to_character_offset directly, but reaches it via normalize.
# Since normalize_uri invokes normalize and normalize invokes _append_modifier, normalize_uri indirectly relies on _append_modifier.
# normalize_uri -> normalize -> _convert_other: normalize_uri has a transitive dependency on _convert_other through normalize.
# Note: normalize_uri does not call getcontext directly, but reaches it via normalize.
# normalize_uri calls normalize, which calls _replace_encoding -- so normalize_uri transitively depends on _replace_encoding.
# normalize_pattern -> normalize -> _dec_from_triple: normalize_pattern has a transitive dependency on _dec_from_triple through normalize.
# normalize_pattern -> normalize -> escape: normalize_pattern has a transitive dependency on escape through normalize.
# Since normalize_pattern invokes normalize and normalize invokes _byte_offset_to_character_offset, normalize_pattern indirectly relies on _byte_offset_to_character_offset.
# Since normalize_pattern invokes normalize and normalize invokes _append_modifier, normalize_pattern indirectly relies on _append_modifier.
# Since normalize_pattern invokes normalize and normalize invokes _convert_other, normalize_pattern indirectly relies on _convert_other.
# normalize_pattern -> normalize -> getcontext: normalize_pattern has a transitive dependency on getcontext through normalize.
# Since normalize_pattern invokes normalize and normalize invokes _replace_encoding, normalize_pattern indirectly relies on _replace_encoding.
# Since _ifconfig_getnode invokes _find_mac_near_keyword and _find_mac_near_keyword invokes get_word_index, _ifconfig_getnode indirectly relies on get_word_index.
# _ifconfig_getnode -> _find_mac_near_keyword -> _is_universal: _ifconfig_getnode has a transitive dependency on _is_universal through _find_mac_near_keyword.
# Since _ifconfig_getnode invokes _find_mac_near_keyword and _find_mac_near_keyword invokes _get_command_stdout, _ifconfig_getnode indirectly relies on _get_command_stdout.
# Since _ip_getnode invokes _find_mac_near_keyword and _find_mac_near_keyword invokes get_word_index, _ip_getnode indirectly relies on get_word_index.
# _ip_getnode calls _find_mac_near_keyword, which calls _is_universal -- so _ip_getnode transitively depends on _is_universal.
# _ip_getnode calls _find_mac_near_keyword, which calls _get_command_stdout -- so _ip_getnode transitively depends on _get_command_stdout.
# Since _arp_getnode invokes _find_mac_near_keyword and _find_mac_near_keyword invokes get_word_index, _arp_getnode indirectly relies on get_word_index.
# _arp_getnode -> _find_mac_near_keyword -> _is_universal: _arp_getnode has a transitive dependency on _is_universal through _find_mac_near_keyword.
# _arp_getnode -> _find_mac_near_keyword -> _get_command_stdout: _arp_getnode has a transitive dependency on _get_command_stdout through _find_mac_near_keyword.
# _lanscan_getnode -> _find_mac_near_keyword -> get_word_index: _lanscan_getnode has a transitive dependency on get_word_index through _find_mac_near_keyword.
# Since _lanscan_getnode invokes _find_mac_near_keyword and _find_mac_near_keyword invokes _is_universal, _lanscan_getnode indirectly relies on _is_universal.
# _lanscan_getnode calls _find_mac_near_keyword, which calls _get_command_stdout -- so _lanscan_getnode transitively depends on _get_command_stdout.
# _netstat_getnode -> _find_mac_under_heading -> _get_command_stdout: _netstat_getnode has a transitive dependency on _get_command_stdout through _find_mac_under_heading.
# _netstat_getnode -> _find_mac_under_heading -> _is_universal: _netstat_getnode has a transitive dependency on _is_universal through _find_mac_under_heading.
# _netstat_getnode calls _find_mac_under_heading, which calls _parse_mac -- so _netstat_getnode transitively depends on _parse_mac.
# Note: uuid1 does not call getter directly, but reaches it via getnode.
# uuid6 -> getnode -> getter: uuid6 has a transitive dependency on getter through getnode.
# get -> register_standard_browsers -> IOSBrowser: get has a transitive dependency on IOSBrowser through register_standard_browsers.
# get -> register_standard_browsers -> register_X_browsers: get has a transitive dependency on register_X_browsers through register_standard_browsers.
# get calls register_standard_browsers, which calls Elinks -- so get transitively depends on Elinks.
# get calls register_standard_browsers, which calls register -- so get transitively depends on register.
# get calls register_standard_browsers, which calls _synthesize -- so get transitively depends on _synthesize.
# get -> register_standard_browsers -> GenericBrowser: get has a transitive dependency on GenericBrowser through register_standard_browsers.
# Note: get does not call Edge directly, but reaches it via register_standard_browsers.
# Note: get does not call MacOSXOSAScript directly, but reaches it via register_standard_browsers.
# get -> register_standard_browsers -> BackgroundBrowser: get has a transitive dependency on BackgroundBrowser through register_standard_browsers.
# Since get invokes func and func invokes getter, get indirectly relies on getter.
# get calls _synthesize, which calls register -- so get transitively depends on register.
# Note: _synthesize does not call _is_valid_dispatch_type directly, but reaches it via register.
# Note: _synthesize does not call get_type_hints directly, but reaches it via register.
# _synthesize calls register, which calls register_standard_browsers -- so _synthesize transitively depends on register_standard_browsers.
# _synthesize -> register -> proxytype: _synthesize has a transitive dependency on proxytype through register.
# _synthesize -> register -> SelectorKey: _synthesize has a transitive dependency on SelectorKey through register.
# Since _synthesize invokes register and register invokes builder, _synthesize indirectly relies on builder.
# _synthesize calls register, which calls _abc_register -- so _synthesize transitively depends on _abc_register.
# _synthesize calls register, which calls dispatch -- so _synthesize transitively depends on dispatch.
# Since _synthesize invokes register and register invokes get_cache_token, _synthesize indirectly relies on get_cache_token.
# Note: register_X_browsers does not call _is_valid_dispatch_type directly, but reaches it via register.
# register_X_browsers -> register -> get_type_hints: register_X_browsers has a transitive dependency on get_type_hints through register.
# register_X_browsers calls register, which calls register_standard_browsers -- so register_X_browsers transitively depends on register_standard_browsers.
# register_X_browsers -> register -> proxytype: register_X_browsers has a transitive dependency on proxytype through register.
# register_X_browsers -> register -> SelectorKey: register_X_browsers has a transitive dependency on SelectorKey through register.
# Note: register_X_browsers does not call builder directly, but reaches it via register.
# register_X_browsers calls register, which calls _abc_register -- so register_X_browsers transitively depends on _abc_register.
# register_X_browsers calls register, which calls dispatch -- so register_X_browsers transitively depends on dispatch.
# Since register_X_browsers invokes register and register invokes get_cache_token, register_X_browsers indirectly relies on get_cache_token.
# Since register_standard_browsers invokes register_X_browsers and register_X_browsers invokes Opera, register_standard_browsers indirectly relies on Opera.
# register_standard_browsers -> register_X_browsers -> register: register_standard_browsers has a transitive dependency on register through register_X_browsers.
# Note: register_standard_browsers does not call Epiphany directly, but reaches it via register_X_browsers.
# Since register_standard_browsers invokes register_X_browsers and register_X_browsers invokes Mozilla, register_standard_browsers indirectly relies on Mozilla.
# register_standard_browsers calls register_X_browsers, which calls Edge -- so register_standard_browsers transitively depends on Edge.
# register_standard_browsers calls register_X_browsers, which calls Chrome -- so register_standard_browsers transitively depends on Chrome.
# register_standard_browsers calls register_X_browsers, which calls Konqueror -- so register_standard_browsers transitively depends on Konqueror.
# register_standard_browsers -> register_X_browsers -> BackgroundBrowser: register_standard_browsers has a transitive dependency on BackgroundBrowser through register_X_browsers.
# Since register_standard_browsers invokes register and register invokes _is_valid_dispatch_type, register_standard_browsers indirectly relies on _is_valid_dispatch_type.
# Note: register_standard_browsers does not call get_type_hints directly, but reaches it via register.
# register_standard_browsers calls register, which calls proxytype -- so register_standard_browsers transitively depends on proxytype.
# Note: register_standard_browsers does not call SelectorKey directly, but reaches it via register.
# Since register_standard_browsers invokes register and register invokes builder, register_standard_browsers indirectly relies on builder.
# register_standard_browsers -> register -> _abc_register: register_standard_browsers has a transitive dependency on _abc_register through register.
# register_standard_browsers calls register, which calls dispatch -- so register_standard_browsers transitively depends on dispatch.
# register_standard_browsers calls register, which calls get_cache_token -- so register_standard_browsers transitively depends on get_cache_token.
# Since register_standard_browsers invokes _synthesize and _synthesize invokes register, register_standard_browsers indirectly relies on register.
# read_environ -> _needs_transcode -> _is_request: read_environ has a transitive dependency on _is_request through _needs_transcode.
# Since demo_app invokes start_response and start_response invokes is_hop_by_hop, demo_app indirectly relies on is_hop_by_hop.
# Since application_uri invokes quote and quote invokes quote_from_bytes, application_uri indirectly relies on quote_from_bytes.
# Since request_uri invokes quote and quote invokes quote_from_bytes, request_uri indirectly relies on quote_from_bytes.
# Note: request_uri does not call quote directly, but reaches it via application_uri.
# Note: validator does not call assert_ directly, but reaches it via check_iterator.
# Since validator invokes start_response and start_response invokes is_hop_by_hop, validator indirectly relies on is_hop_by_hop.
# Since validator invokes check_content_type and check_content_type invokes assert_, validator indirectly relies on assert_.
# validator calls check_content_type, which calls check_string_type -- so validator transitively depends on check_string_type.
# validator -> check_headers -> assert_: validator has a transitive dependency on assert_ through check_headers.
# Note: validator does not call check_string_type directly, but reaches it via check_headers.
# Since validator invokes check_exc_info and check_exc_info invokes assert_, validator indirectly relies on assert_.
# validator calls check_environ, which calls assert_ -- so validator transitively depends on assert_.
# validator -> check_environ -> check_errors: validator has a transitive dependency on check_errors through check_environ.
# validator -> check_environ -> check_input: validator has a transitive dependency on check_input through check_environ.
# validator -> check_status -> assert_: validator has a transitive dependency on assert_ through check_status.
# Since validator invokes check_status and check_status invokes check_string_type, validator indirectly relies on check_string_type.
# Note: check_environ does not call assert_ directly, but reaches it via check_errors.
# Since check_environ invokes check_input and check_input invokes assert_, check_environ indirectly relies on assert_.
# parseString calls make_parser, which calls SAXReaderNotAvailable -- so parseString transitively depends on SAXReaderNotAvailable.
# parseString calls make_parser, which calls _create_parser -- so parseString transitively depends on _create_parser.
# parseString -> _do_pulldom_parse -> func: parseString has a transitive dependency on func through _do_pulldom_parse.
# Note: _do_pulldom_parse does not call getter directly, but reaches it via func.
# include calls _include, which calls loader -- so include transitively depends on loader.
# include calls _include, which calls LimitedRecursiveIncludeError -- so include transitively depends on LimitedRecursiveIncludeError.
# include calls _include, which calls urljoin -- so include transitively depends on urljoin.
# include -> _include -> FatalIncludeError: include has a transitive dependency on FatalIncludeError through _include.
# _include -> urljoin -> _urlsplit: _include has a transitive dependency on _urlsplit through urljoin.
# _include calls urljoin, which calls _coerce_result -- so _include transitively depends on _coerce_result.
# _include -> urljoin -> _coerce_args: _include has a transitive dependency on _coerce_args through urljoin.
# _include calls urljoin, which calls _urlunsplit -- so _include transitively depends on _urlunsplit.
# Since prepare_child invokes _prepare_tag and _prepare_tag invokes _isinstance, prepare_child indirectly relies on _isinstance.
# prepare_descendant -> _prepare_tag -> _isinstance: prepare_descendant has a transitive dependency on _isinstance through _prepare_tag.
# iterfind calls select, which calls fd_to_key_get -- so iterfind transitively depends on fd_to_key_get.
# iterfind calls select, which calls EntryPoints -- so iterfind transitively depends on EntryPoints.
# Note: iterfind does not call select_tag directly, but reaches it via select.
# Note: iterfind does not call select_child directly, but reaches it via select.
# Since iterfind invokes select and select invokes _isinstance, iterfind indirectly relies on _isinstance.
# iterfind -> select -> get_parent_map: iterfind has a transitive dependency on get_parent_map through select.
# Since findtext invokes iterfind and iterfind invokes _SelectorContext, findtext indirectly relies on _SelectorContext.
# Since findtext invokes iterfind and iterfind invokes xpath_tokenizer, findtext indirectly relies on xpath_tokenizer.
# Since findtext invokes iterfind and iterfind invokes select, findtext indirectly relies on select.
# Since _namespaces invokes add_qname and add_qname invokes _raise_serialization_error, _namespaces indirectly relies on _raise_serialization_error.
# _serialize_xml calls _escape_attrib, which calls _raise_serialization_error -- so _serialize_xml transitively depends on _raise_serialization_error.
# _serialize_xml calls write, which calls serialize -- so _serialize_xml transitively depends on serialize.
# Note: _serialize_xml does not call sleep directly, but reaches it via write.
# Note: _serialize_xml does not call _serialize_text directly, but reaches it via write.
# Since _serialize_xml invokes write and write invokes assert_, _serialize_xml indirectly relies on assert_.
# Note: _serialize_xml does not call _namespaces directly, but reaches it via write.
# Since _serialize_xml invokes write and write invokes time, _serialize_xml indirectly relies on time.
# Since _serialize_xml invokes write and write invokes _count_to_size, _serialize_xml indirectly relies on _count_to_size.
# _serialize_xml calls write, which calls quote -- so _serialize_xml transitively depends on quote.
# _serialize_xml -> write -> _nbytes: _serialize_xml has a transitive dependency on _nbytes through write.
# Since _serialize_xml invokes write and write invokes _get_writer, _serialize_xml indirectly relies on _get_writer.
# _serialize_xml -> write -> crc32: _serialize_xml has a transitive dependency on crc32 through write.
# _serialize_xml calls _escape_cdata, which calls _raise_serialization_error -- so _serialize_xml transitively depends on _raise_serialization_error.
# Note: _serialize_html does not call _raise_serialization_error directly, but reaches it via _escape_attrib.
# Note: _serialize_html does not call _raise_serialization_error directly, but reaches it via _escape_attrib_html.
# Since _serialize_html invokes write and write invokes serialize, _serialize_html indirectly relies on serialize.
# Note: _serialize_html does not call sleep directly, but reaches it via write.
# _serialize_html -> write -> _serialize_text: _serialize_html has a transitive dependency on _serialize_text through write.
# Note: _serialize_html does not call assert_ directly, but reaches it via write.
# Since _serialize_html invokes write and write invokes _namespaces, _serialize_html indirectly relies on _namespaces.
# Note: _serialize_html does not call time directly, but reaches it via write.
# Since _serialize_html invokes write and write invokes _count_to_size, _serialize_html indirectly relies on _count_to_size.
# Since _serialize_html invokes write and write invokes quote, _serialize_html indirectly relies on quote.
# Note: _serialize_html does not call _nbytes directly, but reaches it via write.
# _serialize_html -> write -> _get_writer: _serialize_html has a transitive dependency on _get_writer through write.
# Note: _serialize_html does not call crc32 directly, but reaches it via write.
# _serialize_html -> _escape_cdata -> _raise_serialization_error: _serialize_html has a transitive dependency on _raise_serialization_error through _escape_cdata.
# Note: _serialize_text does not call serialize directly, but reaches it via write.
# Since _serialize_text invokes write and write invokes sleep, _serialize_text indirectly relies on sleep.
# Since _serialize_text invokes write and write invokes assert_, _serialize_text indirectly relies on assert_.
# Note: _serialize_text does not call _namespaces directly, but reaches it via write.
# _serialize_text calls write, which calls time -- so _serialize_text transitively depends on time.
# Since _serialize_text invokes write and write invokes _count_to_size, _serialize_text indirectly relies on _count_to_size.
# _serialize_text -> write -> quote: _serialize_text has a transitive dependency on quote through write.
# _serialize_text -> write -> _nbytes: _serialize_text has a transitive dependency on _nbytes through write.
# _serialize_text calls write, which calls _get_writer -- so _serialize_text transitively depends on _get_writer.
# _serialize_text -> write -> crc32: _serialize_text has a transitive dependency on crc32 through write.
# Note: iterparse does not call wr directly, but reaches it via iterator.
# Since canonicalize invokes parse and parse invokes Group, canonicalize indirectly relies on Group.
# Note: canonicalize does not call Address directly, but reaches it via parse.
# Since canonicalize invokes parse and parse invokes RequestRate, canonicalize indirectly relies on RequestRate.
# canonicalize -> parse -> FeedParser: canonicalize has a transitive dependency on FeedParser through parse.
# canonicalize -> parse -> ExpatBuilderNS: canonicalize has a transitive dependency on ExpatBuilderNS through parse.
# canonicalize calls parse, which calls Tokenizer -- so canonicalize transitively depends on Tokenizer.
# canonicalize -> parse -> DOMEventStream: canonicalize has a transitive dependency on DOMEventStream through parse.
# canonicalize calls parse, which calls XMLParser -- so canonicalize transitively depends on XMLParser.
# Note: canonicalize does not call _do_pulldom_parse directly, but reaches it via parse.
# canonicalize calls parse, which calls Example -- so canonicalize transitively depends on Example.
# canonicalize calls parse, which calls cls -- so canonicalize transitively depends on cls.
# canonicalize calls parse, which calls RuleLine -- so canonicalize transitively depends on RuleLine.
# canonicalize calls parse, which calls ExpatBuilder -- so canonicalize transitively depends on ExpatBuilder.
# Note: canonicalize does not call TextIOWrapper directly, but reaches it via parse.
# canonicalize -> parse -> ElementTree: canonicalize has a transitive dependency on ElementTree through parse.
# Note: canonicalize does not call make_parser directly, but reaches it via parse.
# canonicalize -> parse -> ParserCreate: canonicalize has a transitive dependency on ParserCreate through parse.
# canonicalize calls parse, which calls InvalidFileException -- so canonicalize transitively depends on InvalidFileException.
# Note: canonicalize does not call Entry directly, but reaches it via parse.
# canonicalize calls parse, which calls _parse_sub -- so canonicalize transitively depends on _parse_sub.
# Note: canonicalize does not call State directly, but reaches it via parse.
# canonicalize -> parse -> error: canonicalize has a transitive dependency on error through parse.
# canonicalize calls parse, which calls ExpatLocator -- so canonicalize transitively depends on ExpatLocator.
# canonicalize -> parse -> fix_flags: canonicalize has a transitive dependency on fix_flags through parse.
# canonicalize calls parse, which calls ErrorHandler -- so canonicalize transitively depends on ErrorHandler.
# quoteattr calls escape, which calls __dict_replace -- so quoteattr transitively depends on __dict_replace.
# quoteattr -> escape -> replace: quoteattr has a transitive dependency on replace through escape.
# create_archive calls _copy_archive, which calls _maybe_open -- so create_archive transitively depends on _maybe_open.
# create_archive calls _copy_archive, which calls _write_file_prefix -- so create_archive transitively depends on _write_file_prefix.
# _check_zipfile calls _EndRecData, which calls _EndRecData64 -- so _check_zipfile transitively depends on _EndRecData64.
# is_zipfile calls _check_zipfile, which calls _EndRecData -- so is_zipfile transitively depends on _EndRecData.
# Since is_zipfile invokes _check_zipfile and _check_zipfile invokes _handle_prepended_data, is_zipfile indirectly relies on _handle_prepended_data.
# Note: _EndRecData does not call BadZipFile directly, but reaches it via _EndRecData64.
# _ZipDecrypter -> update_keys -> crc32: _ZipDecrypter has a transitive dependency on crc32 through update_keys.
# _ZipDecrypter calls append, which calls Time2Internaldate -- so _ZipDecrypter transitively depends on Time2Internaldate.
# _ZipDecrypter calls append, which calls Charset -- so _ZipDecrypter transitively depends on Charset.
# Since _get_data invokes decompress and decompress invokes ZstdError, _get_data indirectly relies on ZstdError.
# _get_data calls decompress, which calls BadGzipFile -- so _get_data transitively depends on BadGzipFile.
# Since _get_data invokes decompress and decompress invokes ZstdDecompressor, _get_data indirectly relies on ZstdDecompressor.
# Since _get_data invokes decompress and decompress invokes BZ2Decompressor, _get_data indirectly relies on BZ2Decompressor.
# Since _get_data invokes decompress and decompress invokes LZMADecompressor, _get_data indirectly relies on LZMADecompressor.
# Note: _get_data does not call LZMAError directly, but reaches it via decompress.
# _get_data -> decompress -> _read_gzip_header: _get_data has a transitive dependency on _read_gzip_header through decompress.
# Note: _get_data does not call ZipImportError directly, but reaches it via _get_decompress_func.
# Since _unmarshal_code invokes _get_mtime_and_size_of_source and _get_mtime_and_size_of_source invokes _parse_dostime, _unmarshal_code indirectly relies on _parse_dostime.
# _unmarshal_code calls _get_pyc_source, which calls _get_data -- so _unmarshal_code transitively depends on _get_data.
# Note: _get_pyc_source does not call _unpack_uint16 directly, but reaches it via _get_data.
# _get_pyc_source calls _get_data, which calls ZipImportError -- so _get_pyc_source transitively depends on ZipImportError.
# Since _get_pyc_source invokes _get_data and _get_data invokes decompress, _get_pyc_source indirectly relies on decompress.
# Since _get_pyc_source invokes _get_data and _get_data invokes _get_decompress_func, _get_pyc_source indirectly relies on _get_decompress_func.
# Note: _get_module_code does not call _normalize_line_endings directly, but reaches it via _compile_source.
# Note: _get_module_code does not call _unpack_uint32 directly, but reaches it via _unmarshal_code.
# _get_module_code calls _unmarshal_code, which calls _eq_mtime -- so _get_module_code transitively depends on _eq_mtime.
# Since _get_module_code invokes _unmarshal_code and _unmarshal_code invokes _get_mtime_and_size_of_source, _get_module_code indirectly relies on _get_mtime_and_size_of_source.
# Since _get_module_code invokes _unmarshal_code and _unmarshal_code invokes _get_pyc_source, _get_module_code indirectly relies on _get_pyc_source.
# _get_module_code calls _get_data, which calls _unpack_uint16 -- so _get_module_code transitively depends on _unpack_uint16.
# Since _get_module_code invokes _get_data and _get_data invokes ZipImportError, _get_module_code indirectly relies on ZipImportError.
# Note: _get_module_code does not call decompress directly, but reaches it via _get_data.
# Note: _get_module_code does not call _get_decompress_func directly, but reaches it via _get_data.
# Note: _reset_tzpath does not call _get_invalid_paths_message directly, but reaches it via _parse_python_tzpath.
# reset_tzpath -> _reset_tzpath -> _get_invalid_paths_message: reset_tzpath has a transitive dependency on _get_invalid_paths_message through _reset_tzpath.
# Since reset_tzpath invokes _reset_tzpath and _reset_tzpath invokes _parse_python_tzpath, reset_tzpath indirectly relies on _parse_python_tzpath.
# _parse_tz_str calls _parse_dst_start_end, which calls _DayOffset -- so _parse_tz_str transitively depends on _DayOffset.
# Note: _parse_tz_str does not call _CalendarOffset directly, but reaches it via _parse_dst_start_end.
# Since _parse_tz_str invokes _parse_dst_start_end and _parse_dst_start_end invokes _parse_transition_time, _parse_tz_str indirectly relies on _parse_transition_time.
# Note: _parse_tz_str does not call timedelta directly, but reaches it via _load_timedelta.
# Since __init__ invokes _gettextwriter and _gettextwriter invokes _wrapper, __init__ indirectly relies on _wrapper.
# __init__ calls poll, which calls wait -- so __init__ transitively depends on wait.
# __init__ -> get_theme -> can_colorize: __init__ has a transitive dependency on can_colorize through get_theme.
# Note: __init__ does not call trace directly, but reaches it via get_terminal_keycodes.
# Note: __init__ does not call LZMACompressor directly, but reaches it via _get_compressor.
# Note: __init__ does not call _Screen directly, but reaches it via Screen.
# Note: __init__ does not call _wrap_awaitable directly, but reaches it via ensure_future.
# __init__ -> request_host -> urlparse: __init__ has a transitive dependency on urlparse through request_host.
# Since __init__ invokes getproxies and getproxies invokes getproxies_environment, __init__ indirectly relies on getproxies_environment.
# __init__ calls getproxies, which calls getproxies_registry -- so __init__ transitively depends on getproxies_registry.
# __init__ -> getproxies -> getproxies_macosx_sysconf: __init__ has a transitive dependency on getproxies_macosx_sysconf through getproxies.
# Since __init__ invokes init and init invokes MimeTypes, __init__ indirectly relies on MimeTypes.
# __init__ -> parse_keys -> _parse_single_key_sequence: __init__ has a transitive dependency on _parse_single_key_sequence through parse_keys.
# __init__ -> _compute_suggestion_error -> _get_safe___dir__: __init__ has a transitive dependency on _get_safe___dir__ through _compute_suggestion_error.
# Since __init__ invokes _compute_suggestion_error and _compute_suggestion_error invokes _levenshtein_distance, __init__ indirectly relies on _levenshtein_distance.
# Note: __init__ does not call func directly, but reaches it via _safe_string.
# __init__ -> _load_timedelta -> timedelta: __init__ has a transitive dependency on timedelta through _load_timedelta.
# __init__ -> current_thread -> get_ident: __init__ has a transitive dependency on get_ident through current_thread.
# Note: __init__ does not call _DummyThread directly, but reaches it via current_thread.
# __init__ -> _walk_tb_with_full_positions -> _get_code_position: __init__ has a transitive dependency on _get_code_position through _walk_tb_with_full_positions.
# __init__ -> singledispatch -> _is_valid_dispatch_type: __init__ has a transitive dependency on _is_valid_dispatch_type through singledispatch.
# Since __init__ invokes singledispatch and singledispatch invokes MappingProxyType, __init__ indirectly relies on MappingProxyType.
# __init__ calls singledispatch, which calls _find_impl -- so __init__ transitively depends on _find_impl.
# __init__ -> singledispatch -> get_type_hints: __init__ has a transitive dependency on get_type_hints through singledispatch.
# __init__ -> singledispatch -> register: __init__ has a transitive dependency on register through singledispatch.
# Since __init__ invokes singledispatch and singledispatch invokes update_wrapper, __init__ indirectly relies on update_wrapper.
# __init__ calls singledispatch, which calls dispatch -- so __init__ transitively depends on dispatch.
# Since __init__ invokes singledispatch and singledispatch invokes get_cache_token, __init__ indirectly relies on get_cache_token.
# Note: __init__ does not call local_print directly, but reaches it via _make_invoke_excepthook.
# Since __init__ invokes _make_invoke_excepthook and _make_invoke_excepthook invokes sys_excepthook, __init__ indirectly relies on sys_excepthook.
# __init__ -> _make_invoke_excepthook -> hook: __init__ has a transitive dependency on hook through _make_invoke_excepthook.
# Since __init__ invokes _make_invoke_excepthook and _make_invoke_excepthook invokes ExceptHookArgs, __init__ indirectly relies on ExceptHookArgs.
# Note: __init__ does not call sys_exc_info directly, but reaches it via _make_invoke_excepthook.
# Note: __init__ does not call LZMADecompressor directly, but reaches it via _get_decompressor.
# Since __init__ invokes _get_decompressor and _get_decompressor invokes _check_compression, __init__ indirectly relies on _check_compression.
# __init__ -> _parse_exception_table -> _ExceptionTableEntry: __init__ has a transitive dependency on _ExceptionTableEntry through _parse_exception_table.
# __init__ -> _parse_exception_table -> _parse_varint: __init__ has a transitive dependency on _parse_varint through _parse_exception_table.
# Note: __init__ does not call _iso8601_format directly, but reaches it via _strftime.
# __init__ calls RLock, which calls _CRLock -- so __init__ transitively depends on _CRLock.
# Note: __init__ does not call _PyRLock directly, but reaches it via RLock.
# Note: __init__ does not call Path directly, but reaches it via _normalize_uri.
# __init__ calls mkdtemp, which calls _sanitize_params -- so __init__ transitively depends on _sanitize_params.
# Since __init__ invokes mkdtemp and mkdtemp invokes _get_candidate_names, __init__ indirectly relies on _get_candidate_names.
# __init__ calls cast, which calls _cast -- so __init__ transitively depends on _cast.
# __init__ -> _get_code_object -> _try_compile: __init__ has a transitive dependency on _try_compile through _get_code_object.
# __init__ -> error -> basicConfig: __init__ has a transitive dependency on basicConfig through error.
# __init__ calls error, which calls _ -- so __init__ transitively depends on _.
# Note: __init__ does not call perror directly, but reaches it via error.
# Note: __init__ does not call ref directly, but reaches it via update.
# Since __init__ invokes update and update invokes KeyedRef, __init__ indirectly relies on KeyedRef.
# __init__ calls update, which calls _has_control_character -- so __init__ transitively depends on _has_control_character.
# Since __init__ invokes update and update invokes _count_elements, __init__ indirectly relies on _count_elements.
# __init__ calls update, which calls CookieError -- so __init__ transitively depends on CookieError.
# Note: __init__ does not call self_get directly, but reaches it via update.
# Note: __init__ does not call start directly, but reaches it via handler.
# __init__ -> handler -> end: __init__ has a transitive dependency on end through handler.
# Note: __init__ does not call append directly, but reaches it via handler.
# Note: __init__ does not call end_ns directly, but reaches it via handler.
# __init__ calls handler, which calls start_ns -- so __init__ transitively depends on start_ns.
# __init__ -> _newname -> _counter: __init__ has a transitive dependency on _counter through _newname.
# Note: __init__ does not call _unpack_uint16 directly, but reaches it via _read_directory.
# __init__ calls _read_directory, which calls ZipImportError -- so __init__ transitively depends on ZipImportError.
# __init__ calls _read_directory, which calls _unpack_uint32 -- so __init__ transitively depends on _unpack_uint32.
# __init__ calls _read_directory, which calls _unpack_uint64 -- so __init__ transitively depends on _unpack_uint64.
# Since __init__ invokes opener and opener invokes _mkstemp_inner, __init__ indirectly relies on _mkstemp_inner.
# Since __init__ invokes trace and trace invokes _emscripten_log, __init__ indirectly relies on _emscripten_log.
# Since __init__ invokes trace and trace invokes getinnerframes, __init__ indirectly relies on getinnerframes.
# __init__ -> normalize_pattern -> normalize: __init__ has a transitive dependency on normalize through normalize_pattern.
# Note: __init__ does not call _is_unpacked_typevartuple directly, but reaches it via _collect_type_parameters.
# Since __init__ invokes read_environ and read_environ invokes _needs_transcode, __init__ indirectly relies on _needs_transcode.
# __init__ -> tcgetattr -> TermState: __init__ has a transitive dependency on TermState through tcgetattr.
# Note: __init__ does not call different_locale directly, but reaches it via _get_default_locale.
# Since __init__ invokes _ and _ invokes resolve, __init__ indirectly relies on resolve.
# __init__ calls _, which calls _infer_caller -- so __init__ transitively depends on _infer_caller.
# __init__ -> SharedMemory -> dispatch: __init__ has a transitive dependency on dispatch through SharedMemory.
# Since __init__ invokes _cleanup and _cleanup invokes sem_unlink, __init__ indirectly relies on sem_unlink.
# Since __init__ invokes _cleanup and _cleanup invokes unregister, __init__ indirectly relies on unregister.
# Since __init__ invokes register and register invokes _is_valid_dispatch_type, __init__ indirectly relies on _is_valid_dispatch_type.
# Since __init__ invokes register and register invokes get_type_hints, __init__ indirectly relies on get_type_hints.
# __init__ calls register, which calls register_standard_browsers -- so __init__ transitively depends on register_standard_browsers.
# __init__ calls register, which calls proxytype -- so __init__ transitively depends on proxytype.
# Note: __init__ does not call SelectorKey directly, but reaches it via register.
# __init__ -> register -> builder: __init__ has a transitive dependency on builder through register.
# __init__ -> register -> _abc_register: __init__ has a transitive dependency on _abc_register through register.
# __init__ calls register, which calls dispatch -- so __init__ transitively depends on dispatch.
# Note: __init__ does not call get_cache_token directly, but reaches it via register.
# Note: __init__ does not call _path_join directly, but reaches it via _path_abspath.
# Since __init__ invokes _path_abspath and _path_abspath invokes _path_isabs, __init__ indirectly relies on _path_isabs.
# __init__ calls func, which calls getter -- so __init__ transitively depends on getter.
# Since __init__ invokes _what and _what invokes testfn, __init__ indirectly relies on testfn.
# __init__ calls _what, which calls rule -- so __init__ transitively depends on rule.
# Note: __init__ does not call KeySpecError directly, but reaches it via compile_keymap.
# __init__ -> format_exception -> TracebackException: __init__ has a transitive dependency on TracebackException through format_exception.
# __init__ calls format_exception, which calls _parse_value_tb -- so __init__ transitively depends on _parse_value_tb.
# __init__ -> _my_getstr -> InvalidTerminal: __init__ has a transitive dependency on InvalidTerminal through _my_getstr.
# visit_file calls get_source_comments, which calls BytesIO -- so visit_file transitively depends on BytesIO.
# Note: get_specialization_failure_kinds does not call pretty directly, but reaches it via kind_to_text.
# Since iter_specialization_tables invokes calc_specialization_failure_kind_table and calc_specialization_failure_kind_table invokes itemgetter, iter_specialization_tables indirectly relies on itemgetter.
# iter_specialization_tables -> calc_specialization_failure_kind_table -> Ratio: iter_specialization_tables has a transitive dependency on Ratio through calc_specialization_failure_kind_table.
# Note: iter_specialization_tables does not call Count directly, but reaches it via calc_specialization_failure_kind_table.
# iter_specialization_tables calls calc_specialization_table, which calls Doc -- so iter_specialization_tables transitively depends on Doc.
# Note: iter_specialization_tables does not call Ratio directly, but reaches it via calc_specialization_table.
# Note: iter_specialization_tables does not call Count directly, but reaches it via calc_specialization_table.
# Note: iter_specialization_tables does not call Count directly, but reaches it via calc_specialization_success_failure_table.
# Since iter_specialization_tables invokes calc_specialization_success_failure_table and calc_specialization_success_failure_table invokes Ratio, iter_specialization_tables indirectly relies on Ratio.
# Since iter_optimization_tables invokes pair_count_section and pair_count_section invokes Count, iter_optimization_tables indirectly relies on Count.
# iter_optimization_tables calls pair_count_section, which calls itemgetter -- so iter_optimization_tables transitively depends on itemgetter.
# iter_optimization_tables calls pair_count_section, which calls Section -- so iter_optimization_tables transitively depends on Section.
# Since iter_optimization_tables invokes pair_count_section and pair_count_section invokes Table, iter_optimization_tables indirectly relies on Table.
# Note: iter_optimization_tables does not call Ratio directly, but reaches it via pair_count_section.
# iter_optimization_tables -> calc_execution_count_table -> itemgetter: iter_optimization_tables has a transitive dependency on itemgetter through calc_execution_count_table.
# iter_optimization_tables -> calc_execution_count_table -> Count: iter_optimization_tables has a transitive dependency on Count through calc_execution_count_table.
# Since iter_optimization_tables invokes calc_execution_count_table and calc_execution_count_table invokes Ratio, iter_optimization_tables indirectly relies on Ratio.
# iter_optimization_tables calls calc_histogram_table, which calls Ratio -- so iter_optimization_tables transitively depends on Ratio.
# iter_optimization_tables -> calc_histogram_table -> Count: iter_optimization_tables has a transitive dependency on Count through calc_histogram_table.
# __repr__ -> recursive_repr -> user_function: __repr__ has a transitive dependency on user_function through recursive_repr.
# __repr__ calls recursive_repr, which calls get_ident -- so __repr__ transitively depends on get_ident.
# Since __repr__ invokes type_repr and type_repr invokes _template_to_ast, __repr__ indirectly relies on _template_to_ast.
# write calls _namespaces, which calls add_qname -- so write transitively depends on add_qname.
# Note: write does not call _raise_serialization_error directly, but reaches it via _namespaces.
# write calls quote, which calls quote_from_bytes -- so write transitively depends on quote_from_bytes.
# Note: interleave does not call f1 directly, but reaches it via f.
# close -> unlink -> NodeList: close has a transitive dependency on NodeList through unlink.
# __new__ -> RLock -> _CRLock: __new__ has a transitive dependency on _CRLock through RLock.
# Note: __new__ does not call _PyRLock directly, but reaches it via RLock.
# Since __new__ invokes _is_async_obj and _is_async_obj invokes _is_instance_mock, __new__ indirectly relies on _is_instance_mock.
# __new__ calls _is_async_obj, which calls iscoroutinefunction -- so __new__ transitively depends on iscoroutinefunction.
# Since __new__ invokes callback and callback invokes func, __new__ indirectly relies on func.
# Since __new__ invokes callback and callback invokes ensure_future, __new__ indirectly relies on ensure_future.
# Note: __new__ does not call _index directly, but reaches it via _check_time_fields.
# Since __new__ invokes _type_check and _type_check invokes _type_convert, __new__ indirectly relies on _type_convert.
# __new__ -> getcontext -> Context: __new__ has a transitive dependency on Context through getcontext.
# __new__ calls _get_typeddict_qualifiers, which calls get_args -- so __new__ transitively depends on get_args.
# __new__ calls _get_typeddict_qualifiers, which calls get_origin -- so __new__ transitively depends on get_origin.
# Since __new__ invokes _make_eager_annotate and _make_eager_annotate invokes _type_check, __new__ indirectly relies on _type_check.
# __new__ calls _check_date_fields, which calls _index -- so __new__ transitively depends on _index.
# __new__ -> _check_date_fields -> _days_in_month: __new__ has a transitive dependency on _days_in_month through _check_date_fields.
# __reduce__ -> assert_spawning -> get_spawning_popen: __reduce__ has a transitive dependency on get_spawning_popen through assert_spawning.
# Note: __getitem__ does not call _make_forward_ref directly, but reaches it via _type_convert.
# __getitem__ -> f -> f1: __getitem__ has a transitive dependency on f1 through f.
# __getitem__ -> _type_check -> _type_convert: __getitem__ has a transitive dependency on _type_convert through _type_check.
# __le__ -> _convert_for_comparison -> _dec_from_triple: __le__ has a transitive dependency on _dec_from_triple through _convert_for_comparison.
# __le__ -> _convert_for_comparison -> getcontext: __le__ has a transitive dependency on getcontext through _convert_for_comparison.
# Since __le__ invokes _convert_for_comparison and _convert_for_comparison invokes Decimal, __le__ indirectly relies on Decimal.
# __lt__ calls _convert_for_comparison, which calls _dec_from_triple -- so __lt__ transitively depends on _dec_from_triple.
# Note: __lt__ does not call getcontext directly, but reaches it via _convert_for_comparison.
# __lt__ calls _convert_for_comparison, which calls Decimal -- so __lt__ transitively depends on Decimal.
# __gt__ calls _convert_for_comparison, which calls _dec_from_triple -- so __gt__ transitively depends on _dec_from_triple.
# Since __gt__ invokes _convert_for_comparison and _convert_for_comparison invokes getcontext, __gt__ indirectly relies on getcontext.
# Since __gt__ invokes _convert_for_comparison and _convert_for_comparison invokes Decimal, __gt__ indirectly relies on Decimal.
# __ge__ -> _convert_for_comparison -> _dec_from_triple: __ge__ has a transitive dependency on _dec_from_triple through _convert_for_comparison.
# Note: __ge__ does not call getcontext directly, but reaches it via _convert_for_comparison.
# Since __ge__ invokes _convert_for_comparison and _convert_for_comparison invokes Decimal, __ge__ indirectly relies on Decimal.
# __eq__ -> _convert_for_comparison -> _dec_from_triple: __eq__ has a transitive dependency on _dec_from_triple through _convert_for_comparison.
# __eq__ -> _convert_for_comparison -> getcontext: __eq__ has a transitive dependency on getcontext through _convert_for_comparison.
# __eq__ -> _convert_for_comparison -> Decimal: __eq__ has a transitive dependency on Decimal through _convert_for_comparison.
# __sub__ -> _convert_other -> Decimal: __sub__ has a transitive dependency on Decimal through _convert_other.
# __rsub__ calls _convert_other, which calls Decimal -- so __rsub__ transitively depends on Decimal.
# keys calls error, which calls basicConfig -- so keys transitively depends on basicConfig.
# Note: keys does not call _ directly, but reaches it via error.
# keys calls error, which calls perror -- so keys transitively depends on perror.
# Note: __len__ does not call basicConfig directly, but reaches it via error.
# Note: __len__ does not call _ directly, but reaches it via error.
# __len__ calls error, which calls perror -- so __len__ transitively depends on perror.
# update calls _count_elements, which calls mapping_get -- so update transitively depends on mapping_get.
# Note: __instancecheck__ does not call _caller directly, but reaches it via _allow_reckless_class_checks.
# Since __instancecheck__ invokes getattr_static and getattr_static invokes _check_instance, __instancecheck__ indirectly relies on _check_instance.
# Note: __instancecheck__ does not call _shadowed_dict directly, but reaches it via getattr_static.
# Since __instancecheck__ invokes getattr_static and getattr_static invokes _check_class, __instancecheck__ indirectly relies on _check_class.
# Note: __instancecheck__ does not call _static_getmro directly, but reaches it via getattr_static.
# append calls Time2Internaldate, which calls timedelta -- so append transitively depends on timedelta.
# append -> Time2Internaldate -> timezone: append has a transitive dependency on timezone through Time2Internaldate.
# Note: append does not call datetime directly, but reaches it via Time2Internaldate.
# __iter__ -> error -> basicConfig: __iter__ has a transitive dependency on basicConfig through error.
# __iter__ -> error -> _: __iter__ has a transitive dependency on _ through error.
# Since __iter__ invokes error and error invokes perror, __iter__ indirectly relies on perror.
# __iter__ calls _get_code_array, which calls get_executor -- so __iter__ transitively depends on get_executor.
# Note: __iter__ does not call findlabels directly, but reaches it via _make_labels_map.
# __iter__ -> _get_instructions_bytes -> _unpack_opargs: __iter__ has a transitive dependency on _unpack_opargs through _get_instructions_bytes.
# Since __iter__ invokes _get_instructions_bytes and _get_instructions_bytes invokes _get_cache_size, __iter__ indirectly relies on _get_cache_size.
# __iter__ calls _get_instructions_bytes, which calls Instruction -- so __iter__ transitively depends on Instruction.
# Since __iter__ invokes _get_instructions_bytes and _get_instructions_bytes invokes _deoptop, __iter__ indirectly relies on _deoptop.
# Note: __iter__ does not call Positions directly, but reaches it via _get_instructions_bytes.
# Note: __subclasscheck__ does not call _caller directly, but reaches it via _allow_reckless_class_checks.
# Since __enter__ invokes _get_module_lock and _get_module_lock invokes _DummyModuleLock, __enter__ indirectly relies on _DummyModuleLock.
# __enter__ calls _get_module_lock, which calls _ModuleLock -- so __enter__ transitively depends on _ModuleLock.
# Since __enter__ invokes create_autospec and create_autospec invokes child_klass, __enter__ indirectly relies on child_klass.
# __enter__ -> create_autospec -> _is_async_func: __enter__ has a transitive dependency on _is_async_func through create_autospec.
# Since __enter__ invokes create_autospec and create_autospec invokes _check_signature, __enter__ indirectly relies on _check_signature.
# __enter__ -> create_autospec -> _callable: __enter__ has a transitive dependency on _callable through create_autospec.
# __enter__ calls create_autospec, which calls fields -- so __enter__ transitively depends on fields.
# __enter__ -> create_autospec -> _set_signature: __enter__ has a transitive dependency on _set_signature through create_autospec.
# __enter__ -> create_autospec -> iscoroutinefunction: __enter__ has a transitive dependency on iscoroutinefunction through create_autospec.
# Note: __enter__ does not call _is_instance_mock directly, but reaches it via create_autospec.
# Since __enter__ invokes create_autospec and create_autospec invokes Klass, __enter__ indirectly relies on Klass.
# __enter__ calls create_autospec, which calls _instance_callable -- so __enter__ transitively depends on _instance_callable.
# Since __enter__ invokes create_autospec and create_autospec invokes InvalidSpecError, __enter__ indirectly relies on InvalidSpecError.
# __enter__ -> create_autospec -> _is_list: __enter__ has a transitive dependency on _is_list through create_autospec.
# Since __enter__ invokes create_autospec and create_autospec invokes _is_magic, __enter__ indirectly relies on _is_magic.
# Note: __enter__ does not call _set_async_signature directly, but reaches it via create_autospec.
# __enter__ -> create_autospec -> _SpecState: __enter__ has a transitive dependency on _SpecState through create_autospec.
# __enter__ calls create_autospec, which calls _check_spec_arg_typos -- so __enter__ transitively depends on _check_spec_arg_typos.
# __enter__ calls create_autospec, which calls is_dataclass -- so __enter__ transitively depends on is_dataclass.
# Note: __enter__ does not call _must_skip directly, but reaches it via create_autospec.
# Note: __enter__ does not call Context directly, but reaches it via getcontext.
# __enter__ calls _is_async_obj, which calls _is_instance_mock -- so __enter__ transitively depends on _is_instance_mock.
# Since __enter__ invokes _is_async_obj and _is_async_obj invokes iscoroutinefunction, __enter__ indirectly relies on iscoroutinefunction.
# __call__ calls get_pager, which calls plain -- so __call__ transitively depends on plain.
# __call__ -> get_pager -> tempfile_pager: __call__ has a transitive dependency on tempfile_pager through get_pager.
# Note: __call__ does not call pipe_pager directly, but reaches it via get_pager.
# Since __call__ invokes _ and _ invokes resolve, __call__ indirectly relies on resolve.
# __call__ -> _ -> _infer_caller: __call__ has a transitive dependency on _infer_caller through _.
# Note: __call__ does not call get_pager directly, but reaches it via pager.
# __call__ -> wraps -> partial: __call__ has a transitive dependency on partial through wraps.
# Since __call__ invokes func and func invokes getter, __call__ indirectly relies on getter.
# __call__ calls _maybe_compile, which calls compiler -- so __call__ transitively depends on compiler.
# __str__ -> quote_string -> make_quoted_pairs: __str__ has a transitive dependency on make_quoted_pairs through quote_string.
# Since __str__ invokes getcontext and getcontext invokes Context, __str__ indirectly relies on Context.
# __str__ calls _, which calls resolve -- so __str__ transitively depends on resolve.
# Note: __str__ does not call _infer_caller directly, but reaches it via _.
# Note: __add__ does not call Context directly, but reaches it via getcontext.
# __add__ -> _convert_other -> Decimal: __add__ has a transitive dependency on Decimal through _convert_other.
# __neg__ -> getcontext -> Context: __neg__ has a transitive dependency on Context through getcontext.
# Since __mul__ invokes _convert_other and _convert_other invokes Decimal, __mul__ indirectly relies on Decimal.
# Since __mul__ invokes getcontext and getcontext invokes Context, __mul__ indirectly relies on Context.
# __floordiv__ calls _convert_other, which calls Decimal -- so __floordiv__ transitively depends on Decimal.
# Note: __floordiv__ does not call Context directly, but reaches it via getcontext.
# Since __truediv__ invokes _convert_other and _convert_other invokes Decimal, __truediv__ indirectly relies on Decimal.
# Since __truediv__ invokes getcontext and getcontext invokes Context, __truediv__ indirectly relies on Context.
# __mod__ -> _convert_other -> Decimal: __mod__ has a transitive dependency on Decimal through _convert_other.
# Since __mod__ invokes getcontext and getcontext invokes Context, __mod__ indirectly relies on Context.
# Since __divmod__ invokes _convert_other and _convert_other invokes Decimal, __divmod__ indirectly relies on Decimal.
# Note: __divmod__ does not call Context directly, but reaches it via getcontext.
# Since __hash__ invokes _ymd2ord and _ymd2ord invokes _days_before_year, __hash__ indirectly relies on _days_before_year.
# __hash__ calls _ymd2ord, which calls _post_epoch_days_before_year -- so __hash__ transitively depends on _post_epoch_days_before_year.
# __hash__ calls _ymd2ord, which calls _days_before_month -- so __hash__ transitively depends on _days_before_month.
# Since __hash__ invokes _ymd2ord and _ymd2ord invokes _days_in_month, __hash__ indirectly relies on _days_in_month.
# Note: fromordinal does not call _is_leap directly, but reaches it via _ord2ymd.
# Note: fromordinal does not call _days_in_month directly, but reaches it via _ord2ymd.
# Note: fromisoformat does not call _isoweek_to_gregorian directly, but reaches it via _parse_isoformat_date.
# fromisoformat calls _find_isoformat_datetime_separator, which calls _is_ascii_digit -- so fromisoformat transitively depends on _is_ascii_digit.
# Since fromisoformat invokes _parse_isoformat_time and _parse_isoformat_time invokes _parse_hh_mm_ss_ff, fromisoformat indirectly relies on _parse_hh_mm_ss_ff.
# fromisoformat -> _parse_isoformat_time -> timedelta: fromisoformat has a transitive dependency on timedelta through _parse_isoformat_time.
# fromisoformat -> _parse_isoformat_time -> timezone: fromisoformat has a transitive dependency on timezone through _parse_isoformat_time.
# fromisoformat calls _days_in_month, which calls _is_leap -- so fromisoformat transitively depends on _is_leap.
# Since fromisocalendar invokes _isoweek_to_gregorian and _isoweek_to_gregorian invokes _ymd2ord, fromisocalendar indirectly relies on _ymd2ord.
# fromisocalendar -> _isoweek_to_gregorian -> _ord2ymd: fromisocalendar has a transitive dependency on _ord2ymd through _isoweek_to_gregorian.
# Note: fromisocalendar does not call _is_leap directly, but reaches it via _isoweek_to_gregorian.
# Note: fromisocalendar does not call _isoweek1monday directly, but reaches it via _isoweek_to_gregorian.
# strftime calls _wrap_strftime, which calls push -- so strftime transitively depends on push.
# strftime -> _wrap_strftime -> _need_normalize_century: strftime has a transitive dependency on _need_normalize_century through _wrap_strftime.
# strftime calls _wrap_strftime, which calls _format_offset -- so strftime transitively depends on _format_offset.
# Note: __format__ does not call Context directly, but reaches it via getcontext.
# __format__ -> _format_number -> _format_sign: __format__ has a transitive dependency on _format_sign through _format_number.
# Since __format__ invokes _format_number and _format_number invokes _insert_thousands_sep, __format__ indirectly relies on _insert_thousands_sep.
# __format__ calls _format_number, which calls _format_align -- so __format__ transitively depends on _format_align.
# timetuple calls _build_struct_time, which calls _days_before_month -- so timetuple transitively depends on _days_before_month.
# timetuple -> _build_struct_time -> _ymd2ord: timetuple has a transitive dependency on _ymd2ord through _build_struct_time.
# Note: toordinal does not call _days_before_year directly, but reaches it via _ymd2ord.
# Since toordinal invokes _ymd2ord and _ymd2ord invokes _post_epoch_days_before_year, toordinal indirectly relies on _post_epoch_days_before_year.
# toordinal -> _ymd2ord -> _days_before_month: toordinal has a transitive dependency on _days_before_month through _ymd2ord.
# toordinal -> _ymd2ord -> _days_in_month: toordinal has a transitive dependency on _days_in_month through _ymd2ord.
# isocalendar calls _ymd2ord, which calls _days_before_year -- so isocalendar transitively depends on _days_before_year.
# isocalendar calls _ymd2ord, which calls _post_epoch_days_before_year -- so isocalendar transitively depends on _post_epoch_days_before_year.
# isocalendar calls _ymd2ord, which calls _days_before_month -- so isocalendar transitively depends on _days_before_month.
# Note: isocalendar does not call _days_in_month directly, but reaches it via _ymd2ord.
# Since isocalendar invokes _isoweek1monday and _isoweek1monday invokes _ymd2ord, isocalendar indirectly relies on _ymd2ord.
# Note: utcoffset does not call timedelta directly, but reaches it via _check_utc_offset.
# Since dst invokes _check_utc_offset and _check_utc_offset invokes timedelta, dst indirectly relies on timedelta.
# Note: _tzstr does not call timedelta directly, but reaches it via _format_offset.
# Note: isoformat does not call timedelta directly, but reaches it via _format_offset.
# Note: _mktime does not call timedelta directly, but reaches it via local.
# Since _mktime invokes local and local invokes datetime, _mktime indirectly relies on datetime.
# Since utctimetuple invokes _build_struct_time and _build_struct_time invokes _days_before_month, utctimetuple indirectly relies on _days_before_month.
# utctimetuple calls _build_struct_time, which calls _ymd2ord -- so utctimetuple transitively depends on _ymd2ord.
# Note: handle does not call dictConfigClass directly, but reaches it via dictConfig.
# handle calls fileConfig, which calls _install_handlers -- so handle transitively depends on _install_handlers.
# Note: handle does not call _install_loggers directly, but reaches it via fileConfig.
# handle -> fileConfig -> _create_formatters: handle has a transitive dependency on _create_formatters through fileConfig.
# Since handle invokes fileConfig and fileConfig invokes _clearExistingHandlers, handle indirectly relies on _clearExistingHandlers.
# _check_nans calls getcontext, which calls Context -- so _check_nans transitively depends on Context.
# _compare_check_nans -> getcontext -> Context: _compare_check_nans has a transitive dependency on Context through getcontext.
# Note: __pos__ does not call Context directly, but reaches it via getcontext.
# Since __rtruediv__ invokes _convert_other and _convert_other invokes Decimal, __rtruediv__ indirectly relies on Decimal.
# Note: __rdivmod__ does not call Decimal directly, but reaches it via _convert_other.
# Note: __rmod__ does not call Decimal directly, but reaches it via _convert_other.
# remainder_near calls _convert_other, which calls Decimal -- so remainder_near transitively depends on Decimal.
# Since remainder_near invokes getcontext and getcontext invokes Context, remainder_near indirectly relies on Context.
# __rfloordiv__ -> _convert_other -> Decimal: __rfloordiv__ has a transitive dependency on Decimal through _convert_other.
# __int__ -> _index -> _format_stack_entry: __int__ has a transitive dependency on _format_stack_entry through _index.
# _fix -> iter_child_nodes -> iter_fields: _fix has a transitive dependency on iter_fields through iter_child_nodes.
# Note: fma does not call Decimal directly, but reaches it via _convert_other.
# fma calls getcontext, which calls Context -- so fma transitively depends on Context.
# Note: _power_modulo does not call Decimal directly, but reaches it via _convert_other.
# _power_modulo calls getcontext, which calls Context -- so _power_modulo transitively depends on Context.
# Note: __pow__ does not call Decimal directly, but reaches it via _convert_other.
# __pow__ -> getcontext -> Context: __pow__ has a transitive dependency on Context through getcontext.
# Note: __pow__ does not call _dlog directly, but reaches it via _dpower.
# __pow__ -> _dpower -> _div_nearest: __pow__ has a transitive dependency on _div_nearest through _dpower.
# Note: __pow__ does not call _dexp directly, but reaches it via _dpower.
# __rpow__ calls _convert_other, which calls Decimal -- so __rpow__ transitively depends on Decimal.
# quantize -> _convert_other -> Decimal: quantize has a transitive dependency on Decimal through _convert_other.
# Since quantize invokes getcontext and getcontext invokes Context, quantize indirectly relies on Context.
# Note: same_quantum does not call Decimal directly, but reaches it via _convert_other.
# Note: to_integral_exact does not call Decimal directly, but reaches it via _convert_other.
# to_integral_exact -> getcontext -> Context: to_integral_exact has a transitive dependency on Context through getcontext.
# Note: to_integral_value does not call Context directly, but reaches it via getcontext.
# Since to_integral_value invokes _convert_other and _convert_other invokes Decimal, to_integral_value indirectly relies on Decimal.
# Note: sqrt does not call Decimal directly, but reaches it via _convert_other.
# sqrt -> getcontext -> Context: sqrt has a transitive dependency on Context through getcontext.
# max calls getcontext, which calls Context -- so max transitively depends on Context.
# max -> _convert_other -> Decimal: max has a transitive dependency on Decimal through _convert_other.
# Since min invokes getcontext and getcontext invokes Context, min indirectly relies on Context.
# Since min invokes _convert_other and _convert_other invokes Decimal, min indirectly relies on Decimal.
# compare_signal -> _convert_other -> Decimal: compare_signal has a transitive dependency on Decimal through _convert_other.
# compare_total calls _convert_other, which calls Decimal -- so compare_total transitively depends on Decimal.
# Since compare_total_mag invokes _convert_other and _convert_other invokes Decimal, compare_total_mag indirectly relies on Decimal.
# copy_abs calls _convert_other, which calls Decimal -- so copy_abs transitively depends on Decimal.
# Note: copy_negate does not call Decimal directly, but reaches it via _convert_other.
# Note: copy_sign does not call Decimal directly, but reaches it via _convert_other.
# exp -> _convert_other -> Decimal: exp has a transitive dependency on Decimal through _convert_other.
# Since exp invokes _dexp and _dexp invokes _div_nearest, exp indirectly relies on _div_nearest.
# exp calls _dexp, which calls _log10_digits -- so exp transitively depends on _log10_digits.
# Note: exp does not call _iexp directly, but reaches it via _dexp.
# exp -> getcontext -> Context: exp has a transitive dependency on Context through getcontext.
# is_normal -> getcontext -> Context: is_normal has a transitive dependency on Context through getcontext.
# Note: is_normal does not call Decimal directly, but reaches it via _convert_other.
# is_subnormal calls getcontext, which calls Context -- so is_subnormal transitively depends on Context.
# is_subnormal calls _convert_other, which calls Decimal -- so is_subnormal transitively depends on Decimal.
# ln calls _dlog, which calls _div_nearest -- so ln transitively depends on _div_nearest.
# Since ln invokes _dlog and _dlog invokes _log10_digits, ln indirectly relies on _log10_digits.
# Note: ln does not call _ilog directly, but reaches it via _dlog.
# ln calls _convert_other, which calls Decimal -- so ln transitively depends on Decimal.
# ln calls getcontext, which calls Context -- so ln transitively depends on Context.
# Note: log10 does not call _div_nearest directly, but reaches it via _dlog10.
# Note: log10 does not call _log10_digits directly, but reaches it via _dlog10.
# Since log10 invokes _dlog10 and _dlog10 invokes _ilog, log10 indirectly relies on _ilog.
# log10 calls _convert_other, which calls Decimal -- so log10 transitively depends on Decimal.
# log10 -> getcontext -> Context: log10 has a transitive dependency on Context through getcontext.
# logb calls getcontext, which calls Context -- so logb transitively depends on Context.
# Since logb invokes _convert_other and _convert_other invokes Decimal, logb indirectly relies on Decimal.
# Note: logical_and does not call Decimal directly, but reaches it via _convert_other.
# logical_and calls getcontext, which calls Context -- so logical_and transitively depends on Context.
# logical_invert calls _convert_other, which calls Decimal -- so logical_invert transitively depends on Decimal.
# logical_invert calls getcontext, which calls Context -- so logical_invert transitively depends on Context.
# logical_or -> _convert_other -> Decimal: logical_or has a transitive dependency on Decimal through _convert_other.
# Since logical_or invokes getcontext and getcontext invokes Context, logical_or indirectly relies on Context.
# Note: logical_xor does not call Decimal directly, but reaches it via _convert_other.
# Since logical_xor invokes getcontext and getcontext invokes Context, logical_xor indirectly relies on Context.
# Since max_mag invokes getcontext and getcontext invokes Context, max_mag indirectly relies on Context.
# Since max_mag invokes _convert_other and _convert_other invokes Decimal, max_mag indirectly relies on Decimal.
# Since min_mag invokes getcontext and getcontext invokes Context, min_mag indirectly relies on Context.
# min_mag calls _convert_other, which calls Decimal -- so min_mag transitively depends on Decimal.
# next_minus calls _convert_other, which calls Decimal -- so next_minus transitively depends on Decimal.
# Note: next_minus does not call Context directly, but reaches it via getcontext.
# Note: next_plus does not call Decimal directly, but reaches it via _convert_other.
# next_plus -> getcontext -> Context: next_plus has a transitive dependency on Context through getcontext.
# Since next_toward invokes getcontext and getcontext invokes Context, next_toward indirectly relies on Context.
# next_toward calls _convert_other, which calls Decimal -- so next_toward transitively depends on Decimal.
# number_class calls getcontext, which calls Context -- so number_class transitively depends on Context.
# number_class calls _convert_other, which calls Decimal -- so number_class transitively depends on Decimal.
# rotate calls _convert_other, which calls Decimal -- so rotate transitively depends on Decimal.
# Since rotate invokes getcontext and getcontext invokes Context, rotate indirectly relies on Context.
# Note: scaleb does not call Decimal directly, but reaches it via _convert_other.
# scaleb -> getcontext -> Context: scaleb has a transitive dependency on Context through getcontext.
# shift calls _convert_other, which calls Decimal -- so shift transitively depends on Decimal.
# Note: shift does not call Context directly, but reaches it via getcontext.
# __deepcopy__ -> deepcopy -> copier: __deepcopy__ has a transitive dependency on copier through deepcopy.
# __deepcopy__ calls deepcopy, which calls _keep_alive -- so __deepcopy__ transitively depends on _keep_alive.
# Since __deepcopy__ invokes deepcopy and deepcopy invokes reductor, __deepcopy__ indirectly relies on reductor.
# __deepcopy__ calls deepcopy, which calls _reconstruct -- so __deepcopy__ transitively depends on _reconstruct.
# __deepcopy__ -> deepcopy -> Error: __deepcopy__ has a transitive dependency on Error through deepcopy.
# __setattr__ -> _check_and_set_parent -> _extract_mock: __setattr__ has a transitive dependency on _extract_mock through _check_and_set_parent.
# Since __setattr__ invokes _check_and_set_parent and _check_and_set_parent invokes _is_instance_mock, __setattr__ indirectly relies on _is_instance_mock.
# Since __setattr__ invokes _get_method and _get_method invokes func, __setattr__ indirectly relies on func.
# _raise_error -> error -> basicConfig: _raise_error has a transitive dependency on basicConfig through error.
# Since _raise_error invokes error and error invokes _, _raise_error indirectly relies on _.
# Since _raise_error invokes error and error invokes perror, _raise_error indirectly relies on perror.
# Since add invokes _lock_file and _lock_file invokes _create_temporary, add indirectly relies on _create_temporary.
# add -> _lock_file -> ExternalClashError: add has a transitive dependency on ExternalClashError through _lock_file.
# add calls add_func_stats, which calls add_callers -- so add transitively depends on add_callers.
# add calls _sync_close, which calls _sync_flush -- so add transitively depends on _sync_flush.
# add -> _convert_other -> Decimal: add has a transitive dependency on Decimal through _convert_other.
# Note: copy_decimal does not call Decimal directly, but reaches it via _convert_other.
# Note: divide does not call Decimal directly, but reaches it via _convert_other.
# divide_int -> _convert_other -> Decimal: divide_int has a transitive dependency on Decimal through _convert_other.
# Since divmod invokes _convert_other and _convert_other invokes Decimal, divmod indirectly relies on Decimal.
# is_finite calls _convert_other, which calls Decimal -- so is_finite transitively depends on Decimal.
# is_infinite -> _convert_other -> Decimal: is_infinite has a transitive dependency on Decimal through _convert_other.
# Note: is_nan does not call Decimal directly, but reaches it via _convert_other.
# is_qnan calls _convert_other, which calls Decimal -- so is_qnan transitively depends on Decimal.
# Since is_signed invokes _convert_other and _convert_other invokes Decimal, is_signed indirectly relies on Decimal.
# is_snan calls _convert_other, which calls Decimal -- so is_snan transitively depends on Decimal.
# Note: is_zero does not call Decimal directly, but reaches it via _convert_other.
# minus calls _convert_other, which calls Decimal -- so minus transitively depends on Decimal.
# multiply -> _convert_other -> Decimal: multiply has a transitive dependency on Decimal through _convert_other.
# plus -> _convert_other -> Decimal: plus has a transitive dependency on Decimal through _convert_other.
# Since power invokes _convert_other and _convert_other invokes Decimal, power indirectly relies on Decimal.
# remainder calls _convert_other, which calls Decimal -- so remainder transitively depends on Decimal.
# subtract calls _convert_other, which calls Decimal -- so subtract transitively depends on Decimal.
# to_eng_string calls _convert_other, which calls Decimal -- so to_eng_string transitively depends on Decimal.
# to_sci_string calls _convert_other, which calls Decimal -- so to_sci_string transitively depends on Decimal.
# Since getdigits invokes _ilog and _ilog invokes _sqrt_nearest, getdigits indirectly relies on _sqrt_nearest.
# getdigits -> _ilog -> _div_nearest: getdigits has a transitive dependency on _div_nearest through _ilog.
# getdigits calls _ilog, which calls _rshift_nearest -- so getdigits transitively depends on _rshift_nearest.
# seek calls _get_decompressor, which calls LZMADecompressor -- so seek transitively depends on LZMADecompressor.
# seek -> _get_decompressor -> _check_compression: seek has a transitive dependency on _check_compression through _get_decompressor.
# flush -> _lock_file -> _create_temporary: flush has a transitive dependency on _create_temporary through _lock_file.
# Note: flush does not call ExternalClashError directly, but reaches it via _lock_file.
# flush calls _sync_close, which calls _sync_flush -- so flush transitively depends on _sync_flush.
# flush calls _create_temporary, which calls _create_carefully -- so flush transitively depends on _create_carefully.
# Note: __getstate__ does not call get_spawning_popen directly, but reaches it via assert_spawning.
# Since inner invokes method and method invokes func, inner indirectly relies on func.
# inner -> removeHandler -> method: inner has a transitive dependency on method through removeHandler.
# inner -> removeHandler -> wraps: inner has a transitive dependency on wraps through removeHandler.
# inner calls callback, which calls func -- so inner transitively depends on func.
# Since inner invokes callback and callback invokes ensure_future, inner indirectly relies on ensure_future.
# inner calls wraps, which calls partial -- so inner transitively depends on partial.
# inner -> func -> getter: inner has a transitive dependency on getter through func.
# consumer -> inner -> D: consumer has a transitive dependency on D through inner.
# consumer -> inner -> _TLSContentType: consumer has a transitive dependency on _TLSContentType through inner.
# consumer -> inner -> TLSVersion: consumer has a transitive dependency on TLSVersion through inner.
# consumer calls inner, which calls method -- so consumer transitively depends on method.
# consumer calls inner, which calls removeHandler -- so consumer transitively depends on removeHandler.
# Note: consumer does not call callback directly, but reaches it via inner.
# consumer -> inner -> wraps: consumer has a transitive dependency on wraps through inner.
# consumer -> inner -> msg_enum: consumer has a transitive dependency on msg_enum through inner.
# consumer -> inner -> func: consumer has a transitive dependency on func through inner.
# insert calls trace, which calls _emscripten_log -- so insert transitively depends on _emscripten_log.
# Since insert invokes trace and trace invokes getinnerframes, insert indirectly relies on getinnerframes.
# Note: push does not call _emscripten_log directly, but reaches it via trace.
# push -> trace -> getinnerframes: push has a transitive dependency on getinnerframes through trace.
# Since do invokes get_pager and get_pager invokes plain, do indirectly relies on plain.
# Since do invokes get_pager and get_pager invokes tempfile_pager, do indirectly relies on tempfile_pager.
# do calls get_pager, which calls pipe_pager -- so do transitively depends on pipe_pager.
# Note: do does not call _emscripten_log directly, but reaches it via trace.
# do -> trace -> getinnerframes: do has a transitive dependency on getinnerframes through trace.
# do calls build_menu, which calls left_align -- so do transitively depends on left_align.
# Since do invokes pager and pager invokes get_pager, do indirectly relies on get_pager.
# Since calc_screen invokes gen_colors and gen_colors invokes StringIO, calc_screen indirectly relies on StringIO.
# Since calc_screen invokes gen_colors and gen_colors invokes gen_colors_from_token_stream, calc_screen indirectly relies on gen_colors_from_token_stream.
# calc_screen calls gen_colors, which calls recover_unterminated_string -- so calc_screen transitively depends on recover_unterminated_string.
# calc_screen -> trace -> _emscripten_log: calc_screen has a transitive dependency on _emscripten_log through trace.
# Note: calc_screen does not call getinnerframes directly, but reaches it via trace.
# calc_screen calls disp_str, which calls THEME -- so calc_screen transitively depends on THEME.
# calc_screen -> disp_str -> str_width: calc_screen has a transitive dependency on str_width through disp_str.
# Since runcode invokes func and func invokes getter, runcode indirectly relies on getter.
# Note: runsource does not call noecho directly, but reaches it via wrapper.
# Since runsource invokes wrapper and wrapper invokes cache_wrapper, runsource indirectly relies on cache_wrapper.
# Note: runsource does not call py_reduce directly, but reaches it via wrapper.
# runsource calls wrapper, which calls nocbreak -- so runsource transitively depends on nocbreak.
# Note: runsource does not call user_function directly, but reaches it via wrapper.
# Since runsource invokes wrapper and wrapper invokes wraps, runsource indirectly relies on wraps.
# runsource -> wrapper -> echo: runsource has a transitive dependency on echo through wrapper.
# runsource -> wrapper -> initscr: runsource has a transitive dependency on initscr through wrapper.
# runsource -> wrapper -> endwin: runsource has a transitive dependency on endwin through wrapper.
# runsource calls wrapper, which calls func -- so runsource transitively depends on func.
# Note: runsource does not call start_color directly, but reaches it via wrapper.
# runsource calls wrapper, which calls arg -- so runsource transitively depends on arg.
# runsource -> wrapper -> get_ident: runsource has a transitive dependency on get_ident through wrapper.
# Note: runsource does not call cache_len directly, but reaches it via wrapper.
# Note: runsource does not call make_key directly, but reaches it via wrapper.
# runsource -> wrapper -> cache_get: runsource has a transitive dependency on cache_get through wrapper.
# Since runsource invokes wrapper and wrapper invokes dispatch, runsource indirectly relies on dispatch.
# Since runsource invokes wrapper and wrapper invokes cbreak, runsource indirectly relies on cbreak.
# runsource -> wrapper -> cached_method: runsource has a transitive dependency on cached_method through wrapper.
# Note: process_prompt does not call str_width directly, but reaches it via wlen.
# Note: update_cursor does not call _emscripten_log directly, but reaches it via trace.
# update_cursor -> trace -> getinnerframes: update_cursor has a transitive dependency on getinnerframes through trace.
# Since do_cmd invokes trace and trace invokes _emscripten_log, do_cmd indirectly relies on _emscripten_log.
# Since do_cmd invokes trace and trace invokes getinnerframes, do_cmd indirectly relies on getinnerframes.
# run_hooks calls install_threading_hook, which calls field -- so run_hooks transitively depends on field.
# Note: run_hooks does not call ExceptHookHandler directly, but reaches it via install_threading_hook.
# Since _parse_terminfo_file invokes _read_terminfo_file and _read_terminfo_file invokes _get_terminfo_dirs, _parse_terminfo_file indirectly relies on _get_terminfo_dirs.
# Since _parse_terminfo_file invokes _read_terminfo_file and _read_terminfo_file invokes _validate_terminal_name_or_raise, _parse_terminfo_file indirectly relies on _validate_terminal_name_or_raise.
# Since push_char invokes trace and trace invokes _emscripten_log, push_char indirectly relies on _emscripten_log.
# push_char calls trace, which calls getinnerframes -- so push_char transitively depends on getinnerframes.
# __write_changed_line -> wlen -> str_width: __write_changed_line has a transitive dependency on str_width through wlen.
# Note: _scroll does not call FormatError directly, but reaches it via WinError.
# _scroll calls WinError, which calls GetLastError -- so _scroll transitively depends on GetLastError.
# screen_xy calls WinError, which calls FormatError -- so screen_xy transitively depends on FormatError.
# screen_xy calls WinError, which calls GetLastError -- so screen_xy transitively depends on GetLastError.
# getheightwidth -> WinError -> FormatError: getheightwidth has a transitive dependency on FormatError through WinError.
# Since getheightwidth invokes WinError and WinError invokes GetLastError, getheightwidth indirectly relies on GetLastError.
# Since _getscrollbacksize invokes WinError and WinError invokes FormatError, _getscrollbacksize indirectly relies on FormatError.
# _getscrollbacksize -> WinError -> GetLastError: _getscrollbacksize has a transitive dependency on GetLastError through WinError.
# Note: _read_input does not call FormatError directly, but reaches it via WinError.
# Note: _read_input does not call GetLastError directly, but reaches it via WinError.
# _read_input_bulk -> WinError -> FormatError: _read_input_bulk has a transitive dependency on FormatError through WinError.
# _read_input_bulk -> WinError -> GetLastError: _read_input_bulk has a transitive dependency on GetLastError through WinError.
# forgetinput -> WinError -> FormatError: forgetinput has a transitive dependency on FormatError through WinError.
# forgetinput -> WinError -> GetLastError: forgetinput has a transitive dependency on GetLastError through WinError.
# Note: getpending does not call _emscripten_log directly, but reaches it via trace.
# Since getpending invokes trace and trace invokes getinnerframes, getpending indirectly relies on getinnerframes.
# Note: wait_for_event does not call FormatError directly, but reaches it via WinError.
# Since wait_for_event invokes WinError and WinError invokes GetLastError, wait_for_event indirectly relies on GetLastError.
# Note: get_dict does not call get_ident directly, but reaches it via current_thread.
# Since get_dict invokes current_thread and current_thread invokes _DummyThread, get_dict indirectly relies on _DummyThread.
# Note: create_dict does not call get_ident directly, but reaches it via current_thread.
# create_dict -> current_thread -> _DummyThread: create_dict has a transitive dependency on _DummyThread through current_thread.
# __contains__ -> error -> basicConfig: __contains__ has a transitive dependency on basicConfig through error.
# __contains__ calls error, which calls _ -- so __contains__ transitively depends on _.
# __contains__ calls error, which calls perror -- so __contains__ transitively depends on perror.
# Since __convert_to_ast invokes _template_to_ast and _template_to_ast invokes _template_to_ast_literal, __convert_to_ast indirectly relies on _template_to_ast_literal.
# __convert_to_ast -> _template_to_ast -> _template_to_ast_constructor: __convert_to_ast has a transitive dependency on _template_to_ast_constructor through _template_to_ast.
# visit_Name -> type_repr -> _template_to_ast: visit_Name has a transitive dependency on _template_to_ast through type_repr.
# Since _set_color invokes get_theme and get_theme invokes can_colorize, _set_color indirectly relies on can_colorize.
# _set_color calls can_colorize, which calls _safe_getenv -- so _set_color transitively depends on _safe_getenv.
# _format_usage -> get_lines -> get_source: _format_usage has a transitive dependency on get_source through get_lines.
# _format_usage -> _ -> resolve: _format_usage has a transitive dependency on resolve through _.
# _format_usage calls _, which calls _infer_caller -- so _format_usage transitively depends on _infer_caller.
# _get_help_string calls _, which calls resolve -- so _get_help_string transitively depends on resolve.
# Note: _get_help_string does not call _infer_caller directly, but reaches it via _.
# Since _handle_conflict_error invokes ngettext and ngettext invokes _as_int2, _handle_conflict_error indirectly relies on _as_int2.
# _handle_conflict_error calls ngettext, which calls dngettext -- so _handle_conflict_error transitively depends on dngettext.
# Note: add_subparsers does not call resolve directly, but reaches it via _.
# Note: add_subparsers does not call _infer_caller directly, but reaches it via _.
# _parse_known_args calls take_action, which calls ArgumentError -- so _parse_known_args transitively depends on ArgumentError.
# Since _parse_known_args invokes take_action and take_action invokes _get_action_name, _parse_known_args indirectly relies on _get_action_name.
# _parse_known_args calls take_action, which calls _ -- so _parse_known_args transitively depends on _.
# Since _parse_known_args invokes take_action and take_action invokes action, _parse_known_args indirectly relies on action.
# Since _parse_known_args invokes consume_positionals and consume_positionals invokes take_action, _parse_known_args indirectly relies on take_action.
# Since _parse_known_args invokes consume_positionals and consume_positionals invokes _, _parse_known_args indirectly relies on _.
# _parse_known_args -> consume_positionals -> match_partial: _parse_known_args has a transitive dependency on match_partial through consume_positionals.
# _parse_known_args -> consume_optional -> ArgumentError: _parse_known_args has a transitive dependency on ArgumentError through consume_optional.
# _parse_known_args calls consume_optional, which calls take_action -- so _parse_known_args transitively depends on take_action.
# _parse_known_args -> consume_optional -> match_argument: _parse_known_args has a transitive dependency on match_argument through consume_optional.
# _parse_known_args -> consume_optional -> _: _parse_known_args has a transitive dependency on _ through consume_optional.
# _parse_known_args calls _, which calls resolve -- so _parse_known_args transitively depends on resolve.
# _parse_known_args -> _ -> _infer_caller: _parse_known_args has a transitive dependency on _infer_caller through _.
# _match_argument calls _, which calls resolve -- so _match_argument transitively depends on resolve.
# _match_argument -> _ -> _infer_caller: _match_argument has a transitive dependency on _infer_caller through _.
# Since _match_argument invokes ngettext and ngettext invokes _as_int2, _match_argument indirectly relies on _as_int2.
# Note: _match_argument does not call dngettext directly, but reaches it via ngettext.
# _get_option_tuples calls _, which calls resolve -- so _get_option_tuples transitively depends on resolve.
# Note: _get_option_tuples does not call _infer_caller directly, but reaches it via _.
# Since parse_intermixed_args invokes _ and _ invokes resolve, parse_intermixed_args indirectly relies on resolve.
# Note: parse_intermixed_args does not call _infer_caller directly, but reaches it via _.
# Note: _get_value does not call resolve directly, but reaches it via _.
# _get_value -> _ -> _infer_caller: _get_value has a transitive dependency on _infer_caller through _.
# _check_value calls _, which calls resolve -- so _check_value transitively depends on resolve.
# _check_value -> _ -> _infer_caller: _check_value has a transitive dependency on _infer_caller through _.
# _warning calls _, which calls resolve -- so _warning transitively depends on resolve.
# _warning calls _, which calls _infer_caller -- so _warning transitively depends on _infer_caller.
# Note: _convert_signed_num does not call _raise_malformed_node directly, but reaches it via _convert_num.
# Note: _compare does not call Decimal directly, but reaches it via compare.
# _compare calls compare, which calls _compare_attributes -- so _compare transitively depends on _compare_attributes.
# Since _compare invokes compare and compare invokes _convert_other, _compare indirectly relies on _convert_other.
# _compare calls compare, which calls SequenceMatcher -- so _compare transitively depends on SequenceMatcher.
# _compare calls compare, which calls _compare_fields -- so _compare transitively depends on _compare_fields.
# Note: _compare_fields does not call compare directly, but reaches it via _compare.
# set_exception calls exception, which calls CancelledError -- so set_exception transitively depends on CancelledError.
# Note: set_exception does not call error directly, but reaches it via exception.
# Since _set_state invokes _copy_future_state and _copy_future_state invokes _convert_future_exc, _set_state indirectly relies on _convert_future_exc.
# Note: _set_state does not call _convert_future_exc directly, but reaches it via _set_concurrent_future_state.
# _call_set_state -> _set_state -> _copy_future_state: _call_set_state has a transitive dependency on _copy_future_state through _set_state.
# Note: _call_set_state does not call _set_concurrent_future_state directly, but reaches it via _set_state.
# _call_set_state calls _set_state, which calls isfuture -- so _call_set_state transitively depends on isfuture.
# _put calls heappush, which calls _siftdown -- so _put transitively depends on _siftdown.
# Since _get invokes heappop and heappop invokes _siftup, _get indirectly relies on _siftup.
# Since callback invokes func and func invokes getter, callback indirectly relies on getter.
# Since callback invokes ensure_future and ensure_future invokes _wrap_awaitable, callback indirectly relies on _wrap_awaitable.
# Note: _start does not call _raise_serialization_error directly, but reaches it via _escape_cdata_c14n.
# Since _start invokes _escape_attrib_c14n and _escape_attrib_c14n invokes _raise_serialization_error, _start indirectly relies on _raise_serialization_error.
# Note: _start does not call serialize directly, but reaches it via write.
# Since _start invokes write and write invokes sleep, _start indirectly relies on sleep.
# _start calls write, which calls _serialize_text -- so _start transitively depends on _serialize_text.
# _start calls write, which calls assert_ -- so _start transitively depends on assert_.
# Note: _start does not call _namespaces directly, but reaches it via write.
# Note: _start does not call time directly, but reaches it via write.
# Note: _start does not call _count_to_size directly, but reaches it via write.
# _start -> write -> quote: _start has a transitive dependency on quote through write.
# Note: _start does not call _nbytes directly, but reaches it via write.
# _start -> write -> _get_writer: _start has a transitive dependency on _get_writer through write.
# _start calls write, which calls crc32 -- so _start transitively depends on crc32.
# Since _do_wait invokes callback and callback invokes func, _do_wait indirectly relies on func.
# _do_wait calls callback, which calls ensure_future -- so _do_wait transitively depends on ensure_future.
# Note: accept does not call _create_response directly, but reaches it via answer_challenge.
# Note: accept does not call AuthenticationError directly, but reaches it via answer_challenge.
# Note: accept does not call _verify_challenge directly, but reaches it via deliver_challenge.
# _poll -> wait -> DoneAndNotDoneFutures: _poll has a transitive dependency on DoneAndNotDoneFutures through wait.
# Note: _poll does not call _AcquireFutures directly, but reaches it via wait.
# Since _poll invokes wait and wait invokes _time, _poll indirectly relies on _time.
# _poll calls wait, which calls _exhaustive_wait -- so _poll transitively depends on _exhaustive_wait.
# _poll calls wait, which calls _WaitSelector -- so _poll transitively depends on _WaitSelector.
# _poll -> wait -> _create_and_install_waiters: _poll has a transitive dependency on _create_and_install_waiters through wait.
# Note: _poll does not call _allocate_lock directly, but reaches it via wait.
# Note: _poll does not call fileno directly, but reaches it via wait.
# Since _poll invokes callback and callback invokes func, _poll indirectly relies on func.
# _poll calls callback, which calls ensure_future -- so _poll transitively depends on ensure_future.
# Since callback_wrapper invokes func and func invokes getter, callback_wrapper indirectly relies on getter.
# break_here -> effective -> checkfuncname: break_here has a transitive dependency on checkfuncname through effective.
# Since itermonthdays invokes repeat and repeat invokes Timer, itermonthdays indirectly relies on Timer.
# Since itermonthdays invokes monthrange and monthrange invokes _validate_month, itermonthdays indirectly relies on _validate_month.
# itermonthdays -> monthrange -> isleap: itermonthdays has a transitive dependency on isleap through monthrange.
# Since itermonthdays invokes monthrange and monthrange invokes weekday, itermonthdays indirectly relies on weekday.
# Note: itermonthdays3 does not call _validate_month directly, but reaches it via monthrange.
# itermonthdays3 calls monthrange, which calls isleap -- so itermonthdays3 transitively depends on isleap.
# itermonthdays3 -> monthrange -> weekday: itermonthdays3 has a transitive dependency on weekday through monthrange.
# Note: itermonthdays3 does not call isleap directly, but reaches it via _monthlen.
# Since formatmonthname invokes _validate_month and _validate_month invokes IllegalMonthError, formatmonthname indirectly relies on IllegalMonthError.
# formatweek calls get_colors, which calls ANSIColors -- so formatweek transitively depends on ANSIColors.
# formatweek -> get_colors -> can_colorize: formatweek has a transitive dependency on can_colorize through get_colors.
# Note: onecmd does not call getter directly, but reaches it via func.
# Since do_help invokes func and func invokes getter, do_help indirectly relies on getter.
# Note: __setitem__ does not call basicConfig directly, but reaches it via error.
# Note: __setitem__ does not call _ directly, but reaches it via error.
# __setitem__ calls error, which calls perror -- so __setitem__ transitively depends on perror.
# __setitem__ -> _lock_file -> _create_temporary: __setitem__ has a transitive dependency on _create_temporary through _lock_file.
# Note: __setitem__ does not call ExternalClashError directly, but reaches it via _lock_file.
# __setitem__ -> _sync_close -> _sync_flush: __setitem__ has a transitive dependency on _sync_flush through _sync_close.
# __delitem__ -> error -> basicConfig: __delitem__ has a transitive dependency on basicConfig through error.
# Note: __delitem__ does not call _ directly, but reaches it via error.
# __delitem__ -> error -> perror: __delitem__ has a transitive dependency on perror through error.
# Note: _invoke_callbacks does not call func directly, but reaches it via callback.
# _invoke_callbacks -> callback -> ensure_future: _invoke_callbacks has a transitive dependency on ensure_future through callback.
# map calls result_iterator, which calls executor_weakref -- so map transitively depends on executor_weakref.
# Since map invokes result_iterator and result_iterator invokes _result_or_cancel, map indirectly relies on _result_or_cancel.
# Note: _on_queue_feeder_error does not call TracebackException directly, but reaches it via format_exception.
# _on_queue_feeder_error -> format_exception -> _parse_value_tb: _on_queue_feeder_error has a transitive dependency on _parse_value_tb through format_exception.
# Since wait_result_broken_or_wakeup invokes format_exception and format_exception invokes TracebackException, wait_result_broken_or_wakeup indirectly relies on TracebackException.
# wait_result_broken_or_wakeup calls format_exception, which calls _parse_value_tb -- so wait_result_broken_or_wakeup transitively depends on _parse_value_tb.
# _decref -> dispatch -> convert_to_error: _decref has a transitive dependency on convert_to_error through dispatch.
# _decref calls dispatch, which calls _find_impl -- so _decref transitively depends on _find_impl.
# Note: _decref does not call get_cache_token directly, but reaches it via dispatch.
# Note: __set_name__ does not call getter directly, but reaches it via func.
# Since __get__ invokes get and get invokes _impl, __get__ indirectly relies on _impl.
# Note: __get__ does not call ref directly, but reaches it via get.
# Since __get__ invokes get and get invokes NoOptionError, __get__ indirectly relies on NoOptionError.
# Note: __get__ does not call time directly, but reaches it via get.
# __get__ -> get -> GenericBrowser: __get__ has a transitive dependency on GenericBrowser through get.
# __get__ calls get, which calls register_standard_browsers -- so __get__ transitively depends on register_standard_browsers.
# __get__ calls get, which calls func -- so __get__ transitively depends on func.
# Since __get__ invokes get and get invokes BackgroundBrowser, __get__ indirectly relies on BackgroundBrowser.
# __get__ -> get -> _resolve_unbound: __get__ has a transitive dependency on _resolve_unbound through get.
# __get__ calls get, which calls pack -- so __get__ transitively depends on pack.
# Since __get__ invokes get and get invokes _synthesize, __get__ indirectly relies on _synthesize.
# __get__ -> get -> Error: __get__ has a transitive dependency on Error through get.
# __get__ calls get, which calls wr -- so __get__ transitively depends on wr.
# Since helper invokes wraps and wraps invokes partial, helper indirectly relies on partial.
# Since _create_cb_wrapper invokes callback and callback invokes func, _create_cb_wrapper indirectly relies on func.
# Since _create_cb_wrapper invokes callback and callback invokes ensure_future, _create_cb_wrapper indirectly relies on ensure_future.
# Note: _create_async_cb_wrapper does not call func directly, but reaches it via callback.
# _create_async_cb_wrapper -> callback -> ensure_future: _create_async_cb_wrapper has a transitive dependency on ensure_future through callback.
# dllist -> _get_module_handles -> _get_current_process: dllist has a transitive dependency on _get_current_process through _get_module_handles.
# Since dllist invokes _get_module_handles and _get_module_handles invokes _enum_process_modules, dllist indirectly relies on _enum_process_modules.
# Since dllist invokes _get_module_filename and _get_module_filename invokes _k32_get_module_file_name, dllist indirectly relies on _k32_get_module_file_name.
# Note: __annotate__ does not call _type_convert directly, but reaches it via _type_check.
# _verify_open calls error, which calls basicConfig -- so _verify_open transitively depends on basicConfig.
# Since _verify_open invokes error and error invokes _, _verify_open indirectly relies on _.
# Note: _verify_open does not call perror directly, but reaches it via error.
# iterkeys calls error, which calls basicConfig -- so iterkeys transitively depends on basicConfig.
# iterkeys calls error, which calls _ -- so iterkeys transitively depends on _.
# iterkeys -> error -> perror: iterkeys has a transitive dependency on perror through error.
# Note: _execute does not call basicConfig directly, but reaches it via error.
# _execute -> error -> _: _execute has a transitive dependency on _ through error.
# Since _execute invokes error and error invokes perror, _execute indirectly relies on perror.
# Since _line_pair_iterator invokes _line_iterator and _line_iterator invokes _make_line, _line_pair_iterator indirectly relies on _make_line.
# make_table -> _mdiff -> _make_line: make_table has a transitive dependency on _make_line through _mdiff.
# make_table -> _mdiff -> _line_pair_iterator: make_table has a transitive dependency on _line_pair_iterator through _mdiff.
# Since make_table invokes _mdiff and _mdiff invokes ndiff, make_table indirectly relies on ndiff.
# Since make_table invokes _mdiff and _mdiff invokes _line_iterator, make_table indirectly relies on _line_iterator.
# jump_target -> _get_jump_target -> _is_backward_jump: jump_target has a transitive dependency on _is_backward_jump through _get_jump_target.
# Note: jump_target does not call _deoptop directly, but reaches it via _get_jump_target.
# jump_target -> _get_jump_target -> _get_cache_size: jump_target has a transitive dependency on _get_cache_size through _get_jump_target.
# get_argval_argrepr -> _get_name_info -> get_name: get_argval_argrepr has a transitive dependency on get_name through _get_name_info.
# Note: get_argval_argrepr does not call _get_const_value directly, but reaches it via _get_const_info.
# Note: report_unexpected_exception does not call StringIO directly, but reaches it via _exception_traceback.
# Note: _failure_header does not call _safe_getenv directly, but reaches it via can_colorize.
# Since __run invokes _exception_traceback and _exception_traceback invokes StringIO, __run indirectly relies on StringIO.
# Note: __run does not call _get_reader directly, but reaches it via check.
# Since __run invokes check and check invokes process_tokens, __run indirectly relies on process_tokens.
# __run -> check -> errprint: __run has a transitive dependency on errprint through check.
# Note: fold does not call make_quoted_pairs directly, but reaches it via _refold_parse_tree.
# Note: fold does not call _steal_trailing_WSP_if_exists directly, but reaches it via _refold_parse_tree.
# Note: fold does not call ValueTerminal directly, but reaches it via _refold_parse_tree.
# fold calls _refold_parse_tree, which calls _fold_mime_parameters -- so fold transitively depends on _fold_mime_parameters.
# fold -> _refold_parse_tree -> Terminal: fold has a transitive dependency on Terminal through _refold_parse_tree.
# Note: fold does not call make_parenthesis_pairs directly, but reaches it via _refold_parse_tree.
# fold -> _refold_parse_tree -> _fold_as_ew: fold has a transitive dependency on _fold_as_ew through _refold_parse_tree.
# value calls quote_string, which calls make_quoted_pairs -- so value transitively depends on make_quoted_pairs.
# addr_spec -> quote_string -> make_quoted_pairs: addr_spec has a transitive dependency on make_quoted_pairs through quote_string.
# Note: getaddrspec does not call quote_from_bytes directly, but reaches it via quote.
# clone -> deepcopy -> copier: clone has a transitive dependency on copier through deepcopy.
# Note: clone does not call _keep_alive directly, but reaches it via deepcopy.
# clone calls deepcopy, which calls reductor -- so clone transitively depends on reductor.
# clone calls deepcopy, which calls _reconstruct -- so clone transitively depends on _reconstruct.
# clone -> deepcopy -> Error: clone has a transitive dependency on Error through deepcopy.
# set_content -> handler -> start: set_content has a transitive dependency on start through handler.
# Note: set_content does not call end directly, but reaches it via handler.
# set_content -> handler -> append: set_content has a transitive dependency on append through handler.
# Note: set_content does not call end_ns directly, but reaches it via handler.
# set_content calls handler, which calls start_ns -- so set_content transitively depends on start_ns.
# _write -> warn -> _is_internal_frame: _write has a transitive dependency on _is_internal_frame through warn.
# Note: _write does not call _next_external_frame directly, but reaches it via warn.
# Note: _write does not call warning directly, but reaches it via warn.
# _write calls deepcopy, which calls copier -- so _write transitively depends on copier.
# Note: _write does not call _keep_alive directly, but reaches it via deepcopy.
# _write -> deepcopy -> reductor: _write has a transitive dependency on reductor through deepcopy.
# _write calls deepcopy, which calls _reconstruct -- so _write transitively depends on _reconstruct.
# _write -> deepcopy -> Error: _write has a transitive dependency on Error through deepcopy.
# _dispatch calls func, which calls getter -- so _dispatch transitively depends on getter.
# Note: _handle_text does not call copier directly, but reaches it via deepcopy.
# Note: _handle_text does not call _keep_alive directly, but reaches it via deepcopy.
# Since _handle_text invokes deepcopy and deepcopy invokes reductor, _handle_text indirectly relies on reductor.
# _handle_text calls deepcopy, which calls _reconstruct -- so _handle_text transitively depends on _reconstruct.
# _handle_text -> deepcopy -> Error: _handle_text has a transitive dependency on Error through deepcopy.
# Since get_filename invokes _get_module_code and _get_module_code invokes _get_module_path, get_filename indirectly relies on _get_module_path.
# Since get_filename invokes _get_module_code and _get_module_code invokes _compile_source, get_filename indirectly relies on _compile_source.
# get_filename calls _get_module_code, which calls ZipImportError -- so get_filename transitively depends on ZipImportError.
# Note: get_filename does not call _unmarshal_code directly, but reaches it via _get_module_code.
# get_filename calls _get_module_code, which calls _get_data -- so get_filename transitively depends on _get_data.
# win32_code_page_search_function calls create_win32_code_page_codec, which calls code_page_encode -- so win32_code_page_search_function transitively depends on code_page_encode.
# win32_code_page_search_function -> create_win32_code_page_codec -> code_page_decode: win32_code_page_search_function has a transitive dependency on code_page_decode through create_win32_code_page_codec.
# _buffer_encode -> ToASCII -> nameprep: _buffer_encode has a transitive dependency on nameprep through ToASCII.
# _buffer_decode -> ToUnicode -> nameprep: _buffer_decode has a transitive dependency on nameprep through ToUnicode.
# _buffer_decode calls ToUnicode, which calls ToASCII -- so _buffer_decode transitively depends on ToASCII.
# Note: _convert_ does not call _is_descriptor directly, but reaches it via _simple_enum.
# Since _convert_ invokes _simple_enum and _simple_enum invokes _is_single_bit, _convert_ indirectly relies on _is_single_bit.
# _convert_ calls _simple_enum, which calls _is_private -- so _convert_ transitively depends on _is_private.
# Note: _convert_ does not call new_member directly, but reaches it via _simple_enum.
# _convert_ -> _simple_enum -> _is_sunder: _convert_ has a transitive dependency on _is_sunder through _simple_enum.
# Note: _convert_ does not call _is_dunder directly, but reaches it via _simple_enum.
# _convert_ -> _simple_enum -> gnv: _convert_ has a transitive dependency on gnv through _simple_enum.
# Since phase0 invokes _filter and _filter invokes filterfalse, phase0 indirectly relies on filterfalse.
# phase1 -> filterfalse -> _compile_pattern: phase1 has a transitive dependency on _compile_pattern through filterfalse.
# Since phase1 invokes filterfalse and filterfalse invokes match, phase1 indirectly relies on match.
# Since phase3 invokes cmpfiles and cmpfiles invokes _cmp, phase3 indirectly relies on _cmp.
# Since _format_float_style invokes _round_to_figures and _round_to_figures invokes _round_to_exponent, _format_float_style indirectly relies on _round_to_exponent.
# Note: connect does not call address_type directly, but reaches it via Client.
# Since connect invokes Client and Client invokes SocketClient, connect indirectly relies on SocketClient.
# Since connect invokes Client and Client invokes answer_challenge, connect indirectly relies on answer_challenge.
# connect -> Client -> Queue: connect has a transitive dependency on Queue through Client.
# connect -> Client -> deliver_challenge: connect has a transitive dependency on deliver_challenge through Client.
# Since connect invokes Client and Client invokes _validate_family, connect indirectly relies on _validate_family.
# connect -> Client -> PipeClient: connect has a transitive dependency on PipeClient through Client.
# Since connect invokes Client and Client invokes Connection, connect indirectly relies on Connection.
# connect calls dispatch, which calls convert_to_error -- so connect transitively depends on convert_to_error.
# Note: connect does not call _find_impl directly, but reaches it via dispatch.
# Since connect invokes dispatch and dispatch invokes get_cache_token, connect indirectly relies on get_cache_token.
# Since makepasv invokes parse229 and parse229 invokes error_reply, makepasv indirectly relies on error_reply.
# Since makepasv invokes parse229 and parse229 invokes error_proto, makepasv indirectly relies on error_proto.
# Note: makepasv does not call error_reply directly, but reaches it via parse227.
# Since makepasv invokes parse227 and parse227 invokes error_proto, makepasv indirectly relies on error_proto.
# Note: ntransfercmd does not call error_reply directly, but reaches it via parse150.
# retrbinary -> callback -> func: retrbinary has a transitive dependency on func through callback.
# retrbinary calls callback, which calls ensure_future -- so retrbinary transitively depends on ensure_future.
# retrlines calls callback, which calls func -- so retrlines transitively depends on func.
# retrlines -> callback -> ensure_future: retrlines has a transitive dependency on ensure_future through callback.
# Since storbinary invokes callback and callback invokes func, storbinary indirectly relies on func.
# storbinary calls callback, which calls ensure_future -- so storbinary transitively depends on ensure_future.
# storlines calls callback, which calls func -- so storlines transitively depends on func.
# storlines -> callback -> ensure_future: storlines has a transitive dependency on ensure_future through callback.
# mkd calls parse257, which calls error_reply -- so mkd transitively depends on error_reply.
# Note: pwd does not call error_reply directly, but reaches it via parse257.
# __setstate__ -> _partial_prepare_merger -> itemgetter: __setstate__ has a transitive dependency on itemgetter through _partial_prepare_merger.
# decorating_function -> _lru_cache_wrapper -> cache_get: decorating_function has a transitive dependency on cache_get through _lru_cache_wrapper.
# Note: decorating_function does not call cache_len directly, but reaches it via _lru_cache_wrapper.
# Note: decorating_function does not call user_function directly, but reaches it via _lru_cache_wrapper.
# decorating_function -> _lru_cache_wrapper -> _CacheInfo: decorating_function has a transitive dependency on _CacheInfo through _lru_cache_wrapper.
# Since decorating_function invokes _lru_cache_wrapper and _lru_cache_wrapper invokes make_key, decorating_function indirectly relies on make_key.
# decorating_function -> _lru_cache_wrapper -> RLock: decorating_function has a transitive dependency on RLock through _lru_cache_wrapper.
# wildcard_selector calls match, which calls _compile -- so wildcard_selector transitively depends on _compile.
# wildcard_selector -> match -> _StringGlobber: wildcard_selector has a transitive dependency on _StringGlobber through match.
# Note: recursive_selector does not call _compile directly, but reaches it via match.
# recursive_selector -> match -> _StringGlobber: recursive_selector has a transitive dependency on _StringGlobber through match.
# recursive_selector calls select_recursive_step, which calls match -- so recursive_selector transitively depends on match.
# Since recursive_selector invokes select_recursive_step and select_recursive_step invokes select_next, recursive_selector indirectly relies on select_next.
# Note: goahead does not call __dict_replace directly, but reaches it via unescape.
# Note: begin does not call _parse_header_lines directly, but reaches it via parse_headers.
# begin -> parse_headers -> _read_headers: begin has a transitive dependency on _read_headers through parse_headers.
# begin -> _read_headers -> LineTooLong: begin has a transitive dependency on LineTooLong through _read_headers.
# Since begin invokes _read_headers and _read_headers invokes HTTPException, begin indirectly relies on HTTPException.
# Since _tunnel invokes _read_headers and _read_headers invokes LineTooLong, _tunnel indirectly relies on LineTooLong.
# _tunnel calls _read_headers, which calls HTTPException -- so _tunnel transitively depends on HTTPException.
# Since putrequest invokes urlsplit and urlsplit invokes SplitResult, putrequest indirectly relies on SplitResult.
# putrequest calls urlsplit, which calls _urlsplit -- so putrequest transitively depends on _urlsplit.
# putrequest -> urlsplit -> _coerce_args: putrequest has a transitive dependency on _coerce_args through urlsplit.
# putrequest calls urlsplit, which calls _coerce_result -- so putrequest transitively depends on _coerce_result.
# Since is_blocked invokes user_domain_match and user_domain_match invokes liberal_is_HDN, is_blocked indirectly relies on liberal_is_HDN.
# is_not_allowed -> user_domain_match -> liberal_is_HDN: is_not_allowed has a transitive dependency on liberal_is_HDN through user_domain_match.
# Note: set_ok_verifiability does not call domain_match directly, but reaches it via is_third_party.
# set_ok_verifiability -> is_third_party -> request_host: set_ok_verifiability has a transitive dependency on request_host through is_third_party.
# set_ok_verifiability calls is_third_party, which calls reach -- so set_ok_verifiability transitively depends on reach.
# Note: set_ok_path does not call escape_path directly, but reaches it via request_path.
# set_ok_domain calls domain_match, which calls is_HDN -- so set_ok_domain transitively depends on is_HDN.
# Since set_ok_domain invokes eff_request_host and eff_request_host invokes request_host, set_ok_domain indirectly relies on request_host.
# Note: set_ok_port does not call _debug directly, but reaches it via request_port.
# return_ok_verifiability calls is_third_party, which calls domain_match -- so return_ok_verifiability transitively depends on domain_match.
# Note: return_ok_verifiability does not call request_host directly, but reaches it via is_third_party.
# Note: return_ok_verifiability does not call reach directly, but reaches it via is_third_party.
# Since return_ok_port invokes request_port and request_port invokes _debug, return_ok_port indirectly relies on _debug.
# return_ok_domain calls domain_match, which calls is_HDN -- so return_ok_domain transitively depends on is_HDN.
# return_ok_domain calls eff_request_host, which calls request_host -- so return_ok_domain transitively depends on request_host.
# domain_return_ok -> eff_request_host -> request_host: domain_return_ok has a transitive dependency on request_host through eff_request_host.
# Note: path_return_ok does not call escape_path directly, but reaches it via request_path.
# _cookie_from_cookie_tuple -> request_path -> escape_path: _cookie_from_cookie_tuple has a transitive dependency on escape_path through request_path.
# Since _cookie_from_cookie_tuple invokes request_port and request_port invokes _debug, _cookie_from_cookie_tuple indirectly relies on _debug.
# Note: _cookie_from_cookie_tuple does not call request_host directly, but reaches it via eff_request_host.
# Note: make_cookies does not call unmatched directly, but reaches it via split_header_words.
# Since make_cookies invokes parse_ns_headers and parse_ns_headers invokes strip_quotes, make_cookies indirectly relies on strip_quotes.
# make_cookies -> parse_ns_headers -> http2time: make_cookies has a transitive dependency on http2time through parse_ns_headers.
# Note: save does not call function directly, but reaches it via reduce.
# save -> f -> f1: save has a transitive dependency on f1 through f.
# as_lwp_str calls lwp_cookie_str, which calls join_header_words -- so as_lwp_str transitively depends on join_header_words.
# as_lwp_str calls lwp_cookie_str, which calls time2isoz -- so as_lwp_str transitively depends on time2isoz.
# _really_load -> split_header_words -> unmatched: _really_load has a transitive dependency on unmatched through split_header_words.
# _really_load -> iso2time -> _str2time: _really_load has a transitive dependency on _str2time through iso2time.
# Note: OutputString does not call Time2Internaldate directly, but reaches it via append.
# Since OutputString invokes append and append invokes Charset, OutputString indirectly relies on Charset.
# OutputString -> _quote -> _is_legal_key: OutputString has a transitive dependency on _is_legal_key through _quote.
# OutputString calls _getdate, which calls gmtime -- so OutputString transitively depends on gmtime.
# OutputString calls _getdate, which calls time -- so OutputString transitively depends on time.
# Note: value_encode does not call _is_legal_key directly, but reaches it via _quote.
# Since __parse_string invokes _unquote and _unquote invokes unquote_plus, __parse_string indirectly relies on unquote_plus.
# Note: __parse_string does not call unquote_to_bytes directly, but reaches it via _unquote.
# __parse_string -> _unquote -> _unquote_sub: __parse_string has a transitive dependency on _unquote_sub through _unquote.
# Since value_decode invokes _unquote and _unquote invokes unquote_plus, value_decode indirectly relies on unquote_plus.
# Since value_decode invokes _unquote and _unquote invokes unquote_to_bytes, value_decode indirectly relies on unquote_to_bytes.
# value_decode calls _unquote, which calls _unquote_sub -- so value_decode transitively depends on _unquote_sub.
# Since handle_one_request invokes method and method invokes func, handle_one_request indirectly relies on func.
# Since load_module invokes _get_module_code and _get_module_code invokes _get_module_path, load_module indirectly relies on _get_module_path.
# Note: load_module does not call _compile_source directly, but reaches it via _get_module_code.
# load_module calls _get_module_code, which calls ZipImportError -- so load_module transitively depends on ZipImportError.
# load_module calls _get_module_code, which calls _unmarshal_code -- so load_module transitively depends on _unmarshal_code.
# load_module -> _get_module_code -> _get_data: load_module has a transitive dependency on _get_data through _get_module_code.
# load_module -> _load_module_shim -> _exec: load_module has a transitive dependency on _exec through _load_module_shim.
# Note: load_module does not call _load directly, but reaches it via _load_module_shim.
# load_module -> _load_module_shim -> spec_from_loader: load_module has a transitive dependency on spec_from_loader through _load_module_shim.
# Since _commit_removals invokes pop and pop invokes ref, _commit_removals indirectly relies on ref.
# _commit_removals calls pop, which calls itemref -- so _commit_removals transitively depends on itemref.
# Note: _commit_removals does not call ParseError directly, but reaches it via pop.
# Note: create_module does not call f directly, but reaches it via _call_with_frames_removed.
# exec_module calls _call_with_frames_removed, which calls f -- so exec_module transitively depends on f.
# Note: _check_name_wrapper does not call func directly, but reaches it via method.
# is_package -> _get_module_info -> _get_module_path: is_package has a transitive dependency on _get_module_path through _get_module_info.
# get_source calls _get_module_info, which calls _get_module_path -- so get_source transitively depends on _get_module_path.
# get_source -> _get_data -> _unpack_uint16: get_source has a transitive dependency on _unpack_uint16 through _get_data.
# Note: get_source does not call ZipImportError directly, but reaches it via _get_data.
# get_source calls _get_data, which calls decompress -- so get_source transitively depends on decompress.
# get_source -> _get_data -> _get_decompress_func: get_source has a transitive dependency on _get_decompress_func through _get_data.
# get_code calls _code_to_hash_pyc, which calls _pack_uint32 -- so get_code transitively depends on _pack_uint32.
# get_code -> _get_module_code -> _get_module_path: get_code has a transitive dependency on _get_module_path through _get_module_code.
# get_code calls _get_module_code, which calls _compile_source -- so get_code transitively depends on _compile_source.
# Since get_code invokes _get_module_code and _get_module_code invokes ZipImportError, get_code indirectly relies on ZipImportError.
# get_code -> _get_module_code -> _unmarshal_code: get_code has a transitive dependency on _unmarshal_code through _get_module_code.
# Since get_code invokes _get_module_code and _get_module_code invokes _get_data, get_code indirectly relies on _get_data.
# get_code -> cache_from_source -> _path_split: get_code has a transitive dependency on _path_split through cache_from_source.
# Note: get_code does not call _path_join directly, but reaches it via cache_from_source.
# Since get_code invokes cache_from_source and cache_from_source invokes _path_abspath, get_code indirectly relies on _path_abspath.
# Note: get_code does not call _unpack_uint32 directly, but reaches it via _validate_timestamp_pyc.
# get_code -> _code_to_timestamp_pyc -> _pack_uint32: get_code has a transitive dependency on _pack_uint32 through _code_to_timestamp_pyc.
# get_code calls _classify_pyc, which calls _unpack_uint32 -- so get_code transitively depends on _unpack_uint32.
# _cache_bytecode -> _calc_mode -> _path_stat: _cache_bytecode has a transitive dependency on _path_stat through _calc_mode.
# set_data -> _path_isdir -> _path_is_mode_type: set_data has a transitive dependency on _path_is_mode_type through _path_isdir.
# Note: _get_spec does not call _path_abspath directly, but reaches it via spec_from_file_location.
# Since _get_spec invokes spec_from_file_location and spec_from_file_location invokes _path_split, _get_spec indirectly relies on _path_split.
# Note: _get_spec does not call loader_class directly, but reaches it via spec_from_file_location.
# Note: _get_spec does not call _get_supported_file_loaders directly, but reaches it via spec_from_file_location.
# path_hook -> _path_isdir -> _path_is_mode_type: path_hook has a transitive dependency on _path_is_mode_type through _path_isdir.
# Note: metadata does not call _cast directly, but reaches it via cast.
# Note: _read_egg_info_reqs does not call func directly, but reaches it via pass_none.
# Note: _convert_egg_info_reqs_to_simple_reqs does not call make_condition directly, but reaches it via quoted_marker.
# Since _load_json invokes pass_none and pass_none invokes func, _load_json indirectly relies on func.
# Since _normalized_name invokes pass_none and pass_none invokes func, _normalized_name indirectly relies on func.
# Note: read_bytes does not call attr directly, but reaches it via magic_open.
# read_bytes calls magic_open, which calls text_encoding -- so read_bytes transitively depends on text_encoding.
# Since read_bytes invokes magic_open and magic_open invokes TextIOWrapper, read_bytes indirectly relies on TextIOWrapper.
# from_callable calls _signature_from_callable, which calls _signature_get_partial -- so from_callable transitively depends on _signature_get_partial.
# Note: from_callable does not call _signature_is_builtin directly, but reaches it via _signature_from_callable.
# from_callable calls _signature_from_callable, which calls _signature_get_user_defined_method -- so from_callable transitively depends on _signature_get_user_defined_method.
# Since from_callable invokes _signature_from_callable and _signature_from_callable invokes _get_signature_of, from_callable indirectly relies on _get_signature_of.
# from_callable calls _signature_from_callable, which calls _signature_from_function -- so from_callable transitively depends on _signature_from_function.
# Since from_callable invokes _signature_from_callable and _signature_from_callable invokes isfunction, from_callable indirectly relies on isfunction.
# Since from_callable invokes _signature_from_callable and _signature_from_callable invokes _signature_bound_method, from_callable indirectly relies on _signature_bound_method.
# from_callable calls _signature_from_callable, which calls _descriptor_get -- so from_callable transitively depends on _descriptor_get.
# from_callable calls _signature_from_callable, which calls _signature_is_functionlike -- so from_callable transitively depends on _signature_is_functionlike.
# Note: from_callable does not call _signature_fromstr directly, but reaches it via _signature_from_callable.
# Since from_callable invokes _signature_from_callable and _signature_from_callable invokes unwrap, from_callable indirectly relies on unwrap.
# from_callable calls _signature_from_callable, which calls _signature_from_builtin -- so from_callable transitively depends on _signature_from_builtin.
# Since from_callable invokes _signature_from_callable and _signature_from_callable invokes getattr_static, from_callable indirectly relies on getattr_static.
# Note: _split_addr_prefix does not call AddressValueError directly, but reaches it via _split_optional_netmask.
# Since _iterencode_list invokes _iterencode_dict and _iterencode_dict invokes _floatstr, _iterencode_list indirectly relies on _floatstr.
# _iterencode_list calls _iterencode_dict, which calls _encoder -- so _iterencode_list transitively depends on _encoder.
# Note: _iterencode_list does not call _iterencode directly, but reaches it via _iterencode_dict.
# _iterencode_list -> _iterencode_dict -> _intstr: _iterencode_list has a transitive dependency on _intstr through _iterencode_dict.
# Since _iterencode_list invokes _iterencode and _iterencode invokes _floatstr, _iterencode_list indirectly relies on _floatstr.
# Note: _iterencode_list does not call _encoder directly, but reaches it via _iterencode.
# _iterencode_list calls _iterencode, which calls _intstr -- so _iterencode_list transitively depends on _intstr.
# _iterencode_list calls _iterencode, which calls _iterencode_dict -- so _iterencode_list transitively depends on _iterencode_dict.
# _iterencode_list calls _iterencode, which calls _default -- so _iterencode_list transitively depends on _default.
# Since _iterencode_dict invokes _iterencode_list and _iterencode_list invokes _floatstr, _iterencode_dict indirectly relies on _floatstr.
# _iterencode_dict calls _iterencode_list, which calls _encoder -- so _iterencode_dict transitively depends on _encoder.
# _iterencode_dict calls _iterencode_list, which calls _iterencode -- so _iterencode_dict transitively depends on _iterencode.
# _iterencode_dict calls _iterencode_list, which calls _intstr -- so _iterencode_dict transitively depends on _intstr.
# _iterencode_dict calls _iterencode, which calls _floatstr -- so _iterencode_dict transitively depends on _floatstr.
# _iterencode_dict calls _iterencode, which calls _encoder -- so _iterencode_dict transitively depends on _encoder.
# Since _iterencode_dict invokes _iterencode and _iterencode invokes _iterencode_list, _iterencode_dict indirectly relies on _iterencode_list.
# _iterencode_dict -> _iterencode -> _intstr: _iterencode_dict has a transitive dependency on _intstr through _iterencode.
# Note: _iterencode_dict does not call _default directly, but reaches it via _iterencode.
# Since _iterencode invokes _iterencode_list and _iterencode_list invokes _floatstr, _iterencode indirectly relies on _floatstr.
# _iterencode -> _iterencode_list -> _iterencode_dict: _iterencode has a transitive dependency on _iterencode_dict through _iterencode_list.
# _iterencode -> _iterencode_list -> _encoder: _iterencode has a transitive dependency on _encoder through _iterencode_list.
# Since _iterencode invokes _iterencode_list and _iterencode_list invokes _intstr, _iterencode indirectly relies on _intstr.
# _iterencode -> _iterencode_dict -> _floatstr: _iterencode has a transitive dependency on _floatstr through _iterencode_dict.
# _iterencode -> _iterencode_dict -> _iterencode_list: _iterencode has a transitive dependency on _iterencode_list through _iterencode_dict.
# Since _iterencode invokes _iterencode_dict and _iterencode_dict invokes _encoder, _iterencode indirectly relies on _encoder.
# Note: _iterencode does not call _intstr directly, but reaches it via _iterencode_dict.
# Since _iterencode invokes _default and _default invokes data_handler, _iterencode indirectly relies on data_handler.
# _scan_once -> parse_array -> parse_value: _scan_once has a transitive dependency on parse_value through parse_array.
# Since _scan_once invokes parse_array and parse_array invokes skip_comments_and_array_ws, _scan_once indirectly relies on skip_comments_and_array_ws.
# Since _scan_once invokes parse_array and parse_array invokes TOMLDecodeError, _scan_once indirectly relies on TOMLDecodeError.
# Since scan_once invokes _scan_once and _scan_once invokes parse_string, scan_once indirectly relies on parse_string.
# Note: scan_once does not call parse_array directly, but reaches it via _scan_once.
# Note: scan_once does not call parse_object directly, but reaches it via _scan_once.
# Note: scan_once does not call parse_int directly, but reaches it via _scan_once.
# scan_once calls _scan_once, which calls match_number -- so scan_once transitively depends on match_number.
# Since scan_once invokes _scan_once and _scan_once invokes parse_float, scan_once indirectly relies on parse_float.
# scan_once calls _scan_once, which calls parse_constant -- so scan_once transitively depends on parse_constant.
# getpreferredencoding -> setlocale -> Error: getpreferredencoding has a transitive dependency on Error through setlocale.
# getpreferredencoding calls setlocale, which calls _build_localename -- so getpreferredencoding transitively depends on _build_localename.
# getpreferredencoding calls setlocale, which calls _setlocale -- so getpreferredencoding transitively depends on _setlocale.
# Note: getpreferredencoding does not call normalize directly, but reaches it via setlocale.
# findCaller calls _is_internal_frame, which calls _is_internal_filename -- so findCaller transitively depends on _is_internal_filename.
# Since configure_handler invokes factory and factory invokes loader, configure_handler indirectly relies on loader.
# Since configure_handler invokes factory and factory invokes StreamReaderProtocol, configure_handler indirectly relies on StreamReaderProtocol.
# configure_handler -> factory -> cls: configure_handler has a transitive dependency on cls through factory.
# configure_handler calls factory, which calls custom_task_constructor -- so configure_handler transitively depends on custom_task_constructor.
# Since configure_handler invokes factory and factory invokes StreamReader, configure_handler indirectly relies on StreamReader.
# start calls _cleanup, which calls sem_unlink -- so start transitively depends on sem_unlink.
# Note: start does not call unregister directly, but reaches it via _cleanup.
# Note: get_message does not call _create_temporary directly, but reaches it via _lock_file.
# Note: get_message does not call ExternalClashError directly, but reaches it via _lock_file.
# Note: get_bytes does not call _create_temporary directly, but reaches it via _lock_file.
# Since get_bytes invokes _lock_file and _lock_file invokes ExternalClashError, get_bytes indirectly relies on ExternalClashError.
# lock -> _lock_file -> _create_temporary: lock has a transitive dependency on _create_temporary through _lock_file.
# lock calls _lock_file, which calls ExternalClashError -- so lock transitively depends on ExternalClashError.
# unlock -> _sync_close -> _sync_flush: unlock has a transitive dependency on _sync_flush through _sync_close.
# Note: set_sequences does not call _sync_flush directly, but reaches it via _sync_close.
# Note: _read_windows_registry does not call init directly, but reaches it via add_type.
# _read_windows_registry -> add_type -> _deprecated: _read_windows_registry has a transitive dependency on _deprecated through add_type.
# _send calls write, which calls serialize -- so _send transitively depends on serialize.
# _send calls write, which calls sleep -- so _send transitively depends on sleep.
# _send calls write, which calls _serialize_text -- so _send transitively depends on _serialize_text.
# Note: _send does not call assert_ directly, but reaches it via write.
# Note: _send does not call _namespaces directly, but reaches it via write.
# Since _send invokes write and write invokes time, _send indirectly relies on time.
# Note: _send does not call _count_to_size directly, but reaches it via write.
# _send calls write, which calls quote -- so _send transitively depends on quote.
# _send -> write -> _nbytes: _send has a transitive dependency on _nbytes through write.
# Since _send invokes write and write invokes _get_writer, _send indirectly relies on _get_writer.
# _send calls write, which calls crc32 -- so _send transitively depends on crc32.
# Since _recv invokes read and read invokes UnpicklingError, _recv indirectly relies on UnpicklingError.
# _recv -> read -> size_index: _recv has a transitive dependency on size_index through read.
# _recv -> read -> assert_: _recv has a transitive dependency on assert_ through read.
# _recv calls read, which calls ReadError -- so _recv transitively depends on ReadError.
# Note: _recv does not call Pair directly, but reaches it via read.
# PipeClient -> _check_timeout -> _time: PipeClient has a transitive dependency on _time through _check_timeout.
# Since PipeClient invokes _check_timeout and _check_timeout invokes TimeoutExpired, PipeClient indirectly relies on TimeoutExpired.
# Since _handle_request invokes format_exc and format_exc invokes format_exception, _handle_request indirectly relies on format_exception.
# Since _handle_request invokes func and func invokes getter, _handle_request indirectly relies on getter.
# serve_client -> format_exc -> format_exception: serve_client has a transitive dependency on format_exception through format_exc.
# serve_client calls send, which calls SMTPServerDisconnected -- so serve_client transitively depends on SMTPServerDisconnected.
# serve_client calls send, which calls NotConnected -- so serve_client transitively depends on NotConnected.
# _create calls dispatch, which calls convert_to_error -- so _create transitively depends on convert_to_error.
# _create calls dispatch, which calls _find_impl -- so _create transitively depends on _find_impl.
# Since _create invokes dispatch and dispatch invokes get_cache_token, _create indirectly relies on get_cache_token.
# _debug_info -> dispatch -> convert_to_error: _debug_info has a transitive dependency on convert_to_error through dispatch.
# _debug_info calls dispatch, which calls _find_impl -- so _debug_info transitively depends on _find_impl.
# Note: _debug_info does not call get_cache_token directly, but reaches it via dispatch.
# _number_of_objects -> dispatch -> convert_to_error: _number_of_objects has a transitive dependency on convert_to_error through dispatch.
# Since _number_of_objects invokes dispatch and dispatch invokes _find_impl, _number_of_objects indirectly relies on _find_impl.
# Since _number_of_objects invokes dispatch and dispatch invokes get_cache_token, _number_of_objects indirectly relies on get_cache_token.
# Note: _finalize_manager does not call convert_to_error directly, but reaches it via dispatch.
# Note: _finalize_manager does not call _find_impl directly, but reaches it via dispatch.
# Note: _finalize_manager does not call get_cache_token directly, but reaches it via dispatch.
# Since _callmethod invokes convert_to_error and convert_to_error invokes RemoteError, _callmethod indirectly relies on RemoteError.
# _callmethod calls dispatch, which calls convert_to_error -- so _callmethod transitively depends on convert_to_error.
# Since _callmethod invokes dispatch and dispatch invokes _find_impl, _callmethod indirectly relies on _find_impl.
# _callmethod calls dispatch, which calls get_cache_token -- so _callmethod transitively depends on get_cache_token.
# _incref -> dispatch -> convert_to_error: _incref has a transitive dependency on convert_to_error through dispatch.
# Since _incref invokes dispatch and dispatch invokes _find_impl, _incref indirectly relies on _find_impl.
# _incref calls dispatch, which calls get_cache_token -- so _incref transitively depends on get_cache_token.
# _wait_for_updates -> wait -> DoneAndNotDoneFutures: _wait_for_updates has a transitive dependency on DoneAndNotDoneFutures through wait.
# Note: _wait_for_updates does not call _AcquireFutures directly, but reaches it via wait.
# _wait_for_updates -> wait -> _time: _wait_for_updates has a transitive dependency on _time through wait.
# Since _wait_for_updates invokes wait and wait invokes _exhaustive_wait, _wait_for_updates indirectly relies on _exhaustive_wait.
# Since _wait_for_updates invokes wait and wait invokes _WaitSelector, _wait_for_updates indirectly relies on _WaitSelector.
# _wait_for_updates calls wait, which calls _create_and_install_waiters -- so _wait_for_updates transitively depends on _create_and_install_waiters.
# Since _wait_for_updates invokes wait and wait invokes _allocate_lock, _wait_for_updates indirectly relies on _allocate_lock.
# Since _wait_for_updates invokes wait and wait invokes fileno, _wait_for_updates indirectly relies on fileno.
# _handle_tasks -> put -> time: _handle_tasks has a transitive dependency on time through put.
# _handle_tasks -> put -> _serialize_unbound: _handle_tasks has a transitive dependency on _serialize_unbound through put.
# _handle_tasks calls put, which calls pack -- so _handle_tasks transitively depends on pack.
# _handle_results calls get, which calls _impl -- so _handle_results transitively depends on _impl.
# Since _handle_results invokes get and get invokes ref, _handle_results indirectly relies on ref.
# Note: _handle_results does not call NoOptionError directly, but reaches it via get.
# _handle_results -> get -> time: _handle_results has a transitive dependency on time through get.
# Since _handle_results invokes get and get invokes GenericBrowser, _handle_results indirectly relies on GenericBrowser.
# _handle_results calls get, which calls register_standard_browsers -- so _handle_results transitively depends on register_standard_browsers.
# _handle_results calls get, which calls func -- so _handle_results transitively depends on func.
# Since _handle_results invokes get and get invokes BackgroundBrowser, _handle_results indirectly relies on BackgroundBrowser.
# Since _handle_results invokes get and get invokes _resolve_unbound, _handle_results indirectly relies on _resolve_unbound.
# Since _handle_results invokes get and get invokes pack, _handle_results indirectly relies on pack.
# Since _handle_results invokes get and get invokes _synthesize, _handle_results indirectly relies on _synthesize.
# Note: _handle_results does not call Error directly, but reaches it via get.
# Since _handle_results invokes get and get invokes wr, _handle_results indirectly relies on wr.
# Note: poll does not call DoneAndNotDoneFutures directly, but reaches it via wait.
# Note: poll does not call _AcquireFutures directly, but reaches it via wait.
# poll calls wait, which calls _time -- so poll transitively depends on _time.
# poll -> wait -> _exhaustive_wait: poll has a transitive dependency on _exhaustive_wait through wait.
# Note: poll does not call _WaitSelector directly, but reaches it via wait.
# Since poll invokes wait and wait invokes _create_and_install_waiters, poll indirectly relies on _create_and_install_waiters.
# poll calls wait, which calls _allocate_lock -- so poll transitively depends on _allocate_lock.
# Note: poll does not call fileno directly, but reaches it via wait.
# Since is_alive invokes wait and wait invokes DoneAndNotDoneFutures, is_alive indirectly relies on DoneAndNotDoneFutures.
# Since is_alive invokes wait and wait invokes _AcquireFutures, is_alive indirectly relies on _AcquireFutures.
# Note: is_alive does not call _time directly, but reaches it via wait.
# is_alive -> wait -> _exhaustive_wait: is_alive has a transitive dependency on _exhaustive_wait through wait.
# Since is_alive invokes wait and wait invokes _WaitSelector, is_alive indirectly relies on _WaitSelector.
# Since is_alive invokes wait and wait invokes _create_and_install_waiters, is_alive indirectly relies on _create_and_install_waiters.
# is_alive -> wait -> _allocate_lock: is_alive has a transitive dependency on _allocate_lock through wait.
# Since is_alive invokes wait and wait invokes fileno, is_alive indirectly relies on fileno.
# Note: join_thread does not call debug_script directly, but reaches it via debug.
# join_thread calls debug, which calls testsource -- so join_thread transitively depends on testsource.
# Note: join_thread does not call _normalize_module directly, but reaches it via debug.
# Note: join_thread does not call _DebugResult directly, but reaches it via debug.
# join_thread calls debug, which calls basicConfig -- so join_thread transitively depends on basicConfig.
# join_thread -> debug -> DebugRunner: join_thread has a transitive dependency on DebugRunner through debug.
# Since join_thread invokes debug and debug invokes SkipTest, join_thread indirectly relies on SkipTest.
# cancel_join_thread -> debug -> debug_script: cancel_join_thread has a transitive dependency on debug_script through debug.
# Since cancel_join_thread invokes debug and debug invokes testsource, cancel_join_thread indirectly relies on testsource.
# cancel_join_thread calls debug, which calls _normalize_module -- so cancel_join_thread transitively depends on _normalize_module.
# cancel_join_thread -> debug -> _DebugResult: cancel_join_thread has a transitive dependency on _DebugResult through debug.
# Note: cancel_join_thread does not call basicConfig directly, but reaches it via debug.
# cancel_join_thread calls debug, which calls DebugRunner -- so cancel_join_thread transitively depends on DebugRunner.
# Note: cancel_join_thread does not call SkipTest directly, but reaches it via debug.
# _start_thread -> debug -> debug_script: _start_thread has a transitive dependency on debug_script through debug.
# Since _start_thread invokes debug and debug invokes testsource, _start_thread indirectly relies on testsource.
# Since _start_thread invokes debug and debug invokes _normalize_module, _start_thread indirectly relies on _normalize_module.
# _start_thread calls debug, which calls _DebugResult -- so _start_thread transitively depends on _DebugResult.
# Note: _start_thread does not call basicConfig directly, but reaches it via debug.
# _start_thread calls debug, which calls DebugRunner -- so _start_thread transitively depends on DebugRunner.
# Since _start_thread invokes debug and debug invokes SkipTest, _start_thread indirectly relies on SkipTest.
# Note: _finalize_join does not call debug_script directly, but reaches it via debug.
# _finalize_join -> debug -> testsource: _finalize_join has a transitive dependency on testsource through debug.
# _finalize_join -> debug -> _normalize_module: _finalize_join has a transitive dependency on _normalize_module through debug.
# _finalize_join -> debug -> _DebugResult: _finalize_join has a transitive dependency on _DebugResult through debug.
# _finalize_join calls debug, which calls basicConfig -- so _finalize_join transitively depends on basicConfig.
# _finalize_join -> debug -> DebugRunner: _finalize_join has a transitive dependency on DebugRunner through debug.
# _finalize_join -> debug -> SkipTest: _finalize_join has a transitive dependency on SkipTest through debug.
# _finalize_close calls debug, which calls debug_script -- so _finalize_close transitively depends on debug_script.
# _finalize_close calls debug, which calls testsource -- so _finalize_close transitively depends on testsource.
# _finalize_close calls debug, which calls _normalize_module -- so _finalize_close transitively depends on _normalize_module.
# Note: _finalize_close does not call _DebugResult directly, but reaches it via debug.
# _finalize_close calls debug, which calls basicConfig -- so _finalize_close transitively depends on basicConfig.
# Since _finalize_close invokes debug and debug invokes DebugRunner, _finalize_close indirectly relies on DebugRunner.
# _finalize_close calls debug, which calls SkipTest -- so _finalize_close transitively depends on SkipTest.
# _feed calls debug, which calls debug_script -- so _feed transitively depends on debug_script.
# _feed -> debug -> testsource: _feed has a transitive dependency on testsource through debug.
# _feed -> debug -> _normalize_module: _feed has a transitive dependency on _normalize_module through debug.
# _feed calls debug, which calls _DebugResult -- so _feed transitively depends on _DebugResult.
# _feed calls debug, which calls basicConfig -- so _feed transitively depends on basicConfig.
# _feed calls debug, which calls DebugRunner -- so _feed transitively depends on DebugRunner.
# Since _feed invokes debug and debug invokes SkipTest, _feed indirectly relies on SkipTest.
# _feed calls info, which calls PathInfo -- so _feed transitively depends on PathInfo.
# _feed -> info -> _format_code_info: _feed has a transitive dependency on _format_code_info through info.
# _feed -> info -> basicConfig: _feed has a transitive dependency on basicConfig through info.
# Note: _feed does not call onexc directly, but reaches it via onerror.
# _feed calls onerror, which calls callback -- so _feed transitively depends on callback.
# Note: get_connection does not call address_type directly, but reaches it via Client.
# Note: get_connection does not call SocketClient directly, but reaches it via Client.
# get_connection -> Client -> answer_challenge: get_connection has a transitive dependency on answer_challenge through Client.
# get_connection calls Client, which calls Queue -- so get_connection transitively depends on Queue.
# Since get_connection invokes Client and Client invokes deliver_challenge, get_connection indirectly relies on deliver_challenge.
# get_connection -> Client -> _validate_family: get_connection has a transitive dependency on _validate_family through Client.
# get_connection -> Client -> PipeClient: get_connection has a transitive dependency on PipeClient through Client.
# get_connection -> Client -> Connection: get_connection has a transitive dependency on Connection through Client.
# Since stop invokes Client and Client invokes address_type, stop indirectly relies on address_type.
# Note: stop does not call SocketClient directly, but reaches it via Client.
# Since stop invokes Client and Client invokes answer_challenge, stop indirectly relies on answer_challenge.
# Note: stop does not call Queue directly, but reaches it via Client.
# stop calls Client, which calls deliver_challenge -- so stop transitively depends on deliver_challenge.
# stop calls Client, which calls _validate_family -- so stop transitively depends on _validate_family.
# stop -> Client -> PipeClient: stop has a transitive dependency on PipeClient through Client.
# Since stop invokes Client and Client invokes Connection, stop indirectly relies on Connection.
# stop -> close -> Fault: stop has a transitive dependency on Fault through close.
# Since stop invokes close and close invokes write32u, stop indirectly relies on write32u.
# stop calls close, which calls close_handler -- so stop transitively depends on close_handler.
# Note: stop does not call _ClosedDict directly, but reaches it via close.
# stop -> close -> assert_: stop has a transitive dependency on assert_ through close.
# Since stop invokes close and close invokes _ClosedParser, stop indirectly relies on _ClosedParser.
# stop calls close, which calls closehook -- so stop transitively depends on closehook.
# stop calls close, which calls unlink -- so stop transitively depends on unlink.
# Since stop invokes close and close invokes _cancel_all_tasks, stop indirectly relies on _cancel_all_tasks.
# Note: stop does not call ResponseError directly, but reaches it via close.
# Since stop invokes close and close invokes CloseHandle, stop indirectly relies on CloseHandle.
# Since _afterfork invokes close and close invokes Fault, _afterfork indirectly relies on Fault.
# _afterfork -> close -> write32u: _afterfork has a transitive dependency on write32u through close.
# _afterfork -> close -> close_handler: _afterfork has a transitive dependency on close_handler through close.
# Note: _afterfork does not call _ClosedDict directly, but reaches it via close.
# _afterfork calls close, which calls assert_ -- so _afterfork transitively depends on assert_.
# Note: _afterfork does not call _ClosedParser directly, but reaches it via close.
# Since _afterfork invokes close and close invokes closehook, _afterfork indirectly relies on closehook.
# _afterfork -> close -> unlink: _afterfork has a transitive dependency on unlink through close.
# _afterfork -> close -> _cancel_all_tasks: _afterfork has a transitive dependency on _cancel_all_tasks through close.
# _afterfork calls close, which calls ResponseError -- so _afterfork transitively depends on ResponseError.
# _afterfork -> close -> CloseHandle: _afterfork has a transitive dependency on CloseHandle through close.
# _serve -> send -> SMTPServerDisconnected: _serve has a transitive dependency on SMTPServerDisconnected through send.
# _serve -> send -> NotConnected: _serve has a transitive dependency on NotConnected through send.
# Since _serve invokes close and close invokes Fault, _serve indirectly relies on Fault.
# _serve -> close -> write32u: _serve has a transitive dependency on write32u through close.
# _serve -> close -> close_handler: _serve has a transitive dependency on close_handler through close.
# _serve -> close -> _ClosedDict: _serve has a transitive dependency on _ClosedDict through close.
# _serve -> close -> assert_: _serve has a transitive dependency on assert_ through close.
# Note: _serve does not call _ClosedParser directly, but reaches it via close.
# Note: _serve does not call closehook directly, but reaches it via close.
# _serve calls close, which calls unlink -- so _serve transitively depends on unlink.
# _serve calls close, which calls _cancel_all_tasks -- so _serve transitively depends on _cancel_all_tasks.
# Note: _serve does not call ResponseError directly, but reaches it via close.
# Since _serve invokes close and close invokes CloseHandle, _serve indirectly relies on CloseHandle.
# Since _stop_locked invokes close and close invokes Fault, _stop_locked indirectly relies on Fault.
# Since _stop_locked invokes close and close invokes write32u, _stop_locked indirectly relies on write32u.
# _stop_locked -> close -> close_handler: _stop_locked has a transitive dependency on close_handler through close.
# _stop_locked -> close -> _ClosedDict: _stop_locked has a transitive dependency on _ClosedDict through close.
# Note: _stop_locked does not call assert_ directly, but reaches it via close.
# Note: _stop_locked does not call _ClosedParser directly, but reaches it via close.
# Note: _stop_locked does not call closehook directly, but reaches it via close.
# Since _stop_locked invokes close and close invokes unlink, _stop_locked indirectly relies on unlink.
# Note: _stop_locked does not call _cancel_all_tasks directly, but reaches it via close.
# _stop_locked -> close -> ResponseError: _stop_locked has a transitive dependency on ResponseError through close.
# _stop_locked calls close, which calls CloseHandle -- so _stop_locked transitively depends on CloseHandle.
# Since _readlink_deep invokes isabs and isabs invokes _get_sep, _readlink_deep indirectly relies on _get_sep.
# Note: _readlink_deep does not call wait directly, but reaches it via join.
# _readlink_deep -> join -> splitroot: _readlink_deep has a transitive dependency on splitroot through join.
# _readlink_deep calls join, which calls current_thread -- so _readlink_deep transitively depends on current_thread.
# _readlink_deep calls join, which calls _get_sep -- so _readlink_deep transitively depends on _get_sep.
# _readlink_deep calls join, which calls quote -- so _readlink_deep transitively depends on quote.
# _readlink_deep calls normcase, which calls _LCMapStringEx -- so _readlink_deep transitively depends on _LCMapStringEx.
# Since _readlink_deep invokes normpath and normpath invokes splitroot, _readlink_deep indirectly relies on splitroot.
# Note: _readlink_deep does not call split directly, but reaches it via dirname.
# _readlink_deep -> dirname -> _get_sep: _readlink_deep has a transitive dependency on _get_sep through dirname.
# _getfinalpathname_nonstrict calls _readlink_deep, which calls isabs -- so _getfinalpathname_nonstrict transitively depends on isabs.
# _getfinalpathname_nonstrict calls _readlink_deep, which calls _nt_readlink -- so _getfinalpathname_nonstrict transitively depends on _nt_readlink.
# Note: _getfinalpathname_nonstrict does not call join directly, but reaches it via _readlink_deep.
# _getfinalpathname_nonstrict -> _readlink_deep -> islink: _getfinalpathname_nonstrict has a transitive dependency on islink through _readlink_deep.
# Note: _getfinalpathname_nonstrict does not call normcase directly, but reaches it via _readlink_deep.
# Note: _getfinalpathname_nonstrict does not call normpath directly, but reaches it via _readlink_deep.
# _getfinalpathname_nonstrict calls _readlink_deep, which calls dirname -- so _getfinalpathname_nonstrict transitively depends on dirname.
# _getfinalpathname_nonstrict -> join -> wait: _getfinalpathname_nonstrict has a transitive dependency on wait through join.
# Note: _getfinalpathname_nonstrict does not call splitroot directly, but reaches it via join.
# _getfinalpathname_nonstrict -> join -> current_thread: _getfinalpathname_nonstrict has a transitive dependency on current_thread through join.
# Note: _getfinalpathname_nonstrict does not call _get_sep directly, but reaches it via join.
# Since _getfinalpathname_nonstrict invokes join and join invokes quote, _getfinalpathname_nonstrict indirectly relies on quote.
# Since _getfinalpathname_nonstrict invokes split and split invokes _compile, _getfinalpathname_nonstrict indirectly relies on _compile.
# _getfinalpathname_nonstrict calls split, which calls splitroot -- so _getfinalpathname_nonstrict transitively depends on splitroot.
# Note: _getfinalpathname_nonstrict does not call _get_bothseps directly, but reaches it via split.
# Since _getfinalpathname_nonstrict invokes split and split invokes shlex, _getfinalpathname_nonstrict indirectly relies on shlex.
# Since _getfinalpathname_nonstrict invokes split and split invokes _get_sep, _getfinalpathname_nonstrict indirectly relies on _get_sep.
# Since isdevdrive invokes abspath and abspath invokes join, isdevdrive indirectly relies on join.
# isdevdrive calls abspath, which calls splitroot -- so isdevdrive transitively depends on splitroot.
# isdevdrive -> abspath -> getcwd: isdevdrive has a transitive dependency on getcwd through abspath.
# isdevdrive calls abspath, which calls isabs -- so isdevdrive transitively depends on isabs.
# Note: isdevdrive does not call normpath directly, but reaches it via abspath.
# isdevdrive -> abspath -> _getfullpathname: isdevdrive has a transitive dependency on _getfullpathname through abspath.
# Note: format_usage does not call resolve directly, but reaches it via _.
# format_usage -> _ -> _infer_caller: format_usage has a transitive dependency on _infer_caller through _.
# Since take_action invokes _ and _ invokes resolve, take_action indirectly relies on resolve.
# Note: take_action does not call _infer_caller directly, but reaches it via _.
# Note: _add_help_option does not call resolve directly, but reaches it via _.
# Since _add_help_option invokes _ and _ invokes _infer_caller, _add_help_option indirectly relies on _infer_caller.
# Note: _add_version_option does not call resolve directly, but reaches it via _.
# Since _add_version_option invokes _ and _ invokes _infer_caller, _add_version_option indirectly relies on _infer_caller.
# Since set_usage invokes _ and _ invokes resolve, set_usage indirectly relies on resolve.
# set_usage calls _, which calls _infer_caller -- so set_usage transitively depends on _infer_caller.
# _match_long_opt -> _match_abbrev -> AmbiguousOptionError: _match_long_opt has a transitive dependency on AmbiguousOptionError through _match_abbrev.
# Note: _match_long_opt does not call BadOptionError directly, but reaches it via _match_abbrev.
# Note: _process_long_opt does not call resolve directly, but reaches it via _.
# _process_long_opt calls _, which calls _infer_caller -- so _process_long_opt transitively depends on _infer_caller.
# _process_long_opt -> ngettext -> _as_int2: _process_long_opt has a transitive dependency on _as_int2 through ngettext.
# _process_long_opt -> ngettext -> dngettext: _process_long_opt has a transitive dependency on dngettext through ngettext.
# Since _process_short_opts invokes ngettext and ngettext invokes _as_int2, _process_short_opts indirectly relies on _as_int2.
# _process_short_opts -> ngettext -> dngettext: _process_short_opts has a transitive dependency on dngettext through ngettext.
# format_option_help calls _, which calls resolve -- so format_option_help transitively depends on resolve.
# Since format_option_help invokes _ and _ invokes _infer_caller, format_option_help indirectly relies on _infer_caller.
# fwalk -> close -> Fault: fwalk has a transitive dependency on Fault through close.
# fwalk -> close -> write32u: fwalk has a transitive dependency on write32u through close.
# Since fwalk invokes close and close invokes close_handler, fwalk indirectly relies on close_handler.
# fwalk calls close, which calls _ClosedDict -- so fwalk transitively depends on _ClosedDict.
# Note: fwalk does not call assert_ directly, but reaches it via close.
# fwalk calls close, which calls _ClosedParser -- so fwalk transitively depends on _ClosedParser.
# Note: fwalk does not call closehook directly, but reaches it via close.
# fwalk -> close -> unlink: fwalk has a transitive dependency on unlink through close.
# Since fwalk invokes close and close invokes _cancel_all_tasks, fwalk indirectly relies on _cancel_all_tasks.
# fwalk -> close -> ResponseError: fwalk has a transitive dependency on ResponseError through close.
# fwalk calls close, which calls CloseHandle -- so fwalk transitively depends on CloseHandle.
# fwalk calls _fwalk, which calls stat -- so fwalk transitively depends on stat.
# Since fwalk invokes _fwalk and _fwalk invokes fsencode, fwalk indirectly relies on fsencode.
# Since fwalk invokes _fwalk and _fwalk invokes scandir, fwalk indirectly relies on scandir.
# fwalk -> _fwalk -> close: fwalk has a transitive dependency on close through _fwalk.
# Since fwalk invokes _fwalk and _fwalk invokes onerror, fwalk indirectly relies on onerror.
# _fwalk -> stat -> error_proto: _fwalk has a transitive dependency on error_proto through stat.
# _fwalk -> fsencode -> fspath: _fwalk has a transitive dependency on fspath through fsencode.
# Since _fwalk invokes close and close invokes Fault, _fwalk indirectly relies on Fault.
# _fwalk -> close -> write32u: _fwalk has a transitive dependency on write32u through close.
# Since _fwalk invokes close and close invokes close_handler, _fwalk indirectly relies on close_handler.
# _fwalk calls close, which calls _ClosedDict -- so _fwalk transitively depends on _ClosedDict.
# Note: _fwalk does not call assert_ directly, but reaches it via close.
# _fwalk -> close -> _ClosedParser: _fwalk has a transitive dependency on _ClosedParser through close.
# _fwalk -> close -> closehook: _fwalk has a transitive dependency on closehook through close.
# Note: _fwalk does not call unlink directly, but reaches it via close.
# _fwalk -> close -> _cancel_all_tasks: _fwalk has a transitive dependency on _cancel_all_tasks through close.
# _fwalk calls close, which calls ResponseError -- so _fwalk transitively depends on ResponseError.
# _fwalk calls close, which calls CloseHandle -- so _fwalk transitively depends on CloseHandle.
# _fwalk calls onerror, which calls onexc -- so _fwalk transitively depends on onexc.
# Since _fwalk invokes onerror and onerror invokes callback, _fwalk indirectly relies on callback.
# Since reload_environ invokes encodekey and encodekey invokes encode, reload_environ indirectly relies on encode.
# _spawnvef calls fork, which calls openpty -- so _spawnvef transitively depends on openpty.
# Since _spawnvef invokes func and func invokes getter, _spawnvef indirectly relies on getter.
# spawnv -> _spawnvef -> _exit: spawnv has a transitive dependency on _exit through _spawnvef.
# Since spawnv invokes _spawnvef and _spawnvef invokes fork, spawnv indirectly relies on fork.
# Since spawnv invokes _spawnvef and _spawnvef invokes waitstatus_to_exitcode, spawnv indirectly relies on waitstatus_to_exitcode.
# Note: spawnv does not call waitpid directly, but reaches it via _spawnvef.
# spawnv calls _spawnvef, which calls WIFSTOPPED -- so spawnv transitively depends on WIFSTOPPED.
# Note: spawnv does not call func directly, but reaches it via _spawnvef.
# spawnve -> _spawnvef -> _exit: spawnve has a transitive dependency on _exit through _spawnvef.
# Since spawnve invokes _spawnvef and _spawnvef invokes fork, spawnve indirectly relies on fork.
# spawnve -> _spawnvef -> waitstatus_to_exitcode: spawnve has a transitive dependency on waitstatus_to_exitcode through _spawnvef.
# spawnve calls _spawnvef, which calls waitpid -- so spawnve transitively depends on waitpid.
# Since spawnve invokes _spawnvef and _spawnvef invokes WIFSTOPPED, spawnve indirectly relies on WIFSTOPPED.
# Since spawnve invokes _spawnvef and _spawnvef invokes func, spawnve indirectly relies on func.
# Since spawnvp invokes _spawnvef and _spawnvef invokes _exit, spawnvp indirectly relies on _exit.
# Since spawnvp invokes _spawnvef and _spawnvef invokes fork, spawnvp indirectly relies on fork.
# spawnvp -> _spawnvef -> waitstatus_to_exitcode: spawnvp has a transitive dependency on waitstatus_to_exitcode through _spawnvef.
# Note: spawnvp does not call waitpid directly, but reaches it via _spawnvef.
# spawnvp -> _spawnvef -> WIFSTOPPED: spawnvp has a transitive dependency on WIFSTOPPED through _spawnvef.
# Note: spawnvp does not call func directly, but reaches it via _spawnvef.
# spawnvpe -> _spawnvef -> _exit: spawnvpe has a transitive dependency on _exit through _spawnvef.
# Since spawnvpe invokes _spawnvef and _spawnvef invokes fork, spawnvpe indirectly relies on fork.
# spawnvpe -> _spawnvef -> waitstatus_to_exitcode: spawnvpe has a transitive dependency on waitstatus_to_exitcode through _spawnvef.
# Since spawnvpe invokes _spawnvef and _spawnvef invokes waitpid, spawnvpe indirectly relies on waitpid.
# Since spawnvpe invokes _spawnvef and _spawnvef invokes WIFSTOPPED, spawnvpe indirectly relies on WIFSTOPPED.
# Note: spawnvpe does not call func directly, but reaches it via _spawnvef.
# Note: spawnl does not call _spawnvef directly, but reaches it via spawnv.
# Since spawnle invokes spawnve and spawnve invokes _spawnvef, spawnle indirectly relies on _spawnvef.
# Since spawnlp invokes spawnvp and spawnvp invokes _spawnvef, spawnlp indirectly relies on _spawnvef.
# Note: spawnlpe does not call _spawnvef directly, but reaches it via spawnvpe.
# Note: parts does not call split directly, but reaches it via _explode_path.
# parents -> split -> _compile: parents has a transitive dependency on _compile through split.
# Since parents invokes split and split invokes splitroot, parents indirectly relies on splitroot.
# parents -> split -> _get_bothseps: parents has a transitive dependency on _get_bothseps through split.
# parents -> split -> shlex: parents has a transitive dependency on shlex through split.
# Note: parents does not call _get_sep directly, but reaches it via split.
# Since with_name invokes split and split invokes _compile, with_name indirectly relies on _compile.
# Note: with_name does not call splitroot directly, but reaches it via split.
# Note: with_name does not call _get_bothseps directly, but reaches it via split.
# Note: with_name does not call shlex directly, but reaches it via split.
# Note: with_name does not call _get_sep directly, but reaches it via split.
# Since as_uri invokes quote_from_bytes and quote_from_bytes invokes _byte_quoter_factory, as_uri indirectly relies on _byte_quoter_factory.
# as_uri calls pathname2url, which calls quote -- so as_uri transitively depends on quote.
# full_match -> match -> _compile: full_match has a transitive dependency on _compile through match.
# full_match calls match, which calls _StringGlobber -- so full_match transitively depends on _StringGlobber.
# Note: is_dir does not call S_IFMT directly, but reaches it via S_ISDIR.
# is_file calls S_ISREG, which calls S_IFMT -- so is_file transitively depends on S_IFMT.
# Note: is_block_device does not call S_IFMT directly, but reaches it via S_ISBLK.
# Since is_char_device invokes S_ISCHR and S_ISCHR invokes S_IFMT, is_char_device indirectly relies on S_IFMT.
# is_fifo calls S_ISFIFO, which calls S_IFMT -- so is_fifo transitively depends on S_IFMT.
# is_socket calls S_ISSOCK, which calls S_IFMT -- so is_socket transitively depends on S_IFMT.
# write_bytes -> magic_open -> attr: write_bytes has a transitive dependency on attr through magic_open.
# Since write_bytes invokes magic_open and magic_open invokes text_encoding, write_bytes indirectly relies on text_encoding.
# write_bytes calls magic_open, which calls TextIOWrapper -- so write_bytes transitively depends on TextIOWrapper.
# write_text calls magic_open, which calls attr -- so write_text transitively depends on attr.
# write_text -> magic_open -> text_encoding: write_text has a transitive dependency on text_encoding through magic_open.
# write_text -> magic_open -> TextIOWrapper: write_text has a transitive dependency on TextIOWrapper through magic_open.
# _copy_from calls magic_open, which calls attr -- so _copy_from transitively depends on attr.
# _copy_from -> magic_open -> text_encoding: _copy_from has a transitive dependency on text_encoding through magic_open.
# Note: _copy_from does not call TextIOWrapper directly, but reaches it via magic_open.
# _copy_from calls copyfileobj, which calls fdst_write -- so _copy_from transitively depends on fdst_write.
# Note: _copy_from does not call _ficlone directly, but reaches it via copyfileobj.
# Note: _copy_from does not call fsrc_read directly, but reaches it via copyfileobj.
# _copy_from -> copyfileobj -> _sendfile: _copy_from has a transitive dependency on _sendfile through copyfileobj.
# Since _copy_from invokes copyfileobj and copyfileobj invokes write_target, _copy_from indirectly relies on write_target.
# Since _copy_from invokes copyfileobj and copyfileobj invokes read_source, _copy_from indirectly relies on read_source.
# Since _copy_from invokes copyfileobj and copyfileobj invokes exception, _copy_from indirectly relies on exception.
# Note: _copy_from does not call _copy_file_range directly, but reaches it via copyfileobj.
# Note: _copy_from does not call _fcopyfile directly, but reaches it via copyfileobj.
# _copy_from -> ensure_different_files -> target_file_id: _copy_from has a transitive dependency on target_file_id through ensure_different_files.
# Note: _copy_from does not call source_file_id directly, but reaches it via ensure_different_files.
# Since _copy_from_file invokes copyfileobj and copyfileobj invokes fdst_write, _copy_from_file indirectly relies on fdst_write.
# _copy_from_file calls copyfileobj, which calls _ficlone -- so _copy_from_file transitively depends on _ficlone.
# _copy_from_file -> copyfileobj -> fsrc_read: _copy_from_file has a transitive dependency on fsrc_read through copyfileobj.
# Note: _copy_from_file does not call _sendfile directly, but reaches it via copyfileobj.
# _copy_from_file calls copyfileobj, which calls write_target -- so _copy_from_file transitively depends on write_target.
# Since _copy_from_file invokes copyfileobj and copyfileobj invokes read_source, _copy_from_file indirectly relies on read_source.
# _copy_from_file -> copyfileobj -> exception: _copy_from_file has a transitive dependency on exception through copyfileobj.
# Since _copy_from_file invokes copyfileobj and copyfileobj invokes _copy_file_range, _copy_from_file indirectly relies on _copy_file_range.
# _copy_from_file -> copyfileobj -> _fcopyfile: _copy_from_file has a transitive dependency on _fcopyfile through copyfileobj.
# Note: _copy_from_file does not call target_file_id directly, but reaches it via ensure_different_files.
# _copy_from_file -> ensure_different_files -> source_file_id: _copy_from_file has a transitive dependency on source_file_id through ensure_different_files.
# _copy_from_file calls magic_open, which calls attr -- so _copy_from_file transitively depends on attr.
# _copy_from_file -> magic_open -> text_encoding: _copy_from_file has a transitive dependency on text_encoding through magic_open.
# Note: _copy_from_file does not call TextIOWrapper directly, but reaches it via magic_open.
# from_uri calls url2pathname, which calls unquote -- so from_uri transitively depends on unquote.
# from_uri calls url2pathname, which calls urlsplit -- so from_uri transitively depends on urlsplit.
# Since from_uri invokes url2pathname and url2pathname invokes URLError, from_uri indirectly relies on URLError.
# Since from_uri invokes url2pathname and url2pathname invokes _is_local_authority, from_uri indirectly relies on _is_local_authority.
# is_symlink -> S_ISLNK -> S_IFMT: is_symlink has a transitive dependency on S_IFMT through S_ISLNK.
# anchor -> _explode_path -> split: anchor has a transitive dependency on split through _explode_path.
# Since suffixes invokes split and split invokes _compile, suffixes indirectly relies on _compile.
# Since suffixes invokes split and split invokes splitroot, suffixes indirectly relies on splitroot.
# Since suffixes invokes split and split invokes _get_bothseps, suffixes indirectly relies on _get_bothseps.
# suffixes -> split -> shlex: suffixes has a transitive dependency on shlex through split.
# suffixes -> split -> _get_sep: suffixes has a transitive dependency on _get_sep through split.
# Since lineinfo invokes find_function and find_function invokes find_first_executable_line, lineinfo indirectly relies on find_first_executable_line.
# Since _msg_val_func invokes func and func invokes getter, _msg_val_func indirectly relies on getter.
# _safe_repr calls append, which calls Time2Internaldate -- so _safe_repr transitively depends on Time2Internaldate.
# Since _safe_repr invokes append and append invokes Charset, _safe_repr indirectly relies on Charset.
# Note: commit_frame does not call serialize directly, but reaches it via write.
# commit_frame -> write -> sleep: commit_frame has a transitive dependency on sleep through write.
# Since commit_frame invokes write and write invokes _serialize_text, commit_frame indirectly relies on _serialize_text.
# Note: commit_frame does not call assert_ directly, but reaches it via write.
# Note: commit_frame does not call _namespaces directly, but reaches it via write.
# Note: commit_frame does not call time directly, but reaches it via write.
# Since commit_frame invokes write and write invokes _count_to_size, commit_frame indirectly relies on _count_to_size.
# Note: commit_frame does not call quote directly, but reaches it via write.
# Note: commit_frame does not call _nbytes directly, but reaches it via write.
# commit_frame -> write -> _get_writer: commit_frame has a transitive dependency on _get_writer through write.
# Since commit_frame invokes write and write invokes crc32, commit_frame indirectly relies on crc32.
# write_large_bytes calls write, which calls serialize -- so write_large_bytes transitively depends on serialize.
# Note: write_large_bytes does not call sleep directly, but reaches it via write.
# Note: write_large_bytes does not call _serialize_text directly, but reaches it via write.
# write_large_bytes -> write -> assert_: write_large_bytes has a transitive dependency on assert_ through write.
# write_large_bytes -> write -> _namespaces: write_large_bytes has a transitive dependency on _namespaces through write.
# write_large_bytes calls write, which calls time -- so write_large_bytes transitively depends on time.
# write_large_bytes calls write, which calls _count_to_size -- so write_large_bytes transitively depends on _count_to_size.
# Since write_large_bytes invokes write and write invokes quote, write_large_bytes indirectly relies on quote.
# Since write_large_bytes invokes write and write invokes _nbytes, write_large_bytes indirectly relies on _nbytes.
# Note: write_large_bytes does not call _get_writer directly, but reaches it via write.
# Since write_large_bytes invokes write and write invokes crc32, write_large_bytes indirectly relies on crc32.
# save_reduce calls save, which calls Path -- so save_reduce transitively depends on Path.
# Note: save_reduce does not call PicklingError directly, but reaches it via save.
# Since save_reduce invokes save and save invokes _T, save_reduce indirectly relies on _T.
# Note: save_reduce does not call reduce directly, but reaches it via save.
# Since save_reduce invokes save and save invokes f, save_reduce indirectly relies on f.
# save_reduce calls write, which calls serialize -- so save_reduce transitively depends on serialize.
# Since save_reduce invokes write and write invokes sleep, save_reduce indirectly relies on sleep.
# Since save_reduce invokes write and write invokes _serialize_text, save_reduce indirectly relies on _serialize_text.
# Since save_reduce invokes write and write invokes assert_, save_reduce indirectly relies on assert_.
# save_reduce calls write, which calls _namespaces -- so save_reduce transitively depends on _namespaces.
# save_reduce -> write -> time: save_reduce has a transitive dependency on time through write.
# save_reduce -> write -> _count_to_size: save_reduce has a transitive dependency on _count_to_size through write.
# Note: save_reduce does not call quote directly, but reaches it via write.
# save_reduce -> write -> _nbytes: save_reduce has a transitive dependency on _nbytes through write.
# save_reduce -> write -> _get_writer: save_reduce has a transitive dependency on _get_writer through write.
# save_reduce -> write -> crc32: save_reduce has a transitive dependency on crc32 through write.
# save_tuple -> write -> serialize: save_tuple has a transitive dependency on serialize through write.
# save_tuple calls write, which calls sleep -- so save_tuple transitively depends on sleep.
# save_tuple -> write -> _serialize_text: save_tuple has a transitive dependency on _serialize_text through write.
# Note: save_tuple does not call assert_ directly, but reaches it via write.
# save_tuple calls write, which calls _namespaces -- so save_tuple transitively depends on _namespaces.
# Note: save_tuple does not call time directly, but reaches it via write.
# save_tuple -> write -> _count_to_size: save_tuple has a transitive dependency on _count_to_size through write.
# save_tuple -> write -> quote: save_tuple has a transitive dependency on quote through write.
# Note: save_tuple does not call _nbytes directly, but reaches it via write.
# save_tuple calls write, which calls _get_writer -- so save_tuple transitively depends on _get_writer.
# save_tuple calls write, which calls crc32 -- so save_tuple transitively depends on crc32.
# save_tuple calls save, which calls Path -- so save_tuple transitively depends on Path.
# Since save_tuple invokes save and save invokes PicklingError, save_tuple indirectly relies on PicklingError.
# save_tuple -> save -> _T: save_tuple has a transitive dependency on _T through save.
# Since save_tuple invokes save and save invokes reduce, save_tuple indirectly relies on reduce.
# save_tuple calls save, which calls f -- so save_tuple transitively depends on f.
# _batch_appends calls write, which calls serialize -- so _batch_appends transitively depends on serialize.
# _batch_appends calls write, which calls sleep -- so _batch_appends transitively depends on sleep.
# _batch_appends -> write -> _serialize_text: _batch_appends has a transitive dependency on _serialize_text through write.
# Note: _batch_appends does not call assert_ directly, but reaches it via write.
# Note: _batch_appends does not call _namespaces directly, but reaches it via write.
# _batch_appends -> write -> time: _batch_appends has a transitive dependency on time through write.
# _batch_appends -> write -> _count_to_size: _batch_appends has a transitive dependency on _count_to_size through write.
# Note: _batch_appends does not call quote directly, but reaches it via write.
# _batch_appends calls write, which calls _nbytes -- so _batch_appends transitively depends on _nbytes.
# Since _batch_appends invokes write and write invokes _get_writer, _batch_appends indirectly relies on _get_writer.
# _batch_appends -> write -> crc32: _batch_appends has a transitive dependency on crc32 through write.
# _batch_appends -> save -> Path: _batch_appends has a transitive dependency on Path through save.
# Note: _batch_appends does not call PicklingError directly, but reaches it via save.
# _batch_appends -> save -> _T: _batch_appends has a transitive dependency on _T through save.
# _batch_appends calls save, which calls reduce -- so _batch_appends transitively depends on reduce.
# _batch_appends calls save, which calls f -- so _batch_appends transitively depends on f.
# _batch_setitems calls save, which calls Path -- so _batch_setitems transitively depends on Path.
# _batch_setitems calls save, which calls PicklingError -- so _batch_setitems transitively depends on PicklingError.
# _batch_setitems -> save -> _T: _batch_setitems has a transitive dependency on _T through save.
# Since _batch_setitems invokes save and save invokes reduce, _batch_setitems indirectly relies on reduce.
# _batch_setitems calls save, which calls f -- so _batch_setitems transitively depends on f.
# _batch_setitems -> write -> serialize: _batch_setitems has a transitive dependency on serialize through write.
# _batch_setitems -> write -> sleep: _batch_setitems has a transitive dependency on sleep through write.
# Since _batch_setitems invokes write and write invokes _serialize_text, _batch_setitems indirectly relies on _serialize_text.
# _batch_setitems -> write -> assert_: _batch_setitems has a transitive dependency on assert_ through write.
# Since _batch_setitems invokes write and write invokes _namespaces, _batch_setitems indirectly relies on _namespaces.
# Since _batch_setitems invokes write and write invokes time, _batch_setitems indirectly relies on time.
# Since _batch_setitems invokes write and write invokes _count_to_size, _batch_setitems indirectly relies on _count_to_size.
# Note: _batch_setitems does not call quote directly, but reaches it via write.
# Note: _batch_setitems does not call _nbytes directly, but reaches it via write.
# Since _batch_setitems invokes write and write invokes _get_writer, _batch_setitems indirectly relies on _get_writer.
# _batch_setitems calls write, which calls crc32 -- so _batch_setitems transitively depends on crc32.
# save_set -> save -> Path: save_set has a transitive dependency on Path through save.
# Note: save_set does not call PicklingError directly, but reaches it via save.
# save_set -> save -> _T: save_set has a transitive dependency on _T through save.
# save_set -> save -> reduce: save_set has a transitive dependency on reduce through save.
# save_set -> save -> f: save_set has a transitive dependency on f through save.
# Note: save_set does not call serialize directly, but reaches it via write.
# save_set -> write -> sleep: save_set has a transitive dependency on sleep through write.
# Since save_set invokes write and write invokes _serialize_text, save_set indirectly relies on _serialize_text.
# save_set calls write, which calls assert_ -- so save_set transitively depends on assert_.
# Note: save_set does not call _namespaces directly, but reaches it via write.
# Since save_set invokes write and write invokes time, save_set indirectly relies on time.
# save_set -> write -> _count_to_size: save_set has a transitive dependency on _count_to_size through write.
# save_set calls write, which calls quote -- so save_set transitively depends on quote.
# Since save_set invokes write and write invokes _nbytes, save_set indirectly relies on _nbytes.
# Since save_set invokes write and write invokes _get_writer, save_set indirectly relies on _get_writer.
# Since save_set invokes write and write invokes crc32, save_set indirectly relies on crc32.
# Note: save_frozenset does not call Path directly, but reaches it via save.
# save_frozenset -> save -> PicklingError: save_frozenset has a transitive dependency on PicklingError through save.
# save_frozenset calls save, which calls _T -- so save_frozenset transitively depends on _T.
# Note: save_frozenset does not call reduce directly, but reaches it via save.
# Note: save_frozenset does not call f directly, but reaches it via save.
# Since save_frozenset invokes write and write invokes serialize, save_frozenset indirectly relies on serialize.
# Note: save_frozenset does not call sleep directly, but reaches it via write.
# save_frozenset calls write, which calls _serialize_text -- so save_frozenset transitively depends on _serialize_text.
# Since save_frozenset invokes write and write invokes assert_, save_frozenset indirectly relies on assert_.
# Note: save_frozenset does not call _namespaces directly, but reaches it via write.
# Note: save_frozenset does not call time directly, but reaches it via write.
# save_frozenset -> write -> _count_to_size: save_frozenset has a transitive dependency on _count_to_size through write.
# Since save_frozenset invokes write and write invokes quote, save_frozenset indirectly relies on quote.
# save_frozenset calls write, which calls _nbytes -- so save_frozenset transitively depends on _nbytes.
# save_frozenset calls write, which calls _get_writer -- so save_frozenset transitively depends on _get_writer.
# save_frozenset -> write -> crc32: save_frozenset has a transitive dependency on crc32 through write.
# Note: save_global does not call Path directly, but reaches it via save.
# Since save_global invokes save and save invokes PicklingError, save_global indirectly relies on PicklingError.
# Since save_global invokes save and save invokes _T, save_global indirectly relies on _T.
# Since save_global invokes save and save invokes reduce, save_global indirectly relies on reduce.
# Note: save_global does not call f directly, but reaches it via save.
# Since save_global invokes write and write invokes serialize, save_global indirectly relies on serialize.
# Since save_global invokes write and write invokes sleep, save_global indirectly relies on sleep.
# save_global calls write, which calls _serialize_text -- so save_global transitively depends on _serialize_text.
# Since save_global invokes write and write invokes assert_, save_global indirectly relies on assert_.
# save_global calls write, which calls _namespaces -- so save_global transitively depends on _namespaces.
# save_global calls write, which calls time -- so save_global transitively depends on time.
# Since save_global invokes write and write invokes _count_to_size, save_global indirectly relies on _count_to_size.
# save_global calls write, which calls quote -- so save_global transitively depends on quote.
# save_global calls write, which calls _nbytes -- so save_global transitively depends on _nbytes.
# save_global calls write, which calls _get_writer -- so save_global transitively depends on _get_writer.
# Since save_global invokes write and write invokes crc32, save_global indirectly relies on crc32.
# Since save_global invokes whichmodule and whichmodule invokes _getattribute, save_global indirectly relies on _getattribute.
# Since save_global invokes whichmodule and whichmodule invokes PicklingError, save_global indirectly relies on PicklingError.
# Since load_reduce invokes func and func invokes getter, load_reduce indirectly relies on getter.
# load_appends -> append -> Time2Internaldate: load_appends has a transitive dependency on Time2Internaldate through append.
# Note: load_appends does not call Charset directly, but reaches it via append.
# Note: load_additems does not call ref directly, but reaches it via add.
# load_additems calls add, which calls _lock_file -- so load_additems transitively depends on _lock_file.
# load_additems -> add -> bltn_open: load_additems has a transitive dependency on bltn_open through add.
# load_additems calls add, which calls ExternalClashError -- so load_additems transitively depends on ExternalClashError.
# load_additems -> add -> _unlock_file: load_additems has a transitive dependency on _unlock_file through add.
# Note: load_additems does not call Stats directly, but reaches it via add.
# load_additems calls add, which calls add_func_stats -- so load_additems transitively depends on add_func_stats.
# load_additems -> add -> _create_carefully: load_additems has a transitive dependency on _create_carefully through add.
# load_additems calls add, which calls _sync_close -- so load_additems transitively depends on _sync_close.
# load_additems calls add, which calls _convert_other -- so load_additems transitively depends on _convert_other.
# get_win32 calls _get_machine_win32, which calls _wmi_query -- so get_win32 transitively depends on _wmi_query.
# handle_begin_element -> handler -> start: handle_begin_element has a transitive dependency on start through handler.
# handle_begin_element -> handler -> end: handle_begin_element has a transitive dependency on end through handler.
# handle_begin_element calls handler, which calls append -- so handle_begin_element transitively depends on append.
# Since handle_begin_element invokes handler and handler invokes end_ns, handle_begin_element indirectly relies on end_ns.
# handle_begin_element calls handler, which calls start_ns -- so handle_begin_element transitively depends on start_ns.
# handle_end_element -> handler -> start: handle_end_element has a transitive dependency on start through handler.
# handle_end_element -> handler -> end: handle_end_element has a transitive dependency on end through handler.
# handle_end_element -> handler -> append: handle_end_element has a transitive dependency on append through handler.
# Since handle_end_element invokes handler and handler invokes end_ns, handle_end_element indirectly relies on end_ns.
# Note: handle_end_element does not call start_ns directly, but reaches it via handler.
# Note: _pprint_dict does not call serialize directly, but reaches it via write.
# Since _pprint_dict invokes write and write invokes sleep, _pprint_dict indirectly relies on sleep.
# Since _pprint_dict invokes write and write invokes _serialize_text, _pprint_dict indirectly relies on _serialize_text.
# _pprint_dict calls write, which calls assert_ -- so _pprint_dict transitively depends on assert_.
# _pprint_dict calls write, which calls _namespaces -- so _pprint_dict transitively depends on _namespaces.
# Note: _pprint_dict does not call time directly, but reaches it via write.
# Since _pprint_dict invokes write and write invokes _count_to_size, _pprint_dict indirectly relies on _count_to_size.
# Note: _pprint_dict does not call quote directly, but reaches it via write.
# Note: _pprint_dict does not call _nbytes directly, but reaches it via write.
# Since _pprint_dict invokes write and write invokes _get_writer, _pprint_dict indirectly relies on _get_writer.
# _pprint_dict calls write, which calls crc32 -- so _pprint_dict transitively depends on crc32.
# _pprint_str calls write, which calls serialize -- so _pprint_str transitively depends on serialize.
# _pprint_str calls write, which calls sleep -- so _pprint_str transitively depends on sleep.
# Note: _pprint_str does not call _serialize_text directly, but reaches it via write.
# Since _pprint_str invokes write and write invokes assert_, _pprint_str indirectly relies on assert_.
# Since _pprint_str invokes write and write invokes _namespaces, _pprint_str indirectly relies on _namespaces.
# _pprint_str -> write -> time: _pprint_str has a transitive dependency on time through write.
# _pprint_str calls write, which calls _count_to_size -- so _pprint_str transitively depends on _count_to_size.
# _pprint_str calls write, which calls quote -- so _pprint_str transitively depends on quote.
# _pprint_str -> write -> _nbytes: _pprint_str has a transitive dependency on _nbytes through write.
# Since _pprint_str invokes write and write invokes _get_writer, _pprint_str indirectly relies on _get_writer.
# _pprint_str -> write -> crc32: _pprint_str has a transitive dependency on crc32 through write.
# _pprint_bytes -> write -> serialize: _pprint_bytes has a transitive dependency on serialize through write.
# Note: _pprint_bytes does not call sleep directly, but reaches it via write.
# _pprint_bytes calls write, which calls _serialize_text -- so _pprint_bytes transitively depends on _serialize_text.
# Note: _pprint_bytes does not call assert_ directly, but reaches it via write.
# _pprint_bytes calls write, which calls _namespaces -- so _pprint_bytes transitively depends on _namespaces.
# _pprint_bytes -> write -> time: _pprint_bytes has a transitive dependency on time through write.
# _pprint_bytes -> write -> _count_to_size: _pprint_bytes has a transitive dependency on _count_to_size through write.
# Note: _pprint_bytes does not call quote directly, but reaches it via write.
# _pprint_bytes -> write -> _nbytes: _pprint_bytes has a transitive dependency on _nbytes through write.
# Note: _pprint_bytes does not call _get_writer directly, but reaches it via write.
# _pprint_bytes -> write -> crc32: _pprint_bytes has a transitive dependency on crc32 through write.
# _pprint_bytearray calls write, which calls serialize -- so _pprint_bytearray transitively depends on serialize.
# _pprint_bytearray calls write, which calls sleep -- so _pprint_bytearray transitively depends on sleep.
# Note: _pprint_bytearray does not call _serialize_text directly, but reaches it via write.
# Since _pprint_bytearray invokes write and write invokes assert_, _pprint_bytearray indirectly relies on assert_.
# _pprint_bytearray calls write, which calls _namespaces -- so _pprint_bytearray transitively depends on _namespaces.
# _pprint_bytearray calls write, which calls time -- so _pprint_bytearray transitively depends on time.
# Since _pprint_bytearray invokes write and write invokes _count_to_size, _pprint_bytearray indirectly relies on _count_to_size.
# _pprint_bytearray -> write -> quote: _pprint_bytearray has a transitive dependency on quote through write.
# Since _pprint_bytearray invokes write and write invokes _nbytes, _pprint_bytearray indirectly relies on _nbytes.
# _pprint_bytearray calls write, which calls _get_writer -- so _pprint_bytearray transitively depends on _get_writer.
# Since _pprint_bytearray invokes write and write invokes crc32, _pprint_bytearray indirectly relies on crc32.
# Since _format_dict_items invokes write and write invokes serialize, _format_dict_items indirectly relies on serialize.
# _format_dict_items -> write -> sleep: _format_dict_items has a transitive dependency on sleep through write.
# Note: _format_dict_items does not call _serialize_text directly, but reaches it via write.
# _format_dict_items -> write -> assert_: _format_dict_items has a transitive dependency on assert_ through write.
# Note: _format_dict_items does not call _namespaces directly, but reaches it via write.
# Note: _format_dict_items does not call time directly, but reaches it via write.
# Note: _format_dict_items does not call _count_to_size directly, but reaches it via write.
# Since _format_dict_items invokes write and write invokes quote, _format_dict_items indirectly relies on quote.
# Since _format_dict_items invokes write and write invokes _nbytes, _format_dict_items indirectly relies on _nbytes.
# _format_dict_items calls write, which calls _get_writer -- so _format_dict_items transitively depends on _get_writer.
# Since _format_dict_items invokes write and write invokes crc32, _format_dict_items indirectly relies on crc32.
# _format_namespace_items -> write -> serialize: _format_namespace_items has a transitive dependency on serialize through write.
# Since _format_namespace_items invokes write and write invokes sleep, _format_namespace_items indirectly relies on sleep.
# _format_namespace_items -> write -> _serialize_text: _format_namespace_items has a transitive dependency on _serialize_text through write.
# Since _format_namespace_items invokes write and write invokes assert_, _format_namespace_items indirectly relies on assert_.
# _format_namespace_items calls write, which calls _namespaces -- so _format_namespace_items transitively depends on _namespaces.
# Note: _format_namespace_items does not call time directly, but reaches it via write.
# Note: _format_namespace_items does not call _count_to_size directly, but reaches it via write.
# _format_namespace_items -> write -> quote: _format_namespace_items has a transitive dependency on quote through write.
# Since _format_namespace_items invokes write and write invokes _nbytes, _format_namespace_items indirectly relies on _nbytes.
# Since _format_namespace_items invokes write and write invokes _get_writer, _format_namespace_items indirectly relies on _get_writer.
# _format_namespace_items -> write -> crc32: _format_namespace_items has a transitive dependency on crc32 through write.
# _format_items calls write, which calls serialize -- so _format_items transitively depends on serialize.
# Since _format_items invokes write and write invokes sleep, _format_items indirectly relies on sleep.
# _format_items calls write, which calls _serialize_text -- so _format_items transitively depends on _serialize_text.
# _format_items -> write -> assert_: _format_items has a transitive dependency on assert_ through write.
# _format_items calls write, which calls _namespaces -- so _format_items transitively depends on _namespaces.
# Since _format_items invokes write and write invokes time, _format_items indirectly relies on time.
# _format_items calls write, which calls _count_to_size -- so _format_items transitively depends on _count_to_size.
# _format_items calls write, which calls quote -- so _format_items transitively depends on quote.
# _format_items calls write, which calls _nbytes -- so _format_items transitively depends on _nbytes.
# _format_items calls write, which calls _get_writer -- so _format_items transitively depends on _get_writer.
# _format_items calls write, which calls crc32 -- so _format_items transitively depends on crc32.
# Since _calibrate_inner invokes f and f invokes f1, _calibrate_inner indirectly relies on f1.
# Note: sort_stats does not call mycmp directly, but reaches it via cmp_to_key.
# strip_dirs -> add_func_stats -> add_callers: strip_dirs has a transitive dependency on add_callers through add_func_stats.
# visit_Import calls _readmodule, which calls _create_tree -- so visit_Import transitively depends on _create_tree.
# Note: visit_ImportFrom does not call _create_tree directly, but reaches it via _readmodule.
# repr1 calls method, which calls func -- so repr1 transitively depends on func.
# Since repr_string invokes replace and replace invokes _replace, repr_string indirectly relies on _replace.
# repr_string calls replace, which calls _is_dataclass_instance -- so repr_string transitively depends on _is_dataclass_instance.
# Since repr_string invokes replace and replace invokes func, repr_string indirectly relies on func.
# Since preformat invokes replace and replace invokes _replace, preformat indirectly relies on _replace.
# preformat calls replace, which calls _is_dataclass_instance -- so preformat transitively depends on _is_dataclass_instance.
# Note: preformat does not call func directly, but reaches it via replace.
# markup -> escape -> __dict_replace: markup has a transitive dependency on __dict_replace through escape.
# markup -> escape -> replace: markup has a transitive dependency on replace through escape.
# Since docmodule invokes getdoc and getdoc invokes _getdoc, docmodule indirectly relies on _getdoc.
# Note: docmodule does not call cleandoc directly, but reaches it via getdoc.
# docmodule -> getdoc -> _finddoc: docmodule has a transitive dependency on _finddoc through getdoc.
# docclass calls getdoc, which calls _getdoc -- so docclass transitively depends on _getdoc.
# Since docclass invokes getdoc and getdoc invokes cleandoc, docclass indirectly relies on cleandoc.
# docclass calls getdoc, which calls _finddoc -- so docclass transitively depends on _finddoc.
# Since docclass invokes spilldescriptors and spilldescriptors invokes _split_list, docclass indirectly relies on _split_list.
# Note: docclass does not call push directly, but reaches it via spilldescriptors.
# Since docclass invokes makename and makename invokes classname, docclass indirectly relies on classname.
# docclass calls push, which calls trace -- so docclass transitively depends on trace.
# docclass calls push, which calls Event -- so docclass transitively depends on Event.
# docclass -> classify_class_attrs -> getmro: docclass has a transitive dependency on getmro through classify_class_attrs.
# Since docclass invokes classify_class_attrs and classify_class_attrs invokes _is_bound_method, docclass indirectly relies on _is_bound_method.
# docclass calls classify_class_attrs, which calls Attribute -- so docclass transitively depends on Attribute.
# docclass -> classify_class_attrs -> isroutine: docclass has a transitive dependency on isroutine through classify_class_attrs.
# docclass -> spill -> _split_list: docclass has a transitive dependency on _split_list through spill.
# Note: docclass does not call push directly, but reaches it via spill.
# Note: docclass does not call predicate directly, but reaches it via _split_list.
# Since docclass invokes spilldata and spilldata invokes _split_list, docclass indirectly relies on _split_list.
# docclass -> spilldata -> push: docclass has a transitive dependency on push through spilldata.
# Note: docclass does not call getdoc directly, but reaches it via spilldata.
# Note: docroutine does not call _getdoc directly, but reaches it via getdoc.
# docroutine -> getdoc -> cleandoc: docroutine has a transitive dependency on cleandoc through getdoc.
# Note: docroutine does not call _finddoc directly, but reaches it via getdoc.
# Since docdata invokes getdoc and getdoc invokes _getdoc, docdata indirectly relies on _getdoc.
# Note: docdata does not call cleandoc directly, but reaches it via getdoc.
# docdata calls getdoc, which calls _finddoc -- so docdata transitively depends on _finddoc.
# Note: docdata does not call trace directly, but reaches it via push.
# Note: docdata does not call Event directly, but reaches it via push.
# Since docother invokes getdoc and getdoc invokes _getdoc, docother indirectly relies on _getdoc.
# docother calls getdoc, which calls cleandoc -- so docother transitively depends on cleandoc.
# docother -> getdoc -> _finddoc: docother has a transitive dependency on _finddoc through getdoc.
# Since help invokes doc and doc invokes pager, help indirectly relies on pager.
# help calls doc, which calls render_doc -- so help transitively depends on render_doc.
# showtopic calls pager, which calls get_pager -- so showtopic transitively depends on get_pager.
# listmodules calls apropos, which calls ModuleScanner -- so listmodules transitively depends on ModuleScanner.
# Note: listmodules does not call func directly, but reaches it via callback.
# listmodules -> callback -> ensure_future: listmodules has a transitive dependency on ensure_future through callback.
# Since html_getobj invokes locate and locate invokes safeimport, html_getobj indirectly relies on safeimport.
# Since html_error invokes format_exception_only and format_exception_only invokes _safe_string, html_error indirectly relies on _safe_string.
# html_error -> format_exception_only -> _format_final_exc_line: html_error has a transitive dependency on _format_final_exc_line through format_exception_only.
# Note: html_error does not call TracebackException directly, but reaches it via format_exception_only.
# get_html_page calls html_getobj, which calls describe -- so get_html_page transitively depends on describe.
# get_html_page calls html_getobj, which calls locate -- so get_html_page transitively depends on locate.
# Note: get_html_page does not call format_exception_only directly, but reaches it via html_error.
# Note: get_html_page does not call bltinlink directly, but reaches it via html_search.
# get_html_page -> html_search -> ModuleScanner: get_html_page has a transitive dependency on ModuleScanner through html_search.
# get_html_page -> html_topicpage -> Helper: get_html_page has a transitive dependency on Helper through html_topicpage.
# Note: _randbelow_without_getrandbits does not call _is_internal_frame directly, but reaches it via warn.
# _randbelow_without_getrandbits -> warn -> _next_external_frame: _randbelow_without_getrandbits has a transitive dependency on _next_external_frame through warn.
# Note: _randbelow_without_getrandbits does not call warning directly, but reaches it via warn.
# Since _randbelow_without_getrandbits invokes random and random invokes _urandom, _randbelow_without_getrandbits indirectly relies on _urandom.
# Since randrange invokes _index and _index invokes _format_stack_entry, randrange indirectly relies on _format_stack_entry.
# randint calls _index, which calls _format_stack_entry -- so randint transitively depends on _format_stack_entry.
# choices calls random, which calls _urandom -- so choices transitively depends on _urandom.
# Since normalvariate invokes random and random invokes _urandom, normalvariate indirectly relies on _urandom.
# gauss calls random, which calls _urandom -- so gauss transitively depends on _urandom.
# vonmisesvariate -> random -> _urandom: vonmisesvariate has a transitive dependency on _urandom through random.
# gammavariate -> random -> _urandom: gammavariate has a transitive dependency on _urandom through random.
# Note: binomialvariate does not call _format_stack_entry directly, but reaches it via _index.
# Note: binomialvariate does not call _urandom directly, but reaches it via random.
# Since scan invokes match and match invokes _compile, scan indirectly relies on _compile.
# Note: scan does not call _StringGlobber directly, but reaches it via match.
# scan calls append, which calls Time2Internaldate -- so scan transitively depends on Time2Internaldate.
# scan -> append -> Charset: scan has a transitive dependency on Charset through append.
# Note: opengroup does not call basicConfig directly, but reaches it via error.
# Note: opengroup does not call _ directly, but reaches it via error.
# Note: opengroup does not call perror directly, but reaches it via error.
# __next -> error -> basicConfig: __next has a transitive dependency on basicConfig through error.
# __next calls error, which calls _ -- so __next transitively depends on _.
# Since __next invokes error and error invokes perror, __next indirectly relies on perror.
# _repr_iterable -> repr1 -> method: _repr_iterable has a transitive dependency on method through repr1.
# Since _repr_iterable invokes repr1 and repr1 invokes stripid, _repr_iterable indirectly relies on stripid.
# Note: _repr_iterable does not call cram directly, but reaches it via repr1.
# repr_dict -> repr1 -> method: repr_dict has a transitive dependency on method through repr1.
# Since repr_dict invokes repr1 and repr1 invokes stripid, repr_dict indirectly relies on stripid.
# repr_dict calls repr1, which calls cram -- so repr_dict transitively depends on cram.
# onerror -> onexc -> _resetperms: onerror has a transitive dependency on _resetperms through onexc.
# Note: onerror does not call func directly, but reaches it via callback.
# onerror -> callback -> ensure_future: onerror has a transitive dependency on ensure_future through callback.
# Note: pthread_sigmask does not call enum_klass directly, but reaches it via _int_to_enum.
# sigpending calls _int_to_enum, which calls enum_klass -- so sigpending transitively depends on enum_klass.
# Note: sigwait does not call enum_klass directly, but reaches it via _int_to_enum.
# Since valid_signals invokes _int_to_enum and _int_to_enum invokes enum_klass, valid_signals indirectly relies on enum_klass.
# auth -> encode_base64 -> _bencode: auth has a transitive dependency on _bencode through encode_base64.
# Since makefile invokes copyfileobj and copyfileobj invokes fdst_write, makefile indirectly relies on fdst_write.
# makefile calls copyfileobj, which calls _ficlone -- so makefile transitively depends on _ficlone.
# makefile -> copyfileobj -> fsrc_read: makefile has a transitive dependency on fsrc_read through copyfileobj.
# makefile calls copyfileobj, which calls _sendfile -- so makefile transitively depends on _sendfile.
# Note: makefile does not call write_target directly, but reaches it via copyfileobj.
# makefile -> copyfileobj -> read_source: makefile has a transitive dependency on read_source through copyfileobj.
# Note: makefile does not call exception directly, but reaches it via copyfileobj.
# makefile -> copyfileobj -> _copy_file_range: makefile has a transitive dependency on _copy_file_range through copyfileobj.
# Note: makefile does not call _fcopyfile directly, but reaches it via copyfileobj.
# Since family invokes _intenum_converter and _intenum_converter invokes enum_klass, family indirectly relies on enum_klass.
# type -> _intenum_converter -> enum_klass: type has a transitive dependency on enum_klass through _intenum_converter.
# adapt_date calls warn, which calls _is_internal_frame -- so adapt_date transitively depends on _is_internal_frame.
# adapt_date calls warn, which calls _next_external_frame -- so adapt_date transitively depends on _next_external_frame.
# adapt_date calls warn, which calls warning -- so adapt_date transitively depends on warning.
# Note: adapt_datetime does not call _is_internal_frame directly, but reaches it via warn.
# adapt_datetime calls warn, which calls _next_external_frame -- so adapt_datetime transitively depends on _next_external_frame.
# Note: adapt_datetime does not call warning directly, but reaches it via warn.
# convert_date -> warn -> _is_internal_frame: convert_date has a transitive dependency on _is_internal_frame through warn.
# Since convert_date invokes warn and warn invokes _next_external_frame, convert_date indirectly relies on _next_external_frame.
# convert_date -> warn -> warning: convert_date has a transitive dependency on warning through warn.
# convert_timestamp -> warn -> _is_internal_frame: convert_timestamp has a transitive dependency on _is_internal_frame through warn.
# convert_timestamp -> warn -> _next_external_frame: convert_timestamp has a transitive dependency on _next_external_frame through warn.
# convert_timestamp -> warn -> warning: convert_timestamp has a transitive dependency on warning through warn.
# Since _msg_callback invokes callback and callback invokes func, _msg_callback indirectly relies on func.
# _msg_callback -> callback -> ensure_future: _msg_callback has a transitive dependency on ensure_future through callback.
# f_inv -> f -> f1: f_inv has a transitive dependency on f1 through f.
# rand calls random, which calls _urandom -- so rand transitively depends on _urandom.
# from_samples -> _mean_stdev -> _float_sqrt_of_frac: from_samples has a transitive dependency on _float_sqrt_of_frac through _mean_stdev.
# from_samples -> _mean_stdev -> _ss: from_samples has a transitive dependency on _ss through _mean_stdev.
# from_samples -> _mean_stdev -> StatisticsError: from_samples has a transitive dependency on StatisticsError through _mean_stdev.
# samples -> inv_cdf -> _normal_dist_inv_cdf: samples has a transitive dependency on _normal_dist_inv_cdf through inv_cdf.
# Since samples invokes inv_cdf and inv_cdf invokes StatisticsError, samples indirectly relies on StatisticsError.
# Note: samples does not call Timer directly, but reaches it via repeat.
# Note: pdf does not call _dec_from_triple directly, but reaches it via sqrt.
# Since pdf invokes sqrt and sqrt invokes _convert_other, pdf indirectly relies on _convert_other.
# pdf calls sqrt, which calls getcontext -- so pdf transitively depends on getcontext.
# Since pdf invokes sqrt and sqrt invokes _WorkRep, pdf indirectly relies on _WorkRep.
# pdf -> sqrt -> Decimal: pdf has a transitive dependency on Decimal through sqrt.
# Note: pdf does not call key directly, but reaches it via bisect_left.
# Since pdf invokes bisect_right and bisect_right invokes key, pdf indirectly relies on key.
# pdf -> exp -> _dec_from_triple: pdf has a transitive dependency on _dec_from_triple through exp.
# Since pdf invokes exp and exp invokes _convert_other, pdf indirectly relies on _convert_other.
# pdf -> exp -> _dexp: pdf has a transitive dependency on _dexp through exp.
# pdf calls exp, which calls getcontext -- so pdf transitively depends on getcontext.
# pdf calls exp, which calls _WorkRep -- so pdf transitively depends on _WorkRep.
# Since pdf invokes exp and exp invokes Decimal, pdf indirectly relies on Decimal.
# cdf -> bisect_left -> key: cdf has a transitive dependency on key through bisect_left.
# cdf -> bisect_right -> key: cdf has a transitive dependency on key through bisect_right.
# inv_cdf calls _normal_dist_inv_cdf, which calls log -- so inv_cdf transitively depends on log.
# Note: inv_cdf does not call sqrt directly, but reaches it via _normal_dist_inv_cdf.
# inv_cdf calls _normal_dist_inv_cdf, which calls fabs -- so inv_cdf transitively depends on fabs.
# overlap -> sqrt -> _dec_from_triple: overlap has a transitive dependency on _dec_from_triple through sqrt.
# Since overlap invokes sqrt and sqrt invokes _convert_other, overlap indirectly relies on _convert_other.
# Note: overlap does not call getcontext directly, but reaches it via sqrt.
# Since overlap invokes sqrt and sqrt invokes _WorkRep, overlap indirectly relies on _WorkRep.
# Since overlap invokes sqrt and sqrt invokes Decimal, overlap indirectly relies on Decimal.
# overlap -> log -> basicConfig: overlap has a transitive dependency on basicConfig through log.
# _frombuf calls nti, which calls InvalidHeaderError -- so _frombuf transitively depends on InvalidHeaderError.
# _frombuf calls nti, which calls nts -- so _frombuf transitively depends on nts.
# _proc_sparse -> nti -> InvalidHeaderError: _proc_sparse has a transitive dependency on InvalidHeaderError through nti.
# Note: _proc_sparse does not call nts directly, but reaches it via nti.
# Note: addfile does not call fdst_write directly, but reaches it via copyfileobj.
# addfile -> copyfileobj -> _ficlone: addfile has a transitive dependency on _ficlone through copyfileobj.
# Note: addfile does not call fsrc_read directly, but reaches it via copyfileobj.
# addfile -> copyfileobj -> _sendfile: addfile has a transitive dependency on _sendfile through copyfileobj.
# Since addfile invokes copyfileobj and copyfileobj invokes write_target, addfile indirectly relies on write_target.
# addfile calls copyfileobj, which calls read_source -- so addfile transitively depends on read_source.
# addfile calls copyfileobj, which calls exception -- so addfile transitively depends on exception.
# Since addfile invokes copyfileobj and copyfileobj invokes _copy_file_range, addfile indirectly relies on _copy_file_range.
# Note: addfile does not call _fcopyfile directly, but reaches it via copyfileobj.
# cleanup -> unlink -> NodeList: cleanup has a transitive dependency on NodeList through unlink.
# opener calls _mkstemp_inner, which calls _get_candidate_names -- so opener transitively depends on _get_candidate_names.
# TemporaryFile -> _sanitize_params -> _infer_return_type: TemporaryFile has a transitive dependency on _infer_return_type through _sanitize_params.
# Since TemporaryFile invokes _sanitize_params and _sanitize_params invokes gettempdir, TemporaryFile indirectly relies on gettempdir.
# Note: TemporaryFile does not call gettempdirb directly, but reaches it via _sanitize_params.
# Note: TemporaryFile does not call output_type directly, but reaches it via _sanitize_params.
# Since TemporaryFile invokes _mkstemp_inner and _mkstemp_inner invokes _get_candidate_names, TemporaryFile indirectly relies on _get_candidate_names.
# rollover -> TemporaryFile -> _sanitize_params: rollover has a transitive dependency on _sanitize_params through TemporaryFile.
# rollover -> TemporaryFile -> _mkstemp_inner: rollover has a transitive dependency on _mkstemp_inner through TemporaryFile.
# _rmtree -> _resetperms -> _dont_follow_symlinks: _rmtree has a transitive dependency on _dont_follow_symlinks through _resetperms.
# _set_os_name -> _set_name -> _clear_id_cache: _set_os_name has a transitive dependency on _clear_id_cache through _set_name.
# Since invoke_excepthook invokes ExceptHookArgs and ExceptHookArgs invokes _ExceptHookArgs, invoke_excepthook indirectly relies on _ExceptHookArgs.
# autorange -> callback -> func: autorange has a transitive dependency on func through callback.
# Since autorange invokes callback and callback invokes ensure_future, autorange indirectly relies on ensure_future.
# read_or_stop calls readline, which calls UnpicklingError -- so read_or_stop transitively depends on UnpicklingError.
# read_or_stop -> readline -> size_index: read_or_stop has a transitive dependency on size_index through readline.
# Note: read_or_stop does not call assert_ directly, but reaches it via readline.
# Note: read_or_stop does not call ateof directly, but reaches it via readline.
# read_or_stop -> readline -> startup_hook: read_or_stop has a transitive dependency on startup_hook through readline.
# Note: read_or_stop does not call nreadahead directly, but reaches it via readline.
# find_cookie calls lookup, which calls Lookup -- so find_cookie transitively depends on Lookup.
# find_cookie calls lookup, which calls Symbol -- so find_cookie transitively depends on Symbol.
# Since write_results invokes _find_executable_linenos and _find_executable_linenos invokes _find_lines, write_results indirectly relies on _find_lines.
# Since write_results invokes _find_executable_linenos and _find_executable_linenos invokes _find_strings, write_results indirectly relies on _find_strings.
# Note: runfunc does not call getter directly, but reaches it via func.
# Since format_frame_summary invokes _extract_caret_anchors_from_line_segment and _extract_caret_anchors_from_line_segment invokes next_valid_char, format_frame_summary indirectly relies on next_valid_char.
# format_frame_summary -> _extract_caret_anchors_from_line_segment -> nextline: format_frame_summary has a transitive dependency on nextline through _extract_caret_anchors_from_line_segment.
# format_frame_summary -> _extract_caret_anchors_from_line_segment -> increment: format_frame_summary has a transitive dependency on increment through _extract_caret_anchors_from_line_segment.
# format_frame_summary calls _extract_caret_anchors_from_line_segment, which calls increment_until -- so format_frame_summary transitively depends on increment_until.
# Since format_frame_summary invokes _extract_caret_anchors_from_line_segment and _extract_caret_anchors_from_line_segment invokes _byte_offset_to_character_offset, format_frame_summary indirectly relies on _byte_offset_to_character_offset.
# format_frame_summary calls _extract_caret_anchors_from_line_segment, which calls setup_positions -- so format_frame_summary transitively depends on setup_positions.
# format_frame_summary -> _extract_caret_anchors_from_line_segment -> stop: format_frame_summary has a transitive dependency on stop through _extract_caret_anchors_from_line_segment.
# Note: format_frame_summary does not call normalize directly, but reaches it via _extract_caret_anchors_from_line_segment.
# format_frame_summary -> _extract_caret_anchors_from_line_segment -> _Anchors: format_frame_summary has a transitive dependency on _Anchors through _extract_caret_anchors_from_line_segment.
# format_frame_summary -> output_line -> _display_width: format_frame_summary has a transitive dependency on _display_width through output_line.
# increment_until -> stop -> Client: increment_until has a transitive dependency on Client through stop.
# Since increment_until invokes stop and stop invokes close, increment_until indirectly relies on close.
# increment_until calls nextline, which calls next_valid_char -- so increment_until transitively depends on next_valid_char.
# increment_until -> increment -> next_valid_char: increment_until has a transitive dependency on next_valid_char through increment.
# Since setup_positions invokes normalize and normalize invokes _dec_from_triple, setup_positions indirectly relies on _dec_from_triple.
# setup_positions calls normalize, which calls escape -- so setup_positions transitively depends on escape.
# Note: setup_positions does not call _byte_offset_to_character_offset directly, but reaches it via normalize.
# Note: setup_positions does not call _append_modifier directly, but reaches it via normalize.
# setup_positions calls normalize, which calls _convert_other -- so setup_positions transitively depends on _convert_other.
# Note: setup_positions does not call getcontext directly, but reaches it via normalize.
# Note: setup_positions does not call _replace_encoding directly, but reaches it via normalize.
# compare_to calls _compare_grouped_stats, which calls StatisticDiff -- so compare_to transitively depends on StatisticDiff.
# demo1 calls reset, which calls PullDOM -- so demo1 transitively depends on PullDOM.
# Note: demo1 does not call Vec2D directly, but reaches it via reset.
# Note: demo1 does not call fallback_operator directly, but reaches it via forward.
# Since demo1 invokes forward and forward invokes monomorphic_operator, demo1 indirectly relies on monomorphic_operator.
# demo1 -> forward -> Fraction: demo1 has a transitive dependency on Fraction through forward.
# Since demo1 invokes write and write invokes serialize, demo1 indirectly relies on serialize.
# Note: demo1 does not call sleep directly, but reaches it via write.
# Note: demo1 does not call _serialize_text directly, but reaches it via write.
# demo1 calls write, which calls assert_ -- so demo1 transitively depends on assert_.
# demo1 -> write -> _namespaces: demo1 has a transitive dependency on _namespaces through write.
# Note: demo1 does not call time directly, but reaches it via write.
# demo1 -> write -> _count_to_size: demo1 has a transitive dependency on _count_to_size through write.
# Note: demo1 does not call quote directly, but reaches it via write.
# Note: demo1 does not call _nbytes directly, but reaches it via write.
# Note: demo1 does not call _get_writer directly, but reaches it via write.
# demo1 -> write -> crc32: demo1 has a transitive dependency on crc32 through write.
# demo2 -> switchpen -> pu: demo2 has a transitive dependency on pu through switchpen.
# demo2 calls switchpen, which calls isdown -- so demo2 transitively depends on isdown.
# demo2 calls switchpen, which calls pd -- so demo2 transitively depends on pd.
# demo2 calls distance, which calls Vec2D -- so demo2 transitively depends on Vec2D.
# demo2 calls write, which calls serialize -- so demo2 transitively depends on serialize.
# Since demo2 invokes write and write invokes sleep, demo2 indirectly relies on sleep.
# Note: demo2 does not call _serialize_text directly, but reaches it via write.
# Note: demo2 does not call assert_ directly, but reaches it via write.
# demo2 calls write, which calls _namespaces -- so demo2 transitively depends on _namespaces.
# Note: demo2 does not call time directly, but reaches it via write.
# demo2 calls write, which calls _count_to_size -- so demo2 transitively depends on _count_to_size.
# Since demo2 invokes write and write invokes quote, demo2 indirectly relies on quote.
# Since demo2 invokes write and write invokes _nbytes, demo2 indirectly relies on _nbytes.
# Note: demo2 does not call _get_writer directly, but reaches it via write.
# demo2 -> write -> crc32: demo2 has a transitive dependency on crc32 through write.
# Since demo2 invokes towards and towards invokes Vec2D, demo2 indirectly relies on Vec2D.
# demo2 -> reset -> PullDOM: demo2 has a transitive dependency on PullDOM through reset.
# Since demo2 invokes reset and reset invokes Vec2D, demo2 indirectly relies on Vec2D.
# Since demo2 invokes shape and shape invokes TurtleGraphicsError, demo2 indirectly relies on TurtleGraphicsError.
# Note: wrapped does not call getter directly, but reaches it via func.
# Note: decorator does not call getter directly, but reaches it via func.
# Since _determine_new_args invokes prepare and prepare invokes CycleError, _determine_new_args indirectly relies on CycleError.
# _determine_new_args calls prepare, which calls trace -- so _determine_new_args transitively depends on trace.
# _determine_new_args -> prepare -> resolve_task: _determine_new_args has a transitive dependency on resolve_task through prepare.
# Since _determine_new_args invokes prepare and prepare invokes _fixup_main_from_path, _determine_new_args indirectly relies on _fixup_main_from_path.
# Note: _determine_new_args does not call tcgetattr directly, but reaches it via prepare.
# _determine_new_args calls prepare, which calls _fixup_main_from_name -- so _determine_new_args transitively depends on _fixup_main_from_name.
# Since _determine_new_args invokes prepare and prepare invokes SetConsoleMode, _determine_new_args indirectly relies on SetConsoleMode.
# _determine_new_args -> prepare -> cls: _determine_new_args has a transitive dependency on cls through prepare.
# _determine_new_args calls prepare, which calls set_start_method -- so _determine_new_args transitively depends on set_start_method.
# __getitem_inner__ calls _type_check, which calls _type_convert -- so __getitem_inner__ transitively depends on _type_convert.
# __getitem_inner__ -> _type_convert -> _make_forward_ref: __getitem_inner__ has a transitive dependency on _make_forward_ref through _type_convert.
# wrapped_decorator -> decorator -> SkipTest: wrapped_decorator has a transitive dependency on SkipTest through decorator.
# wrapped_decorator -> decorator -> func: wrapped_decorator has a transitive dependency on func through decorator.
# Since annotate invokes _type_check and _type_check invokes _type_convert, annotate indirectly relies on _type_convert.
# Since _callTestMethod invokes method and method invokes func, _callTestMethod indirectly relies on func.
# Note: _callAsync does not call getter directly, but reaches it via func.
# Note: _callMaybeAsync does not call getter directly, but reaches it via func.
# testPartExecutor calls _addSkip, which calls addSkip -- so testPartExecutor transitively depends on addSkip.
# enterContext -> _enter_context -> enter: enterContext has a transitive dependency on enter through _enter_context.
# Note: enterContext does not call addcleanup directly, but reaches it via _enter_context.
# enterClassContext calls _enter_context, which calls enter -- so enterClassContext transitively depends on enter.
# enterClassContext -> _enter_context -> addcleanup: enterClassContext has a transitive dependency on addcleanup through _enter_context.
# _baseAssertEqual -> _common_shorten_repr -> _shorten: _baseAssertEqual has a transitive dependency on _shorten through _common_shorten_repr.
# Note: _baseAssertEqual does not call commonprefix directly, but reaches it via _common_shorten_repr.
# assertSequenceEqual -> _common_shorten_repr -> _shorten: assertSequenceEqual has a transitive dependency on _shorten through _common_shorten_repr.
# assertSequenceEqual -> _common_shorten_repr -> commonprefix: assertSequenceEqual has a transitive dependency on commonprefix through _common_shorten_repr.
# assertDictEqual calls _common_shorten_repr, which calls _shorten -- so assertDictEqual transitively depends on _shorten.
# Note: assertDictEqual does not call commonprefix directly, but reaches it via _common_shorten_repr.
# Since assertCountEqual invokes _count_diff_hashable and _count_diff_hashable invokes Counter, assertCountEqual indirectly relies on Counter.
# assertCountEqual -> _count_diff_hashable -> _Mismatch: assertCountEqual has a transitive dependency on _Mismatch through _count_diff_hashable.
# Note: assertCountEqual does not call _Mismatch directly, but reaches it via _count_diff_all_purpose.
# assertMultiLineEqual calls _common_shorten_repr, which calls _shorten -- so assertMultiLineEqual transitively depends on _shorten.
# Since assertMultiLineEqual invokes _common_shorten_repr and _common_shorten_repr invokes commonprefix, assertMultiLineEqual indirectly relies on commonprefix.
# loadTestsFromModule calls _make_failed_load_tests, which calls _make_failed_test -- so loadTestsFromModule transitively depends on _make_failed_test.
# loadTestsFromName calls _make_failed_import_test, which calls _make_failed_test -- so loadTestsFromName transitively depends on _make_failed_test.
# loadTestsFromName calls _make_failed_test, which calls _FailedTest -- so loadTestsFromName transitively depends on _FailedTest.
# loadTestsFromName -> _make_failed_test -> suiteClass: loadTestsFromName has a transitive dependency on suiteClass through _make_failed_test.
# Note: getTestCaseNames does not call _compile_pattern directly, but reaches it via fnmatchcase.
# Since getTestCaseNames invokes fnmatchcase and fnmatchcase invokes match, getTestCaseNames indirectly relies on match.
# _match_path -> fnmatch -> fnmatchcase: _match_path has a transitive dependency on fnmatchcase through fnmatch.
# _find_test_path calls _make_failed_import_test, which calls _make_failed_test -- so _find_test_path transitively depends on _make_failed_test.
# _find_test_path calls _make_skipped_test, which calls TestClass -- so _find_test_path transitively depends on TestClass.
# _find_test_path calls _make_skipped_test, which calls suiteClass -- so _find_test_path transitively depends on suiteClass.
# parseArgs -> _convert_names -> _convert_name: parseArgs has a transitive dependency on _convert_name through _convert_names.
# Note: runTests does not call _InterruptHandler directly, but reaches it via installHandler.
# _mock_add_spec -> iscoroutinefunction -> _has_code_flag: _mock_add_spec has a transitive dependency on _has_code_flag through iscoroutinefunction.
# Since _mock_add_spec invokes iscoroutinefunction and iscoroutinefunction invokes _has_coroutine_mark, _mock_add_spec indirectly relies on _has_coroutine_mark.
# Note: _mock_add_spec does not call _iscoroutinefunction directly, but reaches it via iscoroutinefunction.
# _mock_add_spec -> _get_signature_object -> partial: _mock_add_spec has a transitive dependency on partial through _get_signature_object.
# __set_return_value -> _check_and_set_parent -> _extract_mock: __set_return_value has a transitive dependency on _extract_mock through _check_and_set_parent.
# __set_return_value -> _check_and_set_parent -> _is_instance_mock: __set_return_value has a transitive dependency on _is_instance_mock through _check_and_set_parent.
# Note: __set_side_effect does not call _is_exception directly, but reaches it via _try_iter.
# Note: __set_side_effect does not call _callable directly, but reaches it via _try_iter.
# _call_matcher -> call -> Popen: _call_matcher has a transitive dependency on Popen through call.
# Note: _call_matcher does not call obj directly, but reaches it via call.
# Note: decorate_class does not call SkipTest directly, but reaches it via decorator.
# Since decorate_class invokes decorator and decorator invokes func, decorate_class indirectly relies on func.
# Since decorate_class invokes _patch_dict and _patch_dict invokes _clear_dict, decorate_class indirectly relies on _clear_dict.
# Note: decorate_callable does not call partial directly, but reaches it via wraps.
# decorate_callable -> f -> f1: decorate_callable has a transitive dependency on f1 through f.
# Since decorate_callable invokes func and func invokes getter, decorate_callable indirectly relies on getter.
# decorate_async_callable calls wraps, which calls partial -- so decorate_async_callable transitively depends on partial.
# Since decorate_async_callable invokes f and f invokes f1, decorate_async_callable indirectly relies on f1.
# Since decorate_async_callable invokes func and func invokes getter, decorate_async_callable indirectly relies on getter.
# method -> func -> getter: method has a transitive dependency on getter through func.
# create_mock -> _set_return_value -> side_effector: create_mock has a transitive dependency on side_effector through _set_return_value.
# create_mock -> _set_return_value -> return_calculator: create_mock has a transitive dependency on return_calculator through _set_return_value.
# reset_data calls _readline_side_effect, which calls _iter_side_effect -- so reset_data transitively depends on _iter_side_effect.
# _exc_info_to_string -> can_colorize -> _safe_getenv: _exc_info_to_string has a transitive dependency on _safe_getenv through can_colorize.
# _handleClassSetUp calls _call_if_exists, which calls func -- so _handleClassSetUp transitively depends on func.
# _handleClassSetUp calls doClassCleanups, which calls function -- so _handleClassSetUp transitively depends on function.
# Since _handleModuleFixture invokes _call_if_exists and _call_if_exists invokes func, _handleModuleFixture indirectly relies on func.
# _handleModuleTearDown calls _call_if_exists, which calls func -- so _handleModuleTearDown transitively depends on func.
# _tearDownPreviousClass -> _call_if_exists -> func: _tearDownPreviousClass has a transitive dependency on func through _call_if_exists.
# _tearDownPreviousClass calls doClassCleanups, which calls function -- so _tearDownPreviousClass transitively depends on function.
# geturl -> urlunsplit -> _coerce_result: geturl has a transitive dependency on _coerce_result through urlunsplit.
# Note: geturl does not call _urlunsplit directly, but reaches it via urlunsplit.
# Note: geturl does not call _coerce_args directly, but reaches it via urlunsplit.
# geturl calls urlunparse, which calls _coerce_result -- so geturl transitively depends on _coerce_result.
# Since geturl invokes urlunparse and urlunparse invokes _urlunsplit, geturl indirectly relies on _urlunsplit.
# geturl -> urlunparse -> _coerce_args: geturl has a transitive dependency on _coerce_args through urlunparse.
# Since full_url invokes unwrap and unwrap invokes stop, full_url indirectly relies on stop.
# Since _call_chain invokes func and func invokes getter, _call_chain indirectly relies on getter.
# http_error_302 -> urlunparse -> _coerce_result: http_error_302 has a transitive dependency on _coerce_result through urlunparse.
# Since http_error_302 invokes urlunparse and urlunparse invokes _urlunsplit, http_error_302 indirectly relies on _urlunsplit.
# http_error_302 calls urlunparse, which calls _coerce_args -- so http_error_302 transitively depends on _coerce_args.
# http_error_302 calls urlparse, which calls _urlparse -- so http_error_302 transitively depends on _urlparse.
# http_error_302 calls urlparse, which calls _coerce_result -- so http_error_302 transitively depends on _coerce_result.
# Since http_error_302 invokes urlparse and urlparse invokes ParseResult, http_error_302 indirectly relies on ParseResult.
# http_error_302 calls urlparse, which calls _coerce_args -- so http_error_302 transitively depends on _coerce_args.
# http_error_302 calls urljoin, which calls _urlsplit -- so http_error_302 transitively depends on _urlsplit.
# Note: http_error_302 does not call _coerce_result directly, but reaches it via urljoin.
# http_error_302 calls urljoin, which calls _coerce_args -- so http_error_302 transitively depends on _coerce_args.
# http_error_302 -> urljoin -> _urlunsplit: http_error_302 has a transitive dependency on _urlunsplit through urljoin.
# Since http_error_302 invokes quote and quote invokes quote_from_bytes, http_error_302 indirectly relies on quote_from_bytes.
# proxy_open -> _parse_proxy -> _splitpasswd: proxy_open has a transitive dependency on _splitpasswd through _parse_proxy.
# Note: proxy_open does not call _splittype directly, but reaches it via _parse_proxy.
# Since proxy_open invokes _parse_proxy and _parse_proxy invokes _splituser, proxy_open indirectly relies on _splituser.
# Since proxy_open invokes unquote and unquote invokes _unquote_impl, proxy_open indirectly relies on _unquote_impl.
# Since proxy_open invokes unquote and unquote invokes _generate_unquoted_parts, proxy_open indirectly relies on _generate_unquoted_parts.
# Note: proxy_open does not call proxy_bypass_registry directly, but reaches it via proxy_bypass.
# Note: proxy_open does not call proxy_bypass_environment directly, but reaches it via proxy_bypass.
# Since proxy_open invokes proxy_bypass and proxy_bypass invokes getproxies_environment, proxy_open indirectly relies on getproxies_environment.
# proxy_open calls proxy_bypass, which calls proxy_bypass_macosx_sysconf -- so proxy_open transitively depends on proxy_bypass_macosx_sysconf.
# Note: reduce_uri does not call SplitResult directly, but reaches it via urlsplit.
# Note: reduce_uri does not call _urlsplit directly, but reaches it via urlsplit.
# Since reduce_uri invokes urlsplit and urlsplit invokes _coerce_args, reduce_uri indirectly relies on _coerce_args.
# Note: reduce_uri does not call _coerce_result directly, but reaches it via urlsplit.
# http_error_401 calls urlparse, which calls _urlparse -- so http_error_401 transitively depends on _urlparse.
# Since http_error_401 invokes urlparse and urlparse invokes _coerce_result, http_error_401 indirectly relies on _coerce_result.
# Since http_error_401 invokes urlparse and urlparse invokes ParseResult, http_error_401 indirectly relies on ParseResult.
# http_error_401 -> urlparse -> _coerce_args: http_error_401 has a transitive dependency on _coerce_args through urlparse.
# Since open_local_file invokes pathname2url and pathname2url invokes quote, open_local_file indirectly relies on quote.
# Note: open_local_file does not call unquote directly, but reaches it via url2pathname.
# open_local_file -> url2pathname -> urlsplit: open_local_file has a transitive dependency on urlsplit through url2pathname.
# Note: open_local_file does not call URLError directly, but reaches it via url2pathname.
# Note: open_local_file does not call _is_local_authority directly, but reaches it via url2pathname.
# ftp_open -> unquote -> _unquote_impl: ftp_open has a transitive dependency on _unquote_impl through unquote.
# Since ftp_open invokes unquote and unquote invokes _generate_unquoted_parts, ftp_open indirectly relies on _generate_unquoted_parts.
# Note: data_open does not call _unquote_impl directly, but reaches it via unquote_to_bytes.
# Note: proxy_bypass_macosx_sysconf does not call ip2num directly, but reaches it via _proxy_bypass_macosx_sysconf.
# Note: proxy_bypass_macosx_sysconf does not call IPv4Address directly, but reaches it via _proxy_bypass_macosx_sysconf.
# Since proxy_bypass_macosx_sysconf invokes _proxy_bypass_macosx_sysconf and _proxy_bypass_macosx_sysconf invokes fnmatch, proxy_bypass_macosx_sysconf indirectly relies on fnmatch.
# Note: proxy_bypass_macosx_sysconf does not call _splitport directly, but reaches it via _proxy_bypass_macosx_sysconf.
# proxy_bypass calls proxy_bypass_registry, which calls _proxy_bypass_winreg_override -- so proxy_bypass transitively depends on _proxy_bypass_winreg_override.
# proxy_bypass calls proxy_bypass_environment, which calls getproxies_environment -- so proxy_bypass transitively depends on getproxies_environment.
# Note: proxy_bypass does not call _splitport directly, but reaches it via proxy_bypass_environment.
# proxy_bypass calls proxy_bypass_macosx_sysconf, which calls _get_proxy_settings -- so proxy_bypass transitively depends on _get_proxy_settings.
# proxy_bypass -> proxy_bypass_macosx_sysconf -> _proxy_bypass_macosx_sysconf: proxy_bypass has a transitive dependency on _proxy_bypass_macosx_sysconf through proxy_bypass_macosx_sysconf.
# getproxies -> getproxies_macosx_sysconf -> _get_proxies: getproxies has a transitive dependency on _get_proxies through getproxies_macosx_sysconf.
# _add_entry -> merge_entries -> Entry: _add_entry has a transitive dependency on Entry through merge_entries.
# Note: can_fetch does not call normalize directly, but reaches it via normalize_uri.
# replace_variables -> quote -> quote_from_bytes: replace_variables has a transitive dependency on quote_from_bytes through quote.
# _exitfunc calls f, which calls f1 -- so _exitfunc transitively depends on f1.
# start_response calls is_hop_by_hop, which calls _hoppish -- so start_response transitively depends on _hoppish.
# log_exception calls print_exception, which calls TracebackException -- so log_exception transitively depends on TracebackException.
# log_exception -> print_exception -> _parse_value_tb: log_exception has a transitive dependency on _parse_value_tb through print_exception.
# Since error_output invokes start_response and start_response invokes is_hop_by_hop, error_output indirectly relies on is_hop_by_hop.
# lint_app -> check_iterator -> assert_: lint_app has a transitive dependency on assert_ through check_iterator.
# Since lint_app invokes start_response and start_response invokes is_hop_by_hop, lint_app indirectly relies on is_hop_by_hop.
# lint_app calls check_content_type, which calls assert_ -- so lint_app transitively depends on assert_.
# lint_app calls check_content_type, which calls check_string_type -- so lint_app transitively depends on check_string_type.
# Since lint_app invokes check_headers and check_headers invokes assert_, lint_app indirectly relies on assert_.
# lint_app calls check_headers, which calls check_string_type -- so lint_app transitively depends on check_string_type.
# lint_app -> check_exc_info -> assert_: lint_app has a transitive dependency on assert_ through check_exc_info.
# Note: lint_app does not call assert_ directly, but reaches it via check_environ.
# lint_app -> check_environ -> check_errors: lint_app has a transitive dependency on check_errors through check_environ.
# lint_app calls check_environ, which calls check_input -- so lint_app transitively depends on check_input.
# lint_app calls check_status, which calls assert_ -- so lint_app transitively depends on assert_.
# lint_app calls check_status, which calls check_string_type -- so lint_app transitively depends on check_string_type.
# Since start_element_handler invokes _set_attribute_node and _set_attribute_node invokes _clear_id_cache, start_element_handler indirectly relies on _clear_id_cache.
# Since start_element_handler invokes _parse_ns_name and _parse_ns_name invokes intern, start_element_handler indirectly relies on intern.
# cloneNode calls _clone_node, which calls Entity -- so cloneNode transitively depends on Entity.
# Note: cloneNode does not call Notation directly, but reaches it via _clone_node.
# Since setAttributeNode invokes _set_attribute_node and _set_attribute_node invokes _clear_id_cache, setAttributeNode indirectly relies on _clear_id_cache.
# createComment -> Comment -> Element: createComment has a transitive dependency on Element through Comment.
# createProcessingInstruction calls ProcessingInstruction, which calls Element -- so createProcessingInstruction transitively depends on Element.
# Since importNode invokes _clone_node and _clone_node invokes Entity, importNode indirectly relies on Entity.
# Since importNode invokes _clone_node and _clone_node invokes Notation, importNode indirectly relies on Notation.
# Note: startElementNS does not call escape directly, but reaches it via quoteattr.
# _handle_single calls factory, which calls loader -- so _handle_single transitively depends on loader.
# Since _handle_single invokes factory and factory invokes StreamReaderProtocol, _handle_single indirectly relies on StreamReaderProtocol.
# Note: _handle_single does not call cls directly, but reaches it via factory.
# Since _handle_single invokes factory and factory invokes custom_task_constructor, _handle_single indirectly relies on custom_task_constructor.
# Note: _handle_single does not call StreamReader directly, but reaches it via factory.
# _setevents -> start -> ProcessError: _setevents has a transitive dependency on ProcessError through start.
# Since _setevents invokes start and start invokes _start_joinable_thread, _setevents indirectly relies on _start_joinable_thread.
# Since _setevents invokes start and start invokes current_process, _setevents indirectly relies on current_process.
# _setevents calls start, which calls _cleanup -- so _setevents transitively depends on _cleanup.
# _setevents -> start -> ResponseError: _setevents has a transitive dependency on ResponseError through start.
# Note: _setevents does not call f directly, but reaches it via end.
# _setevents calls append, which calls Time2Internaldate -- so _setevents transitively depends on Time2Internaldate.
# Note: _setevents does not call Charset directly, but reaches it via append.
# Since _flush invokes _escape_cdata_c14n and _escape_cdata_c14n invokes _raise_serialization_error, _flush indirectly relies on _raise_serialization_error.
# Note: end does not call f1 directly, but reaches it via f.
# Note: comment does not call _raise_serialization_error directly, but reaches it via _escape_cdata_c14n.
# Since pi invokes _escape_cdata_c14n and _escape_cdata_c14n invokes _raise_serialization_error, pi indirectly relies on _raise_serialization_error.
# Note: startElement does not call escape directly, but reaches it via quoteattr.
# characters calls escape, which calls __dict_replace -- so characters transitively depends on __dict_replace.
# characters calls escape, which calls replace -- so characters transitively depends on replace.
# Since __dump invokes f and f invokes f1, __dump indirectly relies on f1.
# dump_nil -> write -> serialize: dump_nil has a transitive dependency on serialize through write.
# dump_nil calls write, which calls sleep -- so dump_nil transitively depends on sleep.
# Since dump_nil invokes write and write invokes _serialize_text, dump_nil indirectly relies on _serialize_text.
# dump_nil calls write, which calls assert_ -- so dump_nil transitively depends on assert_.
# dump_nil calls write, which calls _namespaces -- so dump_nil transitively depends on _namespaces.
# Since dump_nil invokes write and write invokes time, dump_nil indirectly relies on time.
# Since dump_nil invokes write and write invokes _count_to_size, dump_nil indirectly relies on _count_to_size.
# Note: dump_nil does not call quote directly, but reaches it via write.
# dump_nil -> write -> _nbytes: dump_nil has a transitive dependency on _nbytes through write.
# Since dump_nil invokes write and write invokes _get_writer, dump_nil indirectly relies on _get_writer.
# dump_nil calls write, which calls crc32 -- so dump_nil transitively depends on crc32.
# Note: dump_bool does not call serialize directly, but reaches it via write.
# Since dump_bool invokes write and write invokes sleep, dump_bool indirectly relies on sleep.
# Since dump_bool invokes write and write invokes _serialize_text, dump_bool indirectly relies on _serialize_text.
# Since dump_bool invokes write and write invokes assert_, dump_bool indirectly relies on assert_.
# Note: dump_bool does not call _namespaces directly, but reaches it via write.
# Note: dump_bool does not call time directly, but reaches it via write.
# dump_bool -> write -> _count_to_size: dump_bool has a transitive dependency on _count_to_size through write.
# dump_bool -> write -> quote: dump_bool has a transitive dependency on quote through write.
# Since dump_bool invokes write and write invokes _nbytes, dump_bool indirectly relies on _nbytes.
# dump_bool -> write -> _get_writer: dump_bool has a transitive dependency on _get_writer through write.
# dump_bool -> write -> crc32: dump_bool has a transitive dependency on crc32 through write.
# Note: dump_long does not call serialize directly, but reaches it via write.
# dump_long -> write -> sleep: dump_long has a transitive dependency on sleep through write.
# Since dump_long invokes write and write invokes _serialize_text, dump_long indirectly relies on _serialize_text.
# Since dump_long invokes write and write invokes assert_, dump_long indirectly relies on assert_.
# Since dump_long invokes write and write invokes _namespaces, dump_long indirectly relies on _namespaces.
# Note: dump_long does not call time directly, but reaches it via write.
# Since dump_long invokes write and write invokes _count_to_size, dump_long indirectly relies on _count_to_size.
# Note: dump_long does not call quote directly, but reaches it via write.
# dump_long -> write -> _nbytes: dump_long has a transitive dependency on _nbytes through write.
# Since dump_long invokes write and write invokes _get_writer, dump_long indirectly relies on _get_writer.
# Since dump_long invokes write and write invokes crc32, dump_long indirectly relies on crc32.
# dump_double calls write, which calls serialize -- so dump_double transitively depends on serialize.
# dump_double -> write -> sleep: dump_double has a transitive dependency on sleep through write.
# dump_double -> write -> _serialize_text: dump_double has a transitive dependency on _serialize_text through write.
# dump_double calls write, which calls assert_ -- so dump_double transitively depends on assert_.
# dump_double -> write -> _namespaces: dump_double has a transitive dependency on _namespaces through write.
# Since dump_double invokes write and write invokes time, dump_double indirectly relies on time.
# Note: dump_double does not call _count_to_size directly, but reaches it via write.
# Since dump_double invokes write and write invokes quote, dump_double indirectly relies on quote.
# dump_double calls write, which calls _nbytes -- so dump_double transitively depends on _nbytes.
# Since dump_double invokes write and write invokes _get_writer, dump_double indirectly relies on _get_writer.
# Since dump_double invokes write and write invokes crc32, dump_double indirectly relies on crc32.
# dump_unicode -> write -> serialize: dump_unicode has a transitive dependency on serialize through write.
# Note: dump_unicode does not call sleep directly, but reaches it via write.
# Note: dump_unicode does not call _serialize_text directly, but reaches it via write.
# Since dump_unicode invokes write and write invokes assert_, dump_unicode indirectly relies on assert_.
# Since dump_unicode invokes write and write invokes _namespaces, dump_unicode indirectly relies on _namespaces.
# Since dump_unicode invokes write and write invokes time, dump_unicode indirectly relies on time.
# Note: dump_unicode does not call _count_to_size directly, but reaches it via write.
# Since dump_unicode invokes write and write invokes quote, dump_unicode indirectly relies on quote.
# dump_unicode calls write, which calls _nbytes -- so dump_unicode transitively depends on _nbytes.
# dump_unicode calls write, which calls _get_writer -- so dump_unicode transitively depends on _get_writer.
# Note: dump_unicode does not call crc32 directly, but reaches it via write.
# Since dump_unicode invokes escape and escape invokes __dict_replace, dump_unicode indirectly relies on __dict_replace.
# dump_unicode calls escape, which calls replace -- so dump_unicode transitively depends on replace.
# Since dump_bytes invokes write and write invokes serialize, dump_bytes indirectly relies on serialize.
# Note: dump_bytes does not call sleep directly, but reaches it via write.
# Since dump_bytes invokes write and write invokes _serialize_text, dump_bytes indirectly relies on _serialize_text.
# Note: dump_bytes does not call assert_ directly, but reaches it via write.
# dump_bytes calls write, which calls _namespaces -- so dump_bytes transitively depends on _namespaces.
# Note: dump_bytes does not call time directly, but reaches it via write.
# dump_bytes calls write, which calls _count_to_size -- so dump_bytes transitively depends on _count_to_size.
# dump_bytes -> write -> quote: dump_bytes has a transitive dependency on quote through write.
# Note: dump_bytes does not call _nbytes directly, but reaches it via write.
# dump_bytes calls write, which calls _get_writer -- so dump_bytes transitively depends on _get_writer.
# dump_bytes -> write -> crc32: dump_bytes has a transitive dependency on crc32 through write.
# Note: dump_array does not call ElementTree directly, but reaches it via dump.
# dump_array calls dump, which calls pack -- so dump_array transitively depends on pack.
# Note: dump_array does not call PicklingError directly, but reaches it via dump.
# Since dump_array invokes dump and dump invokes ForkingPickler, dump_array indirectly relies on ForkingPickler.
# Since dump_array invokes dump and dump invokes _format, dump_array indirectly relies on _format.
# dump_array -> dump -> cls: dump_array has a transitive dependency on cls through dump.
# Note: dump_array does not call serialize directly, but reaches it via write.
# dump_array -> write -> sleep: dump_array has a transitive dependency on sleep through write.
# dump_array calls write, which calls _serialize_text -- so dump_array transitively depends on _serialize_text.
# dump_array calls write, which calls assert_ -- so dump_array transitively depends on assert_.
# Since dump_array invokes write and write invokes _namespaces, dump_array indirectly relies on _namespaces.
# dump_array calls write, which calls time -- so dump_array transitively depends on time.
# dump_array calls write, which calls _count_to_size -- so dump_array transitively depends on _count_to_size.
# Note: dump_array does not call quote directly, but reaches it via write.
# dump_array -> write -> _nbytes: dump_array has a transitive dependency on _nbytes through write.
# Since dump_array invokes write and write invokes _get_writer, dump_array indirectly relies on _get_writer.
# dump_array calls write, which calls crc32 -- so dump_array transitively depends on crc32.
# dump_struct -> dump -> ElementTree: dump_struct has a transitive dependency on ElementTree through dump.
# Since dump_struct invokes dump and dump invokes pack, dump_struct indirectly relies on pack.
# dump_struct -> dump -> PicklingError: dump_struct has a transitive dependency on PicklingError through dump.
# Note: dump_struct does not call ForkingPickler directly, but reaches it via dump.
# Note: dump_struct does not call _format directly, but reaches it via dump.
# Since dump_struct invokes dump and dump invokes cls, dump_struct indirectly relies on cls.
# Since dump_struct invokes write and write invokes serialize, dump_struct indirectly relies on serialize.
# Note: dump_struct does not call sleep directly, but reaches it via write.
# dump_struct calls write, which calls _serialize_text -- so dump_struct transitively depends on _serialize_text.
# Note: dump_struct does not call assert_ directly, but reaches it via write.
# Note: dump_struct does not call _namespaces directly, but reaches it via write.
# dump_struct calls write, which calls time -- so dump_struct transitively depends on time.
# Note: dump_struct does not call _count_to_size directly, but reaches it via write.
# Since dump_struct invokes write and write invokes quote, dump_struct indirectly relies on quote.
# dump_struct calls write, which calls _nbytes -- so dump_struct transitively depends on _nbytes.
# Since dump_struct invokes write and write invokes _get_writer, dump_struct indirectly relies on _get_writer.
# Since dump_struct invokes write and write invokes crc32, dump_struct indirectly relies on crc32.
# dump_struct -> escape -> __dict_replace: dump_struct has a transitive dependency on __dict_replace through escape.
# Since dump_struct invokes escape and escape invokes replace, dump_struct indirectly relies on replace.
# dump_datetime calls write, which calls serialize -- so dump_datetime transitively depends on serialize.
# Note: dump_datetime does not call sleep directly, but reaches it via write.
# Note: dump_datetime does not call _serialize_text directly, but reaches it via write.
# Note: dump_datetime does not call assert_ directly, but reaches it via write.
# Since dump_datetime invokes write and write invokes _namespaces, dump_datetime indirectly relies on _namespaces.
# dump_datetime calls write, which calls time -- so dump_datetime transitively depends on time.
# dump_datetime calls write, which calls _count_to_size -- so dump_datetime transitively depends on _count_to_size.
# dump_datetime calls write, which calls quote -- so dump_datetime transitively depends on quote.
# dump_datetime calls write, which calls _nbytes -- so dump_datetime transitively depends on _nbytes.
# Note: dump_datetime does not call _get_writer directly, but reaches it via write.
# dump_datetime calls write, which calls crc32 -- so dump_datetime transitively depends on crc32.
# dump_datetime calls _strftime, which calls _iso8601_format -- so dump_datetime transitively depends on _iso8601_format.
# Since end_dispatch invokes f and f invokes f1, end_dispatch indirectly relies on f1.
# Note: send_content does not call BytesIO directly, but reaches it via gzip_encode.
# __request -> dumps -> Marshaller: __request has a transitive dependency on Marshaller through dumps.
# __request -> dumps -> dump: __request has a transitive dependency on dump through dumps.
# __request calls dumps, which calls escape -- so __request transitively depends on escape.
# Note: __request does not call write directly, but reaches it via dumps.
# __request -> dumps -> FastMarshaller: __request has a transitive dependency on FastMarshaller through dumps.
# __request calls dumps, which calls cls -- so __request transitively depends on cls.
# __request -> dumps -> BytesIO: __request has a transitive dependency on BytesIO through dumps.
# _marshaled_dispatch -> dumps -> Marshaller: _marshaled_dispatch has a transitive dependency on Marshaller through dumps.
# _marshaled_dispatch calls dumps, which calls dump -- so _marshaled_dispatch transitively depends on dump.
# _marshaled_dispatch -> dumps -> escape: _marshaled_dispatch has a transitive dependency on escape through dumps.
# _marshaled_dispatch calls dumps, which calls write -- so _marshaled_dispatch transitively depends on write.
# _marshaled_dispatch calls dumps, which calls FastMarshaller -- so _marshaled_dispatch transitively depends on FastMarshaller.
# Note: _marshaled_dispatch does not call cls directly, but reaches it via dumps.
# _marshaled_dispatch -> dumps -> BytesIO: _marshaled_dispatch has a transitive dependency on BytesIO through dumps.
# _marshaled_dispatch calls loads, which calls key_value_rule -- so _marshaled_dispatch transitively depends on key_value_rule.
# Note: _marshaled_dispatch does not call Output directly, but reaches it via loads.
# _marshaled_dispatch -> loads -> detect_encoding: _marshaled_dispatch has a transitive dependency on detect_encoding through loads.
# Since _marshaled_dispatch invokes loads and loads invokes skip_comment, _marshaled_dispatch indirectly relies on skip_comment.
# Since _marshaled_dispatch invokes loads and loads invokes getparser, _marshaled_dispatch indirectly relies on getparser.
# Since _marshaled_dispatch invokes loads and loads invokes TOMLDecodeError, _marshaled_dispatch indirectly relies on TOMLDecodeError.
# Since _marshaled_dispatch invokes loads and loads invokes JSONDecodeError, _marshaled_dispatch indirectly relies on JSONDecodeError.
# _marshaled_dispatch -> loads -> make_safe_parse_float: _marshaled_dispatch has a transitive dependency on make_safe_parse_float through loads.
# Note: _marshaled_dispatch does not call load directly, but reaches it via loads.
# Note: _marshaled_dispatch does not call create_list_rule directly, but reaches it via loads.
# Since _marshaled_dispatch invokes loads and loads invokes create_dict_rule, _marshaled_dispatch indirectly relies on create_dict_rule.
# Since _marshaled_dispatch invokes loads and loads invokes cls, _marshaled_dispatch indirectly relies on cls.
# _marshaled_dispatch -> loads -> BytesIO: _marshaled_dispatch has a transitive dependency on BytesIO through loads.
# Since _marshaled_dispatch invokes loads and loads invokes skip_chars, _marshaled_dispatch indirectly relies on skip_chars.
# do_POST calls gzip_encode, which calls BytesIO -- so do_POST transitively depends on BytesIO.
# Since decode_request_content invokes gzip_decode and gzip_decode invokes BytesIO, decode_request_content indirectly relies on BytesIO.
# Since decrypter invokes update_keys and update_keys invokes crc32, decrypter indirectly relies on crc32.
# decrypter calls append, which calls Time2Internaldate -- so decrypter transitively depends on Time2Internaldate.
# decrypter -> append -> Charset: decrypter has a transitive dependency on Charset through append.
# Note: _init_decrypter does not call crc32 directly, but reaches it via _ZipDecrypter.
# Note: _init_decrypter does not call update_keys directly, but reaches it via _ZipDecrypter.
# _init_decrypter -> _ZipDecrypter -> append: _init_decrypter has a transitive dependency on append through _ZipDecrypter.
# _RealGetContents calls _EndRecData, which calls _EndRecData64 -- so _RealGetContents transitively depends on _EndRecData64.
# Since _get_codename invokes _compile and _compile invokes _compile_charset, _get_codename indirectly relies on _compile_charset.
# Since _get_codename invokes _compile and _compile invokes iscased, _get_codename indirectly relies on iscased.
# _get_codename -> _compile -> PatternError: _get_codename has a transitive dependency on PatternError through _compile.
# Since _get_codename invokes _compile and _compile invokes tailappend, _get_codename indirectly relies on tailappend.
# _get_codename calls _compile, which calls _simple -- so _get_codename transitively depends on _simple.
# _get_codename calls _compile, which calls _combine_flags -- so _get_codename transitively depends on _combine_flags.
# _get_codename -> _compile -> _optimize_charset: _get_codename has a transitive dependency on _optimize_charset through _compile.
# Since _get_codename invokes _compile and _compile invokes emit, _get_codename indirectly relies on emit.
# _get_codename calls _compile, which calls _len -- so _get_codename transitively depends on _len.
# Note: _get_codename does not call tolower directly, but reaches it via _compile.
# _get_codename -> _compile -> error: _get_codename has a transitive dependency on error through _compile.
# _get_files -> _read_directory -> _unpack_uint16: _get_files has a transitive dependency on _unpack_uint16 through _read_directory.
# _get_files calls _read_directory, which calls ZipImportError -- so _get_files transitively depends on ZipImportError.
# Note: _get_files does not call _unpack_uint32 directly, but reaches it via _read_directory.
# _get_files -> _read_directory -> _unpack_uint64: _get_files has a transitive dependency on _unpack_uint64 through _read_directory.
# _load_file -> _parse_tz_str -> _parse_dst_start_end: _load_file has a transitive dependency on _parse_dst_start_end through _parse_tz_str.
# Note: _load_file does not call _ttinfo directly, but reaches it via _parse_tz_str.
# _load_file -> _parse_tz_str -> _TZStr: _load_file has a transitive dependency on _TZStr through _parse_tz_str.
# Note: _load_file does not call _load_timedelta directly, but reaches it via _parse_tz_str.
# _load_file -> _parse_tz_str -> _parse_tz_delta: _load_file has a transitive dependency on _parse_tz_delta through _parse_tz_str.
# _load_file -> _load_timedelta -> timedelta: _load_file has a transitive dependency on timedelta through _load_timedelta.
# Since format_help invokes join and join invokes wait, format_help indirectly relies on wait.
# format_help -> join -> splitroot: format_help has a transitive dependency on splitroot through join.
# Note: format_help does not call current_thread directly, but reaches it via join.
# Since format_help invokes join and join invokes _get_sep, format_help indirectly relies on _get_sep.
# format_help calls join, which calls quote -- so format_help transitively depends on quote.
# format_help calls _, which calls resolve -- so format_help transitively depends on resolve.
# format_help -> _ -> _infer_caller: format_help has a transitive dependency on _infer_caller through _.
# format_help -> func -> getter: format_help has a transitive dependency on getter through func.
# Since consume_optional invokes take_action and take_action invokes ArgumentError, consume_optional indirectly relies on ArgumentError.
# Note: consume_optional does not call _get_action_name directly, but reaches it via take_action.
# consume_optional -> take_action -> _: consume_optional has a transitive dependency on _ through take_action.
# Note: consume_optional does not call action directly, but reaches it via take_action.
# Since consume_optional invokes _ and _ invokes resolve, consume_optional indirectly relies on resolve.
# consume_optional calls _, which calls _infer_caller -- so consume_optional transitively depends on _infer_caller.
# Since consume_positionals invokes take_action and take_action invokes ArgumentError, consume_positionals indirectly relies on ArgumentError.
# consume_positionals -> take_action -> _get_action_name: consume_positionals has a transitive dependency on _get_action_name through take_action.
# Since consume_positionals invokes take_action and take_action invokes _, consume_positionals indirectly relies on _.
# Note: consume_positionals does not call action directly, but reaches it via take_action.
# consume_positionals -> _ -> resolve: consume_positionals has a transitive dependency on resolve through _.
# consume_positionals -> _ -> _infer_caller: consume_positionals has a transitive dependency on _infer_caller through _.
# _exit_wrapper calls callback, which calls func -- so _exit_wrapper transitively depends on func.
# _exit_wrapper -> callback -> ensure_future: _exit_wrapper has a transitive dependency on ensure_future through callback.
# select_wildcard calls match, which calls _compile -- so select_wildcard transitively depends on _compile.
# select_wildcard -> match -> _StringGlobber: select_wildcard has a transitive dependency on _StringGlobber through match.
# select_recursive -> match -> _compile: select_recursive has a transitive dependency on _compile through match.
# select_recursive -> match -> _StringGlobber: select_recursive has a transitive dependency on _StringGlobber through match.
# Since select_recursive invokes select_recursive_step and select_recursive_step invokes match, select_recursive indirectly relies on match.
# Since select_recursive invokes select_recursive_step and select_recursive_step invokes select_next, select_recursive indirectly relies on select_next.
# select_recursive_step -> match -> _compile: select_recursive_step has a transitive dependency on _compile through match.
# select_recursive_step calls match, which calls _StringGlobber -- so select_recursive_step transitively depends on _StringGlobber.
# Since path_hook_for_FileFinder invokes _path_isdir and _path_isdir invokes _path_is_mode_type, path_hook_for_FileFinder indirectly relies on _path_is_mode_type.
# SharedMemory -> dispatch -> convert_to_error: SharedMemory has a transitive dependency on convert_to_error through dispatch.
# Since SharedMemory invokes dispatch and dispatch invokes _find_impl, SharedMemory indirectly relies on _find_impl.
# SharedMemory -> dispatch -> get_cache_token: SharedMemory has a transitive dependency on get_cache_token through dispatch.
# ShareableList -> dispatch -> convert_to_error: ShareableList has a transitive dependency on convert_to_error through dispatch.
# Since ShareableList invokes dispatch and dispatch invokes _find_impl, ShareableList indirectly relies on _find_impl.
# Note: ShareableList does not call get_cache_token directly, but reaches it via dispatch.
# encodekey -> encode -> code_page_encode: encodekey has a transitive dependency on code_page_encode through encode.
# Note: encodekey does not call bz2_encode directly, but reaches it via encode.
# encodekey -> encode -> b2a_qp: encodekey has a transitive dependency on b2a_qp through encode.
# encodekey -> encode -> encode_basestring: encodekey has a transitive dependency on encode_basestring through encode.
# Note: encodekey does not call encode_basestring_ascii directly, but reaches it via encode.
# Note: encodekey does not call uu_encode directly, but reaches it via encode.
# encodekey -> encode -> _ValueFormatter: encodekey has a transitive dependency on _ValueFormatter through encode.
# Since encodekey invokes encode and encode invokes hex_encode, encodekey indirectly relies on hex_encode.
# encodekey calls encode, which calls write -- so encodekey transitively depends on write.
# encodekey -> encode -> needsquoting: encodekey has a transitive dependency on needsquoting through encode.
# encodekey calls encode, which calls quote -- so encodekey transitively depends on quote.
# encodekey -> encode -> ToASCII: encodekey has a transitive dependency on ToASCII through encode.
# Note: encodekey does not call oem_encode directly, but reaches it via encode.
# Since encodekey invokes encode and encode invokes zlib_encode, encodekey indirectly relies on zlib_encode.
# Note: encodekey does not call mbcs_encode directly, but reaches it via encode.
# encodekey calls encode, which calls base64_encode -- so encodekey transitively depends on base64_encode.
# encodekey -> encode -> punycode_encode: encodekey has a transitive dependency on punycode_encode through encode.
# Since encodekey invokes encode and encode invokes quopri_encode, encodekey indirectly relies on quopri_encode.
# Since encodekey invokes encode and encode invokes HeaderParseError, encodekey indirectly relies on HeaderParseError.
# spill calls _split_list, which calls predicate -- so spill transitively depends on predicate.
# spill -> push -> trace: spill has a transitive dependency on trace through push.
# spill -> push -> Event: spill has a transitive dependency on Event through push.
# spilldescriptors calls _split_list, which calls predicate -- so spilldescriptors transitively depends on predicate.
# spilldescriptors -> push -> trace: spilldescriptors has a transitive dependency on trace through push.
# spilldescriptors calls push, which calls Event -- so spilldescriptors transitively depends on Event.
# Since spilldata invokes _split_list and _split_list invokes predicate, spilldata indirectly relies on predicate.
# spilldata -> push -> trace: spilldata has a transitive dependency on trace through push.
# Note: spilldata does not call Event directly, but reaches it via push.
# spilldata calls getdoc, which calls _getdoc -- so spilldata transitively depends on _getdoc.
# Since spilldata invokes getdoc and getdoc invokes cleandoc, spilldata indirectly relies on cleandoc.
# Note: spilldata does not call _finddoc directly, but reaches it via getdoc.
# Since onexc invokes onerror and onerror invokes callback, onexc indirectly relies on callback.
# onexc -> _resetperms -> _dont_follow_symlinks: onexc has a transitive dependency on _dont_follow_symlinks through _resetperms.
# shouldIncludeMethod calls fnmatchcase, which calls _compile_pattern -- so shouldIncludeMethod transitively depends on _compile_pattern.
# shouldIncludeMethod calls fnmatchcase, which calls match -- so shouldIncludeMethod transitively depends on match.
# patched -> wraps -> partial: patched has a transitive dependency on partial through wraps.
# patched -> func -> getter: patched has a transitive dependency on getter through func.
# _inner -> wraps -> partial: _inner has a transitive dependency on partial through wraps.
# _inner calls f, which calls f1 -- so _inner transitively depends on f1.
# proxy_bypass_registry calls _proxy_bypass_winreg_override, which calls fnmatch -- so proxy_bypass_registry transitively depends on fnmatch.
# Note: proxy_bypass_registry does not call _splitport directly, but reaches it via _proxy_bypass_winreg_override.
# Since _cb invokes callback and callback invokes func, _cb indirectly relies on func.
# Note: _cb does not call ensure_future directly, but reaches it via callback.
# Note: start_response_wrapper does not call is_hop_by_hop directly, but reaches it via start_response.
# start_response_wrapper -> check_content_type -> assert_: start_response_wrapper has a transitive dependency on assert_ through check_content_type.
# Since start_response_wrapper invokes check_content_type and check_content_type invokes check_string_type, start_response_wrapper indirectly relies on check_string_type.
# Note: start_response_wrapper does not call assert_ directly, but reaches it via check_headers.
# start_response_wrapper calls check_headers, which calls check_string_type -- so start_response_wrapper transitively depends on check_string_type.
# Since start_response_wrapper invokes check_exc_info and check_exc_info invokes assert_, start_response_wrapper indirectly relies on assert_.
# Since start_response_wrapper invokes check_status and check_status invokes assert_, start_response_wrapper indirectly relies on assert_.
# Note: start_response_wrapper does not call check_string_type directly, but reaches it via check_status.
# end_element_handler calls _parse_ns_name, which calls intern -- so end_element_handler transitively depends on intern.
# get_lines calls get_source, which calls _get_module_path -- so get_lines transitively depends on _get_module_path.
# get_lines -> get_source -> _get_module_info: get_lines has a transitive dependency on _get_module_info through get_source.
# get_lines calls get_source, which calls decode_source -- so get_lines transitively depends on decode_source.
# get_lines -> get_source -> ZipImportError: get_lines has a transitive dependency on ZipImportError through get_source.
# Since get_lines invokes get_source and get_source invokes _get_data, get_lines indirectly relies on _get_data.
# Note: temp does not call convert_to_error directly, but reaches it via dispatch.
# temp calls dispatch, which calls _find_impl -- so temp transitively depends on _find_impl.
# temp calls dispatch, which calls get_cache_token -- so temp transitively depends on get_cache_token.
# Since getprop invokes create_string_buffer and create_string_buffer invokes buftype, getprop indirectly relies on buftype.
# func_wrapper calls func, which calls getter -- so func_wrapper transitively depends on getter.
# Since handler invokes start and start invokes ProcessError, handler indirectly relies on ProcessError.
# Since handler invokes start and start invokes _start_joinable_thread, handler indirectly relies on _start_joinable_thread.
# Since handler invokes start and start invokes current_process, handler indirectly relies on current_process.
# handler -> start -> _cleanup: handler has a transitive dependency on _cleanup through start.
# handler calls start, which calls ResponseError -- so handler transitively depends on ResponseError.
# Since handler invokes end and end invokes f, handler indirectly relies on f.
# handler -> append -> Time2Internaldate: handler has a transitive dependency on Time2Internaldate through append.
# Since handler invokes append and append invokes Charset, handler indirectly relies on Charset.